"""Shared helpers for the pre-release documentation gates.

Imported by every checker under `skills/releasing/*/`. Nothing here knows
which documents exist -- that belongs to each child skill's manifest, so a new
artifact is a new entry rather than a new copy of this logic.
"""
