\ session_prelude.fs — NanoISA session glue for examples/language/forth/
\
\ Jackson tester.fr leaves BASE HEX. The example files write decimal T{ cases.
\ TEST-SUMMARY was the old token-string interpreter's tally word.

DECIMAL
: TEST-SUMMARY ;
