# Test module imports

import "math_utils.nano"

fn main() -> int {
    let result1: int = (add 10 20)
    let result2: int = (multiply 5 6)
    
    (println result1)
    (println result2)
    
    return 0
}

shadow main {
    assert (== (main) 0)
}

