/* fold_constants.c — constant folding optimisation pass (stub)
 *
 * Full implementation lives in feat/compiler-advanced (PR #44).
 * This stub allows main to build until that PR is merged.
 */
#include "nanolang.h"

void fold_constants(ASTNode *root) {
    (void)root;
    /* no-op stub */
}
