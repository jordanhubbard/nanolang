/* par_let_pass.c — parallel let desugaring pass (stub)
 *
 * Full implementation lives in feat/compiler-advanced (PR #44).
 * This stub allows main to build until that PR is merged.
 */
#include "nanolang.h"

void desugar_par_let(ASTNode *root) {
    (void)root;
    /* no-op stub */
}
