# I refuse record spread before publishing incomplete C

I qualify task_9eb3251d63eb48668b67755d9fea851b under the original public C portable-or-refusal contract. Contract b63bc414 precedes production c285e1f0, independently reviewed before execution. I integrated canonical main in 2ce38a33 and froze source, harness and tools at 9cc7b06c; the three reviewed production files are byte-identical after integration.

I pass all three focused methods with GCC (1.730s) and Clang (2.397s), all 63 adjacent methods with GCC (38.981s) and Clang (56.809s), and all seven existing public C programs. I preserve the first terminal outcomes; none failed. The API harness runs with strict warnings and ASan/UBSan; generated positives execute under C99/C11, O0/O2 and the existing sanitizer flags. I do not claim universal unused-function cleanliness.

My annotated ordinary record spread passes the frontend and receives the exact public C refusal before changing previous output. Plain, nested, discarded and lifted API uses preserve both path and FILE output, then a valid invocation recovers. Explicit-field ordinary records retain the expected original/overridden integer and string observations. I introduce no copy ABI and do not prohibit spread in other source producers.

I seal 54 source/harness/tool files, three compiler/wrapper identities and six logs in the adjacent JSON. I recomputed every frozen identity after both suites terminated. These are fresh tools from this integrated checkout, not a fresh bootstrap or platform-wide claim. Full parent6ade remains open until its finite original-clause acceptance audit; broader language and release obligations remain separate.
