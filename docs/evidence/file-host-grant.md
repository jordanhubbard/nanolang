# I qualify the preparatory File host grant and shared gate

I qualify production221c4d0a6 under child
`task_25bbd69e2c9a49428f58a45dad942b8c`, following precode8dc094ddf and root's
source/fixture review. Frozen54822f541 adds only approved process-group timeout
containment before any gate. The [public conjunction contract](../NANOISA_FILE_PUBLIC_CONJUNCTION.md)
still requires separately qualified native handlers and reviewed joint admission.
No VM/native dispatcher, emitter, default provider or existing public File guard
changes in this component.

| Fresh configuration | C11 Make object | Linked + instrumented fixture |
| --- | --- | --- |
| Linux GCC ordinary | PASS0.080s | PASS0.247s |
| Linux Clang ordinary | PASS0.091s | PASS0.262s |
| Linux GCC ASan/UBSan/LSan | PASS0.090s | PASS0.354s |
| Linux Clang ASan/UBSan/LSan | PASS0.066s | PASS0.532s |
| puck AppleClang ordinary | PASS0.130s | PASS0.759s |
| puck HomebrewClang ordinary | PASS0.238s | PASS0.871s |
| puck HomebrewClang ASan/UBSan/LSan | PASS0.139s | PASS1.237s |

Each phase compiles its real owning object through the explicit Make recipe into
a new directory. Both fixture consumers compile under C99; only the owning source
and its separately instrumented fixture copy compile under C11. No object from an
earlier compiler/sanitizer phase is reused. The isolated target intentionally
needs no libffi headers/libraries; the driver passes empty LIBFFI_CFLAGS/LIBFFI_LIBS
to this Make object build. Actual compiler commands, versions and before/after
identities are retained. Linux native Clang uses the previously qualified explicit
GCC13 installation. Darwin uses the selected Xcode compiler and SDK, and explicit
HomebrewClang. Header dependency inventories are **postphase** observations, not
fabricated prior snapshots. I do not change global C99 compilation policy.

All fourteen fixture executions pass. Every instrumented run reports132 allocation
attempts and zero live grants. I exercise32 deterministic held-gate thread
contentions, same-thread and allocator reentry, two separate C99 translation units,
query/grant mutual exclusion, BUSY-before-pointer-inspection, live revocation,
idempotent destruction,128 fresh lifetimes, allocation-failure output preservation,
and restored test-only identity/ABI/catalog/policy mismatches. Each configuration
also requires the owning source's deliberate C99 compile to fail with its explicit
diagnostic; those seven expected negative compiles are positive controls. There
is no unexpected failed terminal. Both ordinary and sanitizer fixture assertions
remain enabled with -UNDEBUG. The runner clears LSAN_OPTIONS and enables leak
detection explicitly; sanitizer scope is the entire isolated grant plus fixture
consumers, not preexisting File cores or a public execution route.

The [seal](file-host-grant/report-sha256.json) covers166 retained report files,
including nine equal before/after pairs: two full9,584-file source maps and seven
selected-tool maps. Linux and Darwin source maps are identical. Current checks
also match all9,584 files on each retained host,22 Linux and15 Darwin tool references,
and all36/27 phase artifact references. The content-addressed
`/tmp/nanolang-file-host-grant-artifacts` retains63 artifact references/62 unique
objects, dependency files and fixture binaries; original phase outputs remain.
The transferred source and Darwin report archives have retained size/hash records.
All advertised report files are committed Git blobs, checked against the seal.

Qualified Linux source remains
`/home/jkh/Src/nanolang-file-host-grant-qualified`; puck source remains
`/private/tmp/nanolang-file-host-grant-54822`. Reports remain
`/tmp/nanolang-file-host-grant-54822-linux` and
`/tmp/nanolang-file-host-grant-54822-darwin` on their respective hosts. No original
qualified tree is reused as an editable final branch.

I also retain the separate97ed0aa5f documentation reconciliation: the previously
unchecked private acyclic546cf row is complete through actual PR848 merge
ab17e610786397670321b846de137f3c8bee9bf6, independently confirmed as an ancestor of
current main and live MAC COMPLETED. The exact original merge evidence ID and
scope are [recorded](file-host-grant/acyclic-roadmap-reconciliation.json), with the
[original body evidence](file-body-transfers.md). This closes no additional File
requirement. Canonical PR886 changes only ROADMAP after my frozen base; it does
not change any qualified production, fixture or compiler input.

This is grant/gate acceptance only. The two C99 consumers model trusted adapters;
they are not generated File programs. Native-name collision/package controls,
actual no-grant refusal before host/loader attempts, startup/handler/cleanup
conjunction, installed commands and full paired source/shadow/loop/indirect
requirements remain open under6fc/72556/82ff/6931. I make no public File execution
or overall5.1 readiness claim.
