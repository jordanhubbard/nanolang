# My source owned-string qualification

I track task_a4326d3ba9a74cdd938b97e777d10132 and ordering prerequisite
task_c396592893874b81924dea82a9f5c769 in PR761. I depend on runtime PR750;
I change no runtime authority. My source contract is
[NANOISA_SOURCE_OWNED_STRING_PRINT.md](../NANOISA_SOURCE_OWNED_STRING_PRINT.md).

## My retained first outcome

I froze production99be5009 with testsd77c41ac. My component build and fresh
full Stage1/Stage2 bootstrap passed. My42-method source gate passed40 and
failed2 exact canonical dump comparisons in357.378seconds. C-seed interned
lexical metadata before later function literals; my selfhost assembly interned
executable literals first. I retain the complete initial log, status and
before/after source/tool hashes. All12 measured hashes stayed unchanged.
My assertion-cleanup output and semantic refusal tests passed, as did all38
preexisting methods. I do not relabel the40/42 outcome as a pass.

I recorded contract5c360ad9 before correctionf8112c30. My bounded pending
binding descriptors retain existing name intervals and publish through the
same validated metadata API after all functions finish successfully. This
changes constant serialization order without dropping metadata or literals.

My first adjacent runtime gate passed:600 owned-string allocation checks,
150 string proof checks with9 admissions,3485 owned-result allocation checks,
117 result preflight checks,1552 owned-result checks,338 graph preflight checks,
529 invocation proof checks,69 verifier-reuse checks,1847 graph lifecycle checks,
104 consuming allocation/90 preflight/3091 lifecycle checks,210 multiple-call
allocation/216 preflight/4542 lifecycle/65 refusal checks,959 assertion lifecycle
checks,93 multi-caller atomic/89 owner allocation/2004 caller checks. These are
existing scoped controls, not evidence of exhaustive allocation coverage.

## My integrated source

I restacked cleanly onto canonical80c2f2f1, including PR762 array compiler
changes, at e56f75bf4405d29e2ebe62a996f31293ab83a90e. I required a fresh full
bootstrap and the original42-method gate at that integrated source; I do not
reuse the earlier compiler-stage claim. I separately hash440 tracked source
files before and after qualification.

My exact-output controls include a literal backslash followed by zero, an
unknown escape, tab/carriage-return/newline, quote/apostrophe, control1/DEL,
empty string and UTF-8. I compare raw VM/native bytes and retain all original
shadows of the unchanged affine-resource example. My full normative ownership
parents and the full product release gate remain separate.

## My completed integrated checks

My fresh full bootstrap passed both compiler-stage hello controls at e56f75bf.
My original42-method paired source suite passed in360.858seconds, status0.
The two earlier canonical equality cases passed without changing their source
fixtures or assertions. My seven adjacent runtime targets passed again with
status0. I measured unchanged hashes for all12 selected source/tools and440
tracked source files; I retained all five new stage emitter/shadow drivers
with matching copy hashes under `/tmp/nanolang-source-owned-string-e56-tools`.

My preserved reports live in [source-owned-string-print](source-owned-string-print/).
`integrated-paired.log` contains the fresh bootstrap and full42 result;
`integrated-runtime.log` contains the seven adjacent target results. The
`first-*` reports retain my failed equality gate and its independently passing
runtime controls. `report-sha256.json` seals these reports.

I exercised the unchanged affine example through C-seed and both canonical
selfhost stages, all three raw emitters, the C shadow producer and all three
stage shadow drivers. Production prints `audit.log` then `resource closed`.
Every selected original shadow executes; its combined output is `audit.log`,
`resource closed`, `resource closed`, `audit.log`, `resource closed`, each with
a newline. Names-stripped modules retain behavior, exact dumps match, and
standalone strict GCC ASan/UBSan/LSan output matches VM bytes. I make no new
Darwin source qualification claim and no full product/release closure claim.
