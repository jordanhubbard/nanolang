# My bounded public File byte boundary

I qualify the explicit temporary-file grant with my serialized byte API, direct generated native functions, installed C99 package, and CLI opt-ins. I do not qualify paired source-language producers, loops, indirect calls, richer borrows, or the full File/Socket/GPU roadmap with these checks.

My production source remains `a6862f732`. The matched private native prerequisite is actual PR888 merge `f1606e2c84e67491e9652a5bf71944d235216d95`. I preserve every earlier qualification tree and first terminal. Fixture corrections change no production source and remove no assertion.

## Measured public matrix

Each complete configuration runs all three public fixture methods. My native corpus compiles and executes generated functions at both O0 and O2. Linked capture builds the retained PR888 emitter afresh and compares its private emitted bytes against the factored private surface before testing the public surface.

| Host/compiler | Fixture | Scope | Seconds | Result |
| --- | --- | --- | ---: | --- |
| Linux GCC | dd401e5ea | ordinary | 64.615 | PASS |
| Linux GCC | dd401e5ea | ASan/UBSan/LSan | 338.691 | PASS |
| puck Apple Clang | dd401e5ea | ordinary | 48.417 | PASS |
| Linux Clang | a46a34e2e | ordinary | 53.403 | PASS |
| Linux Clang | a46a34e2e | ASan/UBSan/LSan | 96.202 | PASS |
| puck Homebrew Clang | a46a34e2e | ordinary | 44.361 | PASS |
| puck Homebrew Clang | a46a34e2e | ASan/UBSan/LSan | 75.924 | PASS |

I record actual compiler paths, versions, hashes, commands and source maps per phase. Linux Clang selects GCC13 explicitly through its compiler command, including Make recipes with independent flags. Darwin selects the Xcode SDK and actual Apple/Homebrew compilers; resolved libcrypto and SDK headers are hashed. I clear inherited LSAN_OPTIONS and retain leak detection and fatal sanitizer diagnostics.

I distinguish instrumented providers from reused common objects. Each configuration's first fixture performs actual `make -B install`, rebuilding the installed products and common closure with its selected flags; source/tool inventories remain equal, while object inventories deliberately change. Individual fixture provider builds apply scoped instrumentation hooks. My per-phase maps and archived binaries describe that distinction; I do not claim a uniform compiler/optimization level for every transitive object.

## Observed behavior

I preserve the manual carrier/frame and actual dispatch corpus assertions through the public VM and generated-native bridges. Instrumented capture records 73 exact modules, 71 accepted emissions and two refusals; linked capture records 69 modules, 67 emissions and two refusals. The instrumented native replay reports 567 calls. My replay assertion checks that the public VM bridge count remains zero before the separate public boundary controls.

I check missing, revoked and busy grants, output sentinels, malformed wire/catalog/startup claims, allocation refusal, real temporary-file lifecycle, modeled partial I/O and cleanup failures, synchronous reentry, concurrent gate contenders and clean-only scalar publication. Real I/O followed by injected reporting errors remains a model; it does not establish arbitrary libc failure behavior.

My installed controls copy and hash the actual archive, commands and 22 headers before uninstall. Two generated translation units use distinct entries and one owning grant/gate. Outside-tree C99 clients execute at O0/O2; native-only linked symbol checks exclude the VM execution member. ABI/fact mismatch refuses, and another program still succeeds using the same grant. Duplicate entry names fail to link.

The installed CLIs check default refusal, paired opt-ins, invalid combinations, bounded input and staged output preservation. BOOL false/true exit as 0/1; INT -257 exits as 255; the C API retains the full scalar value. Modeled fdopen failure checks the rejected descriptor is EBADF. Uninstall removes the owned archive, exact 22 headers and new command while unrelated prefix sentinels survive. Removed installed paths are not claimed current: copied installed artifacts and their maps remain retained.

## Compile preflight and neighbors

At a46 I compile capture/replay crossed with instrumented/linked forms using GCC, Linux Clang, Apple Clang and Homebrew Clang: all 16 strict Werror compile-only controls pass. They execute no service.

After retaining each final public phase's artifacts, I rebuild ordinary neighboring providers. Existing File opcode/transport/default-consumer refusal, grant C11-owner/C99-consumer, and ordinary wrapper controls pass on both hosts. The grant and selected opcode fixtures use their existing strict sanitizers; ordinary wrapper execution uses the recorded ordinary closure. These checks preserve legacy refusal, including consumers without an explicit host grant.

## Preserved first terminals

- Linux01fd: capture compilation found the fixture main macro collision. Correction389 gives the inherited native main a defaulted explicit macro name.
- Linux389: generated registry compilation found misleading abort/return indentation. Correctiondd401 uses unambiguous statements without changing branch behavior.
- Darwin389: strict feature macros hid mkdtemp. Correctiondd401 enables Darwin declarations only for the CLI fixture.
- Darwin dd401: replay compilation found an unread VM-bridge counter. Correction9bac asserts zero bridge calls immediately after native replay.
- Linux dd401 and independent9bac: actual install stopped at assembler-capture compilation because its standalone flags did not select GCC13. The corrected driver puts that selection in the compiler command; I change no Make recipe or warning policy.
- Darwin9bac: instrumented VM/native O0/O2 passed; linked capture compilation found an unread reentry counter. Correctiona46 asserts zero where linked providers have no injected hooks, opposite the retained instrumented positive assertion.
- Linuxa46 neighbor setup: managed native IR embedding needs its separate supported native-only GCC13 selector. The corrected neighbor environment sets NMS_NATIVE_CLANG_FLAGS; Wasm flags remain unchanged. No neighbor fixture ran before this terminal.

I do not replay failed binaries or reinterpret these compilation terminals as executed runtime failures. Earlier successful GCC/Apple configurations retain their exact dd401 attribution; later Clang configurations retain a46. Setup results and all failed/corrected terminal logs are in the seal.

## Retention and scope

My original seal contains 417 reports. The final [report manifest](file-public-admission/report-sha256.json) contains 510 reports, including independent audit and integrated qualification. The [summary](file-public-admission/qualification-summary.json) records 32 equal source/tool before/after pairs and 46,791 artifact references. My [artifact store](file-public-admission/artifact-store.json) retains 16,409 unique files, 4,324,821,877 bytes, including the compile-only objects. External artifacts live at `/tmp/nanolang-file-public-artifacts`; all hashes are verified during sealing. Compressed remote transfers and their byte/hash manifests are retained separately.

Each frozen source inventory contains 10,015 tracked files. Final a46 Linux/Darwin source maps are exactly equal. The current-map reports rehash each retained source tree and actual compiler/tool paths. Intermediate output binaries can be replaced by later configurations in the same frozen tree; their exact earlier contents remain in the content-addressed store. I do not claim that every intermediate output pathname still names its earlier binary.

My source trees are `/home/jkh/Src/nanolang-file-public-qualified-{01fd,389,dd401,9bac,a46}` and puck.local `/private/tmp/nanolang-file-public-{389,dd401,9bac,a46}`. Reports preserve exact full pins and selected tools. Final publication still requires independent evidence review and canonical integration assessment. Task dfa149 and the broader File parents remain open at this checkpoint.

## Canonical integration

I merge actual canonical `5d1d3d39f` in separate ready checkpoint `c3631ecf2`; additive Make target conflict resolution retains both public and cyclic recipes. Every public production/fixture blob remains identical. The canonical private cyclic query factors preparation but the existing public path still calls `nvm_file_code_prepare` with acyclic=true. The exact identity map is retained.

I freeze `58eccb93e` for fresh integration gates. Linux setup/public ordinary/cyclic query/opcode refusal pass in 20.342/64.916/2.320/3.171 seconds; puck passes in 9.115/44.612/2.331/3.621 seconds. Public ordinary qualification again executes all three methods, generated O0/O2 and actual installed/uninstalled package controls. I make no new integrated sanitizer claim.

Eight additional phase pairs preserve 10,896 current tracked sources on each host, with equal cross-host maps; 15 Linux and 16 puck tool entries match current files. The integrated archived inputs/outputs add 11,982 references. The final store has 20,144 unique files and 4,918,529,704 bytes. All earlier reports/artifacts and failure scopes remain preserved; full File source/control-flow parents remain open.
