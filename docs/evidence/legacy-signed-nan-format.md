# My legacy signed nonfinite formatting evidence

I qualify interpreter and paired legacy runtime child `task_df2b1ff3d75d4b43b2471696ee0b613e` and provider-order correction `task_abd14a6133da495a9dc49a6bfc88c10f`. Parent e92 remains open for remaining platform/public-target obligations.

My production checkpoint f7cc47bb uses one shared integer-classified signed nonfinite policy in interpreter scalar/array/generic/format paths and both generated legacy runtimes. Corrective checkpoint0e21f9a3 guards the identical provider and emits it before either generated runtime section. I preserve finite formatting and existing string ownership. My exact generated Nano header provider passes identity checks.

My first component build failed on formatter declaration order; I retain its log. Corrected component build passed54.986s. Actual full self-host bootstrap passed228.039s at cc6d4742 on Linux. Darwin full bootstrap passed305.410s at737b3bdb; its first test launch lacked the separately built interpreter. I retained that setup error, built bin/nano explicitly in0.410s, then froze all tools and source before the corrected four-method gate.

At5074dd40, Darwin passes all four methods in10.799s with unchanged before/after hashes. Those methods cover exact signed quiet/signaling NaNs, infinities, both zero signs, finite boundaries, preserved input bits, interpreter/C-seed/Stage1/Stage2 scalar output and conversion, interpreter array/generic/format output, C-seed array output and exact generated-provider identity. My source print routes retain their pre-existing finite differences: interpreter/C-seed scalar and interpreter arrays use raw `%g`; selfhost scalar and C-seed array conversions retain `.0`. Earlier finite-only fixture mismatches and a malformed array type token are preserved, not relabeled passes.

I integrated canonical main including PR749 at e240a459 and rebuilt Linux C-seed/interpreter. The final four legacy methods pass9.587s and the four public-C provider integration methods pass1.268s, with unchanged source/tool hashes. My selfhost binaries remain the actual cc6d full-bootstrap products: I do not claim a fresh bootstrap at the integrated head. The earlier fresh adjacent interpreter unit suite passes in full.

I retain raw logs, tool inventories, source/tool manifests and independently checked Darwin transfer hashes in [my report directory](legacy-signed-nan-format/). `report-sha256.json` seals every retained report. The Darwin source/tool claim applies to5074; integrated Linux applies to e240. I do not infer complete release readiness, unsupported selfhost array printing, arbitrary printf semantics, or public-C Darwin qualification from these bounded checks.

After PR751/753 merged, I resolved only roadmap text and integrated main at
f586bdb0, keeping my reviewed formatting production unchanged. I rebuilt C-seed
and interpreter. The shared-provider formatting/equality integration passed seven
methods and retained one setup error because the separate nano_virt tool was
absent. After explicitly building parity tools, I reran only that missing-tool
method: public C/interpreter/verified VM/sanitized native parity passes0.502s.
Both runs retain unchanged before/after hashes. I preserve the seven-plus-one
accounting and do not describe the initial eight-method invocation as a pass.
