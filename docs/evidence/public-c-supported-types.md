# I qualify explicit public C value representations

I qualify taskf29a after reviewed contract36749782, declaration-order clarification82a3015a and production383554b9. Review found a standalone union-constructor validation gap before execution; recorded correctionc2b4c715 and reviewed productiona7caffc6 close it for direct/dotted constructors without changing payload semantics. Final harness3b10f450 was frozen before all checks. My adjacent JSON records48 source/harness/tool files, three compiler/wrapper identities and six logs. Every identity matches after the gates; no production or harness mutation occurred during qualification.

All four focused methods pass on their first invocation: GCC3.538s and Clang5.684s. My API inventory enumerates all current Type kinds and refuses unsupported local/global/parameter/result/extern/record/union storage and nested metadata. Missing nominal names, wrong constructor owners, forward/self/cyclic by-value record definitions, incomplete/extern/resource records and both output APIs' prior bytes and recovery are covered. Direct empty union construction remains accepted. Scalar carrier API modes for INT/U8/ENUM append an explicit C observer that passes7 through the generated typed helper; these are labeled API representation controls, not source enum syntax qualification.

Ordinary source programs qualify prior-order nested record definitions with actual int/string/float/bool field observations, and dotted scalar/empty union construction with match observations. Four ordinary opaque/forward/self/cyclic cases reach the backend checked refusals and preserve prior output; no rejected output executes. Generated positives compile and run under strict/pedantic C99/C11 O0/O2 ASan/UBSan for GCC and Clang. API harness compilation additionally uses-Wall-Wextra-Werror.

All53 adjacent callable/profile/array/lifting and scalar/string/union methods pass: GCC32.193s and Clang45.816s. All seven existing C programs pass with the same tools. These gates include existing interpreter and verified-VM source comparisons in their established bounded fixtures. I claim no fresh bootstrap, Darwin, universal source-language coverage or global unused-helper-warning qualification.

The catch-all int64 spelling is replaced by a diagnostic retained through staging. ENUM's previous integer carrier is explicit; unsupported metadata kinds receive refusal, while qualified scalar/string/local nominal contexts remain supported. A by-value record field must have an earlier complete local owner; no new sorting, pointer or recursive-record ABI is introduced. Canonical merge precedes child closure. Original6ade still requires its final clause-to-evidence inventory; shared language-policy and broader release tasks remain independent.

## I qualify canonical781 integration separately

Integration66b61c5a preserves both roadmap records after the sole merge conflict. The incoming production change is in nanovirt/borrow_codegen.inc; every public C source and fixture is byte-identical to the reviewed frozen pin. Rebuilding the selected tools changes only bin/nanoc_c and bin/nano_virt among the48 original file identities. I retain the original manifest and add48 integrated identities, the unchanged three compiler/wrapper identities and three new logs. All integrated identities match after targeted gates. The four focused methods pass GCC3.442s and Clang5.677s. Prior53-method and seven-program results retain their original tool pin; I do not relabel them as integrated reruns.
