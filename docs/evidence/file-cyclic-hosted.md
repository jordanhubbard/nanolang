# I qualify copied cyclic File hosted facts without execution

I qualify only dependency1 of task_15a92c930af7433e9e25b41c7c5c761f and
[my cyclic execution contract](../NANOISA_FILE_CYCLIC_EXECUTION.md).
Production is `bae2badf9f566b0a2124bf395602c2e70d2390b5`; reviewed fixtures and
both frozen checkouts are `46912be39f9a1c7969313ae8f38ea722fbd1e466`.
My separate hosted plan retains every variant, edge and original identity,
checks copied serialization and storage bounds, and remains non-admitting.
No cyclic File bytecode, host service, carrier, VM or native execution is tested
or enabled by this qualification. Public dfa149 remains independently owned.

## My first frozen qualification passes on both hosts

| Phase | Linux seconds | Darwin seconds | Result |
| --- | ---: | ---: | --- |
| Fresh ordinary provider/tool setup |20.864|9.047|PASS |
| Exact make configuration inventory |0.064|0.128|PASS |
| GCC13 / Apple Clang ordinary hosted fixture |7.081|5.355|PASS |
| Clang / Homebrew Clang ordinary hosted fixture |6.429|5.611|PASS |
| GCC13 / Homebrew Clang ASan+UBSan+LSan hosted fixture |34.345|11.832|PASS |
| Linux Clang ASan+UBSan+LSan hosted fixture |17.604|—|PASS on Linux |
| Unchanged cyclic query neighbor |9.285|4.897|PASS |
| Unchanged CODE query neighbor |6.629|4.367|PASS |
| Unchanged body query neighbor |6.830|4.408|PASS |
| Unchanged flow query neighbor |6.280|4.273|PASS |
| Complete unchanged serialized hosted neighbor |20.814|9.580|PASS |
| Unchanged File opcode/refusal neighbor |3.020|3.510|PASS |

Each new fixture phase runs both instrumented and separately linked methods.
The instrumented method reports1,279,978 checks and the linked method237,053.
These are cumulative assertions, including the unchanged hosted corpus and
allocation bookkeeping, not independent programs or proof obligations. The
new cyclic hosted allocation sweep records376 persistent prefixes and376 single
transient refusals, with a fresh successful preparation after every refusal.
It observes no successful publication after an injected failure; the conditional
complete-plan comparison for that case remains unexercised. Ordinary recovery,
full fact comparison, copied bytes and independent lifetime checks pass.

I encounter no unexpected terminal in this matrix and make no corrections or
retries to obtain these results. Every actual command, raw log, return status,
bound and process-group cleanup result is retained. The command bound is240s
inside each fixture and1800s for each outer phase. Successful ordinary completion
does not establish every OS timeout/error path. No failed output is executed.

## I retain exact scope and inputs

The new fixture rebuilds thirteen allocating reader/bridge/query providers in
both methods. Instrumented mode interposes their allocation chain and checks
peak bytes including old-plus-new realloc overlap. Sanitizer configurations
instrument those rebuilt providers and the fixture; shared prepared common
objects remain ordinary. I do not claim whole-program sanitizer coverage or
instrumented unrelated compiler/runtime code. Every selected sanitizer run uses
`detect_leaks=1`, halt-on-error and an explicitly empty `LSAN_OPTIONS`.

Linux selects `/usr/bin/gcc-13` in the compiler command. Its Clang commands and
native managed-IR preparation select GCC13 through
`--gcc-install-dir=/usr/lib/gcc/aarch64-linux-gnu/13`; the native IR knob is
separate from Wasm flags. Darwin uses explicit Apple and Homebrew Clang paths,
the recorded Xcode SDK, libffi include and OpenSSL library path. No shared
system toolchain is changed. Exact resolved executable hashes, versions,
selectors, commands and generated provider manifests are retained.

My full fixture compares all variant input/output facts, references, regions,
edge ordinals and original nominal/import identities against an independent
query. It keeps initial seed ordinal0 and the uninitialized zero-iteration
alternative, real16/17 joins, all five service obligations, owned/borrowed calls,
exact lower-index call and initializer maxima, legacy zero versus undersized
positive max_stack, invalid accessor sentinels, destroyed caller input and
malformed/unsupported/limit boundaries. Old acyclic hosted and generic verifier/
converter refusals remain. Both query and hosted `runtime_admitted` stay false.

## My seal preserves the qualified checkouts

- Linux: `/home/jkh/Src/nanolang-file-cyclic-hosted-46912`, raw reports
  `/tmp/nanolang-file-cyclic-hosted-46912-linux`.
- Darwin: puck `/private/tmp/nanolang-file-cyclic-hosted-46912`, raw reports
  `/tmp/nanolang-file-cyclic-hosted-46912-puck`.
- [Report hashes](file-cyclic-hosted/report-sha256.json):230 report files.
- [Qualification summary](file-cyclic-hosted/qualification-summary.json):23 equal
  source/tool before-after pairs,10,460 tracked sources each,9 Linux and12 Darwin
  tool labels. Aliases can refer to the same executable; these are not distinct
  compiler counts. Current sources/tools and164 Linux/163 Darwin prepared inputs
  are rehashed after the terminal. Existing input maps are equal through phases;
  fresh setup creates its separately inventoried outputs.
- [Artifact index](file-cyclic-hosted/artifact-store.json):945 unique artifacts,
  8,657 references,352,149,174 uncompressed bytes. Retained raw binaries and
  object/log/status artifacts are content-addressed outside Git, with hashes
  and paths committed. No artifact-store payload is claimed committed.
- [Combined archive](file-cyclic-hosted/artifact-archive.json):
  `/tmp/nanolang-file-cyclic-hosted-artifacts.tar.gz`,100,239,963 bytes,
  SHA256 `9e04bfe53c7c044806eeb2c7b72e96f6dd69c356debd4c984d84aec47d9dd275`.
  Both remote archive hashes are verified before local extraction and every
  indexed artifact is rehashed before sealing. Scripts and current Darwin
  source/tool/provider verification are included in the report manifest.

I preserve old qualified trees and the first query486ba failure in its original
PR894 history. This new acceptance neither repeats nor reclassifies that failure.
Independent review and actual merge remain pending. Task15a92 remains open for
physical witnesses/generation reuse, finite fuel, matched private VM/native,
then a separately reviewed public conjunction after actual dfa149 integration.
Closed indirect, richer borrowing, paired source/shadows and full parents remain
required; these hosted facts do not discharge them.
