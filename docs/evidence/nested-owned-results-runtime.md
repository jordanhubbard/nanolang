# I qualify nested owned results before source admission

I retain reviewed query production78d86331 and corrected harness d4342022. My source result guards remain unchanged at runtime-only checkpoint5ae0f2a2. The [sealed report](nested-owned-results-runtime.json) contains exact source, object/tool, generated-artifact archive and retained log identities.

My initial917 gate passes186 descriptor checks, then stops at an allocation-harness assertion. Static review identified that its budget wrongly treated zero-capacity calloc as required; the exact original case/API/fault was unlogged. I retain the first terminal without replay. Corrected d434 preserves every empty-record success/cleanup check, excludes only zero-capacity calloc from required-failure budgets and logs each phase.

Corrected GCC subprocesses pass4.548seconds, including533 existing descriptor/roundtrip checks. Clang subprocesses pass3.125seconds. Each nested route reports186 query controls,3,188 heap-allocation checks across304 budgets and280 actual injected positive-capacity failures,714 four-API result checks, and six generated native cases. Native fixtures run strict ASan/UBSan/leak checks, repeated entry and owner-allocation failures. Linked VM objects use the ordinary build, not a claimed fully sanitized VM build.

The first driver's outer exit is1 despite successful subprocesses: Clang Make preparation relinked two tools inside its overly broad freeze interval. I retain that traceback and distinguish the manifest's subprocess0 from the authoritative outer status. I correct only the external driver to separate preparation from frozen execution. Fresh frozen Clang passes1.715seconds with all binaries/objects unchanged; its tools and test executables are archived before subsequent preparation. Adjacent GCC preparation takes48.401seconds; frozen result/graph/consuming/assertion/binary64 execution passes22.534seconds with all binaries/objects unchanged. Earlier GCC bin paths had already been overwritten by the first Clang preparation; their measured maps remain, but I do not claim an archive or recreate old hashes.

The exact query boundary includes shared-DAG maximum-depth propagation (32 levels pass,33 refuse), field limits256/257, unsupported FLOAT/nonowned child refusal, unchanged outputs when nested scratch allocation fails, and allocation-free scalar/VOID/leaf queries. Fresh runtime modules cover both factory branches, a same-shaped nominal twin, empty owned leaves, relay/destruction, unrelated caller holds, pending/caller assertion cleanup and four public VM APIs. Wrong nominal results are refused before execution. The full adjacent gates retain existing scalar-leaf allocation ceilings and current binary64 operand-stack behavior without widening FLOAT fields or signatures.

This evidence permits a separately reviewed source-admission checkpoint. It does not establish source factory acceptance, fresh source bootstrap or full affine/product completion. No old refused artifact executes.
