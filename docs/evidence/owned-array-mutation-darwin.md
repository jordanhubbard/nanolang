# I qualify prepared mutation cleanup on puck

I retain the [reviewed Darwin contract](../NANOISA_OWNED_ARRAY_MUTATION_DARWIN.md)
and [exact seal](owned-array-mutation-darwin.json). Frozen92ee0e38a includes the
unchanged merged847 fixtures and runtime. My host is puck, arm64 Darwin25.6.0,
with SDK26.2, Apple clang17.0.0 and Homebrew clang23.1.1. This is separately
attributed puck acceptance, not a CXWWHGGJX0 rerun.

Apple provider preparation passes in4.088 seconds. My first driver stops before
any fixture because copy2 attempts protected compiler chflags and receives EPERM.
I retain its partial archives and an explicitly retrospective record of observed
SSH stderr; I do not invent an original redirected traceback log. Reviewed driver
corrections use content-only copyfile with immediate hash comparison and complete
external paths, and terminate timed-out process groups with TERM then KILL.

| Corrected fresh phase | Seconds | Result |
| --- | ---: | --- |
| Apple ordinary mutation | 6.322 | PASS |
| Homebrew ASan/UBSan mutation, leak detection enabled | 13.541 | PASS |
| Apple unchanged private runtime corpus | 10.531 | PASS |

Both mutation configurations exercise true switch/computed-goto with preprocessor
macro evidence, fused/unfused execution, all four synchronous APIs and native
O0/O2. Each mutation VM binary reports1906 checks; each adjacent VM binary
reports4746. The exact normal output, prior-fault prefix, real stack-reallocation
and shell/field allocation sites, root/byte accounting and recovery assertions
remain unchanged. I change no runtime or producer behavior.

I seal663 files,12 unchanged ready inputs,1773 source hashes and211 inventoried
providers/compiler/configuration/sanitizer-runtime files. Inventories match before
and after every phase and at final verification. I archive actual Apple launcher
and Xcode compiler, resolved Homebrew compiler/configuration and sanitizer runtime
libraries. I do not claim to inventory every SDK file or transitive system tool.
Fixture/VM/heap/emitter/generated native units receive sanitizer instrumentation;
ordinary linked providers do not. Leak detection remains enabled. The outer
1800-second bound returns0, and each phase retains its600-second deadline.

I retain all artifacts on puck under
/private/tmp/nanolang-owner-array-mutation-d5da-evidence and preserve the Linux
qualified trees. Taskd5da awaits canonical review/merge; this runtime supplement
does not complete paired source mutation or parent430220.

I integrate canonical private File body848 after qualification in a separate ready
tree. My [ready comparison](owned-array-mutation-darwin-ready.json) preserves the
original seal/input map: eleven non-Make inputs stay identical; canonical Make
adds File-body targets. New private File query code is independently qualified.
I do not rebuild the frozen providers or claim the old gate exercised that new
File body. The puck92ee qualified tree remains untouched.
