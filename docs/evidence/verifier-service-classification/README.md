# I retain invocation-local verifier classification qualification

I qualify source53a09f645 with the unchanged verifier, Forth session, Core, Double and Float targets. Linux ordinary and all three Darwin configurations pass all five targets. Linux GCC ASan/UBSan passes verifier, session and Core, then the original Double600-second alarm returns2 after600.718seconds. Float remains unrun in that configuration. My outer1200-second supervisor does not time out; every launched phase is reaped and its process group absent. I do not claim a complete passing sanitizer matrix.

My five lossless report bundles contain288 reports,24 terminal records,48 equal source/tool pairs, and745 separately indexed local CAS objects. Darwin report and object bytes were copied locally and checked against independently generated remote hashes. Each bundle was decompressed and every member hash rechecked before this seal. My object indexes retain exact local byte locations.

I preserve the first Linux setup refusal before any product command: an old non-NUL Git listing quoted a historical evidence filename. Corrected setup uses NUL-delimited names. The original frozen Linux driver also carries an unused inner_subprocess_seconds30 metadata field; actual command supervision remains1200seconds plus original test-internal bounds. That descriptive field does not establish a30-second child policy. Darwin and later corrected drivers omit it. LSan is disabled in these selected ASan/UBSan configurations; I make no leak qualification claim.

The separate ordered-range source44d4 and its fresh gates are not included here. I retain the failing Linux Double terminal as their historical prerequisite, rather than relabel it after correction.
