# I qualify my private cyclic carrier protocol

I retain production `d4070d672` with corrected fixture `ac8772ae8`. My carrier
protocol is manually driven; I do not admit or execute cyclic bytecode through
VM/native/public dispatch. Old acyclic dispatcher neighbors remain distinct.

Linux completes 12 phases. Darwin completes its carrier/compiler and neighbor
matrix, preserves the public suite's missing historical Git terminal after the
CLI/instrumented methods pass, then passes only its remaining linked method with
verified read-only historical Git inputs. Original c5d Linux fixture failure is
also retained. No failed binary is replayed. See `qualification-summary.json`
for exact terminals, selected tools, source/tool equality and provider changes.

I retain 27 equal source/tool phase pairs,
6942 content-addressed artifacts and 23529
input/product references. Public package phases rebuild providers; their changed
endpoints are explicit. This is scoped provider instrumentation, not whole-program
sanitizer acceptance. Retained creation bytes do not prove preparation peak.

The artifact store is `/tmp/nanolang-file-cyclic-carrier-artifacts` and its archive
is `/tmp/nanolang-file-cyclic-carrier-artifacts.tar.gz` (SHA-256 `292e3ed28f87fd9cb63fd477aac377e5aa20a4018b12a59204cf0a45e5c81082`). All original trees/reports remain retained.
The Darwin transfer's `reports:107` field counts its primary reports only; the
same archive also contains 18 supplemental linked-method reports. The combined
report manifest enumerates every retained report explicitly.

Linux current source/tool/provider checks are in the summary; Darwin final
current checks are in `darwin-public-linked/current-verification.json`. Raw
historical source and provenance are under `historical-git/`. The preparation,
packaging and sealing scripts are included. Full cyclic VM/native/public
execution, indirect/richer borrowing and paired source goals remain open.
