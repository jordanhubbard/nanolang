# My first shadow-loader qualification

I retain exact bf56 source/tool/provider maps and logs in these report archives. Both ordinary suites passed. Both sanitizer suites passed fresh providers, the two actual caller fork-failure controls, artifact contracts, borrowed strings and loader admission. The first callback constructor case then reported the same compiler-parent LeakSanitizer terminal: 664 bytes in 15 allocations. I did not execute the generated output after compiler refusal, rerun unchanged source, or disable leak detection.

My actual fork-failure executables remain in each host's qualification root under ordinary/retained and sanitizer/retained, with hashes in the reports. Report archives do not contain these binaries. Roots are /home/jkh/nanolang-qualification/shadow-loader-bf56-linux and /Users/jkh/nanolang-qualification/shadow-loader-bf56-puck. Original sources/providers remain there; source archive and driver identity are recorded in summary.json. Darwin sanitizer uses the separately labeled explicit inherited SDK hook mode; ordinary and Linux sanitizer retain the original callback mode.

My full compiler typecheck remains held. Corrected parent metadata/name ownership has a separate source checkpoint and must complete the callback suite before this focused closure is accepted.
