# My counted mixed record-array storage evidence

I qualify the manually driven core/module storage contract at frozen
`5f4f0010af06c741517cb606faa73cdf0f3dad7f`, after reviewed fixture `3c0b12ed7`
and testing-only allocator hooks `0ff7f3268`. My canonical generated native
header includes those same guarded hooks. I do not claim generated mixed
bytecode execution, public admission, paired source lowering, or full release
acceptance from these manual adapter sequences.

My [contract](../NANOISA_MIXED_RECORD_ARRAY_COUNTED_RUNTIME.md) records the exact
lifetime, query/catalog correspondence and fault obligations. My
[seal](mixed-counted-runtime/seal-sha256.json) names 327 published report files;
[summary](mixed-counted-runtime/summary.json) identifies nine exact qualified
source/fixture/build paths and the retained Darwin archive. I retain 737 distinct
artifact objects (408,131,200 bytes), 10,508 manifest references and 59 equal
source/tool before-after pairs. Thirty phase terminals include two preserved
Linux failures and 28 successful setup/qualification/continuation phases. My
first setup's separately recorded generated-header change is not an equal pair.

## My measured routes

| Host | Direct native configurations | Additional gates |
| --- | --- | --- |
| Linux | GCC13 ordinary and ASan/UBSan/leak detection; Clang23 ordinary and ASan/UBSan/leak detection | Ordinary LLVM verification, Wasm/Node/Wasmtime, production runtime package; all five named storage neighbors and original origin query |
| Darwin | Apple Clang ordinary; Homebrew Clang23 ordinary and ASan/UBSan/leak detection | The same ordinary LLVM/Wasm/package and named neighbors |

Each native configuration runs O0/O2 with and without allocator observation.
The included runtime follows the selected compiler instrumentation. Query common
objects are ordinary setup providers; I do not call them fully instrumented.
Packaged runtime IR uses its canonical ordinary O2 generator; the native caller
fixture uses sanitizer flags, which does not instrument that linked IR.
`LSAN_OPTIONS` is explicitly empty.

Each observed native run reports 89,182 assertions and status zero. Its final
tracker reports 9,629 allocation requests, 9,628 successful backend allocations,
9,628 frees, zero live requested bytes, and 4,258 peak requested bytes. The
unobserved native run reports 87,908 assertions. These are requested payload
bytes, not allocator metadata or process RSS. Seven fault families measure their
actual allocation positions, fail each position once and persistently, then
perform fresh recovery. I retain each position, status and cleanup counter.

My Wasm O0/O2 observed/unobserved fixtures verify LLVM IR, have no imports, and
link with a 4 MiB maximum memory. Each entry exercises the full 1,024-entry
lifecycle; Node uses three fresh instances and Wasmtime a separate instance.
Production package controls verify source hashes and absence of testing-hook
symbols, then link O0/O2 callers against canonical generated native/Wasm runtime
IR. The old adapter neighbor separately retains its original 256/257 boundary
controls. No old assertions or fixture programs were removed.

## My preserved first terminals

1. At `3c0b12ed7`, Linux setup exits 2 in 0.114 seconds because my external
   driver requested nonexistent `nanoisa`, rather than `nanoisa_dump`. No fixture
   executes. Concurrent canonical generation changes only
   `src/nanoisa/managed_native_source.h`; both source maps remain sealed. I fix
   the external target and commit the exact generated hook/header synchronization
   before fresh `5f4f` setup.
2. At `5f4f`, Linux's old adapter package method passes, then its native/Wasm
   method stops at a strict Clang GCC14/13 installation warning (1.469 seconds).
   I preserve its log and products. An approved external shlex CC command selects
   GCC13 explicitly; only that failed method and previously unrun neighbors
   execute in a fresh report/artifact directory. No passing method is repeated.

Darwin initially lacked Wasmtime. I privately select the official v43.0.0
AArch64 macOS archive, verify its published SHA256 before extraction, and record
its executable hash and version. I install nothing globally. The selection and
actual executable are retained with Darwin evidence.

## My retained inputs and limits

My frozen roots are `/tmp/nanolang-mixed-counted-qualified-5f4f` on Linux and
puck. My source archive deliberately excludes historical `docs/evidence/**`;
all 3,692 selected source paths are recorded. Final live rehashes match both
source maps, 14 Linux/19 Darwin selected tools, and 253/259 last-phase provider
and artifact entries. My tool maps include the actual Python interpreter,
compilers, SDK selectors, LLVM tools and execution engines.

Phase product maps retain `obj/`, `bin/` and each reported fixture directory.
The separate `lib/libnano_file_runtime.a` hash is a current-only supplement;
I do not infer its historical phase immutability. Setup creates providers, and
neighbor gates may create products; only the explicit source/tool pairs are
claimed equal. The original first-setup source tree remains preserved with its
recorded generated-header delta.

Linux reports and objects remain under `/tmp/nanolang-mixed-counted-linux-seal`.
Darwin's original seal remains at `/tmp/nanolang-mixed-counted-puck-seal` on puck;
its verified local copy is `/tmp/nanolang-mixed-counted-puck-seal-local`. The
51,130,659-byte Darwin archive is
`/tmp/nanolang-mixed-counted-puck-evidence.tar.gz`, SHA256
`47281566df393e01a1eef0232c6c844ed728447da7ad0a45ab220f600ee22697`.
I retain the external sealer, launchers, commands, raw output, bounded cleanup
status, before/after maps and source archives. MAC task621 remains open pending
independent review and actual merge; the full mixed parents remain open.
