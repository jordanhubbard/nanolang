# My final synchronous admission integration

I preserve the independently audited [660/033 constructor and admission seal](affine-private-variants.md)
and original [1f9 transaction qualification](service-admission.md). This supplement
qualifies current shared readers and public package consumers; it does not replace
or relabel earlier sanitizer results.

I integrated actual914/915 main99f390 and916 main7bba in bc45eeae4. Only additive
Make/roadmap conflicts needed resolution. Existing constructor/VM/service changes
retain their reviewed bytes. The incoming declaration projection changes shared
provider code but passes NULL/false through old public routes. Public cyclic File
selection remains explicit. I used the actual canonical assembler-capture flag
recipe. No52b gate ran before the916 integration.

Fresh ordinary Linux/GCC and Darwin/Apple providers each passed43086 constructor
checks and3506 admission checks per switch/computed-goto executable, with actual
macro evidence. Both hosts passed actual callback/FFI/allocation controls, complete
mixed query/admission/heap/lifecycle, owner authority/public ARRAY runtime/refusals,
shared contracts/affine bytecode and the new declaration projection query. The
unchanged full imported emitter passed with its original ten-second shadow
policy. Total command times were20.464s Linux and15.993s Darwin; these include work
outside shadow execution. Output hashes and exact bytes are in current verification
reports. I do not claim cross-host equality, full bootstrap or full-suite closure.

Separate fresh bc45 trees ran the original two-method public cyclic corpus and
real installed package: matching VM/native traces, O0/O2, host/API/CLI refusal and
publication controls, outside-tree native links, public header forms, install and
uninstall. Linux completed99.455s; Darwin74.392s. Package Make intentionally
rebuilds providers and records their products. Only source/named-tool pairs are
claimed immutable in those phases; my ordinary provider trees stay separate.

I preserve every integration stop. Initial public setup failed before fixtures
on Linux's known Clang GCC-selection warning and Darwin's missing opt because my
external launch omitted the existing LLVM selectors. Corrected explicit native
compiler/optimizer selection kept strict diagnostics intact. The first corrected
Darwin launch had a separate pre-phase JSON quoting error; its raw environment and
traceback are retained, and the observed SSH status1 is explicitly an agent
attribution rather than a driver-produced phase terminal. A retained Python
launcher fixes serialization. Darwin then stopped while compiling vm_ffi.c
because the external launch omitted Make's SDK ffi include flag. I forwarded the
exact already retained current Make CFLAGS, kept the completed setup, and ran only
read-only configuration/discovery plus the unreached public corpus. No production
or assertion changed in these corrections. Linux's earlier complete pass remains
its own attribution.

[seal-sha256.json](service-admission-final/seal-sha256.json) seals692 report copies,
8100 deduplicated artifacts totaling1880126291 bytes, and74 equal endpoint pairs.
Linux has40 outer terminal records with one retained failure; Darwin38 with two,
plus the separately retained pre-phase quoting failure. Inner logs keep their own
command outcomes. The downloaded Darwin archive is166052271 bytes, SHA256
`a00bcd79142d5657570e593e3c89e6b9596655b93dc8af20bf3680975db7def8`;
all352 manifest reports and6077 objects were checked after extraction. Current
ordinary selected inputs were rehashed3844 Linux/3836 Darwin. Public current
tracked sources number30319 each, with14/17 named tools. This is not complete
transitive system-tool or SDK closure. Older child runners' deleted temporary
artifacts are not reconstructed as historical products.

Final source review restored only unrelated Make comments damaged by merge-marker
cleanup; every noncomment/nonblank line remains identical to bc45. I then merged
actual918 main367476f0, whose only non-document delta is the two native test
compiler selectors, exact3ef bytes. Neither change relabels earlier execution.
The ready-input map records these boundaries. Independent supplemental review,
actual merge, and fresh complete90-method canonical acceptance with supported
selectors remain required. Full roadmap and task2f criteria stay open.
