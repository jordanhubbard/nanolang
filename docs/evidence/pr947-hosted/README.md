# I retain PR947 hosted outcomes

I observed every check at exact head `81cb6eb500cde09fdc5b77e6b4edde29b33b95f7` reach a terminal. I preserve raw failed/cancelled job logs, the successful ARM strict-example log, four actual verifier ZIPs, exact status/policy queries and lossless hashes in this directory.

My original 90-method suite passed on Linux x64 (190.997s), Linux ARM64 (178.705s), Darwin ARM64 (215.055s), and coverage (196.457s). Each completed all ten ArtifactImports methods, including the previously failing native links. Each later refused the same two verifier corpus sources: declared byte value type in `test_u8_basic`, and missing `list_LexerToken_insert` in `token_value_bytes`. The actual ZIPs each contain 524 CRC-checked members. These remain tasks `task_c6b2a040c1434fc784a9d46c02a4981e` and `task_7b805000dfda4da386b55d4691e8c647`.

My sanitizer job stopped at its unchanged 20-minute whole-step deadline after sixteen FlatRecordEmitter methods passed. I retain that incomplete result under `task_9dd1a4c93be74bc79cb4de808eef0d83`; I do not claim a completed sanitizer or artifact suite. The runner recorded cleanup of make, Python and two compiler processes. Pages build/deploy were cancelled; I do not count cancellation as success. Remaining checks, including both strict-example architectures, passed.

My qualified source/base remains `585a6bc12` / `87e0cef0e`. Actual remote main advanced to `162e7c02f` with the separately reviewed RISC-V register-name table and documentation. A read-only merge-tree comparison is clean; against that main, the only non-documentation difference is the one-line explicit Make CC/LDFLAGS propagation. I retain actual main separately from the PR API's older base field. The active main rules require a PR, prohibit deletion/non-fast-forward, and do not require status checks; `gh pr checks --required` reports none. Normal guarded merging needs no administrative bypass. This is bounded Make propagation evidence, not green CI or release acceptance.
