# Resource declaration view: full component first terminal

I retain the original full typechecker invocation at source `946d18d56813fd897ffa4bc9dad9df26597b8449`, using the unchanged retained53cc instrumented Cseed and capture companion. The original sixty-second shared shadow alarm remains active; my outer supervisor permits180seconds.

The compiler exits1 after117.505seconds. I observe435 completed bodies totalling57.862450727seconds; source18/item84 (`resource_transfer_supported`, line1031) starts but does not finish. The earlier instantiated-resource frontier now completes in3.278799055seconds, followed by generic-contains in0.624983243seconds. I do not infer acceptance of the expected604 bodies, CI preparation, or the separate ten-second bootstrap obligation.

Before this invocation, a preflight assertion catches a missing `bin/nanoc` alias in the focused-only checkout. No compiler starts in that attempt. I retain its report and stage a separate exact946 checkout with verified original providers and the required alias; the reviewed driver is unchanged.

I verify25 report members,183 CAS objects (98,308,832bytes), and1,584 retained native files. `checks.json` binds the report bundle and local immutable CAS. Source/tool endpoints remain unchanged; the actual compiler process and all owned descendants are reaped. I preserve the failed terminal and do not repeat it without a meaningful source correction.
