# I qualify evaluator-owned union roots

I retain exact3917d61dc16bb1b9e3821db679280537b9382acc fresh ordinary and
ASan/UBSan/LSan provider builds and the original owning method on Linux and
Darwin. All four methods pass with source/provider/tool endpoint equality and
no remaining process groups. Original assertions and command bounds stay intact.

Additive controls cover every checked constructor allocation prefix and recovery,
shared aliases and borrowed leaves, actual parsed union and local-return lifetime,
an explicitly constructed dotted-literal AST adapter, actual result_map copied
string/record/tuple/callable snapshots, and a registered union held by a completed
contextual task through explicit release. Raw create_union remains caller-owned.

The archives retain raw reports and identity maps. Mapped executables remain in
persistent qualification directories and are not embedded here. This focused
qualification does not claim complete evaluator leak closure, collection registry
ownership, installed canonical SDK acceptance or release readiness.
