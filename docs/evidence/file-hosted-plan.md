# My private serialized File hosted-plan checkpoint

I qualify the first nonexecuting stage of [my hosted conjunction](../NANOISA_FILE_HOSTED_CONJUNCTION.md), task `task_6fc967db5ee6921f84d1619ca5fd084c`. My frozen source is `9ab0fcc9f`: production `fa08fa315`, reviewed ambiguous-status correction `915001bfd`, exact bridged CODE containment `6ec9357b9`, and reviewed fixtures `305d9b2b6` plus startup supplement `9ab0fcc9f`.

I prepare owned reports from actual serialized v2 bytes. I retain exact startup selection, nominal/function facts, wire stack declarations and derived abstract storage bounds. I neither execute File bytecode nor grant hosted/public authority. File carrier cleanup, host operations, source producers, loops, indirect calls and richer borrow regions remain required later stages. Full72556/6931 and hosted6fc remain open.

## My frozen first attempts

| Gate | Linux seconds | Darwin seconds |
|---|---:|---:|
| Normal hosted fixtures | 9.447 | 6.951 |
| GCC ASan/UBSan/LSan hosted fixtures | 19.457 | — |
| Clang ASan/UBSan/LSan hosted fixtures | 13.041 | 10.259 |
| Body/code/flow/nominal/service/ISA adjacency | 8.504 | 9.649 |
| Fresh provider/nano_virt build | 7.668 | 3.914 |
| C-seed build target | 0.698 | 0.632 |
| Actual NanoISA module compilation | 8.839 | 8.373 |
| Module execution | 0.002 | 0.261 |
| Ordinary wrapper generation | 0.170 | 0.412 |
| Ordinary wrapper execution | 0.002 | 0.278 |

Every listed command returned zero on its first frozen attempt. Each hosted instrumented fixture reports 598,191 checks and each directly linked fixture 2,518 checks. Full-chain failure sweeps retain 55 precise MEMORY and 380 ambiguous UNRESOLVED prefixes. Single-failure sweeps retain 434 refusals and one complete recovered plan, followed by the uninjected successful terminal. All failures preserve output, free temporary storage, and permit immediate recovery. Every measured old/new allocation peak stays within the report's conservative bound.

I preserve the successful transient recovery rather than claiming every allocation failure must become a query failure: the legacy preliminary bridge can silently lose CODE, while the final bridge recovers and yields the complete original bytes. This attribution follows static control flow: metadata/debug allocation failures return errors, and the reader creates then discards a preliminary bridge before my final bridge. The retained fixture output reports aggregate counts, not the successful numeric prefix or a per-allocation call trace. My private final bridge size/content check contains partial publication before body analysis. Shared converter defect `task_d34bb03dee3b404508faf8006b1fceef` remains separate.

Startup controls cover accepted INT/BOOL entries; parameter, VOID and owned entry refusals; selected zero-argument scalar and owned initializer refusals; first initializer order and later ignored names; embedded NUL rejection; required feature/envelope checks; exact wire stack depths; and initializer-dominant sequential bounds (23 slots versus entry's 14, with startup 23). Nominal permutations, borrowed input independence, copied getters, unchanged public verifier/native refusal, and pending runtime obligations remain asserted.

## My evidence boundary

Linux runs in `/home/jkh/Src/nanolang-file-hosted-qualified`; Darwin runs in `/tmp/nanolang-file-hosted-9ab` on `jordanh@CXWWHGGJX0.local`. The Darwin snapshot comes from the exact source archive and force-adds all tracked paths, including normally ignored repository files. All four source inventories contain 7,179 files and their before/after maps match. Current qualified sources also match. Nine Linux and ten Darwin actual tool inventories match before/after/current hashes.

Normal Darwin compilation selects the actual Xcode Clang path and SDKROOT; sanitizer compilation selects Homebrew LLVM explicitly. Linux Clang alone receives its established GCC installation flag. Both sanitizer runs retain strict warnings and ASan/UBSan with leak detection. `tests/test_file_hosted.py` names the rebuilt allocator providers; other linked ISA objects retain normal build flags. I archive actual per-provider objects and both fixture binaries after every phase, plus the real module/wrapper artifacts. This is not a whole-program sanitizer claim for unrelated objects.

Fresh C-seed/provider closure is separately timed after the query gates. The provider build already builds required C-seed objects; its later C-seed target may be incremental. I make no Stage1/Stage2 bootstrap or fixed-point claim. Ordinary module/wrapper execution does not execute File service handlers.

My seal contains 69 reports, 2,691 archived phase entries, 623 distinct retained artifact files and eight equal before/after inventory pairs. Both platform source maps are exactly equal. Initial redundant archive packaging was stopped; a completed distinct-artifact archive supplied the same indexed files, all rehashed locally. Packaging did not modify or repeat qualification.

[My sealed report manifest](file-hosted-plan/report-sha256.json) covers commands, results, logs, complete source/tool inventories, artifact indexes and current verification. The content-addressed archive remains at `/tmp/nanolang-file-hosted-plan-artifacts`. Source and tool identities, rather than elapsed times, define these acceptance pins.

My separate ready tree integrates canonical852 `c4529256252e6cb45e63c9eb967101821de297b1`
at merge `1775cc782`. Only independent fixture/document changes arrive; Make
adds the canonical owned-array bit target. Source/modules and all hosted
production/fixtures remain byte-identical to the qualified pin. I preserve the
original qualified and sealed trees and do not relabel this additive integration
as a new bootstrap or repeat unchanged gates.

I subsequently integrate canonical850 `8ddba93` separately. Its independent
reconstruction script and fixtures change; all source/modules and hosted
fixtures remain exactly equal to9ab. The Make delta selects that independent
reconstruction test. I do not repeat unaffected hosted gates.

GitGuardian check105870307952 flags three inherited SHA256 inventory values
in `owned-array-bits-first.json` at merge1775 lines126/144/145. I recompute each
against the retained LexerToken/token/helper object named at that exact line.
All three match. My sealed classification exposes locations and results, not
values; I neither suppress the scanner nor claim its failed check is green.
