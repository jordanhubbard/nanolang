# I qualify private conditional owner ARRAY origins

I qualify productiona70132a65 plus reviewed exact-location correction7661a90e5 at frozen779dcf8b9a70894d1c75ada3af6faec57f407f3f. I integrate canonicalc2b9fb520 before building. I preserve original descriptive query824 and expose only private origin facts; I grant no affine/scalar/runtime permission and leave430220 open for authority/runtime and full paired source acceptance.

My first frozen setup passes in9.409seconds, followed by nine successful phases: GCC normal, GCC sanitizer, Clang18 sanitizer, owned-array descriptors, retained layouts, managed record plans, mixed layout views, mixed FLOAT proof and mixed Samples composition. Each new query phase passes865 assertions and75 allocation-prefix failure cases before success. No phase failed; no production or fixture changed during this run.

I check exact instruction locations against an independent decode walk, including RET rows in helpers. I distinguish reachable conditional helper summaries, unreachable conditional nominal peers and fully concrete entry obligations. Nested relay/factory results, distinct two-level versus direct nominal paths, shared ordinary aliases, branch/loop origin joins, reversed nested consumption, STRING siblings and empty owners are queried without pending-module execution. Refusals include absent initialization, wrong element writes, unmodeled ordinary construction/order, live observations, incompatible result/local nominals, recursive calls, whole-root borrowing, service claims, header/entry boundaries and concrete-site/instruction budgets. I do not claim exhaustive combinations of every storage/work limit.

I verify all1,736 source and157 tool/object hashes before/after with no differences. The [machine-readable seal](owned-array-origin-query.json) retains command logs, runner, source archive and three query executables/object sets. I use actual GCC13 and Clang18.1.3 on Linux. Both sanitizer phases instrument the fixture and retained-layout/layout-decoder/cursor translation units with ASan/UBSan and leak detection; other linked provider objects retain ordinary GCC instrumentation. This is not whole-program sanitizer coverage.

Existing provider adjacency preserves its existing qualified behavior and checks the new isa_decode/isa_get_info dependency from retained_layouts. None of these results admits owned ARRAY layouts to a public verifier, source producer, VM, native emitter or service path. Original module code/layout/ownership bytes survive allocation-prefix failure unchanged, and getter/output sentinels remain intact. The result owns its summary facts after its input module is freed.
