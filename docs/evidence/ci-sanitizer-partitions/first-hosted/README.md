# I retain my first actual partitioned sanitizer run

At exact7f9, CI35679409728 is terminal failure. My inventory passes; dedicated Forth, source and negative workers pass. All fourteen remainder workers fail, and the aggregate correctly rejects the run. No worker reaches its outer twenty-minute test limit in this run. This does not erase the earlier local Forth1200-second or source120-second terminals.

The raw jobs distinguish stale binary64 refusal (units00/02), true compiler bootstrap shadow60-second stops (01/03/10/13), empty array_push receiver provenance (04), missing bootstrap compiler prerequisite (05), absent LLVM opt (06), actual compiler LSan findings (07), missing origin link sanitizer flags (08), original byte/list verifier corpus failures (09), the same two compile failures during dispatch equivalence174/176 (11), and missing nvm2c prerequisite (12). I retain original assertions and deadlines while repairing these boundaries. The bootstrap shadow cause remains unassigned pending comparison with independently qualified native signed string corrections; I do not assume it is just workload size.

This bundle seals raw job logs, exact terminal snapshots and hosted artifact metadata. Provider/report ZIP downloads continue separately: nineteen complete ZIPs were verified before one139MB download stopped after111,951,872bytes in180seconds. Its partial bytes remain retained; subsequent transport uses a measured600-second bound and fresh paths. These are transfer bounds, not relaxed compiler or test limits. No complete hosted artifact-closure claim is made here.
