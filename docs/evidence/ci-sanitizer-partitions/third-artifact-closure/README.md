# My third hosted artifact closure

I retain all39 selected sanitizer and verifier-corpus ZIPs from run35690205396, totaling1,855,748,556 bytes and21,166 members. I verify exact selected artifact IDs against the retained API inventory, every archive digest and byte count, and every member digest and length. My compressed manifest records the durable local paths. This closes artifact retention, not the failed product gates at head82c30f5ccf6ee95ec0bf70e05bca1ad6c25007ed and its actual equal-tree mergec2c813c291d28f9cfc5c58807ffae5bff84f5833.

The first units08 result transfer fails after30.406 seconds while connecting to the artifact blob endpoint. I retain that partial file and terminal separately. A fresh-directory continuation rehashes completed archives and downloads only missing artifacts with the original600-second transport bound. No product check is retried. My transport status inventory preserves both attempts; the prior third-hosted seal preserves all actual job failures.
