# I retain my first isolated sanitizer workers

I ran fresh source `787ffceb15c873d840c2d817c493cba3ee734c0e` with the original compiler/runtime instrumentation, bootstrap, flags, shadow limit, and worker limits. Both sanitizer compiler builds pass in about268seconds. These are terminal qualification observations, not passing product acceptance.

My source worker completes89 of90 methods. The global-ownership method stops at its first command: compiling `src_nano/nanoisa_emit.nano` with `nano_virt`, before the test compares emitter output or executes its generated ownership program. Its original120-second Python subprocess limit expires. The enclosing test phase returns2 after497.425seconds without reaching its1200-second bound. `nano_virt` performs parse/import/typecheck, foreign-module preparation, shadow compilation/verification/execution, then final code generation; the retained command alone does not identify which internal phase exceeded the parent limit. Inner temporary files were removed by the original fixture; I do not reconstruct them or claim their retention. This remains `task_e81ebb92533d4ada91e64ecfe2c7c1cf`.

My Forth worker starts its actual session executable about2seconds after test-step launch, then reaches its unchanged1200-second supervision deadline. The terminal is124 with a measured1200.159seconds. The retained process inventory establishes execution rather than a1200-second compilation. Buffered output does not identify the last internal assertion. This remains `task_4605a913b21847f19101e5706851c5f2`; it is distinct from the earlier hosted whole-suite deadline and source compiler-child timeout.

Both leaders are reaped, process groups disappear, nested remaining-process lists are empty, and cleanup reports no errors. Source/tool maps remain unchanged. My lossless bundles contain commands, raw logs, process/terminal reports, source/tool/product maps, original supervisor and qualification driver, and worker manifest/results. Separate object indexes name the retained immutable local CAS bytes and hashes. I verified every indexed object's SHA256 and every decompressed bundle member. The original source trees, executables and CAS remain in `/home/jkh/nanolang-qualification/ci-sanitizer-787-{forth,source}` and corresponding `-reports` directories.

Independent review corrected the ordinary parallel-Make tail ordering at `3a55ffb2f` before hosted execution. Nine scheduler controls pass, including observed parallel ordering. I retain original787 product attribution and do not replay either failed workload for this graph-only correction. The hosted candidate requires all16 complete unit partitions and the unchanged negative suite; these local failures are not excluded from acceptance.
