# I retain every terminal from my corrected hosted partition run

Run35684809706 is terminal failure. My PR head is fb913d5a6ae81a08c2f5f448cc30d147bfb36711; the actual checkout is GitHub merge27a62e96c5352438cf3139e97658a01b9b9817fa. Both have tree631995fa50c78923f001928db9542f5323987b2d. My lossless bundle retains218 top-level raw logs, snapshots and command statuses, including all23 selected job logs. Provider ZIP transfer is separate and is not claimed complete by this seal.

My source, Forth, negative, units01 and units08 sanitizer workers pass. My inventory and aggregate account for every original obligation; the aggregate fails because other required workers fail. Both strict-example architectures pass in this run. I do not reuse those passes as proof for a later head.

| Worker | Actual first terminal |
| --- | --- |
| units00 | Explicit generic constructors: resource-payload source-borrow refusal, one failure in15 methods. Existing full ownership scope remains required. |
| units02 | Map int/string source controls: LSan16bytes/two objects each from the owned local-name overwrite. |
| units03,06,10,12,13 | Original bootstrap1 shadow60-second deadline. units06 progresses beyond missing LLVM setup; units12 reaches the actual bootstrap before its source corpus. |
| units04 | Empty array_push receiver lacks nominal record inference in both original compiler controls. |
| units05 | Managed array-shape and graph-origin setup links omit sanitizer runtime flags for actual instrumented providers. Both classes fail before assertions. |
| units07 | Native float-record source compilation reports distinct local-name, parser, checker field/registry and mixed helper-result lifetime leaks. |
| units09 | Original verifier corpus retains byte/list source compile failures. |
| units11 | Original dispatch equivalence retains the same two source compile failures. |

The three ordinary hosted platforms and coverage also retain the original byte/list compile failures. My raw terminal evidence is authoritative; a passing retry does not erase any earlier failure. I preserve the open bootstrap, source, ownership and release tasks without lowering limits or removing assertions.
