# My private matched cyclic File dispatch qualification

I qualify the private VM adapter and genuine generated native C functions at
production `4a12db5e0`. I do not activate cyclic public byte APIs, installed entry
points, source lowering, closed-indirect calls, or richer borrowed signatures.
Task `15a92` remains open for its later complete conjunction and publication.

My final Linux fixture is `6a65b87ff`; Darwin uses `100c5a1fd`. The sole fixture
code difference is a newline between a registry `if` and `return`, needed for
GCC's existing strict warning policy. All 22 instrumented emitted programs from
Linux before and after this correction have identical bytes. Production source
blobs are identical in every frozen checkpoint listed in the seal.

I run exactly two new unittest methods per configuration. Linux uses GCC13 and
Clang ordinary plus each compiler's ASan/UBSan/leak configuration. Darwin uses
Apple Clang ordinary, Homebrew Clang ordinary and Homebrew sanitizer. Each runs
actual private VM capture and matching native O0/O2 replay against exact complete
report fields, scalar publication and ordered host events. Normal instrumented
instruction entry, loop edges, calls/returns and witnesses run with zero project
allocation budget; core live-owner/borrow masks are checked before disposal.
I retain finite persistent/transient preparation and emitter allocation sweeps,
unchanged output sentinels and fresh independent recovery after every refusal.

The corpus retains initializer/entry/helper shared fuel at N, N-1 and zero;
zero/one/two/258 loop acquisitions, nested iterations, early return, two owners,
held references, both result arms, denial, assertion, close cleanup and staged
call/return/service generation failures. Native isolation links no VM dispatcher
and checks semantic ABI size/revision, non-first variants, reference and edge
facts before acquisition, plus forced zero-variant label refusal. Unsupported
decoded operations remain refused even when unreachable.

I also retain the old carrier's default main, core values, cyclic query, copied
hosted plan, private VM/native and unchanged public/refusal/archive neighbors.
The archive boundary excludes every actual public archive member from explicit
objects and retains member lists and symbol provenance. On Darwin only the
historical public helper reads verified f160 source through the separate retained
Git directory; it cannot change the frozen source/provider checkout.

Instrumentation is scoped to the explicitly compiled query/carrier/core and
fixture/generated providers. Shared common objects remain ordinarily built and
inventoried; this is not whole-program sanitizer coverage. Retained-byte/root
checks do not establish a measured peak for every preparation allocation.

## My first terminals remain evidence

* `9284` on both hosts compiled no dispatch executable: an inherited broad main
  macro conflicted with the capture entry. `7913` adds a dedicated entry macro;
  the retained carrier still defaults to its original main.
* `7913` on both hosts correctly refused the first malformed fixture before
  acquisition: unreachable final NOP fell outside its decoded body. `100c` adds
  unreachable RET, preserving the zero-variant label and positive instruction
  count; the unsupported-op negative targets the NOP independently.
* Linux `100c` passed its 23-module VM capture and emitter allocation controls,
  then strict registry compilation refused misleading indentation. `6a65`
  changes only fixture-generated whitespace. No failed output was executed.
* A separate `7913` preparation launcher exited before its driver existed.
  The retained launcher record distinguishes this from all setup/test results;
  successful preparation and transfer were awaited before corrected launches.

I retain raw terminal bounds, reaped leader/group cleanup outcomes, commands,
source/tool endpoints and content-addressed artifacts. `report-sha256.json`
seals the committed reports. `artifact-store.json` identifies retained exact
bytes and `artifact-archive.json` seals their standalone archive. Provider
snapshots record setup additions and any public-package rebuilds explicitly;
no whole-build immutability is inferred from stable source/tool maps.
