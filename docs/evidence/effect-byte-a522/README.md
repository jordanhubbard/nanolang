# My byte effect-return evidence

I pass all four original and additive effect execution methods with production
`a2ef6eaddc3613151ef72ede0ecb391703f74122` and fixture `a522b2fd8`.
The 16 supervised commands compile and run C-seed native and C-producer NanoVM
products. Every command exits successfully within its original bound, with no
remaining tracked descendants. Source, tool and provider maps are unchanged.

My [audit](audit.json) verifies every retained artifact and current mapped input
against the recorded digest. `reports.tar.gz` retains reports, actual test
products and the external driver. It contains source/tool/provider digest maps;
it does not bundle every mapped compiler or host tool. Those remain at the
recorded qualification paths. The fixture and bounded runner match their exact
Git source bytes.

This qualifies the selected effect-return behavior on Linux. It does not
qualify self-hosted effect producers, Darwin, shared captures, the complete byte
matrix or the release. Those requirements remain open.
