# I retain every checked indirect File target without executing it

I qualified source and fixtures at `5acd1b370`, including production `a3cd593cb`.
My copied report describes local same-module callable targets and exact common
signatures. It is not an ownership proof, hosted plan, runtime selector or source
compiler acceptance. Task2c135 and the complete call/control parents remain open.

All seven query configurations passed 2726 allocation-instrumented checks and
2067 separately linked checks each: Linux AArch64 GCC13 ordinary and
ASan/UBSan/LSan, Clang23 ordinary and ASan/UBSan/LSan; macOS AArch64 Apple17
ordinary, Homebrew23 ordinary and ASan/UBSan/LSan. Apple LSan is not claimed.
The query translation unit and included nominal-plan provider are rebuilt with
instrumentation in those configurations; other linked providers remain ordinary.
I do not describe this as a fully sanitized compiler or VM.

The fixtures inspect complete branch and late-backedge target sets, reached
uninitialized alternatives, local overwrite/copy, incompatible signatures,
wrong counts/indices, unreachable indirect instructions, direct plus indirect
recursion, unused-body recursion, exact size refusals, getter sentinels and
input-destroyed report lifetime. Measured allocation prefixes and single failures
retain failure outputs and recover without tracked live objects. Normal File
category transfers remain covered without executing a File service.

Separate code, body, flow, hosted, opcode-guard and cyclic-query neighbor suites
passed on both hosts. The new fixture does not silently substitute for them.
All 23 setup/configuration/query/neighbor phase terminals are successful. The
first puck extraction command failed before building because system Python did
not support the requested tar extraction filter. I preserve that terminal and
use supported Homebrew Python with the same verified archive in a fresh tree.
No failed product was used as later acceptance evidence.

[My report manifest](file-indirect-targets/report-sha256.json) seals the exact
commands, phase logs, drivers, source/tool maps and retained product references.
The seal has 653 unique artifacts, 331263234 bytes, 4497 artifact references and
46 equal source/tool before-after pairs. Each phase hashes all 11580 tracked
source files. Current final endpoints were also rehashed: Linux 9 selected tools
and 190 product paths; puck 12 selected tools and 195 product paths, with no
mismatch. These are explicit selected tools, not a complete transitive system
library inventory or a claim about intermediate immutability.

The consolidated artifacts remain at `/tmp/nanolang-file-indirect-artifacts`.
The downloaded puck report archive is
`/tmp/nanolang-file-indirect-5acd-puck-reports.tar.gz`, SHA256
`c01e173da70a7c4b708b68775680862415ea70b336c2ac6109cdc02ac340a988`.
The Linux qualification tree and fresh puck tree remain frozen. Candidate
ownership composition, actual VM/native indirect dispatch, callable argument
and result propagation, richer borrowed targets, public installed acceptance,
paired source/shadows and full 5.1 release gates remain required.
