# My sanitizer bootstrap dependency audit

I inspect exact PR948 head `82c30f5ccf6ee95ec0bf70e05bca1ad6c25007ed`. This is read-only dependency and artifact analysis, not another product run. My resolved Make database, complete283-obligation plan, source hashes, target-to-bootstrap paths and fixture compiler references are retained beside this report.

Of283 unit obligations,53 genuinely consume native self-hosted compilers. They occur in13 remainder workers. Forty-seven targets name native stage products in their fixtures; six use the shared GenericAffineIdentity helper, whose default compiler set contains Cseed, Stage1 and Stage2. Forth, source, units07 and negative have no full-native-bootstrap dependency. Replacing these genuine consumer prerequisites with the C-reference stage would remove required producer coverage.

My names distinguish three separate preparations. `stage1` builds C-reference tools. `make build` also builds and executes the three component drivers. `bootstrap`/`bootstrap3` constructs nanoc_stage1 from Cseed, constructs nanoc_stage2 from Stage1, runs smoke controls and checks the established bootstrap boundary. Each worker already runs instrumented `make sanitize` and `make build`;53 selected tests then require full native bootstrap. Production of that same full native compiler graph is duplicated across13 shards, while their subsequent test workloads are distinct.

The new units00/02/05 failures are Cseed supervised shadows before Stage1 publication, under the original60-second limit. The Cseed path first checks retained foreign callback policies and otherwise runs interpreter shadows. Exact failing graph metadata still determines that route. Existing evaluator string length already uses signed nl_cstr_length, so the separately corrected native generated-C strlen mapping is not a demonstrated cause of these particular terminals.

I independently extracted only three products from the retained successful previous fb913 units01 provider ZIP, verified each against its retained member hash, and inspected symbols without execution. Actual nanoc_c has undefined ASan/UBSan symbols; actual nanoc_stage1 and nanoc_stage2 do not. Source inspection agrees: generated C takes injected flags from NANO_CFLAGS, which the current workflow does not set. I therefore describe current coverage as an instrumented Cseed producing and exercising native stages, not as proved instrumentation of every generated compiler body. Exact archive/member hashes and raw llvm-nm output are retained.

A future scheduling change could construct one exact-head instrumented provider closure in a required producer job and distribute verified independent copies to consumers. Such a design must retain all compiler/component/bootstrap assertions, original60-second shadow bounds, all283 test obligations and sanitizer settings; record complete source/tool/runtime/cache closure, modes and symlinks; refuse mismatched inputs; and require the producer result in the final aggregate. It must also preserve a target that actually exercises compiler production, rather than replacing coverage with artifact existence. This would remove duplicate production work. It would not repair a genuine failure in the required producer job. I make no Make/workflow change or timeout proposal in this audit.
