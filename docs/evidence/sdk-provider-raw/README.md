# My raw SDK descriptor codec controls

At df714877f I pass the reviewed raw-codec fixture in Linux GCC ordinary/sanitized and Darwin Apple ordinary/Homebrew sanitized configurations. ASan, UBSan and LSan remain enabled. I retain all three exact input files, tool identities, complete command/status logs and each executable, with byte-length/SHA256 member seals. Source and selected compiler identities match before/after. All child groups are absent and no deadline fired.

These controls establish bounded raw row transport, owned copying, by-value accessors, reserved/slice/count refusals and unchanged-output allocation failures with recovery. They do not establish module cross-validation, shared typed ownership revision, immutable-generation attachment, provider ABI execution or installed SDK acceptance. The raw codec remains unlinked from product consumers and the new feature is not admitted.
