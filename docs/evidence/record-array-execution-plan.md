# My copied mixed record-array execution-plan qualification

I qualify non-admitting preparation at `c032c0a21730b6abca4ce71add2048c96cbb36d9`.
My original preparation source is `c39caf591`; reviewed `a75a90f8a` refuses
embedded NUL function names before analysis. Reviewed `429da44bc` distinguishes
actual mixed-layout allocation failure through a separate private detail bit;
`c032c0a21` adds the fixture's missing defining header only. No runtime, emitter,
public selector or source compiler calls this plan API.

All seven selected query configurations passed. Each runs linked and observed
forms of the same C fixture, retaining the unchanged old origin corpus:

| Host | Selected compiler/configuration | Linked / observed checks |
| --- | --- | --- |
| Linux | GCC13.3 ordinary |2792 /6377 |
| Linux | Clang23 ordinary, explicit GCC13 support |2792 /6377 |
| Linux | GCC13.3 ASan/UBSan, leak detection |2792 /6377 |
| Linux | Clang23 ASan/UBSan, leak detection, GCC13 support |2792 /6377 |
| Darwin/puck | Apple Clang17 ordinary |2792 /6377 |
| Darwin/puck | Homebrew Clang23.1.1 ordinary |2792 /6377 |
| Darwin/puck | Homebrew Clang23.1.1 ASan/UBSan, leak detection |2792 /6377 |

The observer measures94 combined-plan allocation positions and74382 simultaneous
requested payload bytes. Every injected one-shot and persistent failure returns
exactly MEMORY, preserves input/output, leaves zero tracked live objects/bytes
and independently recovers. The inherited origin corpus separately measures40
positions and56546 payload bytes. These are requested payload measurements;
allocator metadata and ordinary linked providers are outside the measurement.
The observed wrapper plus six separately rebuilt dependency translation units
cover seven selected query/verifier/declaration providers. The unobserved form
rebuilds those same seven without hooks. Other linked objects remain ordinary;
I do not claim complete-program sanitizer instrumentation.

My copied facts survive original module destruction, external counted string,
metadata and debug mutation, and destruction of a second independently owned
plan. I cover all five flat element tags, interleaved unions, normal initializer
and exact counted-NUL function refusal, copied parameters/empty rows, calls,
globals, branch joins and a decoded backedge. All getter failures preserve output.
Actual256/257 function and4096/4097 string boundaries remain distinct from the
1024 future dynamic-frame obligation. All92 opcode recipes and their immediate,
stack-effect and edge rows are checked; arbitrary isolated rows and65536/65537
row guards are decoder-only controls, not new whole-query admission claims.

Both hosts also pass the unchanged ownership contracts, ordinary record
contracts, ordinary array authority, affine scalar unions, complete declaration
projection, public ownership target and v2 conversion/end-to-end neighbors.
The additional actual `test_nvm_v2_layouts` and private layout fixture pass with
the old wrappers and symbol-renamed embedded fault decoder. These controls do
not broaden the unchanged supported query or public runtime routes.

## My retained first terminals

1. Linux `16767e0c1`: fresh setup, configuration and one-method discovery passed;
   the linked fixture passed2792. The observed old corpus and new opcode checks
   passed, then allocation18 returned UNRESOLVED instead of MEMORY. Static review
   identified the old private decoder's documented TRUNCATED/OOM conflation.
   I preserved it and added an explicit detailed result rather than weakening
   the new exact MEMORY assertion. No Darwin gate was launched at this pin.
2. Linux and Darwin `429da44bc`: fresh setup and linked2792 controls passed;
   the instrumented fixture failed strict compilation because its new direct
   calls lacked `ownership_layouts_private.h`. No corrected fault sweep ran at
   this pin. The one-line header correction preserved source and predicates.
3. Fresh `c032c0a21` providers and all corrected phases passed. I retain all
   preceding products/logs; no failed output was executed or relabeled.

## My retained evidence and limits

[The seal](record-array-execution-plan/seal-sha256.json) lists577 actual report
files; the aggregate index retains1240 unique objects,433262533 bytes and19073
references. All114 source/tool before-after pairs match. There are57 outer
terminals: three preserved failures and54 successes, including45 final corrected
phases across the two hosts. Reports include raw bounded commands, actual driver
Python identity, selected compilers, Darwin Xcode/SDK tools, provider maps and
inner fixture artifacts. Every current frozen source/tool/last-product map was
rehashed by the sealer; source maps contain3700 tracked non-evidence files each.
Historical `docs/evidence/**` is explicitly excluded from source-only archives.

Linux reports/objects remain at `/tmp/nanolang-record-execution-linux-seal`;
Darwin reports/objects remain on puck at `/tmp/nanolang-record-execution-puck-seal`
and locally at `/tmp/nanolang-record-execution-puck-seal-local`. The independently
copied Darwin archive `/tmp/nanolang-record-execution-puck-evidence.tar.gz` is
73179026 bytes with SHA256
`cceab6b9920d09a3a4fdfd69fea1c70c9b61ce68de7d66178f2adeb8ca6992c1`.
Qualified trees `/tmp/nanolang-record-execution-qualified-c032c` remain on both
hosts. The earlier16767 Linux and429da both-host trees also remain unchanged.

I preserve exact current provider/source correspondence and phase-built objects,
not an unmeasured promise that every transitive OS library was immutable. The
setup-built public runtime archive is not separately claimed as a historically
hashed archive; selected fixture links and retained binaries/objects are the
measured route. No bootstrap, complete product suite, generated mixed graph
execution or public/source acceptance follows from this query-only milestone.
Full f36/15f/488 parents and subsequent counted VM/native/LLVM/Wasm, admission,
paired source and richer graph requirements remain open.
