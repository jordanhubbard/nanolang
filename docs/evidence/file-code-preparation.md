# My private File CODE preparation checkpoint

I qualify an independently owned declaration/decode/DAG plan, not a stack,
owner, refinement, exit or callee-body certificate. No public consumer uses this
API and no File instruction or host service executes. Task546cf and the full
File72556/6931 parents remain open for dependent checks and execution/source
acceptance. Wrapper89b8 alone is reconciled from actual PR841 mergefd56e78a9.

My reviewed production is9c124c9ff plus07f49e8e8. I freeze fixtures and the logical
entry clarification atc31f60075. Later transfer/body documentation is a proposal,
not implementation or qualification. The plan copies exact declarations and
CODE, checks complete disjoint function coverage and instruction boundaries,
retains exact operand/catalog identities, and orders all acyclic CFG/call nodes.
Success explicitly does not establish hosted HAS_MAIN/initializer behavior.

| First attempt atc31 | Linux | Darwin |
|---|---|---|
| Normal selected compiler, instrumented and linked preparation | PASS5.615s | PASS4.020s |
| GCC / Homebrew Clang strict sanitizer methods | PASS4.075s | PASS4.676s |
| Additional Linux Clang strict sanitizer methods | PASS3.792s | Not selected |
| Existing flow, nominal, service and ISA adjacency | PASS4.674s | PASS8.007s |
| Fresh nano_virt provider | PASS7.622s | PASS4.201s |
| Fresh C seed | PASS0.732s | PASS1.395s |
| Actual NanoISA module compilation and execution | PASS8.441s /0.001s | PASS17.599s /0.513s |
| Actual ordinary wrapper compilation and execution | PASS0.157s /0.002s | PASS0.439s /0.304s |

Every preparation build reports2409 instrumented or1817 linked checks. The
instrumented fixture includes the nominal plan and flow/CODE preparation
production under counted allocator hooks, checking all five allocation-failure
prefixes, untouched output sentinels, complete cleanup and immediate recovery.
The linked fixture consumes independent provider objects. Sanitizer harnesses
compile those private providers plus nominal/catalog code under ASan/UBSan,
Werror and LSan1; I do not claim all linked neighboring objects are instrumented.

Controls cover malformed and unreachable operands, truncated instructions,
wrong function/result declarations, overlapping/out-of-range and reordered
extents, unsupported uncovered bytes, branch interiors/extremes, reachable and
unreachable CFG cycles, self/mutual call cycles, call ordering, exact permuted
catalog mappings, borrowed-call shapes, local/reference indices, input lifetime,
getter failure preservation and64-function/256-local/256-instruction/4096-total
limits. A deliberately wrong owner-return body still prepares, demonstrating
that this object is not a transfer certificate. HAS_MAIN-cleared and named
initializer cases likewise establish only the documented logical preparation
scope, never hosted entry approval.

Both platforms pass their first attempts; I retain those exact terminals without
retries. Linux Clang's native GCC13 selection, actual compiler paths and Darwin
SDK/libcrypto configuration are explicit. Darwin uses an isolated archive with
synthetic Git snapshot metadata for schema drift checks; its source hashes equal
the canonical c31 source snapshot. The synthetic commit is separately recorded
and is not canonical ancestry. No Stage1/2 bootstrap or release gate is claimed.

My [manifest](file-code-preparation/report-sha256.json) seals69 reports. The
[artifact index](file-code-preparation/artifact-index.json) retains2263 entries
and350 unique files under `/tmp/nanolang-file-code-preparation-artifacts`, including
actual linked/sanitizer fixtures and each provider/module/wrapper phase. My
[inventory pairs](file-code-preparation/inventory-pairs.json) record8 equal
before/after pairs:6763 source files per scope,9 Linux tools and10 Darwin tools.
The frozen Linux tree is `/home/jkh/Src/nanolang-file-code-preparation-qualified`;
Darwin retains `/tmp/nanolang-file-code-c31` on `jordanh@CXWWHGGJX0.local`.
