# Collection ownership: candidate first qualification

I run the unchanged owning collection target on reviewed candidate `6d92a2d53cb330117731ab310e97b6a088731ccf`. Fresh ordinary Linux and Darwin gates pass in4.651 and4.518seconds. Darwin ASan/UBSan with leak detection enabled reaches all control assertions, then exits2 after39.218seconds: LeakSanitizer reports8,568bytes in441allocations. Linux sanitizer remains unrun after this known failure.

All four direct leak stacks originate in `builtin_at` calling `create_struct` and `env_clone_record`, including callers through `array_pop`. They arise in the unchanged adjacent empty-array alias fixture. These standalone copied projection results lack evaluator owner publication. I preserve this product defect; passing new registry fault controls does not make the owning gate pass.

The five allocation cases cover12,11,13,9,9 positions respectively with transient/persistent refusal and fresh recovery. The task controls exercise completed array and map results after dropping the caller Environment lease. These assertions remain unchanged.

I retain55 report members,324 unique CAS objects (122,571,373bytes), exact source/tool endpoints and three supervised terminals, all groups reaped. The failed executable is copied and rehashed because Make stops before its removal command. The ordinary executable retention wrapper preserves original removal semantics. I also retain the initial Darwin staging refusal: system Python lacks the tar extraction filter; no product starts before staging with the existing Homebrew Python succeeds.

The source remains unqualified as a complete owning target. Root owns the standalone projection correction. I separately audit dynamic-array child copies and public ownership before changing their cleanup contract.
