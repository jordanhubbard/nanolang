# My standard C union payload evidence

I qualify named payload task_f5782cbcbe2d45fc8ac7f04b4f05b4e4 and precise terminal prerequisite task_b0351cb144594a3ca8a66a415e903242 at frozen source/harness `9ec4a5736414562fc7a39f35c976e9c5889422e8`. My reviewed production checkpoints are `9dafc96d` and `6db93a4c`. My [contract](../PUBLIC_C_MATCH_PAYLOAD_CONTRACT.md) precedes each change. My [machine evidence](public-c-match-payloads.json) records 25 source/provider/harness/tool hashes, two compiler hashes and seven logs; I verified all identities after every gate terminated. Final publication only adds documentation.

I replace GNU typeof payload copies with private types resolved from exact union declaration and variant indexes. Union members and binding copies use the same type. I preserve field order/types and tag order, skip nonexistent payload copies for empty/discard bindings, provide private storage for all-empty unions, and keep the once-evaluated scrutinee in a collision-free generated namespace. This does not admit generic/heap/foreign layout forms.

I append a checked diagnostic/exit else only when one exact union has nonempty, complete, unique, unguarded variant coverage. Unknown tags never select the final variant by fallback. Partial, guarded, duplicate, unknown and wildcard cases do not acquire this terminal assumption. I do not execute invalid runtime tags in qualification.

| Final gate | Result |
|---|---|
| GCC match4 and unchanged adjacent28 | 32 pass, 14.512s |
| Clang match4 and unchanged adjacent28 | 32 pass, 19.634s |
| Unchanged public C backend programs | 7/7 pass |

My ordinary fixtures cover exact INT/BOOL/FLOAT/STRING payload observers, multi/single/empty fields, nested lexical bindings, discard preserving an outer underscore binding, once evaluation, old temporary/private type-name collisions, all-empty unions and distinct declarations sharing variant spellings. Actual public C99/C11 output runs at O0/O2 with ASan/UBSan and default Linux leak checking. The same ordinary fixtures pass interpreter and verified VM execution. Existing05_match and06_single_field_variant remain unchanged and now also pass strict portable compilation with return-type errors enabled. Existing scalar provider, string ownership, conversion, concat and publication controls pass unchanged.

Direct API controls check exact declaration/variant lookup, missing/unknown/wildcard checked refusal, first diagnostic, path/stream preservation and later valid recovery. Static predicate controls cover reordered complete variants and partial/guarded/duplicate/unknown/wildcard/zero-arm boundaries; guarded emitted output does not receive the terminal diagnostic.

I retain initial GCC/Clang logs: three new methods passed, while both unchanged match programs stopped at strict C compilation because the original if/else-if chain did not express exhaustive control flow. No rejected binary ran. I recorded the precise terminal prerequisite, obtained review, rebuilt the affected compiler and froze again before the final passing integration.

Required guard-before-payload/wildcard semantics remain task7a99; wildcard currently receives a checked exact-variant refusal rather than invalid generated TAG__. Exact union-valued function results remain task8106. Expression-block control-flow lifting, advertised options/captures and full public6ade acceptance remain open. I make no bootstrap, Darwin or complete target claim. Canonical merge precedes child reconciliation.
