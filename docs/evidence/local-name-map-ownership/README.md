# I retain my local nominal-name overwrite qualification

I build fresh providers from44d4eaf4f, which contains the reviewed9919b90da allocate-before-replace correction, and run the unchanged owning test-map-declared-tags target. All seven original declared-map/native-global methods pass with ordinary providers in17.351seconds and GCC ASan/UBSan providers with LSan enabled in68.857seconds. The original fixture retains its60/90-second subprocess bounds and actual generated-native sanitizer flags. My outer command bound remains1200seconds.

My two lossless bundles retain32 reports, four equal source/tool pairs, two successful reaped/group-absent terminals and346 separately indexed provider CAS objects. I rehash all CAS bytes and every decompressed report member before sealing. Original fixture TemporaryDirectory cleanup removes its inner generated products; I do not claim those deleted files are retained or reconstruct them as originals. Fresh compiler/runtime executables and object providers are retained.

This gate isolates the existing local-name overwrite reported by hosted int/string map fixtures. It does not qualify the distinct parser, field metadata or mixed get_struct_type_name result ownership leaks found in native-float-records. Those known failing paths are not replayed. I do not close full compiler ownership or release scope from this bounded pass.
