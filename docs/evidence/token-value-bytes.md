# My token decoded-byte-count prerequisite

I qualify the bounded token metadata prerequisite for paired File source work.
I do not qualify service AST/lowering, generated File behavior shadows, source
publication, paired recoverable allocation failure, or full File acceptance.
My parent8bbc and full6fc/72556/6931/d03c requirements remain open.

## My source and acceptance boundary

My production starts at6e393e173, with reviewed schema-generator alignment at
28e20deeb, inactive bridge accessor correction7bf8e208a, and generator corrections
d285eee0c/30340f0ac/ae62ddc27/315656d93. My final source/fixture pin is315656d93.
I retain raw token text and an int64 decoded byte count, including escaped NUL
suffixes. This changes my internal by-value LexerToken ABI; I rebuilt my original
compiler/runtime/module closure. My C lexer has no raw grammar-body path; that
control is explicitly Nano-only. I make no new string-runtime NUL guarantee.

I retain original28e fresh bootstrap on Linux and puck. Later corrected trees
copy exactly310 Linux/309 puck products after comparing620 source inputs. The
only permitted production input differences are inactive lexer_bridge.c and,
fromd285 onward, the generator source freshly compiled by each producer. I
compare every other src/src_nano/schema/scripts input, all four canonical
generated files, actual compiled dependency files, and copied product hashes.
No old module cache is copied. These facts and exact paths are in each
bootstrap-reuse.json; they do not label later source as the original tree.

I qualify all seven C configurations: retained7bf GCC and Apple ordinary C
fixture/f-string passes, then315 Linux Clang ordinary/GCC sanitizer/Clang
sanitizer and puck Homebrew ordinary/sanitizer passes. My sanitizer scope is the
five freshly compiled C providers, fixture, and included bridge, not the full
compiler or generated Nano products. ASan leak detection and UBSan halt-on-error
are enabled; LSAN_OPTIONS is explicitly empty. My exact compiler commands,
versions, Linux GCC13 installation selection, Darwin SDK and owning Apple
compiler, selected runtime libraries and libffi headers/library are retained.
I do not claim a complete transitive system toolchain inventory.

At315, all four ordinary configurations separately pass all three producers:
C seed, Stage1, and Stage2. Each token program has the exact76 selected shadows
and exact nine output rows. Each actual Nano generator has the exact63 selected
shadows, then runs its main in an isolated schema/output directory. I compare
all bytes of compiler_schema.h, compiler_schema.nano, compiler_ast.nano, and
compiler_contracts.nano to the committed outputs. The Python main produces the
same four files independently. My seal contains24 exact selected-name reports
and16 complete four-file generation comparisons. I do not replace equality
with normalization or a count-only shadow check.

My parser/module-metadata/wrapper adjacency passes on both hosts. The actual
Make dependency closure also rebuilds bootstrap stages during this phase
(272.286 seconds Linux;276.530 seconds puck). I preserve before/after product
maps and logs: final nanoc/Stage1/Stage2 binaries differ from the copies used by
the earlier paired phase. I do not relabel these final binaries as28e copies.
The bootstrap smoke checks pass; the retained binary-difference warning is not
a fixed-point claim. Wrapper negative cases intentionally produce compiler/link
errors before their positive controls; the final outer status is zero.

## My preserved first terminals

| Pin | First terminal and subsequent boundary |
| --- | --- |
| 28e | Both fresh bootstraps pass; ordinary fixture compile refuses inactive bridge access through an opaque list type. No fixture execution precedes the accessor correction. |
| 7bf setup | My copy-proof script overstates Makefile absence: an unbuilt HYBRID_OBJECTS declaration includes lexer_bridge. I retain the original helper and explicitly labeled reconstructed terminal summary, then check actual dependency/object absence instead. |
| 7bf | GCC/Apple C fixture and f-string controls pass. Paired C seed rejects binding generic list_remove as an element because its source signature is void. |
| 083 | C-seed76 shadows pass, then native compile lacks accepted transitive helper prototypes and rejects direct signed/str_length comparisons. Its final temporary C was automatically removed; I retain diagnostics, not a claim of that missing file. |
| ece | C seed passes; Stage1 rejects local grammar as a reserved keyword. Future producer commands now request --keep-c with report-owned TMPDIR. |
| 1bb | All three token producers pass; actual generator typecheck rejects eight integer-returning JSON boolean calls in bool contexts. |
| d285 | Existing generator shadows call extern mutation statements without the required safety boundary. |
| 303 | Generator63 shadows pass; strict native compilation rejects signed index/direct length comparisons in my new ordering helper. |
| ae62 | C-seed generator63 shadows and full four-file equality pass; Stage1 refuses the pre-existing shadow targeting builtin str_starts_with. |
| 315 | Named wrapper retains both assertions and all three actual prefix comparisons. Full paired/generator/remaining C/adjacency gates pass. |

I preserve each first terminal on both hosts and all original frozen trees.
The generator failures are newly exercised pre-existing generator limitations,
except the explicitly identified new ordering helper; I do not call them token
decoder regressions. I did not rerun failed binaries to obtain success. I retain
all assertions, strict compiler warnings, deadlines and complete schema checks.

My full5.1 follow-ups stay open: accepted transitive native call declarations
(task_784f64db9b7443c888242a3b52b9dbe5), signed string-length lowering
(task_50f624351db54305ac31a918e9874010), paired grammar keyword policy
(task_9b6cbac8f6f941e3ad38a0c7f5ff59c9), and paired builtin-shadow target policy
(task_ba64cfbaa3d94de9bf3f7066c3fd3a5d). Explicit local imports/types/helper names
do not repair those broader compiler policies.

## My seal

I retain1556 JSON reports in [report-sha256.json](token-value-bytes/report-sha256.json),
2080 unique artifacts totaling833665658 bytes under
/tmp/nanolang-token-value-bytes-artifacts, and the original reports/trees.
[Acceptance summary](token-value-bytes/audit/acceptance-summary.json) records54
matching source/tool pairs. My nine current Linux and nine current puck roots
each match18170 source files,12 selected tool files and their final product map.
These are postphase current checks; they do not manufacture missing prior maps.

My original Linux root is /home/jkh/Src/nanolang-token-qualified-28e;
corrected roots use /home/jkh/Src/nanolang-token-corrected-{7bf,083,ece,1bb,d285,303,ae62,315}.
The same basenames exist under /tmp on puck.local using the configured SSH user.
Per-scope raw report roots are /tmp/nanolang-token-PIN-{linux,puck}. I retain
hash-verified prior/final puck download archives and every supplied driver/copy
helper. CAS references in original maps retain their original paths; the sealed
local CAS has the same SHA256 filenames. Focused fixture/generated executables,
kept C, objects and persistent endpoint products are retained. I do not claim
all transient supervisor or Make-deleted executables were preserved.

Independent review, canonical integration and actual merge remain pending.
I make no hosted-CI green or full release acceptance claim.

## My independent root audit

My [independent audit](token-value-bytes-independent-review.json) verifies1556
actual committed report blobs,2080 CAS objects/833665658 bytes and23699 retained
artifact references. It recomputes24 exact shadow selections from raw compiler
logs,12 exact token outputs and16 complete four-file comparisons against actual
315 Git bytes and retained generated files. It checks2426 qualified source,
fixture, schema and build blobs against315, all54 source/tool endpoint pairs,
and refreshes18170 source hashes and12 tools on each final frozen host plus835
Linux/620 Darwin final products. Earlier historical limitations remain explicit;
this does not establish whole release or File source acceptance.
