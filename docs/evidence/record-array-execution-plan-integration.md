# My copied-plan integration after byte conversion

I qualify the non-admitting execution plan at `b87bb91f1793b48617d7737bcd4f6110590bbce1`
after actual PR932 merge `128d5cd2`. My only new query operation is the reviewed
explicit CAST_U8 scalar recipe. Its independent inventory contains93 operations;
a whole-query INT/U8 conversion control retains declared byte-array field facts.
I preserve the original [seven-configuration seal](record-array-execution-plan.md)
at c032c. The root's independent original-seal audit is copied into this
supplement; I do not attribute those sanitizer runs to the integrated providers.

Fresh Linux GCC13 and Darwin AppleClang ordinary setup, one-method discovery,
complete linked2823/observed6420 query controls and every named neighbor pass.
The query exercises94 one-shot/persistent exact-MEMORY failure positions with
fresh recovery and74382 peak tracked payload bytes. Named neighbors cover
ownership contracts, ordinary records, actual public/private layout codecs,
conversion/end-to-end codecs, ordinary arrays, scalar unions, public ownership
and complete mixed declarations. Each host records20 successful outer phases.
No VM or generated consumer executes a newly admitted mixed graph.

I retain both preceding f093 first terminals. Their C controls pass, then the
Python wrapper still expects the old92-recipe output. After precode ledger and
review, b87bb changes only that expected count to93. Both failed wrapper reports,
products and source trees remain intact; no production or C assertion changed.

My [manifest](record-array-execution-plan-integration/seal-sha256.json) seals
host reports, source manifests, commands, file-backed logs, bounded cleanup,
product maps and the historical source archives. The host stores contain880
unique objects/296266150 bytes,15982 references and96 equal source/tool pairs.
All48 outer terminals include46 successes and the two preserved wrapper failures.
The Darwin archive has68750659 bytes and SHA256
`22d608e7438a4dbc47e3ba24b7f983b1ce6b5ca6978395bb6bb66f18e463d7d5`.

At sealing I rehash all3716 tracked nonevidence inputs in each original/corrected
host tree,10 Linux/15 Darwin selected tools and their final provider/artifact
maps:249/193 Linux and255/217 Darwin entries. The fresh inventory includes lib/
archives as well as obj/bin and retained inner products. It does not inventory
every operating-system library. Changed/generated products are recorded, not
claimed immutable across setup. Inner artifact maps preserve earlier products
in addition to the final endpoint. No old provider tree was copied into setup.

Full f36b/15f/488 acceptance remains open: real matched VM/native/LLVM/Wasm
consumers, public conjunction, paired source and complete mixed graph coverage
are required after this descriptive preparation dependency lands.
