# My byte mutation qualification boundary

At `facc7221f`, my fresh component build, callable authority controls, full native bootstrap, backend build and all three canonical producer builds pass. My original byte program and numeric operand method pass across the selected producers and backends.

My next mutation method passes C-seed shadows/native execution and C-produced NanoVM execution. Generated C at O0 then stops at `nvalue_require_int`. The retained module contains `ARR_LITERAL 1 1`: the producer lost its declared byte-array context and emitted an integer array. Static backend review separately finds that byte-array identity needs preservation in native reconstruction. Both repairs remain open under `task_046cd53fee0443aca7d87a26bfe4a9fd`. No later selected method or complete release gate is claimed.

I retain 123 reports and logs in the sealed bundle. Its object index references 4,501 local content-addressed objects, rehashed during sealing; those object bytes and inner fixture directories are not embedded. Every phase retains equal source/tool endpoints, reaped leaders and absent process groups and tracked descendants. The final failure is preserved without retry or weaker assertions.
