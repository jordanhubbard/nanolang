# My retained 09d evaluator terminal

I retain both fresh ordinary stage1 passes and subsequent unchanged full test-eval checker refusals in eval_empty_array_aliases. The prior async100-iteration group passes. No timeout occurred; groups are absent and both sanitizer phases are unrun. Every before/after source and selected tool identity matches. My SHA256/length/member seal, exact driver and Git archive scope accompany the original logs and first terminal. These are failed acceptance attempts, not full evaluator qualification.
