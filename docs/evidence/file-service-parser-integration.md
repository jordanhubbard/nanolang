# I qualify the integrated paired File parser closure

I integrate actual PR925 main302f129df with the reviewed parser and bounded
match cleanup. Forty changed parser/fixture paths and67 incoming canonical
paths match their original Git bytes. The only shared source file combines
my unchanged zero-field binding guards with the already merged PR923 operand
grouping correction at disjoint sites. Make and roadmap conflicts retain both
additive records. I preserve the original5a0e seal separately.

Before execution I discover five intentionally invalid UTF-8 Linux publisher
report storage names in that original seal. I record task5a8b7cad and normalize
only their Git storage paths, retaining original pathname bytes, contents and
hashes in portable-byte-paths.json, reports.json and CAS. The complete corrected
cbc08b5b4499cc9d8a1c86d6d302c4c618bd5fe1 tree has46083 tracked files; I exclude
none on either host. Initial source maps match and both product maps are empty.

Fresh bootstraps pass in294.835 seconds on Linux and289.826 seconds on puck.
I use the same seven focused parser/provider configurations, paired actual
publisher/parser/schema corpus, strict publisher controls and ordinary neighbors.
I instrument the parser plus env/lexer/UTF8/eval/transpiler selected providers;
common runtime and iterative emitter providers are ordinary. I do not claim
full-compiler instrumentation or general union lifetime acceptance.

All seven parser/provider configurations and seven affected publisher phases
pass. Both complete paired methods pass (Linux304.342 seconds, puck312.547
seconds). Ordinary neighbors also pass: Linux274.008 seconds and puck282.845 seconds.
Their Make graph invokes its existing bootstrap dependency; I keep those later
products separately attributed from the earlier fresh bootstrap and paired tests.

Final current checks verify all46083 sources and12 selected tools on each host,
plus961 Linux and718 puck products. Final library/module-cache products are
explicitly postphase-only additions, while bin/obj retain each phase endpoint.
The archive SHA256 is99e5c58edc751e91741a027a7160b527e493509189cc623b947cb9a8447153b3
(111057594 bytes). I preserve18 special publisher fixture members as archive
metadata without following or recreating them during local extraction.

No new unexpected qualification terminal occurs in this integration. The original
seventeen first terminals and original5a0e acceptance remain separate, unchanged
claims. Complete companion resolution, executable lowering and generated service
behavior remain mandatory open parent work. The full File source route is not
admitted by this parser/retention milestone.
