# I qualify independent record name storage

I retain exact51d0b9a998e3cfae5d255d31b9e9ae14f1519e14 fresh complete provider builds and both original owning lifetime and collection methods on Linux and macOS, ordinary and ASan/UBSan/LSan. All eight methods pass, with source/provider/tool endpoint equality, original bounds, and no remaining groups.

I preserve the original assertions and add empty, long, repeated and mutable field names, source destruction, independent copies from packed records, legacy individually owned names, and every allocation prefix with one-shot/persistent failure and recovery. Compiler timeout and complete release qualification remain open. The C StructValue layout changed, so I rebuilt every provider instead of reusing old object files.

Raw reports and identity maps are embedded; exact executables remain in persistent host qualification directories, including retained collection binaries. This evidence establishes lifetime behavior, not a measured compiler speedup.
