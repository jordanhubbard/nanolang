# I observe exact owner-array transport and extreme indices

I qualify the [reviewed fixture contract](../NANOISA_OWNED_ARRAY_BITS_BOUNDARIES.md)
without runtime, producer or admission changes. Linux freezes2e0eb5289; puck
freezes6db8e572b. Their thirteen selected production/fixture inputs are identical;
Linux additionally tracks the corrected Darwin setup script. I preserve the
[first seal](owned-array-bits-first.json), [Linux seal](owned-array-bits-linux.json)
and [Darwin seal](owned-array-bits-darwin.json).

| Corrected fresh phase | Seconds | Result |
| --- | ---: | --- |
| Linux GCC ordinary |13.918|PASS|
| Linux GCC ASan/UBSan |57.029|PASS|
| Linux Clang18 ASan/UBSan |34.670|PASS|
| Linux GCC unchanged private corpus |10.607|PASS|
| puck Apple17 ordinary |14.405|PASS|
| puck Homebrew23 ASan/UBSan |28.626|PASS|
| puck Apple17 unchanged private corpus |10.867|PASS|

Each bit fixture VM binary reports17305 checks, with true-switch/computed-goto
macro evidence, both fusion settings and four public synchronous APIs. Every
native module executes at O0/O2. Adjacent VM binaries report4746 checks. Sanitizer
phases retain detect_leaks=1 and halt-on-error; fixture/VM/heap/emitter/generated
native units are instrumented, while linked providers remain ordinary objects.

The transparent VM observer checks exact array pointer, index, function, decoded
next-PC, tag and memcpy-obtained uint64 bits before returning the unchanged value.
It accounts for the actual previous-value read during SET. Both signed zeros and
signed quiet-NaN payloads survive literal/pack/projection/relay/reverse-unpack,
alias mutation and growth. No floating equality or NaN arithmetic supplies the
bit oracle. The native internal carrier probe calls actual emitted factory,
relay and consuming helpers after full-module admission/emission, and observes
NmsValue bits through retained aliases. This remains separate from normal public
nvm_owned_entry execution; I claim no new public helper-entry route.

Raw missing GET/POP, optional typed comparison and failed SET cover -1,
INT64_MIN and INT64_MAX. I retain exact existing VM/native error conventions,
normal `7\n9\n` and failure `7\n` output, root/byte baselines and repeated
same-process fresh-VM recovery. The native carrier separately checks unchanged
surviving alias contents after refused stores and releases every root before
finish/dispose. Complete module admission precedes every valid execution.

My first Linux phase passed the switch VM matrix and first public native case0,
then failed the internal probe's active-context assertion before its first helper.
I omitted nms_begin in fixture setup. The reviewed correction mirrors emitted
bind_records(NULL,0), begin, finish and inactive disposal; no assertion was removed.
The first Darwin driver failed Python parsing before checkout/build; I preserve
that observed stderr record and original script, rather than fabricate a redirected
first log. Neither terminal is a runtime failure or a complete matrix pass.

Linux seals986 files and thirteen selected inputs;1780 source-category hashes
and148 providers/compiler identities match around the gate. A separate final-only
7183-file inventory matches the frozen Git index; it is not called a before/after
full-tree measurement. Darwin seals716 files, identical thirteen inputs,
7182 full tracked before/after source hashes and211 providers/compiler/configuration/
sanitizer-runtime identities. Every per-phase tool map is equal. Host attribution
is puck arm64, Apple17/Homebrew23, SDK26.2, distinct from CXWWHGGJX0. Both outer
1800-second bounds return0; each phase retains a600-second deadline. Inventories
do not cover every transitive system tool or SDK file.

I retain original and corrected artifacts under
/tmp/nanolang-owned-array-bits-linux and, on puck,
/private/tmp/nanolang-owner-array-bits-15ae-evidence. Task15ae awaits canonical
review/merge. Parent430220 also requires the pending paired source mutation
acceptance; wider product, target and release obligations remain separate.
