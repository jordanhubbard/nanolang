# I qualify exact ordered-range validation without changing test limits

Source44d4eaf4f passes the original verifier, Forth session, Core, Double and Float targets in all five selected configurations: Linux GCC ordinary and ASan/UBSan, Darwin Apple ordinary and Homebrew Clang ordinary/ASan/UBSan. Every verifier run includes98 checks. I retain all original corpus assertions and internal deadlines, including Double600seconds and each outer1200-second command bound. LSan is disabled in these selected timing gates; separate ownership evidence states its own instrumentation.

Linux sanitizer completes session510.332, Core332.431, Double482.738 and Float455.627seconds. Darwin sanitizer completes session46.340, Core34.531, Double58.544 and Float52.883seconds. These are observed whole-command times, including preparation where required, not a general performance guarantee. I retain the earlier Linux53a Double alarm separately in the service-classification seal; it remains a failed historical terminal.

My five bundles contain300 raw reports,25 successful reaped/group-absent terminals and50 equal source/tool pairs. I hash all745 separately indexed local CAS objects and recheck every decompressed bundle member. All627 Darwin report/product files were copied locally and compared against independent remote SHA-256 maps before sealing. Source and tool selections are explicit in each original report.

My first Darwin44d4 staging attempt selects SSH's older default Python, which lacks hashlib.file_digest. It stops before extraction/build/execution. I retain that setup terminal and select the already inventoried Python3.14 explicitly for unchanged staging. I do not reclassify it as a product failure or modify the host provider.

This bounded qualification supports the reviewed invocation-local maximum-end shortcut and original unordered fallback. It grants no new module admission, removes no structural verification call, and does not close the wider compiler/bootstrap or release gates. Canonical integration remains a separate obligation.
