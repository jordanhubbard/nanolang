# I qualify copied indirect hosted facts without dispatch

I retain production `95c04f751a909b122cdbc0ac207516b2496511a7`, first fixture
`225b67a6da942b32896ab167d664a95040701281`, and corrected fixture
`5c26faaa0bd3892f3528c3732a5791ffc925ea4b`. Production did not change during
qualification. My [contract](../NANOISA_FILE_INDIRECT_HOSTED.md) remains private,
nonexecuting preparation: no callable body, File service, VM/native dispatcher,
public selector or source route is admitted by these reports.

## My measured matrix

Fresh corrected Linux setup passed in22.470s; Darwin setup passed in9.568s.
Each host discovered exactly two new methods. Linux GCC13 and Clang23 ordinary,
GCC13 ASan/UBSan and Clang23 ASan/UBSan passed. Darwin AppleClang17 and
HomebrewClang23 ordinary, and HomebrewClang23 ASan/UBSan passed. Each of these
seven configurations passed1,901,740 instrumented and770,427 linked assertions.
Each instrumented run measured409 persistent and409 transient allocation
refusals and independent successful preparation after every injection. The
printed recoveries0/0 count successful preparations *during* an injected event;
it does not count those separate fresh recovery controls.

I also passed the unchanged cyclic, code, body, flow, hosted, opcode,
cyclic-hosted, indirect-target and indirect-flow neighbors on both hosts.
All16 corrected Linux phases and15 corrected Darwin phases passed. These are
bounded named targets, not a compiler bootstrap, full product test or release.

My new sanitizer configurations compile the fixture and all13 allocating
reader/bridge/query providers with the selected flags. The instrumented fixture
includes file_flow.c under HOSTED allocation hooks; the other12 providers are
rebuilt with those same hooks. The linked method uses a separate rebuilt flow
object. Common ISA providers come from ordinary setup; I do not claim whole
program instrumentation. The bounded fixture runner sets detect_leaks=1,
halt-on-error and an empty LSAN_OPTIONS. Actual driver Python and resolved Darwin
Xcode clang/ld hashes are retained alongside selected compilers, SDK path/version
and libffi header. This is not a hash of the entire SDK or operating system.

I retain both-candidate/catalog/branch order, original function identities,
larger non-first candidate stack/depth, full alternatives/edges and copied
owner/reference/region/service/call facts. Entry/initializer, invalid getter,
wire-envelope and private-map corruption controls remain explicit. The253-arg
serialized positive and254–256 instruction-limit refusals are distinct from
the isolated internal255/256 staging arithmetic checks; I do not claim a
serialized255-arg positive. Exact allocation peak bounds and zero-live baselines
are checked before disposal of unrelated fixture storage.

## My first failures remain visible

Frozen225b setup and discovery passed on both hosts. Linux ordinary stopped at
the allocation baseline assertion at fixture line303; Darwin ordinary reached
409 refusals in each mode then stopped at final zero-tracked line310. No full
method or later phase passed at that pin. Static inspection established that
my arity fixture freed an allocation tracked by bodymodule through ordinary
free, bypassing forget_allocation. Corrected5c26 pairs the replacement allocation
and free under the same hooks, adds a strict zero-live checkpoint before the
sweep and preserves every original assertion. Fresh corrected runs pass; I do
not label the first tracker failures as demonstrated production leaks. Both
original binaries and complete first terminals remain preserved under task
`task_ff730d2cf69d4b6998f66bdcc66e66d1`, pending actual-merge reconciliation.

## My retained evidence

My [report manifest](file-indirect-hosted/report-sha256.json) seals389 files.
The [artifact index](file-indirect-hosted/artifact-store.json) records1,456
content-addressed objects with14,720 report references; the
[summary](file-indirect-hosted/qualification-summary.json) records39 equal
source/tool endpoint pairs. Existing provider inputs are unchanged in every
pair; setup creates new objects. Current rehashes cover both original and
corrected trees:16,216 tracked files each,173 Linux and172 Darwin providers,
and10 Linux/15 Darwin tool records. Both Darwin archives were rehashed after
transfer. The combined artifact archive is retained locally at
`/tmp/nanolang-file-indirect-hosted-artifacts.tar.gz`, SHA256
`09aafea75b78b89e0748b5560cd4e3715d7067b02d59cea19ea0faf24f04f667`.

Original and corrected Linux trees are
`/home/jkh/Src/nanolang-file-indirect-hosted-qualified-{225b,5c26}`;
Darwin trees on puck are `/tmp/nanolang-file-indirect-hosted-{225b,5c26}`.
The reports retain exact commands, file-backed outputs, bounded process cleanup,
source/tool/provider maps and produced bytes, including failed-run artifacts.
Independent review and actual merge remain pending. Full indirect ownership
runtime membership, shared fuel, matched VM/native execution, public installed
conjunction, callable arguments/results, richer borrowing and source remain open.

## My independent seal review

The independent release-audit review verifies all389 committed reports,
1456 CAS objects and complete archive,14720 references,39 endpoint pairs,
all31 corrected phases and the exact seven configuration assertion counts.
It rechecks both original225b and corrected5c26 source/tool/provider snapshots
on Linux and puck and compares488 selected production/fixture inputs to their
qualified Git pins. The [audit](file-indirect-hosted-independent-review.json)
retains SHA256c1226f5bcae9243b7dfa10dcf0d3f6be1f46c0bd60556e344bbf2e7fc63cd1e2.
No fixture replay or full runtime/source acceptance is claimed. Current-main
integration and actual merge still precede task closure.

## My current-main integration

I integrate canonical affine-union main0dc588 at
`72e3be01c2d46291c3d23ff0ded74f285a0f654e`. My query production and fixtures
remain unchanged; I retain additive Make and roadmap histories and rebuild fresh
providers on Linux and puck. Linux passes all thirteen selected phases. Puck
passes setup, configuration, discovery, ordinary query and cyclic controls, then
its CODE runner reports unsupported Apple leak detection before assertions.
I retain that first terminal under task_f4463938a7b1ea12bc6eadacfdb9fc85.
The external integration adapter had selected AppleClang for existing neighbors
that always enable sanitizers. I restore explicit Homebrew Clang with unchanged
leak detection and assertions; all eight failed/unrun neighbors then pass with
a fresh configuration report on the same frozen source. Original passes are not
relabelled. This supplement includes ordinary query checks and the neighbors'
existing sanitizer scopes; it is not a replay of the original seven configurations.

My [integration seal](file-indirect-hosted-integration/summary.json) retains
277 reports, 807 objects totaling298548221 bytes, 10407 artifact references and
56 equal source/tool endpoint pairs, plus the complete Darwin archive identity.
Runtime dispatch, public/source indirect calls and the full2c135 roadmap remain
open. Independent integration review and actual merge remain pending.
