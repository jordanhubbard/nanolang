# I qualify exact reference-transport child ownership

I qualified fixture checkpoint `8dc7edb0` after contract `2f25a55a` and independent static review. Production `nanocore_export.c` is unchanged. My test translation unit tracks only positive evaluator/writer PIDs returned on successful creation; every recorded child must immediately return ECHILD after evaluation. I do not sleep or retry a failed assertion. EINTR handling preserves the same check.

All eight methods pass on Linux arm64 with GCC13.3 (1.329 seconds) and Clang18.1 (1.133 seconds), and on Darwin arm64 with Homebrew Clang23.1.1 (10.529 seconds). Each run builds fresh strict C11 ASan/UBSan code, retains detect_leaks=1:abort_on_error=1, and preserves the original seven methods. The added method checks a caller-owned child across both successful evaluation and missing-program refusal. A pipe handshake establishes readiness; the child remains live until fixture cleanup explicitly terminates and reaps it.

The suite retains quoted input/paths, PATH lookup, large full-duplex input/output, empty/nonzero/missing programs, early exit and SIGPIPE policy, allocation failure, 30 repeated calls and closed standard descriptors. Common fixture cleanup now closes files/descriptors, releases text/result and cleans its own children on assertion failure, avoiding the secondary input leak without hiding a failed assertion.

My [22 sealed reports](reference-transport-child-ownership/reports.sha256.json) include commands, logs, source inventories, selected compiler hashes and three diagnostic captures. All 1,710 tracked source/test/script/build inputs matched before and after each qualification. Selected compiler binaries remained unchanged. The final branch incorporates canonical PR797 afterward; its only additional tested-source change is the separately qualified four-line SDK macro fixture correction. My exporter and transport fixture remain byte-identical to `8dc7edb0`. I do not claim a combined aggregate rerun from these results.

I preserve the original Darwin status17 and secondary ten-byte input leak. Fresh diagnostic `byu3ecp4` observes process-wide waitpid returning zero, then ECHILD one millisecond later without a positive reap by the fixture. Diagnostic `7284kxcj` records missing-program spawn returning ENOENT with initialized PID -1. Diagnostic `bsdmiszv` shows that an earlier baseline wait changes the observation. These diagnostics are not acceptance tests. Symbolizer warnings do not identify the transient child; its origin remains unproven. I correct the fixture's ownership claim rather than assign an unsupported cause to production.

Diagnostic artifacts remain under `/var/folders/9z/xpmfgw8j09l4g6wwxrxtt97w0000gn/T/nanolang-reference-diagnose-*` on CXWWHGGJX0. The first diagnostic script's C-string quoting error is retained in `duut5ed2/diagnostic.log`; it executed no test. A diagnostic process listing in `5qyv53d9` remains external and is not published. Qualified binaries remain in `/tmp/nanolang-reference-qualified-{gcc,clang}-8dc7/artifacts` locally and the corresponding `darwin` directory on CXWWHGGJX0. No historical failed executable was replayed.

This supplies the bounded acceptance for `task_c564d9e9089845ab9566b004a7828643`, pending canonical merge. Full NanoCore, shared match policy, installed product and release acceptance remain separate.
