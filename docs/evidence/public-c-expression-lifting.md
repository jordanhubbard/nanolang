# I qualify statement-aware public C values

I qualify reviewed production480bc23d plus completion correction4d74a705, after preimplementation contract3856541a and correction contract171064eb. My integrated frozen436ea9c8 includes canonical64301352, PR772 AST metadata and PR774 source short-circuit/control completion. Only roadmap conflict resolution was needed; my reviewed lifting production is unchanged. I rebuild my own C compiler, interpreter, NanoVirt, VM and native translator before freezing. The adjacent JSON records40 file identities, three compiler/wrapper identities and ten retained logs; all final identities match after qualification.

My unchanged five-method source/API lifting suite plus39 adjacent methods pass under GCC in24.740s and Clang in33.270s. Actual generated programs use strict C99/C11, O0/O2, ASan/UBSan, no GNU expression/typeof output. Lifting sources additionally run in the local interpreter and through local mandatory-shadow NanoVirt emission, verification and VM. Integer-bit FLOAT observers, string content, nominal union results, both branch arms, nested local/payload scopes, lazy effects, operand/call ordering, discarded calls, outer loop break/continue and nested initializer returns retain their assertions. API controls preserve prior file/stream output on unknown/missing/mixed/insertion-context refusal, then recover in the same process. All seven unchanged existing C-backend programs pass. I claim neither Darwin nor bootstrap qualification here.

I retain the first e923c05d GCC/Clang4/5 outcomes and37 unchanged source/compiler identities. Their source lazy-operand VM assertion required separately reviewed PR774; I did not weaken the fixture or replay its module. Interim explicit external-producer gates at425a9b01 pass5/5 under GCC and corrected Clang. The first external Clang API compile failed because CC=clang bypassed the established wrapper's explicit GCC installation; I retain that log and use the existing wrapper without warning suppression. Final integrated gates use no external producer selector.

Independent review found a missing-declaration path when a nested let initializer exits. My recorded completion correction propagates control-only outcomes through consumers and suppresses unreachable tails; it does not fabricate values. No pre-repair artifact executed for that static finding.

Task33f74 remains pending canonical merge. Repeated loop headers/global lifted values retain checked refusal and separate taskd0c6. Shared early/multiple wildcard and no-success policies remain tasks477bdd/70c5. Parent6ade requires an exact option/expression inventory with portable support or checked refusal; this bounded success does not close it or invent new callable/ownership ABIs.
