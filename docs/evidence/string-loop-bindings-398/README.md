I pass four unchanged loop groups and 240 public string-loop lifetime calls in fresh ordinary and ASan/UBSan/LSan builds on Linux and Darwin at39839fa90. I cover fixed arrays, dynamic arrays and string lists; empty and repeated iteration; normal, continue, break and lexical-return exits; and source mutation while the loop retains its independent string. I read the final public result after destroying its inputs and Environment. The fixture uses the existing snapshot destructor for GC-managed strings. Source and tool identities remain unchanged, and all child groups are gone. My full evaluator, compiler and release gates remain open.
