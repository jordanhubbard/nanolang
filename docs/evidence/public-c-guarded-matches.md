# I qualify a bounded total guarded statement profile

I qualify reviewed production `accde6ee` and harness `66824443`, amended at `6b88ff94` only to select an explicit corrected external NanoVirt producer. My [contract](../PUBLIC_C_GUARDED_MATCH_CONTRACT.md) bounds exact scalar union statement matching to source-order named arms and one final wildcard with static legal-tag coverage. I bind payloads before exact BOOL guards, evaluate the scrutinee once, and use unique forward labels without capturing enclosing return/break/continue. Expression matches, shared early/multiple wildcard and no-success policy remain required and open.

My original GCC and Clang gates each passed three of four methods. The nested-loop/payload fixture passed all public C and interpreter observations, then its verified VM outer-payload assertion failed. I retained both logs and all original identities. Separate PR765 repairs NanoVirt binding scope. With its explicitly selected producer, the unchanged fixtures passed four GCC methods in 1.920s and four Clang methods in 2.934s. I do not describe this external-producer gate as canonical own-tool integration.

I also passed all 32 adjacent methods under GCC in 15.036s and Clang in 19.614s, plus all seven existing public C programs. Generated C is compiled as C99/C11 at O0/O2 with ASan/UBSan where the individual harness specifies that matrix. My [manifest](public-c-guarded-matches.json) retains production, harness, provider, compiler and tool hashes and eight logs. Every original identity remained unchanged except the documented producer-selector amendment.

My older public C API refusal control intentionally changed wildcard to still-unsupported `OR:Item` before the original frozen gate because final wildcard is now admitted. I preserved its output/refusal assertions and added specific early/multiple wildcard, unknown guard and incomplete-coverage controls. I do not claim that API fixture was unchanged or that bounded acceptance closes full public C release scope.

## I qualify canonical own-tool integration

I integrated canonical PR765 at `dbc87343` into `4bad0046`. The only merge conflict was the roadmap; I retained all required rows and marked the merged binding prerequisite complete. My public C production is byte-identical to reviewed `accde6ee`. After rebuilding local tools, I froze all 29 source/provider/harness/tool files and both compiler identities before execution. All 31 identities remained unchanged afterward.

With no external producer override, I passed all 36 combined methods under GCC in 17.057s and Clang in 23.522s, and all seven existing public C programs. The four new guarded methods include paired interpreter and verified VM execution from my local rebuilt producer. My manifest retains the original failure, external correction and final own-tool logs separately (12 logs total). I qualify this exact Linux integration, not a new selfhost bootstrap, Darwin run or whole-language C99 claim.
