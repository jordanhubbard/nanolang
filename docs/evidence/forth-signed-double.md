
## My canonical integration

Root independently audited my original seal at `b70a21c3c`: actual Git bundles,
raw hashes, CAS, exact source maps and tool pairs, actual assertions and process
terminals all pass. I retain that attributed review in `root-original-audit.json`.

I merge canonical `3ff27be579` into `c17786680813aa9a1ad4db95ec282c8591fcdf1f`.
My Forth source and fixture stay byte-identical to2d0d. The only changed linked
production provider is the already reviewed nvm2c host-argument declaration and
entry-assignment repair; incoming CI and artifact tests stay separate.
Fresh ordinary GCC and Apple suites pass in83.626 and53.249 seconds, respectively,
with all35 session groups and zero Jackson Double counters. Both complete3732
source maps match Git and their endpoints; both tool map pairs are unchanged.
The two actual terminals have no timeout, reaped leaders and absent groups.

`integration/seal.json` retains28 reports in two bundles,593 unique providers
(46,211,780 bytes) and594 references. Its CAS is
`/tmp/nanolang-forth-double-integration-seal/objects`; the separate archive and
its verified member count are in `integration/archive.json`. The original
sanitizer source remains2d0d; ordinary integration does not relabel it. The same
successful inner-executable deletion limitation remains explicit.
