# My 303 native-list checkpoint

I qualify only the scopes that actually completed at `303e85d00`. Fresh Make
build, complete bootstrap, and provider preparation passed on Linux and Darwin.
My build times were 74.277 and 81.220 seconds; bootstrap took 290.274 and
301.956 seconds. The original ten-second shadow deadline remained unchanged.

All five focused configurations passed: Linux GCC ordinary, GCC sanitizer,
Clang sanitizer, Darwin Apple ordinary, and Homebrew sanitizer. Each ran the
shared native-storage fixture plus the original checked evaluator ownership /
scheduler allocation and array declaration-identity methods. The shared native
fixture reported 3,560 assertions and seven allocation positions in prefix and
transient modes. Instrumentation applies to the fixture's owning translation
units and shared header; the remaining prepared providers are ordinary builds.
I do not claim whole-compiler sanitizer coverage.

The full eight-method native suite then stopped in its first method,
`test_native_declared_and_callback_precedence`, on both hosts. C-seed O0 and O2
routes had compiled and executed. Stage1 O0 strict shadow C compilation rejected
unused runtime support helpers. Linux returned 1 after 6.293458 seconds; Darwin
returned 1 after 4.408036 seconds. No native method completed, and the unchanged
eighteen-method source suite was unreached. These are product strict-compilation
failures, not infrastructure or successful semantic list qualification.

Unlike the earlier original cold-worklist incident, these generated C files,
compiler argv records, products and command statuses are retained. I preserve
all report bytes, with deterministic gzip for large reports and both original
and stored hashes. The artifact index names 5,067 locally retained unique CAS
objects totaling 1,550,782,270 bytes. The report manifest contains 841 reports
before this scope note. Darwin report/CAS copies live under my persistent local
home and have persistent remote home originals.

Source/tool and selected prepared-provider maps remain the authority for each
phase. Generated module objects and binaries are explicitly mutable outputs
with separate before/after maps; their change is not input equality. Named
compiler/runtime inventories are selected-tool evidence, not complete linker
trace closure. No passing 303 scope is relabeled as a later corrected pin.
The separate optional-helper diagnostic seal preserves subsequent proposals;
fresh actual producer and full source acceptance remain required.
