# My private read-text query integration

I integrate qualified seal `dee968861` with canonical `5f98a27a0` in a separate
ready tree. I preserve every original report and the qualified trees. The sole
merge conflict was the two additive Make targets; I retain both and preserve
canonical separator comments byte-for-byte. My final Make delta is only the
five-line private read-text target.

My query, header, C/Python fixtures, original report manifest and selected common
verifier/decoder/format/service providers are byte-identical to the qualified
seal, as recorded in `inputs.json`. Incoming production adds the separate private
cyclic-hosted query through one include in file_flow.c; it changes no existing
reader or public selector. I do not rerun unchanged baseline gates or relabel
qualification as execution at this integrated head.

The copied independent root audit verifies all 492 original Git report blobs,
634 artifacts, 22 equal endpoint map pairs and retained first terminals. Original
configuration and instrumentation limits remain in ../portable-read-query.md.
I grant declaration facts only, not execution, host capability or type/lifetime
authority. My real native LLVM/Wasm adapters and parent linkage work remain open.
