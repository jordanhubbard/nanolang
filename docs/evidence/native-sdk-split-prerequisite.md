# I retain the split prerequisite and one merger diagnostic

At fixture pin `d1e41d0e9`, my current integrated Linux C seed passes the bounded primitive, declaration/local/qualified precedence and argument-refusal controls (6.790 seconds), and the actual evaluator/native ownership and allocation-failure fixture, including all original cases plus a physical NUL buffer. My two retained `1ee9b0f33` Nano refresh products pass the same bounded primitive controls on Darwin (27.366 seconds). Their successful historical refresh terminals and complete retained source/provider/tool identities are checked before use; all endpoints remain unchanged afterward. These are attributed prerequisite results, not a fresh integrated bootstrap or the complete original paired/installed SDK corpus.

I also retain the single copied `4e4a17be0` timing diagnostic with the original ten-second child limit and 120-second outer bound. Its 1,142 completed shadows all report status zero; `merge_with_imports` is active at the deadline. The preceding `merge_with_imports_mode` completes in 1.040023 wall/1.039002 process-CPU seconds. The compiler returns 1, the sentinel survives, and process groups/descendants are absent. This does not establish scheduling cause or future performance margin.

My [manifest](native-sdk-split-prerequisite/manifest.json) retains every report and generated temporary file with original path, mode, length and SHA-256. Reports over 40,000 bytes use lossless gzip with separate stored hashes. Executable/object images remain in the immutable local content store named by their complete original digest; their exact byte and mode identities remain in the manifest. The retained source trees, original reports and both Nano products remain untouched. The remote Darwin copy is independently compared against its remote byte/length/mode inventory.

My separately reviewed `fe6450ea0` source change is not executed by these results. Fresh complete bootstrap, every original merger shadow and deadline, the full paired File/STRING/visibility corpus and installed source-hidden/read-only SDK acceptance remain open.
