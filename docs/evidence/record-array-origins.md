# My private record-array origin qualification

I qualify copied facts, not mixed runtime admission. My reviewed production is
c4ff98f0e, following the mechanical verifier factoring and same-original-module
preparation. My first fixture5504545ba failed on both hosts because an unused
union's layout/field names were encoded as NO_INDEX. I retain both complete
terminals and artifacts. The exact two-operand fixture repair2e9faf477 names
existing strings; no production or assertion changes accompany it.

At the frozen corrected pin, all seven selected query configurations pass:
Linux GCC13 ordinary/ASan+UBSan and Clang23 ordinary/ASan+UBSan; Darwin Xcode
Clang ordinary and Homebrew Clang ordinary/ASan+UBSan. Each configuration runs
787 linked checks and1289 instrumented checks. I measure40 successful-path
allocation positions, fail each in one-shot and persistent modes, and prove a
fresh successful recovery after every refusal with zero retained tracked bytes
or objects. My observed live payload peak is56546 bytes, below the conservative
reported reservation. Allocator metadata and internal realloc overlap are not
measured; seven selected allocation-owning TUs are instrumented, not every linked
provider. Strict warnings and leak detection remain enabled; LSAN_OPTIONS is
explicitly empty.

Both hosts also pass the unchanged ownership-contract185, ordinary-authority252
(with14/8 allocation positions), converter365, end-to-end20, old ordinary-array,
affine scalar-union runtime, public ownership and complete mixed declaration
neighbors. These old runtime neighbors do not execute the newly queried mixed
modules. All new controls remain query-only. The full mixed runtime, LLVM/Wasm,
source and release parent requirements remain open.

My [seal](record-array-origins/seal-sha256.json) covers456 actual report entries,
1006 content-addressed objects/340873277 bytes and15021 artifact references.
Ninety source/tool endpoint pairs are equal. There are37 corrected outer phases
(19 Linux,18 Darwin), all successful, plus the two original four-phase histories
whose final ordinary phases fail as recorded. Original setup/discovery success
is not relabeled full query acceptance.

Fresh setup compiled providers separately for each host and each fixture pin.
Every query mode rebuilt the selected providers; I retain their actual products
and command lines. Source maps cover3684 tracked files, excluding historical
`docs/evidence/**` only; the exact source archives and manifests are retained.
Current-input checks independently rehash both source roots, selected tools and
last-phase provider/artifact maps on each host. This is a named-tool inventory,
not a claim about all transitive system headers or libraries. The driver captures
its actual Python executable, selected compiler identities and Darwin SDK/ffi
selection. The outer driver preserves bounded process-group cleanup and every
inner retained artifact directory.

The Darwin archive is `/tmp/nanolang-record-array-puck-evidence.tar.gz`, SHA256
`d1ec98f8db62522823ee8822605d768a55e77dc32e6aafdd13799c7bf141b0b8`.
The Linux and downloaded Darwin CAS roots are
`/tmp/nanolang-record-array-linux-seal` and
`/tmp/nanolang-record-array-puck-seal-local`. Original and corrected frozen trees
remain `/tmp/nanolang-record-array-qualified-55045` and
`/tmp/nanolang-record-array-qualified-2e9fa` on their respective hosts.

## My ready integration

I integrate actual canonical4b2422ffb in a separate ready tree. Incoming changes
since ca377 are the Game of Life example, macOS CI selector, the four private
symbol aliases in the existing layout fixture, and documentation/evidence. No
compiler/runtime/query provider or Make source list changes. My selected source,
new fixture and Make bytes remain exact2e9faf477; the integration record preserves
those hashes. I resolve only the additive roadmap tail, retaining both histories.
I do not relabel the frozen qualification as execution at the ready commit and
do not repeat unchanged gates for this documentation/example/CI integration.
