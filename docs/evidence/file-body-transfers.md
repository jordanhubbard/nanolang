# I qualify private acyclic File body analysis

I qualify production77fb70924 and reviewed fixtures at frozen `a9aae53e4`.
My query freshly prepares actual CODE and checks every logical function body in
callee-first order. I retain exact per-site runtime obligations, discharging only
the separately checked callee-body requirement. I grant no hosted entry,
declared max_stack ABI, service execution, source or public backend authority.
Full File72556/6931 and the later loops/indirect/richer-borrow boundaries remain
open. This report is not a Stage1/Stage2 bootstrap claim.

## My first terminal results

All selected phases pass on their first attempt. I execute only C query fixtures
and the ordinary module/wrapper neighbor programs, never the analyzed File CODE.

| Phase | Linux seconds | Darwin seconds |
| --- | ---: | ---: |
| Normal instrumented + linked body fixtures | 6.266 | 4.258 |
| GCC / Homebrew Clang sanitizer fixtures | 4.580 | 4.809 |
| Additional Linux Clang sanitizer fixtures | 4.398 | — |
| Preparation/flow/nominal/service/ISA adjacency | 6.316 | 9.678 |
| Fresh nano_virt provider build | 7.661 | 4.250 |
| Fresh C-seed build | 0.691 | 0.837 |
| NanoISA module compile / ordinary run | 8.583 / 0.002 | 9.590 / 0.274 |
| Ordinary wrapper compile / run | 0.172 / 0.002 | 0.947 / 0.298 |

Each new instrumented fixture reports9,507 checks; each linked fixture reports
3,662. Both compile under strict Werror. Sanitizer routes retain ASan, UBSan,
LSan detect_leaks=1 and halt-on-error. Linux Clang alone selects its qualified
GCC13 installation; no Wasm flags or service execution are involved. Darwin uses
actual Xcode Clang for normal builds, selected Homebrew Clang for sanitizer
fixtures, explicit SDKROOT and Homebrew libcrypto search. Exact selected compiler
and library hashes appear in the sealed tool maps.

My fixture checks both open and close outcomes, all five service identities,
permuted nominal/import maps, borrowed and owned helper calls, exact return
bodies including uncalled helpers, known-arm unreachable paths, conservative
joins, scalar operations, input lifetime, getters and unchanged public refusal.
Drops, assertions, calls, services and returns keep explicit cleanup facts.
Whole-query allocation-prefix recovery and a direct second-refinement-clone
failure retain output sentinels and balanced live allocations. Actual256/257
logical stack tests and exact16MiB room controls are labeled separately from
my256-instruction CODE cap; I do not claim a returning body reaches256 values.

## My preserved inputs and artifacts

My [manifest](file-body-transfers/report-sha256.json) seals66 text reports,
including8 equal source/tool before/after pairs. The artifact index retains2,291
phase entries and354 unique content-addressed files under
`/tmp/nanolang-file-body-transfers-artifacts`, including actual normal,
instrumented, linked, sanitizer and provider/module/wrapper binaries. I retain
every phase's original objects and logs; later relinks do not replace evidence.

Linux source maps each contain6,914 files and9 actual host tools. Darwin maps
contain6,895 files and10 actual tools. Before/after maps and current identities
match in their stated scopes. The Darwin synthetic Git snapshot respected ignore
rules and omitted19 archive paths from its original maps: `.claude/settings.local.json`,
six `bench/*.nano` examples, nine `examples/opl/examples/output/*` generated
examples, and the three `modules/std/{collections,json,peg}/.build/source_hashes.json`
files. None is a selected fixture or production/provider input. I preserve the
original maps unchanged. A separate6,914-file full archive/current comparison
passes after all phases; its manifest is explicitly postphase, not invented
before-run evidence. All6,895 common initial source hashes also match Linux.

I preserve the exact scopes:

- Linux qualified checkout: `/home/jkh/Src/nanolang-file-body-qualified`.
- Linux reports: `/tmp/nanolang-file-body-a9aae-linux` and its `-closure` sibling.
- Darwin host: `jordanh@CXWWHGGJX0.local`.
- Darwin checkout: `/tmp/nanolang-file-body-a9aae`, source archive froma9aae.
- Darwin reports: `/tmp/nanolang-file-body-a9aae-darwin` and its `-closure` sibling.
- Local copies: `/tmp/nanolang-file-body-remote-a9aae` and its `-closure` sibling.

My recorded synthetic Darwin Git head identifies the qualification snapshot,
not a canonical repository commit. No production or fixture changes occur
between these selected phases.
