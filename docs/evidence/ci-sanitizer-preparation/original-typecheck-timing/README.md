# Original full-selection timing

I retain the failed original typecheck preparation invocation at source `53cc4f4e039f2a8765b6200c2d8fc3abfb2302c0`. The unchanged 60-second aggregate shadow deadline stopped it; the additional 180-second supervisor did not time out, and every owned process group disappeared. The compiler took 117.440 seconds including preparation. This is diagnostic evidence, not preparation acceptance.

I completed 432 shadows, with 58.323442346 seconds in completed bodies. Source 9 items 273/278 are my original recursive ordinary/unsafe 520-statement parser controls, taking 13.957336305/14.101570467 seconds. Their cost agrees with the separate parser measurements. Source 18 is my typechecker: item 30 is `resource_concrete_payload_fact` (4.920267571 seconds), item 36 is `resource_type_is_declared` (8.563604432 seconds), and pending item 73 is `resource_instantiated_classify`. Only 26 of its 190 shadows completed, using 16.990924677 seconds. Another 164 checker shadows and the root shadow remain; source inspection cannot assign reliable execution seconds to that remainder.

I retain the original stdout-derived labels in `timing-attribution.json`, but buffering makes those ordinal joins unreliable. `source-item-attribution.json` instead records the exact frozen source declarations and their zero-based top-level item indexes, checking every completed shadow index in the two relevant files. Raw source/item/ordinal timing records remain authoritative.

I changed only timing enablement, output location, and the stricter outer supervisor. I retained the original imported-shadow selection, source paths, compiler/capture companion, instrumentation environment, and inner deadline. Existing preparation uses `ASAN_OPTIONS=detect_leaks=0`; I make no LSan acceptance claim. The bundle retains every report byte; the manifest indexes 353 immutable CAS objects and 1,584 generated native files with modes. No temporary product was deleted by this diagnosis.
