# My original preparation terminal at53cc

I execute the reviewed a095 driver on exact candidate53cc4f4e039f2a8765b6200c2d8fc3abfb2302c0. Plan, verify and before snapshot pass. Actual make sanitize exits2 in356.711seconds; its original60-second aggregate typecheck shadow deadline expires with a partial mb_private_target label. The outer1798-second remaining phase bound does not expire. The leader is reaped, its process group disappears, no cleanup error occurs, and final source/tool/head maps match.

My parser component builds successfully. Its retained command includes -O1 -g -fsanitize=address,undefined -fno-omit-frame-pointer. This does not claim the unrun native bootstrap or its instrumented-product assertions. Preparation remains false; my full worker result correctly refuses because assigned tests never ran.

I preserve both complete component logs and all2382 files in the reported native-work root with byte hashes and modes. My seal contains65 reports and352 unique CAS objects. Original cce540 and focused parser successes remain separately attributed. Native's same-source Darwin record statement-expression compile failure is a separate known downstream boundary; I do not replay this complete preparation unchanged.
