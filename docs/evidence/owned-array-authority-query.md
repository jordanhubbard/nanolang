# I qualify a private owner ARRAY authority query

I qualify reviewed production `db8f32f74` at frozen source and harness `8371c7083f87714a68ab24b0a3356ce79e52f31a`, based on canonical origin query PR829. I compose fresh origin facts with independent affine lifetime analysis and common structural validation. I do not change public selectors, source producers, VM execution or native emission. Task430220 remains open for the later runtime, activation and unchanged Bundle/PREFIX acceptance.

My first setup passes in 9.439 seconds. All 16 subsequent phases pass: GCC normal, GCC sanitizer, Clang18 sanitizer, owned-array origins, owned-array layouts, retained layouts, managed record plans, mixed layout views, mixed FLOAT proof, mixed Samples, owned value results, nested owned results, owned binary64, owned STRING fields, owned STRING joins and mixed Samples admission. Each new authority query run passes 1,403 checks. No fixture or production correction was needed during these gates.

I distinguish origin-PROVED modules from independently valid owner lifetimes: unconsumed roots, empty shells and an uncalled helper with an unconsumed parameter remain refused by the complete query. Positive controls cover nested factory/relay/consume results, independent nominal paths, empty owner joins, ordinary array aliases surviving owner consumption, ARRAY and STRING initialization on both arms, exact signatures/maps/transport bytes and every decoded instruction location. One-arm initialization, owner overwrite, live observations, mismatched owner joins, service claims and optional FLOAT typed consumers remain refused. I validate maximum-stack and publication boundaries without claiming exhaustive combinations of every numerical work/storage limit. A prepared plan remains valid after its input module is destroyed.

I preserve the shared boolean structural validator's diagnostic limitation. Each fault sweep reports exactly 564 directly classified MEMORY failures and five INVALID failures, at allocation prefixes 563–567, attributed by a fixture-only wrapper around the verifier's retained-layout call. INVALID is not accepted at any other allocation boundary. I require unchanged output sentinels and original code/layout/ownership bytes, then successful recovery. The sanitizer runs report no leaked allocations. I do not claim that the shared boolean validator now exposes a distinct allocation status.

I verify all 1,742 source hashes and 157 compiler/tool/object hashes before and after the run, and again before sealing, with no differences. My [machine-readable seal](owned-array-authority-query.json) retains 69 reports, 176 artifacts and 21 current input identities. It includes the actual runner, commands, logs, host/compiler identities, original source copies, three query executables and their object files. Adjacent fixture executables/objects are copied after each phase; repeated archives are intentional. Existing nested/STRING harnesses retain their requested artifacts. Other existing harnesses retain their normal temporary-file behavior, so I do not claim every generated intermediate was archived.

I use Linux GCC13 and Clang18.1.3. ASan/UBSan with leak detection instrument the new fixture and five translation units: retained layouts, layout decoder, cursor, affine state and verifier. Other linked providers use ordinary GCC objects; this is not whole-program sanitizer coverage. The only structural-boundary hook renames the verifier's call to a fixture wrapper which delegates to the unchanged reader. Existing runtime adjacency executes only its previously admitted scalar, nested-result, STRING and mixed profiles. No pending owner ARRAY module executes in these query fixtures, and private preparation is not public execution permission.
