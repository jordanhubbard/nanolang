# I qualify private every-target File ownership composition

I qualify production5687cf13c with reviewed fixture293e9c131. The new opaque
report combines every target in each conservative indirect call set with the
checked cyclic ownership transition. It grants no hosted/runtime authority and
executes no File service. Task2c135 and full5.1 remain open.

All seven configurations pass: Linux GCC13 ordinary and ASan/UBSan/LSan,
Clang23 ordinary and ASan/UBSan/LSan, puck Apple17 ordinary and Homebrew23
ordinary and ASan/UBSan/LSan. Every configuration passes10277 instrumented and
3724 linked ownership checks. The instrumented query measures41 allocating
preparation sites and tests every persistent prefix and single transient failure
with unchanged output/input, zero tracked allocations and fresh recovery.
Instrumentation covers the query/nominal-plan translation units rebuilt by the
fixture, not every prebuilt compiler provider. Sanitizer options retain leak
checking and an empty suppression environment.

The corpus checks scalar late-backedge target discovery, File/OpenResult owned
argument/result transport, both catalog permutations and target branch orders,
caller and higher-index candidate borrow violations, missing cleanup, uncalled
invalid bodies, old public refusals, copied facts after input destruction,
selected invalid getters and bounded reports. An internal successful two-target
transfer runs with allocation budget zero; the exact candidate-application limit
refuses before another application. I do not claim an exhaustive getter space
or execution/fuel acceptance from a declaration/ownership query.

The original code/body/flow/hosted/opcode neighbors pass on both hosts. The old
cyclic neighbor then fails compilation before execution because its internal
allocation fixture omitted the new context argument. Both first terminals and
compiler diagnostics remain retained. Reviewed fixture-only7d4c7bce2 supplies
explicit NULL and leaves its LIMIT predicate unchanged. Fresh continuation trees
verify all12320 original source identities, reuse172 Linux/171 puck provider
products by recorded hash, and change only that fixture plus the failure ledger.
Corrected cyclic and previously unreached indirect-target neighbors pass on both
hosts. Production and the seven already successful new-query gates are unchanged.

[My manifest](file-indirect-flow/report-sha256.json) seals298 report files,
676 retained artifact objects totaling345374869 bytes,5576 archive references,
and58 equal source/tool map pairs. Of29 retained phase terminals,27 pass and the
two original cyclic compilation failures remain explicit. The artifact store is
`/tmp/nanolang-file-indirect-flow-artifacts`; the downloaded puck archive and its
hash/size are recorded in the qualification summary. Original failed and qualified
trees remain preserved. Endpoint equality does not prove intermediate immutability.

Hosted scalar/startup conjunction, matched indirect VM/native dispatch, callable
arguments/results, richer borrows, public installed admission and paired source
with mandatory shadows remain required next checkpoints. No ownership query
alone completes the File or release parent.
