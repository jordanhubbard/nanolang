# My first borrowed-string COP terminals

I retain exact780f28636 ordinary fresh setup, signature-contract and original zero-argument passes on both hosts. The new in-process borrowed0/1/2 VM program passes, then the original isolated invocation refuses its first exact artifact symbol. Sanitizers, subsequent native emission and the direct fault/pipe fixture remain unreached.

The source cause is the inherited SDK shutdown-registration refusal reached by loader init/open after fork, separate from STRING ABI. I preserve that existing SDK contract and record the child-local loader proposal in COP_FORK_LOCAL_ARTIFACT_LOADER.md. All bounded process groups are gone; source/tool terminal maps remain unchanged. No failed route is replayed or counted as qualified.
