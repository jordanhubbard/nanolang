# My canonical byte frontend boundaries

I retain three frozen Linux histories with the original bounds and assertions. My `d002` producers build, then my unchanged byte program refuses a byte integer operand. At `a4ba`, all three producers build, then that same program refuses scalar `to_string`. At `e332`, all three producers build and the unchanged original byte program passes across all four producer paths. My additive arithmetic method then stops at the C-seed checker refusal of unary byte negation.

These are terminal histories, not full matrix acceptance. No later methods are claimed after each first failure. My original shared-capture method remains excluded and open. The fresh `aa8` compiler rebuild is separate.

Each compressed bundle preserves the exact top-level reports and logs. `seal.json` records bundle and member hashes; each object index refers to retained local content-addressed bytes, all rehashed during sealing. These bundles do not embed that object store or every inner temporary fixture directory. I verified source/tool endpoint equality, leader reaping, process-group absence and descendant cleanup for each phase.
