I retain the first three completed failing hosted jobs at exact PR946 head `8ad34a272d16c6dff957f285e0bf46381c5779d0`. Both Linux jobs report failure compiling the original `test_u8_basic.nano` and `token_value_bytes.nano` corpus entries. Their full raw logs are retained; those lines alone do not provide the omitted per-case compiler logs. Existing tasks c6b2a040c1434fc784a9d46c02a4981e and 7b805000dfda4da386b55d4691e8c647 remain open.

The coverage log retains three ArtifactImports native link failures for missing gcov runtime symbols. The same actual Make override/export boundary is under task96a578e4e4d6f645c396c0a7c453a677 and its correction is separately under qualification. These failures are not evidence that my RISC-V target executed incorrectly, nor do passing focused RISC-V checks override them. Other hosted jobs were still active at this snapshot; I do not claim complete or green CI or merge approval.

My remaining two hosted jobs are now terminal. The macOS log reports the same two original corpus compilation failures; I retain its raw bytes, but the omitted per-case artifacts are still needed for exact compiler diagnostics. The sanitizer job instead reaches sixteen passing FlatRecordEmitter methods after its Cut A86/86 pass, then the entire original twenty-minute step expires. I do not classify it as the PR945 missing-runtime-symbol failure, a product defect, or infrastructure without further evidence. Task `task_9dd1a4c93be74bc79cb4de808eef0d83` owns this deadline diagnosis. All hosted checks are terminal; the RISC-V, C and PTX compile jobs pass. Full CI is not green.

I subsequently retrieve all three platform diagnostic ZIPs, each with524 members. Their actual per-case compiler logs agree: the original byte source fails its shadow at line8 with `I require the declared byte value type`; token_value_bytes fails its shadow at line21 with undefined `list_LexerToken_insert`. This establishes the precise diagnostic connection to the existing byte-source and generic-list tasks. `corpus-artifacts.json` records archive hashes and extracted failure text. This supersedes the earlier missing-artifact limitation for those three jobs; the separate sanitizer deadline remains unexplained.
