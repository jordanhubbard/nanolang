# My paired descriptive File source plans

I qualify production `701dca50f` on Linux aarch64 and puck Darwin arm64. I
implement explicit C and Nano request plans and immutable catalog data access;
I do not parse service declarations, generate bindings, lower File source,
publish compiler output from these plans, or grant File execution authority.
My caller still supplies a validated NSI document and complete source namespace
facts. A matching descriptive catalog is not a verifier certificate.

My existing child `task_8bbc1cf5295b4b59b314640ef57c725f` remains under the full
File/source parents6fc/72556/6931/d03c. Their paired source/shadows/startup,
cyclic/fuel, indirect and richer-borrow obligations remain open. My prior public
acyclic File route is unchanged by this work.

## What I exercised

I compare all58 unchanged explicit request cases against independent expected
rows, and compare complete canonical catalog output between C and Nano. Every
ordinary configuration compiles and executes my full imported Nano source and
catalog module through actual C-seed, Stage1 and Stage2. Each producer must
report exactly75 selected shadows:58 case helpers, the emit helper, two boundary
helpers,12 source-plan helpers and two catalog bridge helpers. I retain C-seed
completion JSON plus actual verbose names, and actual self-hosted trace names;
my fixture rejects missing, duplicate and unexpected selections.

I cover reordered bindings/requests, distinct module identities with identical
short names, aliases and collisions, exact categories/modes/IDs, unresolved wire
indices, malformed catalog/ordinal/kind facts, count/ID limits, counted embedded
NUL rejection, truthful shorter prefixes, non-ASCII bytes and partial UTF-8
prefixes. Nano-only invalid signed or over-available extents do not masquerade
as valid C memory spans. C controls mutate every catalog byte, preserve failure
sentinels, fail the one plan allocation, check exact owning size, and mutate/free
input strings before observing copied rows. Borrowed getters are used only
while their owning C plan lives. Both languages exercise the accepted1MiB text
budget and one-byte-excess refusal; the original whole-suite10-second shadow
deadline remains unchanged.

Every C configuration passes7,503 linked checks and7,518 instrumented checks.
The sanitizer configurations rebuild all three small C providers and both C
fixtures with ASan/UBSan, leak detection enabled and empty `LSAN_OPTIONS`. I do
not claim sanitizer instrumentation of every generated Nano product, or paired
recoverable allocation-failure behavior: Nano allocation failure remains a
process/runtime limitation of this descriptive checkpoint.

| Final701 configuration | Outer status | Seconds | Paired source runs |
| --- | --- | ---: | --- |
| Linux GCC ordinary | PASS | 36.054 | C-seed, Stage1, Stage2 |
| Linux Clang ordinary | PASS | 34.143 | C-seed, Stage1, Stage2 |
| Linux GCC sanitizer | PASS | 8.535 | C ownership controls only |
| Linux Clang sanitizer | PASS | 1.518 | C ownership controls only |
| puck Apple Clang ordinary | PASS | 32.881 | C-seed, Stage1, Stage2 |
| puck Homebrew Clang ordinary | PASS | 35.462 | C-seed, Stage1, Stage2 |
| puck Homebrew Clang sanitizer | PASS | 1.927 | C ownership controls only |

## Compiler and failure provenance

My fresh original32ade bootstraps pass on Linux in281.203s and puck in278.261s.
I preserve these bootstrap claims at32ade. Final701 trees copy306 Linux and305
Darwin compiler/provider products by exact hashes from those successful builds,
excluding all generated-module/module-cache directories and all module-local
`.build` directories. All518 src/src_nano input identities match except the
explicitly tested, separately imported File source-plan module. No other Nano
compiler source references that module. I rebuild the actual bridge and imported
plan in fresh trees; I do not relabel copied tools as a fresh701 bootstrap.

I preserve every first terminal:

- Original32ade Linux: C ownership passes; generated C corpus compilation fails
  strict misleading-indentation before any Nano fixture compilation. Precode
  df926 records the failure; b03 braces the same predicates/sentinel checks.
- Independent32ade puck: C ownership and canonical C corpus pass; C-seed stops
  before shadows because the pure imported module lacks the new bridge's local
  header directory, and two fixture result expressions lack struct inference.
  Precode9d3 records both.74c advertises the existing `nanoisa/file_source_plan.h`
  under the compiler's existing src include path and uses typed fixture locals.
  I do not claim to fix generic transitive module include propagation.
- Corrected74c Linux: C controls and both Nano modules compile; selected shadows
  hit the existing10-second deadline. The buffered terminal does not identify
  which shadow was running.
- One explicitly approved isolated unbuffered74c diagnostic preserves the same
  full corpus and deadline. It records75 starts,74 PASSED, no FAILED, and expiry
  during `nano_budget_case`. This is a whole-suite deadline observation, not a
  measured standalone duration. Its10,993 source and5 selected tool hashes match
  before/after. It is diagnostic evidence, not passing qualification.

After precode21fa,701 uses existing byte-string equality only for positive equal
counts that equal both backing byte lengths. The original counted-prefix loop
remains for shorter spans. I retain every original case and boundary shadow;
I do not partition the suite, increase its deadline or weaken an assertion.
Puck74c qualification was never started; no passing result is invented for it.

## Retained evidence and limits

[My report manifest](file-source-descriptive-plan/report-sha256.json) hashes892
committed reports, including26 equal source/tool inventory pairs. Every ordinary
and sanitizer source map covers all10,993 tracked files at its frozen pin;
selected final tool maps contain12 Linux and11 Darwin entries. My artifact index
retains1,352 unique files totaling315,394,075 bytes in
`/tmp/nanolang-file-source-artifacts`, with10,016 original archived-map references
and additional explicitly labeled retained module-generation files. The source
and tool maps preserve their original paths and scope.

The original Darwin maps contain the selected `/usr/bin/clang` driver shim,
actual Homebrew clang, actual Python, ffi headers/library and selected sanitizer
runtime. My resolved owning Xcode clang hash, version and SDK identity supplement
is **postphase-only**. I do not turn it into a historical before-map or claim a
complete transitive system toolchain inventory. Likewise, original product
endpoint maps cover bin/obj and fixture outputs; retained catalog `.build`
generations are inventoried and archived postphase, tied to actual compile/load
paths. Both compiler generations remain available; no lost binary is inferred
from a current-only pathname.

The puck evidence archive has26,507,141 bytes and SHA256
`fd17ffc384d58b9a570bc7caf78720ed846a4dd086edb6900f25ccb8b086ea22`, matching the
remote archive before extraction. I preserve the original qualified and failed
trees on both hosts. No historical failed fixture binary was replayed; the one
unbuffered compiler diagnostic is separately authorized and labeled above.
Independent final seal review and canonical merge remain pending.

## Canonical provider integration

I integrate actual canonical `8b84b9cd` at `af68d82ae` in separate trees.
All ten qualified plan, catalog, module and fixture files retain their exact
bytes; all 21 incoming canonical source/test files retain theirs. The Make and
roadmap conflicts are additive. Canonical private cyclic-hosted, indirect and
read-text queries do not select my source plans or widen their authority.

I rebuild the entire C-seed/provider object closure from empty object directories
on each host. I copy only the retained `nanoc`, Stage1 and Stage2 executables by
hash and compare every `src_nano` input with qualified701. Imported bridge/module
caches start empty. I do not claim another full bootstrap or sanitizer matrix.

| Host | Fresh C-seed/providers | Ordinary full paired gate |
| --- | ---: | ---: |
| Linux GCC | 20.317 s, PASS | 35.601 s, PASS |
| puck Apple Clang | 8.677 s, PASS | 32.400 s, PASS |

Each ordinary gate retains all 58 cases, exact canonical outputs and 75 selected
shadows through each of C-seed, Stage1 and Stage2, plus 7,503 linked and 7,518
instrumented C checks. Instrumented here means allocation observation; these
integration runs do not add sanitizer acceptance.

[My supplemental manifest](file-source-descriptive-plan-integration/report-sha256.json)
hashes 293 reports, including the root's independent original-seal audit and its
read-only script. Eight source/tool pairs match; every source map contains
13,220 files. I retain 698 unique artifacts totaling 185,678,820 bytes in
`/tmp/nanolang-file-source-integration-artifacts`, with 2,085 original archived-map
references. Integration product endpoints also include actual catalog `.build`
artifacts. The owning Xcode compiler hash/version is captured before these new
gates, without changing the original postphase-only claim.

The supplemental puck archive SHA256 is
`810558777f9812301ee4815af3b02e4d568857a985caede1c5d4989b484c319c`, checked against
the remote archive before extraction. Both integration trees remain retained.
All advertised report bytes are checked as actual Git blobs before publication.
Independent supplemental review and actual merge remain pending.
