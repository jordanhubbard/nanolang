# My bounded admission stage observation

I built one fresh GCC13 O3 complete diagnostic nano_virt at `f0de3405b4be39f4e7c2c1f651cfd6fa432a1f17` after source review. Preparation passed in 20.213 seconds. My unchanged original full imported emitter returned 1 after 21.547 seconds with the original internal ten-second shadow deadline. The outer 120-second bound did not fire and no emitter output was published. I keep task_4931a66f39c04b1a96981a4e00d43646 open.

I retain 407 markers, with first/last function identities and byte offsets matching the earlier 1e53 observation. Their delta spans 3764.204 milliseconds. The 495 full successful admissions took 3661.909477 milliseconds; 519 reused entries took 0.028961 milliseconds. Admission totals 3661.938438 milliseconds, 97.2832% of this observed interval.

My disjoint outer stages measured owner routing 215.336800 milliseconds, mixed routing 215.250677, readiness 647.722518, ownership support 2583.571849, and remainder 0.056594. Exact integer outer sums equal the independent total at every marker. Full/reuse counts and nanoseconds likewise sum exactly.

My nested inclusive service-pending query ran 8415 times and occupied 3661.264718 milliseconds: 17 calls per full entry. Owner routing ran 2970 times for 1293.196822 milliseconds and mixed candidacy 2970 times for 1292.584617 milliseconds; those times overlap service checking and must not be added to it or to outer stages. Ownership-contract validation and owned-transfer scans had zero completed calls inside the observed successful intervals. Static source shows service-pending checks bindings and scans function CODE for File opcodes; this run did not subdivide those two substeps.

The final marker enters `$shadow_201_parse_block_recursive`. I do not attribute the unmarked final interval, early refusals, or public admission outside the private core to these counters. Every classifier still forwards exact arguments/results and performs its original checks. Macro-enabled wrapper/clock overhead is present; this is one diagnostic comparison, not an uninstrumented performance or release qualification. No broader reuse or proof based solely on pointer identity follows from these measurements.

All inventoried source bytes and selected tool bytes are equal before/after. I added 405 generated module products and changed or removed no existing provider. The inventory covers tracked sources except historical docs/evidence and the selected GCC/make/ar/ld/Python executables, not every transitive system dependency. My content-addressed store retains all source/tool/provider/product bytes referenced by the maps. This diagnostic rebuilt hook-bearing providers and VM together; it makes no standalone verifier-link claim. The runner, analysis, full logs, exact statuses and maps are retained below.
