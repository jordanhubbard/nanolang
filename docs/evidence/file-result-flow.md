# I qualify private File and Result flow states

I qualify the reviewed declarations/state checkpoint `d4606de4f` and service,
refinement, call and join checkpoint `7bcfd0d5c` under
`task_6739329b1fcde420706bb5a10f481919`. Frozen fixtures are at `38a19922a`.
My final integration pin `24ddc72de` incorporates canonical `2cb8728a3`
(PR834–836). The File production, fixtures, nominal sizing helper and provider
lists are byte-identical across those pins. The Makefile conflict was additive:
I preserve both File controls and the separate private owned-array target.

I do not certify CODE, execute the synthetic service modules, invoke a File host
operation, discharge runtime rights or admit any public profile. Actual executed
module/wrapper controls below are ordinary neighbors. Full72556/6931 remains open.

## My controls and exact scope

Each instrumented run reports3293 checks; each independently linked run reports3164.
I cover exact/permuted nominal/import identities, copied declaration lifetime,
owned and borrowed formals, exclusive alias rejection, all five logical service
transfers, close consumption on both outcomes, explicit arm selection, unreachable
known arms, stale refinement after moves, exact helper arguments/results, pending
call obligations, divergent fresh owner identities, conflicting same-site events,
conservative joins, owner/stack/region/obligation limits and complete exits.
Allocation-prefix controls cover nominal/declaration/state/clone allocation and
second-clone failure preserving both output sentinels and balanced references.
Shared validator/verifier/native emission still refuse these service modules.

The instrumented fixture includes the reviewed flow and nominal-plan C sources
with bounded allocation wrappers; the linked fixture compiles those providers as
ordinary translation units. Strict sanitizer builds instrument those sources,
the raw nominal codec and immutable catalog; linked dependency objects come from
the fresh normal build. I do not claim every dependency object was sanitizer-built.
Both retain `-Werror`, ASan, UBSan and `detect_leaks=1`.

| Frozen phase | Linux seconds | Darwin seconds | Result |
| --- | ---: | ---: | --- |
|38a normal two fixtures|5.521|3.736|PASS|
|38a GCC / Homebrew Clang strict sanitizer two methods|3.170|4.160|PASS|
|38a corrected Linux Clang sanitizer two methods|2.810|—|PASS|
|38a nominal/raw service adjacency|1.014|1.808|PASS|
|38a provider build|7.575|3.878|PASS|
|38a fresh C-seed build|0.679|0.779|PASS|
|38a actual module compile / execution|8.604 /0.002|8.552 /0.295|PASS|
|38a ordinary wrapper compile / execution|0.170 /0.003|0.528 /0.300|PASS|
|24dd integrated normal two fixtures|5.251|8.361|PASS|
|24dd fresh C-seed build|9.239|4.813|PASS|
|24dd actual module compile / execution|8.421 /0.001|8.567 /0.309|PASS|
|24dd corrected provider build|0.323|0.753|PASS|
|24dd ordinary wrapper compile / execution|0.174 /0.003|0.570 /0.284|PASS|

The nominal controls report19766/19743 checks; raw service controls report586.
I compile and run `modules/nanoisa/mvp.nano` through the fresh C seed, and compile
and run `tests/test_minimal.nano` through the actual wrapper generator. These
exercise the module manifest and wrapper object-list providers. No Stage1/2/full
bootstrap or release-wide acceptance claim follows from this bounded gate.

## My retained first terminals and tool identities

The first Linux Clang sanitizer phase stopped before fixture execution on the
host compiler's GCC-install selection warning under Werror (outer status2,
0.097s). I recorded it before selecting
`--gcc-install-dir=/usr/lib/gcc/aarch64-linux-gnu/13` for the fresh Clang phase.
No assertion, sanitizer flag or production source changed. Normal/GCC passes
were retained without repetition.

The first integrated driver passed normal/C-seed/module controls on both hosts,
then raised `FileNotFoundError: bin/nano_virt` before launching the wrapper. The
fresh checkout needed an explicit provider build that my driver had omitted.
I preserved the terminal and captured after-inventories before recording the
correction, building `nano_virt`, and running only the remaining wrapper controls.
I did not rerun the completed normal/module phases.

Darwin uses Homebrew LLVM for strict sanitizer fixtures, selected through
`NANO_FILE_FLOW_CC`, and explicit SDKROOT plus OpenSSL LIBRARY_PATH. Its normal
38a/24dd compiler command was `/usr/bin/cc`; before/after maps capture that
system dispatcher. `apple-toolchain.json` separately captures the actual Xcode
Apple Clang21 binary and SDK identity **after** the original phases. I do not
relabel that capture as a before-map. The corrected24dd provider build explicitly
selects the full Xcode compiler path, captured before and after that phase.
Homebrew sanitizer compiler identity is captured before and after its runs.

## My seals and retained paths

[My report manifest](file-result-flow/report-sha256.json) seals126 reports, logs,
inventories and runner sources. [My summary](file-result-flow/summary.json)
records18 equal before/after pairs: original source maps contain6418 tracked
files and integrated maps contain6521; tool maps contain8 entries, with9 for the
explicit corrected Darwin provider. These broad maps include tracked evidence,
not just production source. The initial/remaining phases retain separate pins
and binary maps rather than claiming one undifferentiated run.

All4236 archived artifact entries resolve to664 independently hashed unique files
at `/tmp/nanolang-file-flow-artifacts`. Per-phase inventories retain actual linked
and sanitizer executables, provider objects, C seed and module/wrapper binaries.
I retain source trees `/home/jkh/Src/nanolang-file-flow-qualified` at38a and
`/home/jkh/Src/nanolang-file-flow-integrated` at24dd. Publication happens from a
separate ready tree, preserving both qualified trees and their tools.

Darwin source trees remain `/tmp/nanolang-file-flow-38a` and
`/tmp/nanolang-file-flow-24dd` on `jordanh@CXWWHGGJX0.local`. Its phase reports are
`/tmp/nanolang-file-flow-38a-darwin{,-closure}` and
`/tmp/nanolang-file-flow-24dd-darwin{,-wrapper}`. Local report directories use the
same names with `linux`, plus the retained `38a-linux-corrected` Clang phase.
Later docs-only commits do not claim a new qualification or bootstrap pin.
