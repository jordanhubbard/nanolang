# I retain generated LLVM and Wasm qualification evidence

I qualify my private generated ordinary record/flat-array consumer against the
actual VM corpus. These reports do not admit public mixed graphs or complete
paired source generation, nested/cyclic graphs, or the full 5.1 release.

## I retain the completed Darwin matrix

My immutable execution source is 7804258431ed8fba21fa33c5507b06ad9a1bbd94.
The fresh persistent source and artifact tree lives under
`/Users/jkh/nanolang-qualification/vm-effects-recovery-20260921` on puck.
I preserve all 15 successful selected phase terminals: setup, configuration,
discovery, 73-product C parity, three compiler/instrumentation configurations
with emission and complete native O0/O2 corpus, both-engine Wasm O0/O2 corpus,
and four unchanged private VM/query neighbors. The native sanitizer phase
takes 1263.516 seconds; the full Wasm phase takes 553.359 seconds.

I compare all 40 actual C ABI signatures before execution. The native generated
IR receives the recorded ASan pass; I do not infer retrospective IR UBSan
instrumentation. The linked C runtime uses the selected strict sanitizer flags.
The exact restricted link inputs establish the no-VM product boundary; nm
provides additional symbol evidence. All 73 products and fault positions retain
both failure modes, independent recovery, lifecycle/output assertions, requested
allocation accounting and worker coverage. Wasm runs in Node and Wasmtime.

The [Darwin summary](record-array-llvm/darwin-recovery/summary.json) identifies
181 report files, 17,599 content-addressed artifacts (638,937,640 bytes), 60,000
artifact references and 30 equal source/tool pairs. The report manifest names
actual committed report bytes. The content-addressed artifacts remain under
`/tmp/nanolang-record-llvm-darwin-recovery-seal/objects`, also retained by
verified hard links under
`/home/jkh/nanolang-qualification/vm-effects-20260921/`.

My [archive record](record-array-llvm/darwin-recovery-archive.json) retains the
202,387,709-byte gzip archive and its SHA256. I read all 17,783 decompressed
regular file members and compared their hashes to the completed seal, rejecting
extra, duplicate, missing or nonregular members.

My final current-input check rehashes 3,752 source files, 25 selected tool files
and 167 providers against the recorded endpoints. This covers those exact
files; it does not claim immutability of every SDK or system library. I copied
the completed evidence locally before publishing this checkpoint.

## I keep historical failures and remaining gates distinct

The earlier puck reboot removed remote-only temporary LLVM evidence; its
interrupted final outcome remains unknown. I retain the local observations and
original surviving C archive, and do not fabricate missing raw terminals. This
new persistent Darwin matrix is separately attributed. My earlier Linux ABI
refusal and the source correction remain separate histories for the final seal.

Linux ordinary GCC/Clang native, full Wasm and neighboring queries have passed.
Linux generated-C fault qualification is complete and separately sealed. The
LLVM sanitizer continuation remains active at this documentation checkpoint. The independent package
425bcbc786 correction passes its unchanged two-method suite on both hosts, with
headers byte-identical to the qualified 78042 outputs; it is not a relabeling of
execution source. I will publish the complete cumulative manifests and fresh
current-main integration separately before requesting final acceptance.
