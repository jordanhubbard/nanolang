# My integrated product acceptance

I freeze production `ece241ba56c34ae6e47639cd7d618f43f20ba25d`, integrating canonical main through PR678, the qualified metadata boundaries and the NanoISA-only product driver. PR666 supplies concrete generic instances; PR671 retains explicit constructor arguments; PR673 supplies native scalar variant storage. The held product includes the measured source-emitter setup budget and canonical prefix conversion repairs.

My Linux fresh bootstrap passes in 187.088 seconds, followed by rebuilt tools in 25.450 seconds. All 65 focused methods pass in 1025.536 seconds, including module facts, output/shadow preservation, declared result metadata, canonical product routes, strings/filesystem, prefix conversion, generic unions and three explicit-constructor methods. The generic suite includes all 25 unchanged affine module/generic decisions. [Pinned prefix manifest](product-gates-ece241ba-prefix.json).

My full Linux quick gate is still in progress. My Darwin full quick gate failed with exit 2 after 1411.45 seconds: `test-forth-core` reached its configured 600-second alarm while loading `core.fr`. Bootstrap, 17 core examples, 244 eligible VM examples, generic suites and preceding frontend checks passed before that failure. I retain the [sealed Darwin report](product-acceptance-ece241ba-darwin.txt), including compiler identities and full-log hash. Task `task_1b914e18e8c830e9db6fcb2be90e73b1` tracks this blocker. Static inspection finds an interactive ACCEPT exercise and a terminal-dependent input path; the failed run alone does not establish its cause. I do not label either full product gate passed from intermediate results.

My separate [proof refresh](product-formal-ece241ba.md) passes on the Darwin peer at this exact pin after the Linux environment stopped before proof compilation. It validates the formal model, not production correspondence. My earlier VM/native raw fixed points remain pinned to `84e04f5d` and do not establish convergence for this new source. Full roadmap, platform and release acceptance remain open; PR522 remains held.
