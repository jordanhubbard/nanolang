# I retain the first combined byte-array fixture terminal

At exact9bc3cd88e60d57505db1aefbe6087683763b0e1c I build ordinary fresh
compiler/providers and pass shape controls on Linux and Darwin. The first three
byte methods pass. The additive combined nested-string/byte fixture stops during
assembly with `Expected u32 operand`: PUSH_STR requires a constant symbol/index,
not my fixture's quoted literal. No module from that new fixture executes.
Remaining methods and sanitizer phases are not started.

I retain complete raw phase/child logs, exact inputs, commands, cleanup states,
source/tool maps and first terminals in the two report archives. All tracked
source byte/size/mode endpoints and actual tool hashes compare equal; every
reported group is absent. The source archive and active-scope map remain under
`/home/jkh/nanolang-qualification/candidate-byte-9bc-drivers`, identified by
`first-report-maps.json`. The archive contains3953 exact active Git inputs and
excludes historical evidence only (plus an indexed-code cache if present).
Original source/products remain at `candidate-byte-9bc-linux` and the same named
Darwin root under `/Users/jkh/nanolang-qualification`.

Linux build21.308719s and shapes0.648079s pass; the partial method group exits1
in0.869300s. Darwin build13.686689s and shapes0.706110s pass; the partial method
group exits1 in2.389467s. No timeout, retry, deadline/assertion change or
infrastructure attribution follows. Fixture-only29a3b7c4a uses the same string
values through declared constants and remains separate pending qualification.
