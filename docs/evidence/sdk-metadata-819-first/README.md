# I retain the first typed metadata fixture-discovery terminal

Frozen `81947ae390dc85ce5d1a93e5b403adf55ce13875` builds fresh ordinary Linux providers in3.231seconds. Both new metadata test methods pass. The same unittest module also exposes its directly imported `FileCyclic` helper class, whose unrelated setup refuses a missing `FILE_CYCLIC_OBJECTS` environment entry. The target ends status2 after1.088seconds, without timeout or remaining process groups. The sanitizer is unrun; Puck is staged but unlaunched.

I preserve raw reports and exact member hashes here. Source and provider endpoint maps agree. The correction will import the helper module and bind the identical `command` function, retaining every metadata assertion. This is test discovery, not a demonstrated parser failure; successful scoped methods do not make the complete gate pass.
