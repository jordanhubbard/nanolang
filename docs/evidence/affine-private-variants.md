# My private affine constructor correction and current admission evidence

I retain the original service-admission qualification in
[service-admission.md](service-admission.md). This supplement records the first
canonical51d integration, its actual constructor defect, reviewed correction,
and fresh660/033 qualification. It does not close my full-suite or separate
Darwin native-timeout work.

## My source and measured boundary

I qualified service transaction integration51d6f0420 on canonicale59fc09b.
Both hosts passed the focused switch/computed-goto matrix and actual callback/FFI
controls, then the unchanged mixed admission executable received SIGSEGV during
its first verifier call, before enabling allocation failure. My one authorized
bounded retained-binary debugger located shared clone copying a null variants
pointer from the private mixed constructor. Its debugger shell did not preserve
an independent exit status; I claim the retained signal/backtrace and unchanged
binary/provider hashes, not a diagnostic pass. Source inspection found the same
missing allocation in the private owner ARRAY constructor.

Production52a9e3925 allocates minimum variant storage and initializes every
local to UNKNOWN in those two constructors. Existing partial cleanup frees the
new allocation. Shared clone/public construction, authority and public profile
decisions are unchanged. Root independently reviewed source and fixtures before
execution. Frozen660e91f70 contains the full focused fixtures and explicit Make
compiler-flag forwarding. The descriptor-only constructor fixture includes the
actual affine TU and real layout decoder under allocation hooks; it does not
execute bytecode or establish complete profile authority.

Every GCC ordinary/GCC sanitizer/Clang sanitizer Linux and Apple ordinary/
Homebrew sanitizer Darwin configuration passed 43,086 constructor checks. Each
checks zero/three/256 locals across public, mixed and owner constructors, poisoned
storage initialization, independent clones, survival after original destruction,
and every measured permanent/transient allocation failure with fresh recovery.
The public constructor makes13 hooked allocations, mixed9 and owner9; variant
allocation zero-based indices are11,7,8. Clone makes4, with variant at2. Pointer
identity identifies each actual allocation. These counts describe the selected
instrumented TUs, not all process allocation. Sanitizers rebuild the fixture,
affine TU and decoder; other providers remain ordinary.

All five configurations also passed the unchanged focused service-admission
fixture in both compiler-selected dispatch modes, with3506 checks per executable
and retained macro evidence. Its VM/service TUs are separately instrumented;
modeled host hooks are not substituted for actual embedding acceptance.

Both hosts passed actual callbacks, callback failure/allocation controls and
28 VM FFI controls; complete mixed query/admission/heap/lifecycle; owner ARRAY
public/refusal runtime; complete owner authority prefix controls; shared ownership
contracts and affine bytecode neighbors. The unchanged full imported emitter
passed with the original ten-second shadow policy: Linux21.370s and Darwin16.088s
are total command times, not shadow-only durations. Linux output303296 bytes has
SHA256 `d97429a2c29dd09382b8957487e3a133011538dd4b9bcee3b18ac165c701e030`;
Darwin303304 bytes has `f905bba86c66c5cc25ba337ecc750b1c375be893d9ce9fe38360f69796d8be35`.
I do not claim cross-host byte equality or full-bootstrap acceptance.

## My retained failures and continuation

My first660 Linux constructor link received the external driver's larger
VM/compiler object list and failed on unresolved g_argc/g_argv, before execution.
I corrected only external selection to the reviewed Make target's exact filtered
NANOISA_OBJECTS plus NANOISA_UTF8, rehashed all3817 selected old inputs, and reused
unchanged prepared providers. No failed executable existed. Darwin had not run
that driver version. The first terminal remains a failure.

Darwin660's old owner-authority Python runner then dropped prepared OpenSSL search
flags, failing at link on libcrypto. Linux's same fixture had already passed.
Reviewed033203384 forwards optional exact caller LDFLAGS and preserves its old
default and every assertion. I loaded that exact corrected Python file externally
with ROOT explicitly set to the untouched660 tree, retained its bytes and new
products, verified all3818 prior inputs, and ran only owner authority and the
unreached continuation. Both earlier failures are retained alongside both51d
segfault terminals. Legacy child runners that delete temporary products retain
only their original logs and outer final-product scope; I do not reconstruct
missing historical binaries as original artifacts.

## My seal and remaining integration

[seal-sha256.json](affine-private-variants/seal-sha256.json) covers the exact
committed report copies. Its source/tool/provider maps have94 equal endpoint
pairs. Selected current Linux3826 and Darwin3820 paths were independently
rehashed by the retained helper. Named tools/runtime files are inventoried;
this is not complete transitive system-tool closure. Per-command products are
retained where the reviewed runner supports them and may change intentionally.

The downloaded Darwin archive is121850944 bytes, SHA256
`622eca68935c296aa28fa558dd60b0d16beb6707cb8995d7c7abd16de9540b23`.
Every509 manifest report and4712 content-addressed objects were checked after
safe extraction. Linux has703 manifest reports and4645 objects. Combined report
copies preserve separate pins and execution histories; the local combined CAS
index deduplicates content without changing those histories.

Current canonical914/915 integration, independent final evidence review and actual
merge remain required before task closure. I preserve all frozen qualification
trees. Other full roadmap criteria remain open.
