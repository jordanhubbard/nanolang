# I qualify private File frame transfers

I qualify the private frame/call API from83cb11f9b under
`task_bb3381ff39934b379067adaad2785ced`, following
[my reviewed contract](../NANOISA_FILE_RUNTIME_FRAMES.md).
I manually drive exact prepared frame sites. I do not execute File CODE through
a VM dispatcher, generate native File handlers, or admit public File programs.
Those remain required work under82ff and the full File parents.

I integrate actual carrier PR858 at21ed843950d0bd9a397d663cf9dfd3184f5d156a
before freezing56bb0836e. Only additive ROADMAP text conflicted. Reviewed frame
production and initial fixtures remain byte-identical83cb11f9b/ddcd1df09.
The final measured fixture is8644219d8, with two reviewed fixture-only corrections.

| Gate | Linux | Darwin puck |
|---|---:|---:|
| Fresh provider setup | PASS11.405s at56bb | PASS11.161s at041 |
| Ordinary linked and instrumented frames | PASS6.912s | PASS6.038s |
| GCC ASan/UBSan/LSan | PASS23.212s | Not selected |
| Clang ASan/UBSan/LSan | PASS23.471s | PASS13.904s |
| Hosted plan | PASS19.424s | PASS9.697s |
| Body query | PASS5.441s | PASS4.221s |
| Flow query | PASS4.672s | PASS4.276s |
| Private File values | PASS1.654s | PASS2.209s |
| File opcode/public refusal | PASS3.096s | PASS3.950s |
| Ordinary wrapper publication | PASS3.185s | PASS7.328s |

All post-setup rows above use864 sources and fresh per-phase fixture binaries.
I copy unchanged prepared providers into fresh corrected trees, verify each
copy, and freeze their identities; I do not attribute a repeated phony build
to an earlier inventory. Normal and sanitizer methods rebuild the complete
instrumented allocation chain and separately linked production providers.
Actual compiler paths/hashes, SDK/libffi headers/link stub, OpenSSL library and
commands are retained. Linux's explicit GCC13 selection applies only to native
Clang. Sanitizers retain leak detection and strict warnings.

Each corrected fixture run reports42,381 new instrumented and15,469 new linked
frame checks. The preserved carrier suite reports84,505/11,934 checks, including
one outer assertion counted before its renamed main executes. No carrier
assertion was removed. The carrier's printed64 acquisition attempts and63 real
opens/closes precede the new frame suite; I do not report them as whole-frame
aggregate counts. Instrumented frame checks observe real descriptor closure,
require all recorded streams closed and check exact two-close cleanup on every
partial-generation failure. Linked-mode hook counters are zero because its
host core is not interposed; they do not claim absence of actual I/O.

I test later-argument VM overlap, caller result reuse of the callee first local,
caller prefix/local preservation, native disjoint placement, repeated calls,
nested formal forwarding,64-frame bounds and explicit mode persistence when
bounds coincide. I return File, OpenResult, scalar Result, INT and no value.
Six actual generation-limit failures preserve the owner at its source, staging
or installed root; first failure sites and separate modeled close-report errors
remain checked. An initializer's handled close Error still prevents entry.
Successful frame calls/returns allocate nothing under a zero allocation budget.
Public verifier/VM/native/FFI and wrapper refusals remain unchanged.

## I retain the first terminals

- Linux56bb setup fails status2/0.274s before any fixture because the known
  native Clang GCC-install selection flag was omitted. The fresh corrected
  setup above passes with unchanged production and Werror.
- Its first normal run fails status1/3.510s at a fixture expecting INVALID for
  two borrowed CALL_REF formals. The original enum was not printed. Static
  CODE preflight returns UNRESOLVED before logical duplicate-origin checking.
  Correction041031e15 requires that exact status and preserves the output
  sentinel and zero attempted acquisition assertions.
- Independent041 normal attempts fail Linux3.396s and puck2.994s at final tracked
  allocation zero, after the preceding semantic checks. New fixture builders
  freed tracked input buffers outside the tracker macro scope. Correction
  8644219d8 restores all four allocator hooks over those builders; it changes
  no production or zero assertion. The logs do not report outstanding counts.

I retain these binaries/reports without replay. Corrected864 runs pass the
original final zero check. Earlier syntax-only builder-member mistakes remain
recorded in the contract/ROADMAP and never executed a fixture.

## I seal the measured identities

[My manifest](file-runtime-frames/report-sha256.json) seals249 reports,
23 equal source/tool before-after pairs and8,303 artifact references to1,082
unique content-addressed files (688,071,220 bytes). The artifact store is
`/tmp/nanolang-file-frames-artifacts`; every entry was rehashed. Downloaded puck
archive hashes are independently checked against its packaging manifest.

All four retained Linux scopes and both puck scopes still match their8,511-file
source inventories. The9 Linux and14 Darwin selected tool labels also match;
some labels resolve to the same compiler binary. Corrected Linux and Darwin
source maps are exactly equal. The pkg-config version/selection text is an
explicit postphase observation of the same hash-identified tool, not a
manufactured prephase record. Current-source checks do not silently substitute
this ready evidence tree for any frozen qualification tree.

Canonical integration after this seal is reported separately. The private
carrier/frame milestone does not complete runtime82ff, hosted6fc, File72556/6931,
File/Socket/GPU d03c, or the full5.1 roadmap.
