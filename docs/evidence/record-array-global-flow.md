# My private record-array global-flow qualification

I qualify production a250e5cdb through corrected fixture
b5754844254e19763de6d9b44cd098e832ba725b. I retain first fixture
fa222db084f7c956212d8795675b7d7d33557bbf separately. This is query-only evidence,
not private VM execution, public admission or complete mixed-graph acceptance.
My original stopped88ae VM fixture and all VM source files remain unchanged.

I ran seven fresh selected compiler configurations: Linux GCC13 and Clang23
ordinary, GCC13 and Clang23 ASan/UBSan with leak detection; Darwin AppleClang17
and HomebrewClang23 ordinary, HomebrewClang23 ASan/UBSan with leak detection.
Every configuration passed the linked360 and observed1352 checks. Each observed
run measured59 allocation positions, failed each in one-shot and persistent
modes, required exact MEMORY with untouched output/zero remaining payloads, and
proved independent fresh recovery. Measured peak payload was66819 bytes and
fit the query's conservative reservation. I do not count allocator metadata.

I rebuilt the seven query-provider translation units for each configuration.
The observed form includes the main analysis TU under allocator hooks and
instruments the other six TUs through the same explicit allocator shim. Ordinary
common/UTF8 providers are inventoried separately; I do not claim whole-product
sanitizer instrumentation. LSAN_OPTIONS was explicitly empty. Actual Python,
compiler and Darwin Xcode/SDK tool identities are retained.

Both hosts also passed the unchanged ownership-contract, ordinary-record,
layout/private-layout, converter/end-to-end, ordinary-array, affine-union,
public-ownership, complete-declaration, record-array-origin and copied-execution
plan neighbors. Fresh setup and exact one-method discovery passed. The57 outer
terminals contain55 passes and the two preserved first fixture failures. All
recorded leaders were reaped and process groups disappeared without timeouts.

The firstfa222 query was already eligible. My new field-origin assertion
incorrectly expected bit2 for a derived slice. Static scanner order reserves
ARRAY0 and record1 before transfer creates slice2, whose bit is4; the older
alias/copy fixture independently uses this ordering. b575 changes only the
fixture expectation, adds exact returned origin-kind checks and records the
repair in my roadmap. No analysis predicate or original VM assertion changed.

My seal contains622 actual report files,1112 unique content-addressed artifacts
(396621413 bytes),19743 artifact references and114 equal source/tool pairs.
Each host's current-input report rehashes both frozen source roots, actual tools
and the final providers/fixture products. I preserve complete source archives
(3727 tracked files each, historical docs/evidence excluded explicitly). Those
archives include the exact source and fixture bytes, not reconstructed binaries.
The Darwin archive SHA-256 is
`e97c1c21b162e897824e033d3b1b8ff1f5df353ec5bb1d49fdb4414f1859d10b`.

The manifest is `record-array-global-flow/seal-sha256.json`; the local object
index is `record-array-global-flow/artifact-store-local.json`. Local stores are
`/tmp/nanolang-record-global-linux-seal/objects` and
`/tmp/nanolang-record-global-puck-seal-local/objects`. The downloaded archive is
`/tmp/nanolang-record-global-puck-evidence.tar.gz`. I used verified immutable
CAS hardlinks when sealing Linux/puck evidence; source/provider trees remain
independent and untouched. Capacity preflights required2GiB before setup and
1GiB before each phase. No evidence was deleted.

This evidence discharges the global-flow prerequisite for resuming my unchanged
private VM corpus. Runtime VM, matched generated C/LLVM/Wasm, nested/cyclic
ordinary graphs and full paired source remain required under my open parents.
