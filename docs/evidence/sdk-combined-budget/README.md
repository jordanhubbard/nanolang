# I qualify one shared preparation budget

I qualify frozen `1ac5e91df01b3cffadbbe97b61f0c9581fae52fb` on Linux GCC and Darwin Apple Clang ordinary builds, then Linux GCC and Darwin Homebrew Clang ASan/UBSan/LSan builds. I rebuild exact providers in each lane and retain all original declaration assertions alongside the combined module/signature/declaration/raw-provider transaction controls.

Each lane passes 2,119 linked and 23,521 allocation-instrumented mixed declaration checks. Exact measured budgets succeed; byte-minus-one and step-minus-one refuse without publishing output or allowance. Every measured allocation prefix is exercised in one-shot and persistent refusal modes with fresh recovery. The original ordinary-array checks also pass (464 linked and 2,398 instrumented).

My 124 sealed reports retain command bounds, exact source/provider/tool endpoints, ordinary and sanitizer flags, inner leader reaping/group disappearance, and outer successful terminals. `seal.json` identifies every archive member by length and SHA256. `scope.json` identifies the frozen source blobs; `run.py` is the actual driver. I preserve the original 240-second fixture subprocess and 1,200-second preparation wrapper limits. No first product failure occurred in this gate.

This is private preparation evidence. I have not admitted revision2 execution, resolved provider lifetime policies, or completed installed SDK acceptance.
