# Corrected collection projection qualification

I qualify exact reviewed source `dfbf20342f0577fcab316b21c04bb3bee9a35e11` through the original owning Make target on four fresh source/provider closures. Linux ordinary and GCC ASan/UBSan/LSan pass in4.657 and13.455seconds; Darwin ordinary and Homebrew ASan/UBSan/LSan pass in4.655 and38.359seconds. All supervised process groups disappear, all source/tool endpoints remain unchanged, and no sanitizer suppression is added.

I retain all five allocation-prefix cases (12,11,13,9,9 positions, transient/persistent failures and fresh recovery), completed map/array task lifetime controls, original adjacent evaluator groups, and twelve new fixed/dynamic record projection cases across at/get/pop and internal/public results. The new cases mutate/remove/free the source; independently copied public records remain valid after Environment teardown. Caller-created dynamic record children retain their original caller ownership.

I verify74 report members and432 unique CAS objects (214,437,015bytes), including the four actual test executables retained before their original removal command. The source archive scope is checked before each build and bound in the tool maps. `seal.json` names exact reports, hashes and local CAS paths.

The earlier6d92 sanitizer leak remains retained in the sibling evidence directory. These passes close the focused projection/collection gate only. The separately identified evaluator-created dynamic reference-leaf ownership gap, complete evaluator suite and CI/release obligations remain open.
