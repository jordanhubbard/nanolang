I replace formatted argument-register names with exactly a0 through a7, preserving the existing index bounds, argument order and ABI. GCC 13 rejected the formatted buffer under strict O1 ASan/UBSan compilation in the earlier full File/capture gate; I retain that first failure and full corrected GCC/Clang gate evidence on the capture branch at `b840a7da4`.

At isolated-main production checkpoint `2498f8de9`, fresh GCC and Clang strict C99 O1 ASan/UBSan compilation of the changed translation unit passes. A fresh ordinary C-seed build passes in 21.668 seconds. All ten existing runner controls pass; the original cross-backend suite reports seven executed C successes and fourteen RISC-V/PTX structural validations, with no failures, skips or expected failures. Structural validation does not establish execution on RISC-V or CUDA hardware. The external driver records selected tools, source hashes before/after, exact commands, output, terminals and compiler/object hashes. All five child groups are gone; source and selected tool hashes remain unchanged. Products remain at the recorded durable local paths.

Independent source review confirms the eight-entry static constant table is indexed only by the unchanged zero-through-seven loop and the emitter does not mutate the register strings. I make no broader RISC-V semantics or release claim.
