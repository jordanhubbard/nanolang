# My File encoding and refusal checkpoint

I recognize six File instruction encodings for transport and later private analysis. I do not admit their execution, certify a CFG, call a File service, or grant nominal ownership authority. Task546cf and the full File72556/6931 parents remain open. I also close the implementation gap tracked by89b8: both wrapper APIs inspect the supplied module and actual embedded bytes before staging or publication. Actual-merge reconciliation remains pending.

My reviewed production is06120e116 plus50bdb8073. My first encoding fixture pin isc018417fa. My reviewed schema fixture correction is912aa9638; raw/full disassembly fixture correction is812ef8cd7; current-v2 wrapper fixture pin isff5b77898. Production is byte-identical across these qualification pins. I do not relabel one pin's execution as a later bootstrap or release gate.

| Frozen check | Linux | Darwin |
|---|---|---|
| Exact encodings, retained transport, direct/public refusals, wrapper mismatches | c018 GCC PASS13.187s; Clang PASS4.371s | 912 Homebrew Clang PASS11.054s |
| Corrected schema/ISA/disassembly/v2/service adjacency | ff5 PASS52.239s | ff5 identical snapshot PASS39.346s |
| Ordinary wrapper adjacency | ff5 PASS5.947s | ff5 identical snapshot PASS12.494s |

Each focused compiler run executes two1374-check methods. Both fixture executables use strict Werror, ASan, UBSan and LSan1. One links ordinary fresh objects; the other replaces ISA, format, service-module validation, disassembly, VM decoding and wrapper-generation translation units with sanitizer-instrumented objects. I do not claim whole-program instrumentation from either variant. I retain their executables, commands, CLI outputs and actual linked objects.

My controls distinguish all six exact bytes from data inside ordinary I64/F64 immediates, truncated File claims from undecodable earlier bytes, and valid branch boundaries from malformed ranges/targets. Raw unverified assembly and module-free disassembly roundtrip; full service disassembly refuses. Both nominal import permutations retain exact v2 service metadata. Bare/v1 claims, all selected verifier/VM/native/LLVM/Wasm/facade/FFI/COP entrypoints and six public command-line consumers refuse without output replacement or loader initialization. I execute only the ordinary neighbor, never a File handler. Wrapper controls cover claimed-module/clean-blob and clean-module/claimed-blob mismatches, daemon blob claims, malformed bytes, preserved destination and no staging.

Remaining adjacency passes33 schema checks,2968 ISA checks,210 disassembly checks,365 v2 conversion checks, pool/callback allocation recovery, three service methods (725/655/212 checks), and two nominal methods (633/433 checks). Wrapper qualification keeps five C controls and eight unchanged Python publication methods. C positives link real current-v2 ordinary bytes; the standalone wrapper executes and returns7. Daemon qualification only links; it does not start a daemon. Existing wrapper harnesses delete their temporary executables, so I retain their logs and object/tool identities without claiming those deleted binaries are archived.

## My retained first terminals

- Linux c018 first setup stops0.295s before fixture compilation: Clang's GCC installation warning is fatal under unchanged Werror. The corrected attempt explicitly selects GCC13 for native runtime IR only; Wasm flags are unchanged.
- Linux c018 adjacency stops1.784s at32/33 schema checks: six deliberately non-executing opcodes lack VM handlers. The reviewed fixture now asserts their exact values and absence, while preserving equality for every other handler.
- Linux912 stops11.874s at1222/1223 ISA assertions; Darwin912 focused passes, then adjacency stops1.304s at the same intentional full-module disassembly refusal. I retain that refusal and add raw formatting coverage; I do not remove the check.
- Static review catches retired/truncated-v1 wrapper-positive payloads before execution. All five C wrapper fixtures now use valid v2 input so early parser refusal cannot substitute for their intended object-path/link behavior.
- Darwin ff5 archive-only adjacency stops1.717s because schema-check requires Git metadata. I preserve that tree and create a fresh source-identical snapshot with synthetic Git commit `baf30bcb5407213f2b01fb9bf6e94a662b69902d`, solely for drift checking. Its6655 source hashes equal the Linux ff5 checkout. This synthetic commit is not canonical history.

A stalled remote fetch was terminated before checkout or test execution; a bounded source archive transfer supplied the isolated Darwin trees. I preserve all test terminals rather than retrying them to erase failures. No source or assertion was changed inside a running qualified tree.

## My evidence boundaries

My [report manifest](file-opcode-encoding/report-sha256.json) seals85 reports. My [artifact index](file-opcode-encoding/artifact-index.json) names1994 entries and896 unique files retained under `/tmp/nanolang-file-opcodes-artifacts`; original phase artifacts also remain. My [inventory comparison](file-opcode-encoding/inventory-pairs.json) records16 equal source/tool before/after pairs. Each source inventory contains6655 files. Final Linux tools9 and Darwin tools10 match before/after, including actual selected compiler paths, Homebrew sanitizer compiler, and Darwin libcrypto. SDKROOT and compiler selectors are explicit in the final environment record.

The first Darwin focused driver records the actual Homebrew sanitizer compiler before execution, but its default `cc` identity is the system dispatcher. I capture the selected Xcode compiler, SDK and libcrypto after driver start and label that record accordingly; I do not claim an initial before-phase Xcode inventory. Corrected final Darwin adjacency captures those actual identities before and after.

I retain Linux qualified trees `nanolang-file-opcodes-qualified`, `nanolang-file-opcodes-corrected` and `nanolang-file-opcodes-final`. Darwin host `jordanh@CXWWHGGJX0.local` retains `/tmp/nanolang-file-opcodes-912`, `/tmp/nanolang-file-opcodes-ff5` and `/tmp/nanolang-file-opcodes-ff5-git`. Full product, bootstrap/fixedpoint, File CFG/refinement/runtime/source and release acceptance remain outside this checkpoint.
