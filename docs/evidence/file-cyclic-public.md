# I qualify explicit granted cyclic File execution

I qualify the reviewed public cyclic API, native emitter and CLI candidate from
414c1042f, retaining the original aff3 first terminals. The candidate derives
from reviewed4578 production and a6f fixtures, integrated with actual private
runtime PR907 merge6dd5. My later a11cdad02 fixture adds one native zero-VM-bridge
assertion; it changes no runtime, emitter, API, admission or existing assertion.
Canonical integration and actual public merge remain separate requirements.

My seven compiler configurations pass cumulatively on Linux and puck. Linux
GCC13 ordinary passes at414c; corrected compiler selection at the same source
passes Clang ordinary, GCC ASan/UBSan and Clang ASan/UBSan. Darwin Apple Clang
ordinary passes at414c with the schema-only interpreter correction; a11c passes
Homebrew Clang ordinary and ASan/UBSan. Sanitizer commands retain leak detection
and empty LSAN_OPTIONS. A fresh Linux a11c linked public/install method passes
in51.578 seconds to qualify the additional native assertion without replaying
all seven configurations.

I retain the complete private corpus under public grant adapters and compare
all report fields and host traces. Actual generated native C executes at O0/O2.
The fixtures check explicit zero and bounded fuel, initializer/helper sharing,
pre-acquisition ABI/fact disagreement, grant lifetime, nested busy noninspection,
allocation refusal/recovery, cleanup and publication. Fresh installed-only users
check all30 headers, C99/C11/C++11/C++17 inclusion, real VM/emitter CLI limits and
errors, native multi-unit linking, archive membership and the absence of generic
VM/private-dispatch objects from the native linkage. My old acyclic APIs and
generic File refusals retain their separate unchanged controls. Required private
VM/native, carrier, core, cyclic queries, old public and archive-boundary neighbors
pass on both hosts.

I preserve five unexpected terminals before correction. Original aff3 Linux and
Darwin stop at strict installed C++11 inclusion. My canonical schema generator
now emits complete zero-valued opcode initializers, and the three ISA predicates
use the language-appropriate assertion spelling. Existing generator consistency
and all33 schema tests pass on both hosts. The first corrected Darwin schema
command lacks PyYAML under Python3.14; only those two commands subsequently use
existing Python3.13, with the executable and PyYAML source/extension files hashed.
Linux's first Clang package build omits a CFLAGS-only GCC13 discovery option in
a hardcoded Make recipe; the corrected compiler command supplies that option
without suppressing diagnostics. The separate flag-propagation task remains
open. Darwin's first Homebrew native replay exposes a written counter with no
native-mode read; a11c asserts that native replay calls the VM bridge zero times.
I do not execute failed outputs or relabel those first terminals as passing.

Full installs rebuild common providers with each phase's selected flags. I retain
the resulting provider inventory changes instead of claiming immutable objects.
Allocation hooks cover the listed rebuilt fixture providers and generated C;
this is not all-libc-allocation or whole-program sanitizer coverage. I record
actual Python/compiler/Xcode tools and selected SDK/header identity, not a full
SDK or operating-system hash. Every source/tool endpoint pair is checked, with
current frozen-tree/product rehashes retained separately.

Closed-indirect calls, richer borrowing and paired source publication remain
required under parent15a92 and the full File/release goals. This measured public
candidate does not close them or establish compiler fixed points or full5.1
release acceptance.

My [committed report index](file-cyclic-public/report-sha256.json) links the exact
phase logs, commands, source/tool maps and provider deltas. Its53 completed phase
endpoint sets contain106 separate equal source/tool before-after comparisons.
The content-addressed store retains19,463 unique objects totaling9,173,998,875
bytes and54,301 references. All eight frozen trees retain20,576 tracked source
files; final source/tool/product rehashes have no mismatch. The original and
corrected Darwin archives remain separate from the combined artifact archive.
Independent seal review and current-main integration are still pending.
