# My integer evaluator and callback evidence

I retain four exact source histories. My first `1b80030ce` ordinary negation gate passes, then its Linux ASan/UBSan/LSan gate fails with1128 leaked bytes in60 allocations from declared callback name/signature creation and parameter-type scratch. I preserve that first terminal and its failed executable. I do not call it infrastructure.

My reviewed `dfd7e53a3` correction passes unary endpoints and synchronous declaration/borrowed callback controls in four Linux/Darwin ordinary and sanitizer configurations. My combined `439cdb8ab` source adds the reviewed integer binary operations and passes both owning targets in all four configurations. Those scoped passes precede two additional static lifetime findings: lookup can skip checker-only slots, and a callback can replace the Symbol that owns its name.

My final `62a77f512110fcabaa26b4d30f832a04733a5c86` correction checks all live callable Symbol identities and copies the synchronous callback name before its first invocation. I release that independent snapshot on every return and refuse allocation failure before callback execution. The dedicated hooked evaluator object changes only the snapshot allocation in the fixture; my binary arithmetic target retains its ordinary evaluator object. I retain my original30 unary and405 binary results, six builtin aliases with owned/borrowed/checker-placeholder callbacks, and24 mutation/failure/recovery cases. Each failure preserves the owning Symbol and array input. Each recovery executes both iterations with the original callback identity after its global binding changes.

| Host and configuration | Unary, ownership and snapshot seconds | Binary seconds |
| --- | ---: | ---: |
| Linux GCC ordinary | 3.648 | 0.756 |
| Linux GCC ASan/UBSan/LSan | 8.386 | 1.784 |
| Darwin Apple Clang ordinary | 4.772 | 0.983 |
| Darwin Homebrew Clang ASan/UBSan/LSan | 7.179 | 1.721 |

Every phase retains its original1200-second supervisor bound,2GiB preflight and1.5GiB active output guard. Leak detection is enabled, with no suppressions. I retain source/tool maps, actual compiler/link commands, raw output, descendant cleanup and all22 inner executables before successful Make cleanup. The first failed binary remains at its original path. Both Darwin report/product archives are copied locally and verified before this seal; all three successful Darwin histories are present locally.

My seal verifies352 raw reports in14 lossless bundles,1510 unique retained content objects totaling819,174,719 bytes,44 equal source/tool map pairs and22 actual phase terminals. It also compares every source-map entry with its exact Git blob at all four source pins. `seal.json` records durable local object and executable paths, exact hashes, executable modes and original terminal status. The raw reports distinguish the single first failure from21 passes.

These focused gates do not close the original complete `test-eval`, native bootstrap, CI partition or release obligations. The isolated source lacks the separately qualified parser auxiliary-name, Environment field-vector and module snapshot ownership closure. Its original full evaluator record cases use those allocations, so I do not replay their known leaks. I require the unchanged complete evaluator gate on the combined candidate after those prerequisites and this repair are integrated. General factory-return, indirect and coroutine callable ownership is outside this synchronous callback repair.
