I independently verify the fresh combined backend integration seal: exact report set and bytes, all content-addressed object hashes, referenced objects and source/tool endpoint pairs. I retain the original failed macOS package invocation alongside its corrected continuation. This audit covers local evidence integrity; committed Git correspondence and semantic integration coverage remain separate checks.

I separately check actual retained LLVM native executables on four ordinary host/compiler configurations, with 292 products and 18,976 recoveries per configuration. I check both Wasm engines on Linux and Darwin, each with 292 primary and two limited-memory modules, 2,524 engine invocations and 37,952 recoveries. These audits inspect invocation paths, raw output, contiguous fault ranges, successful child terminals, native symbol exclusions and Wasm import sections. They do not substitute for source correspondence, generated-C coverage, package/startup checks, sanitizer attribution or final release acceptance.
