# My ordinary admission qualification and remaining timeout

I retain task_4931a66f39c04b1a96981a4e00d43646 open. My narrow production checkpoint9b4c7e163 has focused and adjacency acceptance, but it has **not** repaired the full imported emitter timeout. I do not extend the ten-second shadow deadline or infer full5.1 readiness.

## My source and fixture identities

I qualified production from canonicalca0c9f6a plus the reviewed ordinary admission diff. Fixtureedd51 first failed strict GCC compilation before execution; fixture8d731 changes only braces/line boundaries after that terminal. The production VM source is byte-identical across those fixtures. Independent docs/checkpoints remain separate from the frozen qualification trees. PR893's later canonical ownership changes are not included in these results; integration remains required.

Linux first preparation built ordinary providers, then stopped at `-Werror=misleading-indentation`. Before corrected reuse I rehashed3765 retained input entries, compared3602 unchanged participating files, and copied/verified293 object/dependency products. Only the expected two docs and fixture changed. Darwin preparation was fresh. Source scope is all tracked files excluding historical `docs/evidence`, not every installed system file.

## My focused results

Every configuration ran both explicit switch and computed-goto binaries, with retained preprocessor macro assertions and644 checks per executable. Linux GCC ordinary passed3.726seconds, GCC sanitizer10.640seconds, Clang sanitizer9.538seconds. Puck Apple ordinary passed2.795seconds and Homebrew sanitizer6.784seconds. These are driver phase durations, not compiler performance acceptance.

The included VM and new fixture are instrumented in sanitizer modes; linked providers are ordinary prepared objects. ASan/UBSan and leak detection remain enabled and LSAN_OPTIONS empty. PRINT formatting is real after the modeled hook; FFI and pump hooks are expressly modeled. Those controls prove no real FFI/callback ABI acceptance by themselves. Exact certificate state and forwarding classification counters cover ASSERT reuse, helper/effect transitions, raw-store invalidation, ordinary exclusions, selected fusion, managed condition release, scalar-owned separation and refusal cleanup.

## My actual unchanged neighbors

Both hosts passed the existing actual callback lifecycle suite (including40000 cross-thread calls), callback failures/allocation and all28 vm_ffi cases. I retained their fresh binaries and real shared-library fixtures. Both passed127 mixed admission checks,13358 mixed VM heap checks (488 budgets/440 injected failures), and the12-case VM/native mixed lifecycle corpus (2006 lifecycle checks plus generated native fault controls). Generated mixed native harnesses used supported unsuppressed compiler/runtime selections; this is not a full-provider sanitizer claim.

The unchanged public owner ARRAY corpus passed in both dispatch forms,18849 checks each, including public API/refusal/cleanup controls, generated native O0/O2 cases, and existing closed-backend output sentinels. Linux owner phase took12.048seconds; puck13.184seconds. Linux mixed lifecycle phase took33.051seconds; puck22.200seconds. Exact test outputs, commands and produced files remain in retained maps/CAS. Legacy neighbor runners do not archive every intermediate child command/product before overwrite: the outer reports retain their output/status and final products, with their original internal assertion logic unchanged.

## My unchanged emitter failures

The original full imported command `nano_virt src_nano/nanoisa_emit.nano --emit-nvm --strip-debug` still returned1 with `I stopped shadow execution after 10 seconds`. Linux total duration was21.219seconds, puck18.513seconds, each including preceding module work. Neither reached the120-second outer supervisor and neither published an emitter module. These are first attempts on the corrected ordinary production, retained unchanged. I propose a separately reviewed diagnostic observer before any broader optimization.

## My preparation history and map limits

I retain the external Linux neighbor preflight missing-provider failure before any command: relative provider paths used the process directory rather than the frozen qualification root. The reviewed correction preserves absolute paths and checks every provider before launching.

Next, both neighbor CLI preparations stopped before tests: Linux's default Clang lacked explicit GCC13 selection under strict warnings, and puck's default opt was absent from PATH. Corrected setup pins clang/opt and uses NMS_NATIVE_CLANG_FLAGS only for Linux native IR. It does not suppress warnings or alter Wasm arguments.

Puck's corrected setup returned0, then the external inventory guard stopped on a build output mistakenly treated as immutable input: the logged Make recipe relinked bin/nanoisa_hl_facts. I retain both hashes and the original guard stop. Before continuing, I rehashed all prior setup endpoint inputs/products, checked the exact single changed path and logged build command, and skipped setup. Only the unreached phases ran with fresh maps. The one unequal pair in my puck summary is this documented setup-output transition, not a claim of universal equality.

All26 Linux map pairs are equal. Puck has21 pairs:20 equal and the one attributed setup output above. Latest current maps were independently rehashed at3775 Linux and3767 puck paths. Named compiler/runtime/subtool identities are retained; this is not a complete transitive operating-system tool inventory. Existing providers are unchanged by test phases; newly built CLI/module-cache/native outputs are retained as products.

## My retained publication

My Linux seal contains353 reports and4427 unique objects (918891507 bytes). My puck seal contains238 reports and4409 objects (287549665 bytes). The downloaded puck archive is74426694 bytes, SHA256 `90bd06ae85b20a4f9c8682c0473ee817cbcc479d0c2bf9063e8c915b69fafb88`; all4648 regular members were independently rehashed, including every238 report entry and4409 objects. `docs/evidence/ordinary-admission/seal-sha256.json` seals both report sets, endpoint audits, provider reuse evidence and a deduplicated local CAS map. Original source trees, reports and archives remain retained.
