The extended unchanged global_aggregate_initialization.nano comparison passes VM execution but native translation refuses map_put values seen count: count is carried as a tagged global scalar. Retained baseline /tmp/pr522-globals-emitter.log has 90/91 methods passing. Require checked tagged scalar transport into HM_SET, preserving map value type checks and negative controls; qualify original fixture and translator instrumentation.

The original aggregate initialization fixture now passes both emitted modules through VM and native execution. Final translator gates pass 2657 assertions and 1614 shape checks ordinarily and with fresh ASan/UBSan/leak/UAR, including wrong/missing tag controls. The existing runtime map setter retains exact destination tag checks. Evidence: docs/evidence/pr522-implementation-2026-09-23/aggregate-globals.