Real globals emitter parity passes seed/selfhost opcode checks and VM, but nvm2c LOAD_GLOBAL simulates generic VALUE while HM operations require MAP; STORE_GLOBAL explicitly rejects map/aggregate values. Implement truthful tagged global map/record transport and access plus lifetime rooting; test initializer function returns, mutation, field/map operations and VM/native equivalence. Required full compiler native execution; current scalar/string-array global support does not fulfill this.

Local repair now preserves aggregate global value copies, variant payload facts, managed field roots, and checked tagged map values. Final source passes 2657 native translator assertions and 1614 shape checks in fresh ordinary and ASan/UBSan/leak/UAR builds, plus 74 ordinary neighbors and 21 instrumented generated-product source methods. Full-compiler and hosted acceptance remain open. Evidence: docs/evidence/pr522-implementation-2026-09-23/aggregate-globals.