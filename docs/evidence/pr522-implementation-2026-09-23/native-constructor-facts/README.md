# Native constructor facts and stack guards — partial integration

I retain native constructor payload representations separately from flat field vectors. Persistent maps follow union construction, local/global storage, direct calls, tail calls, returns and operand-stack joins. Each tag retains its own field facts; incompatible representations within one tag become an explicit mixed fact. Missing and address-taken caller contracts retain unknown provenance. Function-scoped instruction keys keep distinct functions from sharing constructor or join facts, even if their code ranges overlap. These representation summaries are not nominal or ownership proofs.

I connect those facts to tag tests on retained operand-stack values. `AGG_TAG` records a value witness; `EQ`/`NE` and their typed integer forms relate it to a constant tag. The matching conditional edge can select that payload for stack aliases of the same value. Each control-flow target receives fresh witnesses, and comparisons do not survive joins. A new load after local reassignment cannot inherit the earlier value's guard. Functions whose addresses are taken retain unknown callers and cannot use the direct-call constructor summary as complete evidence.

I retain flat field and shape compatibility checks. Native mixed record/scalar and differently shaped record unions still fail the original six source subtests. The new constructor facts must next replace the flat representation joins and connect selected views to the constructor-indexed shape graph. Nested aggregate provenance, local-load guard relations, full managed projection/lifetime integration and the broader PR522 acceptance gates remain unfinished. I do not mark the parent task complete.

## Matched evidence

The previous translator at `b74dd9965` accepts three raw cases that return a selected string payload from an integer-result function after differing callers reach the same union parameter. The new translator refuses those cases and preserves previous output. I retain the baseline build commands and three failed refusal assertions. The baseline binary was built privately from the previous translator source with the current unchanged support objects; it did not replace the working translator.

Positive controls retain an old stack value while reassigning its source local, then consume the new local through its correct string operation. An address-taken-function control retains unknown caller evidence. The existing scalar and integer-array variant tests remain unchanged, including same-variant generic instantiations, unreachable/bypass checks, aliases, padding and invalid heap contracts.

The first broader test command selected Apple Clang, whose runtime explicitly rejects `detect_leaks=1` on this platform. I retain that output and use Homebrew LLVM for the corrected run, with leak detection still enabled. I do not classify those failures as product memory defects or silently disable the check.

The expanded refusal test covers 15 cases across direct calls, local returns, tail calls, globals and stack joins. The previous translator accepts all 15; the new translator refuses them with the expected diagnostic and preserves the prior output. `pr522-constructor-guards-routes-before.log` and `pr522-constructor-guards-routes.log` retain the matched outcomes. The earlier three-case baseline remains separately archived.

The final 28-method source command completes in 68.542 seconds with exactly the original six native aggregate-union failures; all other cases pass. Generated products are instrumented. The raw paired-test helper explicitly sets leak detection and UBSan halt behavior; it does not preserve the command's UAR option, so this command alone does not establish UAR coverage for those raw products. The full fresh translator sanitizer gate separately retains its configured leak/UAR checks. This 28-method run precedes only the additive expansion from three to 15 refusal cases; the expanded method passes afterward against the same translator binary.

Earlier build/source logs retain intermediate checkpoints. `pr522-constructor-facts-native.log` predates guarded selection; `pr522-constructor-guards-native.log` predates address-taken unknown propagation and the final instruction-key scoping. The final ordinary gate is `pr522-constructor-guards-final-native.log`.

Final ordinary and fresh private instrumented gates each pass 2,657 translator assertions and 1,820 shape checks. The sanitizer driver verifies actual translator/shape object instrumentation and runs with ASan/UBSan, leak and use-after-return detection enabled. `pr522-constructor-guards-final-sanitized.log` records the terminal pass. Constructor-map allocation and cleanup are exercised by the complete translator corpus.
