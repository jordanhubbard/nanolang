# Native nested union storage

I retain constructor representation facts across ordinary record fields, selected variant payload fields, copies, direct/tail calls and globals. Shared nested edges describe storage families; they do not transfer value identity or tag guards. Pair worklists avoid recursively cloning fact graphs. An unknown parent's provenance taints a projected child's constructor facts. Exact shape copies remain separate and retain their existing compatibility checks.

The new Envelope/Choice source case passes C-seed and both VM stages at `f785bf1bd`, but both native stages reject it. I retain that baseline. After nested propagation, I also correct premature selection of plain-record shapes from a native carrier shared by records and variants, and retain a proved guard tag while the projected value's kind is still unresolved. A deeper raw test requires the record-carrier kind supplied by the nested producer edge instead of losing it after the first projection.

My controls cover one and three record layers, two fields sharing a union value, both producer orders, local/global/tail transport, owned strings and record/string/unit alternatives. Nested wrong-tag, wrong-field, unguarded, bypass and unknown-parent cases refuse without replacing prior output. The first expanded raw run used an invalid assembly result name (`record` instead of `struct`); I retain that fixture error and the corrected runs separately.

## Recursive copy diagnosis

The recursive wrapper program passes VM verification/execution but reaches the native translator's unchanged 60-second deadline. A live process sample identifies `flow_one` in the shape solver. I retain the assembly, timeout and sample. The first correction closes a direct source-descendant copy at an ancestor destination and passes four depth/known-kind graph controls, but the real program still times out. That failure remains retained.

A temporary shape trace shows growth across multiple conversions. Fresh copies now retain their source identity. If those copies return to an empty inferred ancestor destination, I can close that recursive constraint without replacing an existing edge or merging separately constrained views. Six conversion-order controls check bounded node counts, distinct roots and idempotent solving. The corrected graph gate passes 1,942 assertions, and all 11 focused raw/source methods pass in 21.503 seconds. Existing constrained-view and wrong-layout controls remain unchanged.

The first overlap build stopped at a missing initializer under `-Werror`; its accidentally launched old-binary test was explicitly terminated and supplies no qualification evidence. The initializer was corrected before the subsequent builds. Temporary graph tracing is removed from production.

## Qualification and remaining scope

Fresh bootstrap passes its rebuilt-stage smoke, comparison and install checks. The fresh private ASan/UBSan gate passes 2,657 translator assertions and 1,942 shape checks, with leak and use-after-return detection enabled and actual object instrumentation verified. The ordinary gate also passes 2,657 translator assertions and 1,942 shape checks; the actual Make target passes all four raw methods. All 53 rebuilt-stage source/neighbor methods pass in 268.759 seconds with generated-product instrumentation. The command retains all prior assertions, deadlines and sanitizer settings, including the standalone shadow-driver build. These local Darwin checks do not replace final hosted/platform qualification or refreshed raw NanoISA fixed points. Raw paired helpers preserve their own leak/UBSan options; they do not establish use-after-return coverage. Compiler and generated-product instrumentation have separate scopes.

I do not claim general constructor provenance through arrays/maps, fresh-local guard refinement, same-tag generic record-layout compatibility, or resource-bearing union ownership. The recursive controls test the retained programs and graph families; they do not prove termination for every possible constraint graph. PR522 and its parent tasks remain open for those contracts and complete platform/backend/fixed-point/hosted qualification.

The retained `run_instrumented_nested.py` harness builds another fresh private translator, verifies ASan/UBSan symbols in both changed objects, then runs the unchanged four raw and seven source methods. It routes 30 raw translator calls to that private executable and uses the existing `NANO_NVM2C` hook for both canonical native stages. It preserves all fixture assertions, output-preservation checks and deadlines, adding UAR to the raw translator environment while retaining leak/halt options. All 11 methods pass in 48.782 seconds. This separately checks the new translator allocation/failure paths, in addition to generated-product instrumentation.

The recursive baseline uses the new nested-fact classifier with the shape solver preceding this repair (`f785bf1bd`); only the shape solver changes between the retained recursive timeout and its correction. The initial Envelope baseline uses the full preceding checkpoint. `source-sha256.txt` identifies final sources, and the committed fixture/assembly retain the inputs. The diagnostic trace is supplemental evidence; the unchanged raw runtime control and graph tests establish the corrected outcomes.
