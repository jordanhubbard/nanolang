The mixed aggregate formatter exposed declared enum payloads being treated as structs by the C-seed ordinary union constructor checker. Resolve the enum annotation and compare exact nominal identity using the existing exact-type checker. Valid Mode payloads now pass; unrelated enum and raw integer controls reject while preserving previous output. Five focused methods pass through a fresh ASan/UBSan instrumented C seed and generated products with leak/UAR enabled; fresh bootstrap, complete typechecker/transpiler and all 61 ordinary neighboring methods pass. Generic/resource-bearing union work remains separate. Evidence: docs/evidence/pr522-implementation-2026-09-23/aggregate-format-parity.