# Native constructor-indexed union storage

I connect native constructor facts to constructor-indexed shape storage. A storage family containing record payloads and more than one payload-bearing tag retains separate payload layouts by tag; scalar-only families retain their existing finite tagged carriers. Copies, calls, tail calls, globals and stack joins propagate the storage requirement without equating different constructor payloads. A proved value guard selects the corresponding shape view before projection. My existing managed record snapshots and runtime tag checks remain in use.

The original five source methods now pass through the C seed and both native/VM stages, clearing the six native failures retained at `a74c8e96b`. The initial 28-method generated-product instrumentation run also passes. The expanded retained-global/allocation-churn fixture then exposed two native refusals: a proved tag was discarded while caller payload facts were still arriving, permanently widening a selected local to an unknown tag. I retain the tag established by the unchanged stack value's guard without inventing missing payload facts. The corrected 21-method run passes. That run includes imported scalar-carrier methods; the final test module avoids rediscovering the imported TestCase.

Raw paired controls cover record/string/unit payloads through local, global, tail-call and stack-join routes, in both producer orders. Wrong tags, wrong nested field consumers, unguarded projections and a bypass around the guard must refuse without replacing prior output. The source fixture retains a record with an owned concatenated string and string array while 300 global reassignments allocate new strings, then checks the saved payload and its formatted text.

## Qualification boundary

Before the storage-choice correction below, ordinary and fresh private ASan/UBSan translator gates each pass 2,657 translator assertions and 1,820 shape checks. The instrumented run retains leak and use-after-return detection and verifies actual object instrumentation. Bootstrap and the rebuilt-stage 31-method generated-product-instrumented source command pass at that checkpoint. Corrected final qualification is recorded below. Generated-product instrumentation does not instrument the compiler itself. The raw paired helper sets its own ASan options with leak detection; its run does not establish use-after-return instrumentation. The private translator gate separately enables leak and use-after-return checks and verifies actual ASan/UBSan object symbols.

I have not completed nested union constructor provenance through every aggregate/array projection, guard relations on fresh local loads, same-tag generic record-layout compatibility, or resource-bearing payload ownership. Those remain under the existing native/generic union tasks. Full platform/LLVM/Wasm coverage, refreshed final fixed points and hosted acceptance remain required. This checkpoint does not close PR522 or its parent tasks.

The first final-source command passes 30 methods and reports one launch error in the remaining method: `bin/nano_vm` was missing while the concurrent bootstrap command relinked that binary. I retain the traceback and bootstrap commands. I rerun the complete source command after bootstrap, with unchanged assertions, deadlines and sanitizer settings. This scheduling error is not a successful qualification result.

The hosted snapshot records the preceding pushed head `a74c8e96b`, not this checkpoint: 14 successful checks, 9 in progress, 14 queued, one skipped and one neutral. It does not establish final hosted acceptance.

## Nested compatibility correction

The broadened 23-method nested/source suite finds five refusals for the same unchanged nested generic record-field fixture, across both native stages, standalone emitters and standalone shadows. The private translator built from `a74c8e96b` accepts the retained module, and its strict C11 output executes successfully. I retain the failure matrix and baseline.

I activated indexed storage too broadly: a union with one payload-bearing constructor has one payload layout, even beside unit constructors. It retains its existing flat storage. I require indexed storage when a record payload meets another payload-bearing tag, and propagate that requirement to all connected storage and producers. This preserves the nested Box fixture without dropping the mixed record/string/unit and distinct-record-layout cases. All nine focused methods pass after this correction. General constructor provenance through nested heterogeneous containers remains unfinished; preserving this existing case does not establish that broader contract.

## Corrected final gates

The fresh corrected ASan/UBSan gate passes all 2,657 translator assertions and 1,820 shape checks with leak and use-after-return detection enabled. Actual object instrumentation is verified. The corrected bootstrap rebuilds both stages and passes its smoke, comparison and install checks. This is not a refreshed raw NanoISA fixed-point acceptance result. The corrected ordinary gate also passes 2,657 translator assertions and 1,820 shape checks. The new `make test-native-indexed-union-payloads` target passes both methods (eight paired transport cases and four refusal cases). The final combined 52-method source command passes in 258.535 seconds, including all previously failing nested stage, standalone and shadow cases. Generated products use ASan/UBSan with leak checks; source helpers that retain the command environment also enable use-after-return detection. The earlier full transpiler gate passed before the storage-choice correction; the final bootstrap and source suites exercise that correction. No assertion, deadline or sanitizer check was weakened.
