# My manifest extern compatibility baseline

At repaired checkpoint `59ff072cc`, all three unchanged native module-linking
methods fail before linking with `unsupported extern result or symbol
fixture_value`. Hosted Darwin job `107625332746` retains the same refusal in
the sibling record-projection evidence archive.

My source audit finds the declaration reaches `nisa_register_extern`; its
name is absent from both the self-hosted artifact catalog and native
`import_host` catalog. Existing immutable manifest artifact construction and
VM typed libffi marshalling are reusable prerequisites, not proof that native
publication accepts arbitrary declarations. I record the required implementation
and exact acceptance in `docs/NANOISA_DECLARED_SCALAR_ARTIFACTS.md`. No production
admission changes or passing-linker claim belong to this baseline.
