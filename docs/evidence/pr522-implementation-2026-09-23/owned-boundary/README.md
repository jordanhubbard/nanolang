# My owned-union boundary checkpoint

I rerun the unchanged resource/ordinary selected-arm acceptance method separately
for each installed compiler. The C seed passes; Stage 1 and Stage 2 refuse exact concrete scalar union
signatures. The terminal files record those outcomes;
`manifest.json` pins their hashes and the actual compiler binaries. These tools
were already present; this is not a fresh bootstrap of the source head.

My implementation contract is in `docs/NANOISA_OWNED_UNION_CONTRACT.md`.
This checkpoint establishes the remaining boundary, not feature completion.
