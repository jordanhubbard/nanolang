# String boolean conversion

At `3bd3f335d`, I retain five failures from the same source fixture: my legacy bytecode frontend fails a shadow assertion for `"false"`; my C seed emits an incompatible string-to-integer helper call; my canonical emitter and both native stages refuse the conversion. `baseline.log` records those original failures. My interpreter already recognizes only `"true"` and `"1"` as true.

I preserve that source rule in both bytecode frontends by evaluating the argument once, duplicating the resulting string, comparing against those two spellings and combining the booleans. I use the existing generic `EQ` operation for strings. My C emitters select an exact string helper; numeric helpers and raw ISA execution remain unchanged. I correct the standard-library documentation's former nonempty-string claim.

The fixture checks exact accepted spellings, empty strings, case, whitespace, leading zeroes and other rejected spellings. Its mutable counter checks that true and false conversions each evaluate their source argument once. Function shadows execute the same contract before native publication.

`corrected-frontends.log` records both freshly rebuilt bytecode frontends executing the fixture in NanoVM and as strict O0 standalone native C under ASan/UBSan, leak and stack-use-after-return checks. The compilers and VM themselves are ordinary builds. `raw-control.log` retains raw `CAST_BOOL` accepting non-null empty and `"false"` strings in NanoVM, plus the unchanged explicit native refusal for raw string truthiness.

My fresh `bootstrap3` passes both compiler smoke tests and seed-independence verification. This does not establish a canonical module fixed point. The complete 15-method driver suite passes ordinarily in 4.876 seconds and with instrumented generated native programs in 6.765 seconds, including all five routes through the string fixture. The complete `make -o nanoisa_emit test-nanoisa-src-nano` gate passes 86 bytecode comparisons and all 91 Python methods in 297.002 seconds, using the freshly rebuilt emitter. `make test-transpiler` passes its StringBuilder boundary checks and two publication tests. Final hosted platform/sanitizer qualification and fresh canonical fixed points remain separate PR acceptance gates.

I run the driver with `NANO_CC=/opt/homebrew/opt/llvm/bin/clang` and the existing CI shadow deadline `NANO_SHADOW_TIMEOUT_SECONDS=60`. The instrumented run sets `NANO_NATIVE_TEST_CC` to that compiler plus `-O0 -fsanitize=address,undefined -fno-sanitize-recover=all -fno-omit-frame-pointer`; `NANO_CFLAGS` contains the same flags; `NANO_LDFLAGS` supplies `-fsanitize=address,undefined -L/opt/homebrew/opt/openssl@3/lib`. I retain `ASAN_OPTIONS=detect_leaks=1:detect_stack_use_after_return=1` and `UBSAN_OPTIONS=halt_on_error=1`. The invoked command is `python3 -m unittest -v tests.test_nanoisa_emit_driver`.
