# I qualify private SDK lifetime wire transport

I qualify frozen `255f8b241f2a4ee50c20645b2686472e4ee1541d` on Linux GCC and Darwin Apple Clang ordinary builds, then Linux GCC and Darwin Homebrew Clang ASan/UBSan/LSan builds. All four lanes rebuild providers, run the unchanged declaration fixtures, and run the original raw SDK codec plus additive revision2 lifetime controls.

Each declaration lane passes 2,189 linked and 25,493 allocation-instrumented mixed checks, alongside 464 linked and 2,398 instrumented ordinary-array checks. The raw codec retains exact bytes, owned copying, unused policies, reserved-byte/slice/count refusals, legacy revision refusal, exact/minus-one budgets, every measured allocation prefix in both failure modes, and fresh recovery. Callback ABI2 refuses; only the existing shared V1 ABI and execution values are accepted as transport fields.

My 140 reports retain source/provider/tool endpoint equality, commands, flags, inner process-group cleanup, and zero outer terminals. `seal.json` records exact member lengths and SHA256 values; `scope.json` records frozen Git blobs; `run.py` is the actual driver. Declaration subprocess/preparation bounds remain 240/1,200 seconds; each codec compile and execution retains its 60-second bound. No first product failure occurred in this gate.

I have qualified private raw transport only. Semantic crossvalidation, provider image attachment, typed aggregate adapters, callback/COP envelopes and installed SDK execution acceptance remain open. No executable consumer has gained revision2 admission.
