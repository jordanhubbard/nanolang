# Dynamic child owning gate: first checker terminal

I retain exact c34a50190 ordinary Linux owning gate exit2 after5.612seconds. Fresh providers compile and link; the new dynamic fixture stops at run_ctx_init before its dynamic operations. Other configurations remain unrun. I preserve the failed executable and all endpoint maps.

A separate approved checker-only diagnostic links101 rehashed providers and checks the exact unchanged source literal with stderr visible. It calls tokenize/parse/type_check, never run_program or evaluator operations. Its compile passes in0.264seconds; checker exits1 in0.0037seconds. Provider bytes remain identical and both process groups are gone. The diagnostic identifies incomplete destination representation and declaration-owner facts on pair/right/left STRING-array arithmetic returns. Earlier speculation about basic string-array operator admission was incorrect; the coarse checker already accepts PLUS and the complete result adapter is missing.

I verify26 report members and107 CAS objects (28,448,569bytes). The complete diagnostic source, command, provider maps and terminal records are included. Buffered owning stdout is not used to infer prior assertion acceptance. Source correction22fb follows precode18a; no corrected gate result is claimed here.
