# My cyclic dispatch canonical integration evidence

I integrate sealed private dispatch31f42 with canonical af8809b32 at frozen
`3ea2dfa983fe67fb97e625781e90e2d42e8c547f`. Incoming904 adds a distinct indirect
ownership query and factors the existing cyclic query behind an explicit NULL
extension;905 adds strict binding and portable-read providers. My eight runtime/
adapter source blobs and six fixtures remain exact. The only merge conflict
was additive Makefile targets; both sets remain. All27 incoming source/test
paths match canonical Git blobs exactly.

I rebuild fresh providers on Linux and puck, then pass nine ordinary phases on
each: setup, configuration, exact two-test discovery, matched cyclic VM/native
O0/O2, cyclic query, cyclic hosted plan, indirect ownership query, public API and
actual linked-archive boundary. The archive closure retains disjoint explicit
objects, member inventories and symbol provenance. This supplement does not
relabel original sanitizer results as integrated or claim a new bootstrap.
Public/source cyclic admission, indirect execution and richer borrowed profiles
remain outside this checkpoint and required by their open parents.

## My interpreter provenance correction

Independent review found that Linux gate argv used `/usr/bin/python3` through
`sys.executable`, while `tools()['python3']` hashed the PATH-selected Homebrew
interpreter. This applies to both the original qualification and this supplement.
The actual Linux interpreter's historical hash was not captured. I retain the
valid compiled C results, argv and measured source/compiler/provider identities;
I do not claim complete toolchain immutability or retroactively recover that hash.
Darwin's actual interpreter matches its tool map.

The current-only interpreter observation is separately labeled. Per-host
`driver.py` files are the exact executed runners. The top-level integration
driver copy is a prospective correction, adding `sys.executable` explicitly;
it did not produce these historical runs. Root accepted the attribution limit
without gate replay solely for this gap.

I preserve the original seal unchanged and include the independent native audit,
which rehashed537 Git reports,9917 unique artifacts/the archive,49 source/tool
pairs and all seven frozen roots, and checked exact VM/native traces and native
isolation. The audit records the same interpreter limitation.

`report-sha256.json` seals this supplement's reports; `artifact-store.json` and
`artifact-archive.json` identify its retained bytes. Source and recorded tool
maps are equal before/after each phase; setup additions and any changed existing
provider are recorded separately. Original scope and partial instrumentation
remain as described in `../file-cyclic-dispatch/README.md`.
