# My private mixed record-array VM qualification

I qualify the distinct private ordinary VM at
`15d1a298a1a6e5749da16aa86738fa298d8d7fd7`, integrated with actual canonical
main6b5713fd9 (including PR935 source locations and the invocation-local service
code cache). The sole manual runtime integration line initializes the private
root's instruction_ip. Query production and the original stopped88ae VM fixture
remain byte-identical through this integration. My separately sealed global-flow
query prerequisite is recorded in `record-array-global-flow.md`.

I ran seven selected configurations with fresh15d1 providers: Linux GCC13 and
Clang23 ordinary plus each compiler's ASan/UBSan/leak-detection form; Darwin
AppleClang17 and HomebrewClang23 ordinary plus Homebrew ASan/UBSan/leak detection.
Each configuration passed both actual switch and computed-goto dispatch, with
macro selection checked, in linked2375-check and observed5948-check forms.
All28 complete program-corpus runs passed. Every observed run retired all93
recipes and inspected all256 opcode decisions and independent decoded maps.

The unchanged corpus covers global/record aliases and distinct copies, all five
flat element kinds, wrong-tag consumed-root cleanup, prior results and committed
globals after failure, initializer-result prefixes, strings and mutation,
zero-field construction, declared acyclic graphs, exact1024-frame success and
1025-frame refusal, thread/busy/epoch and retain overflow, and partial boxed-copy
rollback. Every observed run measured86 preparation and8 execution allocation
positions and exercised one-shot/persistent failures with independent recovery.
The256-level heap release control remains distinct from actual bytecode graphs
within the64-origin preparation bound. I do not infer general cycle collection.

I rebuilt the listed query/VM translation units for each selected configuration;
the observed fixture includes the VM TU for private controls and allocator hooks.
Common providers retain ordinary setup attribution. I record exact compiler,
Python, selected Darwin SDK/Xcode tools, flags, objects and archives. LSAN_OPTIONS
was empty. These are scoped sanitizer results, not whole-product instrumentation
or proof that every external system header/library stayed immutable.

Both hosts passed the unchanged global-flow/origin/copied-execution neighbors.
The first old ordinary-admission compilation then identified13 direct calls of
the internal function's former three-argument signature. Fixture-only
`46c4d1692cf9bc9b52c2f84e71464dee8642d75b` supplies explicit NULL for the new
private context. The same full caller inventory corrected two older string-proof
calls to NULL,NULL. All predicates remain unchanged; no production bytes changed.
Fresh continuation trees verified every copied provider against the final15d1
inventory before running only the corrected/unrun neighbors. Dependency `.d`
and schema `.stamp` files are separately hashed current-only build metadata.
They are not retrospectively claimed in the earlier provider inventory.

The corrected neighbors passed ordinary admission, the standalone owned-string
proof (150 checks and9 admissions), owner-array overwrite, private File VM and
the complete public cyclic corpus/installed package. Final package phases took
136.266 seconds on Linux and71.858 seconds on Darwin. Passing private VM matrix
phases were not replayed for these fixture-only changes. Package preparation
legitimately rebuilds providers; I preserve its before/after products and do not
claim those mutable provider endpoints are identical.

## My retained first terminals

- Both3115 setups/discovery passed, then the nested fixture main macro and
  strict unconditional-break formatting stopped compilation.
- Both4dc corrected compilations reached linking, which exposed missing
  standalone g_argc/g_argv fixture globals.
- Linux88ae setup stopped with an explicit temporary-file ENOSPC diagnostic.
  Darwin88ae reached its first graph preparation, which refused global-loaded
  exact field installation. Neither history executed that private graph.
- All15d1 private matrix and query phases passed before each host's old
  ordinary-admission fixture stopped at its internal argument-count mismatch.
- The first46c continuation preflights stopped before testing: Linux below the
  promised2GiB minimum; puck at a schema stamp outside the old product inventory.
  The corrected launcher classified that metadata explicitly in fresh paths.

The56 bounded outer terminals contain48 passes and these eight historical
compile/link/setup/query failures. Both46c preflight failures have retained raw
launcher logs and are not invented test terminals. Every recorded outer leader
was reaped, its process group disappeared, and no timeout occurred. The query
prerequisite's two earlier slice-identity fixture failures remain in its separate
seal and are not folded into these56 counts.

## My evidence and storage

`record-array-private-vm/seal-sha256.json` covers671 report files. The object
index covers5001 unique artifacts (1933567723 bytes),25829 references in the
committed reports, and112 equal source/tool pairs. Source inventories cover
3724 historical files or3732 integrated files, with historical docs/evidence
explicitly excluded from source archives. Current-input reports rehash all
frozen historical/current roots, selected tools and final products.

Linux stores are `/tmp/nanolang-record-vm-linux-original-seal/objects` and
`/tmp/nanolang-record-vm-linux-neighbor-seal/objects`. Complete Darwin bytes are
local in `/tmp/nanolang-record-vm-puck-evidence.tar.gz`, SHA-256
`71eb1a14810029ab9446b9b621ef13a8301fabcb7880bcf694eae445bf146bd7`,
247526551 bytes. I streamed and rehashed every member, including all2680 Darwin
CAS objects. To avoid846MB of redundant extraction under limited Linux space,
`artifact-store-local.json` indexes each Darwin object's exact archive/member.
Every byte is retained locally and in the untouched remote sealed stores.
An auditor can hash `tar.extractfile(member)` directly; decompression is not a
missing evidence claim.

I reclaimed only30508913 duplicate payload bytes within my two self-owned query
CAS stores by atomic verified hardlinks. Every original path and SHA remained
unchanged; the exact manifest is retained under `extra/`. No source/provider or
sole evidence was removed. My new ready worktree initially lacked its index;
its first merge refused before any source merge. I initialized that new tree's
index from its exact HEAD, verified clean state, then merged canonical source,
resolving only additive roadmap content. The retained setup records do not
claim a compiler or runtime failure. A local packaging attempt also needed its
report parent directory created before writing an archive index; no fixture ran
and no previously sealed bytes changed during that routine packaging correction.

This completes only the reviewed private VM milestone. Public mixed admission,
real generated C/native LLVM/Wasm execution, full nested/cyclic ordinary graphs
and paired source remain required. I keep their full parent tasks open.
