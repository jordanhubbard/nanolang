# My qualified opaque transport prerequisite

I qualify production `07cc4fa7b` with the two fixture ownership corrections at
`88669142f`. My original fresh Linux and Darwin ordinary runs pass the opaque
identity, protocol, fuzz, FFI and lifecycle controls. Both fresh sanitizer runs
pass the new opaque controls, then stop on the original protocol fixture's
208-byte leak. I preserve those first terminals without relabeling them.

My corrected continuation passes all fourteen recorded phases across both hosts:
the unchanged protocol and FFI assertions in ordinary and ASan/UBSan/LSan
configurations, plus the previously unreached sanitizer fuzz and lifecycle
controls. Each continuation proves unchanged production source, tool identity,
compiler arguments and copied provider bytes before execution. Both corrected
fixtures release only their owned results and assert empty heaps.

[My checks](checks.json) rehash all 160 report members and all 1,233 retained
product objects covering 1,416 paths. The durable product archive locations and
hashes are recorded there. The original recipes remove their protocol, fuzz and
FFI test executables; I do not claim to retain those removed executables. The
opaque owning-control and lifecycle executables remain in the retained products.
Source and tool endpoints are unchanged, and every supervised process group is
gone. I preserve the exact continuation driver and its identity alongside the
four raw report archives.

This establishes the bounded generation/issued-slot transport prerequisite and
the corrected fixture lifetimes. It does not establish logical provider object
lifetime, nested opaque or callback transport, threaded fork safety, an exec
worker, the integrated candidate, or release readiness. Those obligations remain
open under `task_f1cde0aa640f4c1fb33cbdf3ab3fb678` and the complete SDK contract.
