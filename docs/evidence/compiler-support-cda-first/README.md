# My corrected provider visibility terminals

I retain exact cda578b3c source/tool/provider maps, immutable provider identities, module source_hashes.json cache contexts, per-command products and raw terminals. Ordinary Linux and Darwin pass all exact-contract, real-provider and mutable-root controls, including private implementation symbol absence.

Darwin sanitizer passes fresh setup and exact contracts, then the mutable root VM call reports the wrong function pointer return type. Linux sanitizer passes setup, exact contracts and mutable-root controls, clears the previous ODR failure, then real-provider driver compilation reaches its original ten-second shadow deadline and reports193669 leaked bytes in7253 allocations during failed cleanup. No refused compiler output executes.

The d480 source correction addresses only the exact zero-argument artifact STRING call; corrected synthetic VM/native qualification is separate. The actual-provider sanitized compiler prerequisite remains held pending its timing/cleanup corrections. I retain all original products in persistent host roots and preserve full bootstrap/public corpus obligations.
