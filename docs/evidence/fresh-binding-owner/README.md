# My fresh binding nominal ownership qualification

I repair task_f0a5c0b7f72b9041482d1c808af2aeff from candidate55cc9c83e. Each new binding receives its own annotation and explicit producer metadata. Generic emitter parameters retain concrete instance names and complete declared annotations. I synchronize the optional name index before reusing popped slots; that maintenance no longer depends on a same-name metadata lookup.

Sourcec57d96975 passes all ordinary gates on Linux97907 and Darwin39865: original14 TCO cases plus one additive mixed record/tuple/scalar/callback case, original parameter3, direct generic metadata, and original/additive symbol-index controls. My original-method inventory checks all14 source methods remain byte-identical.

The original generic emission sanitizer control exposed a separate AST_CALL concrete_func_name destructor omission. Sourceead442cf9 fixes only that owned-cache release. Fresh sanitizer provider/index/nominal gates pass on Linux65613 and Darwin89836. Darwin also passes full TCO15 and parameter3. Linux stops at the original200000-iteration deep recursion shadow deadline; task_e2225a242c0f42eba5c8623bdfd53850 remains open, without a deadline increase or full-suite pass claim.

Linux continuation22111 independently verifies every original source/provider/tool hash and compiler wrapper before and after the remaining unchanged recursive-tuple, additive mixed-name, and parameter3 sanitizer selection. All five methods pass in18.552s; all process groups are absent. Thus the requested tuple boundary passes ordinary and ASan/UBSan/LSan on both hosts. The complete Linux TCO sanitizer suite remains unqualified.

The ead driver executes sanitizer only. Its inherited terminal scope text says ordinary/sanitizer; the recorded phase directories and this correction are authoritative. Prior c57 ordinary evidence is separate. I retain first terminals in [the preceding attempts](first-terminals.md). No retained-dispatch source or fixture changes are included.

| Report archive | Members | SHA256 |
|---|---:|---|
| ead Linux | 25 | `4e9225a137c02445b2d70dcaa93799d5e0bcdc0044c68e121a676e284f087549` |
| ead Darwin | 27 | `df63a51d4f4b493a5def531eea504565fdd9f36d0953f5f0a11e95895f7b0343` |
| focused Linux | 6 | `1ada82047a2f2bde73ca083920f7b523c6346267bef7df619fd95b6ed24966f5` |

Actual provider and owning test executables remain in the host qualification roots. These archives retain reports; original Python compile/run temporary outputs follow the unchanged fixture cleanup contract. Source, provider and tool invariance and process cleanup are checked at each terminal. This is focused repair qualification, not a full release or installed acceptance claim.
