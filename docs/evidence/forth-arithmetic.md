# My corrected Forth arithmetic qualification

I qualify source and additive original-session fixtures at `e384c06bd4a59314bf4782955c56ade9faeef116`, based on actual PR943 merge `92720ef41d5050b54214b27a0b91d8d0c51d1ea4`. My independent root source/fixture review approved unsigned modular double arithmetic, checked signed/unsigned/triple division, explicit throw codes and finite/capacity-checked F>D before execution.

I ran all four original targets in order: `test-forth-session`, `test-forth-core`, `test-forth-double`, and `test-forth-float`. All 28 phase terminals pass: Linux GCC13 and Clang ordinary plus strict UBSan, and Darwin Apple ordinary, Homebrew ordinary and Homebrew strict UBSan. Each session reports 38 passed and zero failed. Every Core, Double and floating corpus reports zero error counters. I do not claim a Standard System or close the full 5.1 parent.

I retain the original Linux external inventory failure separately: the driver initially looked for collect2 under `/usr/lib/gcc/aarch64-linux-gnu/13`; the selected GCC reports `/usr/libexec/gcc/aarch64-linux-gnu/13/collect2`. It stopped before compilation. My corrected driver used a fresh tree and the identical source archive. No program assertion or compiler warning policy changed. Darwin uses its actual current xcrun SDK explicitly. UBSan halts on its first finding; LSAN_OPTIONS is empty.

My complete 3,733-file tracked input closure includes build inputs, source, fixtures, examples and Forth specifications. Source and actual tool inventories agree before/after on both hosts. Every phase records its command, environment, stdout/stderr, 900-second deadline, return code, reaped leader and absent process group. No corrected phase timed out or reported a cleanup error. The shared Core/Double/float executable is built and executed sequentially. A compiler wrapper copies each actual linked executable before Make can run or delete it; all 28 executed binaries are retained and hashed with mode and actual compiler argv. Object/dependency maps name the actual rebuilt products, rather than inferring them from a source revision.

My [seal](forth-arithmetic/seal.json) indexes three lossless report bundles with 200 raw reports, 2,091 content-addressed artifacts (495,341,584 bytes) and 8,344 product references. Each member records raw and stored hashes. The local store is `/tmp/nanolang-forth-arithmetic-seal/objects`. My [local audit](forth-arithmetic/local-audit.json) rehashed every bundle/member/object and all 3,733 corresponding Git blobs. Independent root review and canonical integration remain separate prerequisites for merge; original sanitizer attribution remains this exact source.

My persistent Darwin tree is `/Users/jkh/nanolang-qualification/forth-arithmetic-e384-20260921`; its complete reports, executables and final products are copied locally. Linux uses `/tmp/nanolang-forth-arithmetic-e384-r2-linux`, with the failed inventory preserved at the original `...-e384-linux` path. The previous PR943 qualification remains immutable, including its explicitly deleted inner-product limitation. This checkpoint supplies newly retained binaries and does not reconstruct that historical evidence.
