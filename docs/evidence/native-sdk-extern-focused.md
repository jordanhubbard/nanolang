# I retain the focused extern-owner qualification

I preserve three original Linux terminals with exact source manifests, tools, command arguments, stdout/stderr, descendants and provider identities in `native-sdk-extern-focused/manifest.json`.

| Source | Actual terminal |
|---|---|
| `25b5a77ee` | Strict compilation refuses the fixture's missing `(Function)` compound-literal cast; no executable runs. |
| `ddc1d9943` | Strict GCC O3 compilation refuses the existing signed-negative union allocation range; no executable runs. |
| `5daac37cf` | The corrected actual checker fixture builds and passes all original extern-owner, alias, full-signature and staged-label allocation controls, plus root/module negative variant-count refusal. |

I compile the included corrected checker with actual Make flags and link the independently rehashed ordinary `1bf6ab758` closure, excluding its `main.o` and `typechecker.o`. I verify every retained source, tool and product before reuse and every retained product afterward. The corrected negative-count controls restore their AST counts before destruction. This guards the variant dimension only; it is not a claim about every nested allocation count.

My manifest preserves each original byte length, mode and SHA-256, and each stored byte length and SHA-256. Reports larger than 40,000 bytes are losslessly gzip-compressed, with complete decompressed readback verification. The successful helper executable is retained by the same rule. Historical source and provider trees remain intact. Darwin focused checks and the separate combined canonical-record/bootstrap/installed SDK acceptance remain open; none of these focused terminals establishes full release readiness.
