# My checked list/trim bootstrap checkpoint

I qualified source 0e72a33a6c4dcee95c08b70f0a658433775b4da9 in fresh isolated
Linux and Darwin trees. I did not run the full eighteen-method source corpus:
my separately recorded native specialization prerequisite remains required.
The earlier cb489/diagnostic first terminals are retained at their original
paths and reports; I do not relabel them as this corrected run.

| Host | Fresh Make build | Fresh bootstrap | Provider preparation |
| --- | ---: | ---: | ---: |
| Linux | 76.284s | 284.633s | 1.568s |
| Darwin | 93.304s | 295.590s | 0.950s |

Both bootstraps pass the unchanged complete component/root shadow selection,
Stage1 construction and Stage2 construction at the original ten-second shadow
bound. These runs clear the prior nineteen list-set discarded-fact errors and
str_trim destination error on both hosts. A bootstrap pass is not a fixed-point
or release claim.

My focused runner then runs exactly the two existing methods
`test_checked_storage_scheduler_and_allocation_prefixes` and
`test_array_intrinsic_and_declaration_identity`. Each configuration passes both
methods, with no skipped method:

| Configuration | Retained outer command time |
| --- | ---: |
| Linux GCC ordinary | 3.806s |
| Linux GCC ASan/UBSan/LSan | 9.646s |
| Linux Clang ASan/UBSan/LSan | 8.705s |
| Darwin Apple Clang ordinary | 3.646s |
| Darwin Homebrew Clang ASan/UBSan/LSan | 7.098s |

The sanitizer scope is the fixture's five documented helper builds and owning
TUs, linked with fresh ordinary providers. It is not complete compiler
instrumentation or all-compiler allocator failure recovery. Legacy fatal
allocation boundaries remain explicit. The runner clears LSAN_OPTIONS and
retains selected compiler, runtime, linker and provider identities; it does
not claim a complete transitive toolchain trace.

I retained command output/status before checking it, phase source/tool/product
maps, bounded process-group cleanup results, capacity observations, actual
helper artifacts and selected configurations. All non-product before/after
input pairs in this report set compare equal. Generated products intentionally
change. The helper files remain at their recorded original paths; their
content hashes and sizes are in each retained-summary.json.

The [report manifest](record-lists-0e72-checker/report-sha256.json) binds 243
report files. Files larger than 65536 bytes are stored as deterministic gzip;
the per-phase retained summaries record both original and stored hashes. This
is a bounded report checkpoint, not an independent whole-CAS/archive audit.
Complete original CAS objects and frozen roots remain available:

* Linux: /home/jkh/Src/nanolang-record-lists-qualified-0e72 and
  /tmp/nanolang-record-lists-0e72-linux-{prepare,focused}.
* Darwin: puck.local:/private/tmp/nanolang-record-lists-qualified-0e72 and
  /private/tmp/nanolang-record-lists-0e72-puck-{prepare,focused}.

The external preparation runner differs from cb489 only by exact source pin
and addition of the cb489 immutable CAS pools. The focused runner uses the
reviewed matrix supervision and selectors, updates the pin, and stops after
its five total focused configurations: the full-source phase is explicitly
held, not silently passed. Both external runners are archived by runner.json.

My full list/evaluator/source, enum parity, canonical integration, fixed-point,
whole-Make and release tasks remain open. The newly added eighteenth method
and every original source method remain required after native specialization
repair; I have not executed known-missing native operations to rediscover them.
