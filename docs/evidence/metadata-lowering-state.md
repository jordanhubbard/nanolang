# My defensive metadata lowering boundary

I stop indexed module-name lowering when its index expression fails. I retain the first diagnostic and do not allocate selection labels or intern exported names after that failure. I also validate every introspection declaration before lowering public shadow modules, including unused declarations. My program emitter already applies that declaration rule.

I track this bounded repair as `task_a600db3ae64f4f63b8b85d77856c593a`, stacked on product source `63876a4d41b1716ad6f8c1eb2652af2dc03f2cab`. Neither finding establishes a cause or resolution for retained export-shadow abort `task_dd74b033c3984805bc27ce5017096c3c`. I do not replay its historical binaries or fixture in these gates.

My new inline shadows check failed index emission, unchanged label/string tables, the original unsupported-extern diagnostic, and unused malformed declaration refusal through both public APIs. My new ordinary Python fixture uses inferred mutable int/string locals inside a shadow, unsafe metadata assignments, empty and nonempty export tables, absent indices, and a counted index expression. It checks both fresh compiler stages and verifies and executes their products in VM and native C translation.

The initial failure-state test ran with whole-source mode, which defers extern registration. Its assertion therefore did not exercise the intended immediate index failure. I retained `/tmp/nanolang-metadata-state-build.log`, set program mode explicitly in that test, and ran fresh bootstrap again. This was a test setup correction, not an attribution of the historical product incident.

At source checkpoint `da8f8258`, fresh `make -j4 bootstrap nano_vm nvm2c nanoisa_dump` passed both stages and the bootstrap comparison/installed smoke gates (`/tmp/nanolang-metadata-state-build-r2.log`). Eleven selected adjacent module-facts and canonical VM-shadow methods passed in 1.816 seconds (`/tmp/nanolang-metadata-state-adjacent.log`); the historical export-shadow method was not selected.

The first new Python fixture mistakenly used bare `println` syntax. Stage1 diagnosed that invalid expression; Stage2 terminated with signal 6 without a diagnostic. I retain `/tmp/nanolang-metadata-state-focused.log` without attributing that outcome or replaying that invalid fixture. After correcting the source, a separate stdout assertion was removed because check-shadows mode suppresses ordinary output. The corrected ordinary Stage1 cases passed in 0.180 seconds, including verified VM execution and strict native translation/execution (`/tmp/nanolang-metadata-state-stage1-final.log`). I also checked the explicit `NANOC` driver override after correcting its path handling (`/tmp/nanolang-metadata-state-stage1-driver.log`). The corrected valid fixture then ran once through fresh Stage2: both cases terminated with signal 6 without a diagnostic, in 2.194 seconds (`/tmp/nanolang-metadata-state-stage2-final.log`). I retain the failed assertions, do not rerun these inputs, and do not attribute the outcome. Binary and test hashes are in `/tmp/nanolang-metadata-state-final-hashes.txt`; the initial invalid fixture generator and log hashes are separately retained in `/tmp/nanolang-metadata-state-invalid-hashes.txt`. The temporary generated input directories were cleaned by unittest; the committed generators retain their contents apart from temporary paths. MAC evidence `ev_e26f106422854bcf9702d74a5353feef` attaches these observations to the open product investigation. This child stays draft and does not close product acceptance.
