# I qualify the complete C checker on the integrated candidate

I run the whole tests/test_typechecker.c executable at frozen
6d92a2d53cb330117731ab310e97b6a088731ccf on Linux GCC and Darwin Apple Clang,
then fresh ASan/UBSan/LSan builds (Homebrew Clang on Darwin). All four pass.
I retain every original assertion, plus reviewed reduce identity/authority
controls. Source, provider, executable and tool endpoint maps remain equal;
all supervised groups are gone. The executable bound remains180 seconds.
This is C checker qualification, not self-hosted parity or release acceptance.

My196 sealed reports also retain both da88 first link failures (the driver
omitted Make-expanded crypto/ffi flags), both corrected da88 first Point reduce
identity failures, the checker-only unchanged Point diagnostic and its prior
setup/link attempts, and the1c4 Linux new-fixture nested-callback parser refusal.
Puck1c4 and its sanitizer phases were never executed. I corrected only the
additive lexical callback's annotation/third argument to supported syntax;
Other acceptance and Point refusal assertions remain. The original eight reduce
identity cases and original refusal group now pass.

Each archive member is independently hashed and checked against seal.json.
Full source inventories and exact drivers are retained. Executables stay in
persistent qualification directories; these archives contain reports and maps.
