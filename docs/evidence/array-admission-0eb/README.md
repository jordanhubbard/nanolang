# My corrected C arithmetic admission evidence

I qualified exact source `0eb14852e6718881f88f26f2afc94aa496ddefee` on Linux and Darwin, ordinary and ASan/UBSan/LSan. My original full checker and evaluator targets and explicit nonexecuting array-opcode refusal selector passed in all four configurations: twelve test phases, plus four fresh stage1 builds. I retained all twelve executed fixture binaries, actual sanitizer symbol inventories, 146 report members and 1,162 content-addressed provider objects (501,337,349 bytes). My seal validates source, tool, existing provider and Make-fragment endpoint equality, successful bounded terminals and absent process groups.

My codegen result covers only `--array-arithmetic-refusals`; it is not full codegen qualification. My Nano checker changes remain unexecuted and need deliberately integrated fresh bootstrap qualification. My original 60-second compiler deadline failure and full 5.1 release gates remain open. I preserve prior fixture failures separately, without replacing their terminals.

My executable bytes are in the local content-addressed paths recorded by `seal.json`; the compressed Git report bundles retain hashes and commands, not those executable bytes. `seal.py` checks the source reports and local objects; `run.py` records the actual bounded qualification procedure.
