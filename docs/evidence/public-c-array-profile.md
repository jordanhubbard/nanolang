# I qualify a checked public C array boundary

I qualify task8026 after preimplementation contract85d56bb2 and independently reviewed production6a916c33/cc2b15eb. Approved cleanup55a0d2af removes eight unreachable array-pointer declaration lines without changing the guards. My corrected harness ef3bba72 is the final frozen qualification pin. The adjacent manifest records44 file identities, three compiler/wrapper identities and eight logs; all final identities match after gates.

Four focused methods pass GCC2.217s and Clang3.114s. I test ten ordinary source array cases, including empty/nonempty/nested literals, float elements, parameters/results, globals/locals, record/union payloads and str_split. Each reaches the backend array refusal and preserves prior output. Public API controls additionally cover nested retained nominal/callable metadata, array result/parameter signatures and externs, exact scalar-result builtin families, explicit array value facts and both path/FILE publication recovery. The registry audit matches all32 currently registered array-producing/consuming builtin names exactly. Direct and qualified scalar declarations sharing an array-family name compile/run through the public API under C99/C11 O0/O2 ASan/UBSan; source array_length_like remains accepted without prefix guessing.

My first frozen55a focused gates passed3/4 under each compiler. The intended array_length source declaration was correctly refused by the shared checker's builtin reservation before backend entry. I retain both terminal logs and original44 unchanged identities. Recorded correction9f484510 preserves that reservation as an expected source refusal, tests exact same-spelled declarations only through the AST API, and adds an ordinary nonbuiltin-prefix source control. No shared checker or production semantics changed. The corrected fixture is frozen before fresh gates; no rejected output executes.

All46 adjacent methods pass GCC28.376s and Clang38.676s (the prior44 plus two option/profile methods), including local interpreter and verified-VM lifting comparisons. All seven existing public C programs pass. Emitted output uses the established strict/pedantic C99/C11 O0/O2 and ASan/UBSan flags; API compilation additionally uses-Wall-Wextra-Werror. I make no fresh bootstrap, Darwin, universal-language or global unused-helper-warning claim.

Arrays receive checked refusal instead of bare initializer fragments, pointer/default-int64 storage or undeclared builtin calls. No array ABI, allocation/ownership policy or source-wide array restriction is introduced. Exact declared/lexical callee facts precede builtin spelling. Semantic output refusal and invocation-local recovery retain the existing777 contract.

Canonical merge precedes child8026 closure. Original6ade remains open: final inventory separately found named function-value annotations/calls still reaching default-int64 spelling without a callable ABI. That distinct boundary requires its own checked-refusal contract; this array patch is frozen and does not claim it repaired. Shared match policy and broader release requirements retain independent acceptance.
