# My bounded cyclic relink after PR910 and PR906

I integrate reviewed92b9fd677 with actual canonical PR906/ca0c9f6a8 after
PR910/6b86de83d. The frozen gate pin is3df299892. The sole conflict is the
additive roadmap tail; Make adds exactly the five-line private Wasm target.
My [identity map](../file-cyclic-dispatch-restack-910-identity.json) retains exact
private runtime/fixture bytes and incoming canonical source/test bytes.

The shared typechecker object is in my fixture's common link set. I therefore
build fresh providers on Linux and puck and run only the existing ordinary
linked matched cyclic VM/native method, including O0/O2 generated programs and
all its isolated refusal controls. Setup, configuration, exact two-method
discovery and the selected linked method pass on each host. Linux setup/fixture
elapsed19.516/38.408 seconds; puck7.978/22.638 seconds. I compare33 VM traces
with33 actual native traces at each optimization on each host, across23 exact
modules. All trace bytes agree. No original sanitizer/instrumented acceptance
is relabeled as this new integrated gate; no matrix or bootstrap replay follows.

My first puck launcher selects an older SSH-default Python that refuses the
archive filter keyword before extraction or build. I retain that launcher,
terminal and empty destination. After the precode roadmap record I explicitly
select installed Homebrew Python and a new isolated destination. No product or
fixture change is involved. My actual driver interpreter is hashed this time;
this does not recover the historical interpreter hash missing from the earlier
907 evidence. SDK, libffi headers, owning linker and selected tools are named.

My report manifest seals93 files. The CAS at
/tmp/nanolang-cyclic-907-restack-artifacts retains834 unique objects totaling
231170733 bytes, with3393 original report references. Eight source/tool endpoint
pairs match and existing providers stay unchanged. Fresh current rehashes match
20469 source files on each host,12 Linux and18 puck tool records, and154 linked
provider inputs each. These are scoped endpoint/current checks, not a complete
transitive system inventory. The exact downloaded puck archive hash/size is in
qualification-summary.json; original reviewed manifests remain unchanged.

The Linux frozen tree is /home/jkh/Src/nanolang-cyclic-907-restack-3df; puck uses
/tmp/nanolang-cyclic-907-restack-3df-py314. Both raw report roots are
/tmp/nanolang-cyclic-907-restack-{linux,puck}. Root independent review and actual
merge remain pending. Public cyclic/source/indirect/richer-borrow and full5.1
acceptance remain open; I do not claim hosted CI or previously blocked suites pass.
