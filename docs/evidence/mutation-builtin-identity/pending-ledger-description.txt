I record a static prerequisite for owned-array source mutation. At canonical5f988ed79 src_nano/typecheck.nano check_expr_node resolves call spelling, then its array_push two-argument result inference and array_set generic builtin result return before env_lookup without a declared/local-name guard. tc_bound_name explicitly preserves a lexical variable spelling, so builtin type selection can mask that binding; array_push declaration ownership/mangling must also be tested rather than assumed. This is static checker-path evidence, not a runtime reproduction or general builtin defect claim. Before source mutation acceptance I pin bounded intended identity rules, require declared/local/formal/initializer controls and retain C's existing reserved array_set declaration refusal. If correction is required, use a separately reviewed scoped guard returning to existing ordinary symbol checking, mandatory shadows and paired diagnostic/output preservation controls. Do not widen all builtin resolution or execute rejected artifacts.

Frozen66a4686e1 fresh bootstrap first terminal: status2 after20.035s; Stage0 passes, Stage1 source parse refuses new mandatory shadow lines4966/4985, each with one excess closing parenthesis around resource_test_parser. Source and eight host-tool identities are unchanged. Evidence /tmp/nanolang-mutation-identity-66a-bootstrap is preserved. I record this harness-source syntax defect before correcting only those two delimiters in a fresh worktree; checker production guards remain unchanged. Fresh corrected bootstrap/qualification requires independent review, no previous failing artifact replay.

Corrected82ce bootstrap passes269.511s with equal source/eight host-tool hashes. Frozen54ea first focused terminal retains three passing methods:36 full-source checker-driver cases,16 typed refusals with output retention, and C reserved array_set policy. The unbound mutation fixture has three producer parse failures because two array<float> annotations abut assignment (=), forming GE tokens. No rejected output is executed; adjacency remains unrun. I record this test spelling defect before adding spaces only in those two lines. All sources/host tools unchanged during97.103s gate. Evidence /tmp/nanolang-mutation-identity-54ea-focused, artifacts /tmp/nanolang-mutation-identity-7i1p1jr6. Compiler production/qualified binaries need no rebuild for this fixture-only correction. Independent auto-worker failed state remains distinct; no duplicate dispatch or completion claim.
