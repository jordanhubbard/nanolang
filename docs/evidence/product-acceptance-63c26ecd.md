# My frozen candidate acceptance

I freeze production `63c26ecd2723f8c480eaba4b25ebdf797f4764bd` through PR697 while later canonical work continues separately. This candidate is not ready for release.

My fresh Linux bootstrap passes in 189.669 seconds; tools build in 25.129 seconds. All 65 executed focused methods pass, but borrowed-source setup fails before its 26 methods execute: Stage2 compiling `src_nano/nanoisa_emit.nano` exceeds the ordinary 180-second deadline. The focused command exits 1 in 1357.081 seconds. My fail-closed runner does not start full `make test-quick`. I preserve the [initial manifest](product-gates-63c26ecd-initial.json) and [complete focused log](product-gates-63c26ecd-initial-focused.log), SHA-256 `9601791a3cce0207870a6209b262892d3b2829b967954fbc7fc57db42a211183`.

Task `task_ef9261a163f64e0397a93d377d623ed7` measures exact current setup separately before a setup-only budget correction. Ordinary assertion deadlines stay unchanged. The standalone runner also omitted the existing local-binding probe prerequisite; I built it before starting the corrected suite. A separate measurement compiles the same emitter in 459.369 seconds; the setup-only 900-second correction is under qualification in PR706. The corrected borrowed-source suite and the separately described Linux full quick gate was started. These are test preparation findings, not a full product pass.

My fresh isolated VM fixed-point run passes at 63c26ecd. Initial, first-generation and second-generation compiler modules are exactly 407,992 bytes with SHA-256 `d4f5dfb18fc63b8f689403c16c159c9636cd0acfce1215ce743fac1375d06011`. Both generations verify and retain identical three-library closure; Stage2 compiles, verifies and executes hello. Stage1 takes 884.566 seconds and Stage2 takes 885.655 seconds under their original 1200-second limits. [Manifest](product-vm-fixedpoint-63c26ecd-manifest.json) and [post-run integrity](product-vm-fixedpoint-63c26ecd-post-run-integrity.json) retain exact source, tool and library identities; tracked source is clean and unchanged. The manifest top-level 1800-second field is inherited runner metadata; each VM stage records and enforces 1200 seconds. Raw fixed points do not establish compiler semantic equivalence or release readiness.

My fresh isolated native fixed-point run also passes. Initial, first-generation and second-generation compiler modules are exactly 408,004 bytes with SHA-256 `a453c236b783383a7d99ae50ba962747347d6ee6726f5e08bc5877581b7486dd`. Stage1 takes 1167.941 seconds and Stage2 takes 1184.547 seconds under unchanged 1800-second limits. All module verification, host-library closure, native help and hello compile/verify/execute checks pass. The [manifest](product-native-fixedpoint-63c26ecd-manifest.json) and [post-run integrity](product-native-fixedpoint-63c26ecd-post-run-integrity.json) confirm unchanged source, helpers, translator and host libraries. VM and native runs embed distinct checkout paths; I make no cross-run raw-equality claim.

## Corrected Darwin comparison at the earlier compiler source

The peer completes full `make -j8 test-quick` under PTY at `c9c747858ee4c557563f0b3c3ca9e86a7407dec0`, exit 0 in 985.04 seconds. I independently compared Makefile.gnu, src, src_nano and modules against Linux-qualified `ece241ba`: there is no difference. Relative to evidence-only d021ad99, the corrected pin changes only the roadmap and Forth harness stdin isolation.

I retain the peer's [sealed report](product-acceptance-ece241ba-darwin-corrected.txt), published in evidence-only commit `cd142d78`. I copied and independently hashed its 192219-byte full log at `/tmp/nanolang-product-ece-forth-corrected-c9c74785-test-quick.log`: `47ef1a62b9004841cffea574998f783b02177ddd01b7fe5e00670c49562c1d26`. Bootstrap, 17 core examples, 244 eligible VM examples, all Jackson wordsets, 280 Forth cases, production PTY REPL and IDE build/smoke pass. The report's older component-driver validation means that those drivers compiled and exited successfully; it does not establish all imported shadows or the new PR691 entry assertions.

This closes the corrected earlier-source platform comparison. It does not qualify the later 63c production integration. Current-candidate Darwin acceptance is separately requested; all broader release parents and the publication hold remain open.

## Bounded Sail model refresh

At documentation-only f38b6409, the existing `scripts/check_sail_container.sh --rocq-check` completes on the peer in 529.47 seconds. I independently verify the [report](sail-rocq-f38b6409-evidence.txt) and [log](sail-rocq-f38b6409.log) hashes against MAC evidence, count all nine closed-assumption reports and the independent-check marker, and match the model/proof/script/checker inputs to this 63c source. Task f352 is completed. This proof-only gate does not run decoder/VM corpora and does not close full formal foundations or production correspondence.

## My isolated Linux full-gate failure

A fresh detached checkout at the same production pin completes bootstrap in 195.998 seconds and tools in 26.133 seconds. Plain `make test-quick` then exits 2 after 1222.257 seconds during Stage2 transpiler-driver publication. The explicit transpiler entry assertions pass, but native translation refuses the verified module with `I require a string hashmap key`. I retain the [manifest](product-quick-63c26ecd-failed.json), [full log](product-quick-63c26ecd-failed.log), and separately labeled [post-failure tool observation](product-quick-63c26ecd-post-failure-tools.json). Full log SHA-256 is `99ada7b64f57447ac5ae8fc43a1b69a8314757e83ed9274c08f51c87ba35d226`. This gate did not reach later full-suite stages.

Task `task_a4ab109d881d432ab1728299006b121c` investigates the precise source/classifier boundary before a repair. I do not assume that the refused key is semantically a string merely from this message. The source remains clean; compiler identity and current symlink destination are retained as post-failure observations, not an invented before/after proof. The Darwin peer is checking its exact selected compiler and gate invocation independently. This failure blocks current product acceptance without invalidating the separately pinned fixed-point results.
