# I qualify copied complete mixed declarations

I qualify the non-admitting query from reviewed production31c68af10 and fixture
849a57ab1, integrated with actual canonical public914 mergef219 at frozen
dd5a3ab637959c5cae1c3fd0a6212b516bb64265. The corrected fixture pin is
5a367e16e61587a424f7572cfd9bb68b1b4ee68f. No production bytes changed during
qualification. My separate mixed private layout entry admits scalar unions beside
forward record DAGs only to this descriptive query; old public, ordinary and
executable selector decisions remain unchanged.

Seven configurations pass cumulatively. Linux GCC13 ordinary passes atdd5a;
corrected5a367 passes Linux Clang ordinary, GCC13 ASan/UBSan and Clang ASan/UBSan.
Darwin5a367 passes Apple Clang ordinary, Homebrew Clang ordinary and Homebrew
ASan/UBSan. Sanitizer execution retains leak detection and empty LSAN_OPTIONS.
Every configuration reports1,814 linked and10,960 allocation-observed checks,
including13 measured mixed allocation positions in transient and persistent
failure modes with zero tracked live pointers and independent fresh recovery.
The unchanged original ordinary-array controls also execute inside this fixture.

I check complete numeric identities across equal-shaped unions and records,
forward record edges and all five flat array element tags. Every getter survives
destroyed heap input and every error preserves its output sentinel. Independent
malformed halves, descriptors, paths, framing and graph cycles fail atomically.
A consumed unsupported borrowed descriptor can defer to UNKNOWN while a later
malformed extension still returns INVALID; the old ordinary query keeps its
prior early UNKNOWN. Actual maximum mixed tables contain256 layouts,65,536
fields/bindings and4,096 types; next counts refuse. Seven unions each with256
variants fit the conservative two-pass work budget, eight unions refuse before
allocation, and257 variants refuse. I do not infer65,536 reachable variants from
the separate storage cap when the work budget is stricter.

I retain the first Linux Clang compile terminal: a cleanup statement on the same
line as the nested loop brace triggers strict misleading-indentation. The
correction separates only that line. GCC's earlier success is not replayed.
Darwin was transferred but unrun at the first pin. The corrected Linux tree
receives copied original providers only after all28,172 source files and every
retained provider hash are checked, with exactly the fixture and roadmap changed.
Fresh corrected fixture binaries are built; Darwin performs a fresh setup.
Task349ba retains this repair history until actual merge reconciliation.

All corrected15 Linux and17 Darwin phases pass. Unchanged neighbors include185
ownership-contract checks,252 ordinary authority checks,365 conversion and20
transport checks, the original ordinary-array query,546/856 affine bytecode
checks with actual scalar-union VM/native sanitizer cleanup, and the original
public ownership transport/execution/refusal method. These outcomes do not
establish unrelated constructor or broader affine-source acceptance.

Allocation hooks and query sanitizers cover ownership_contracts.c and
nvm_v2_layouts.c, including transient graph scratch allocations and final copied
report rows. Reference-helper allocations and ordinary common providers are
outside that hook/instrumentation scope. I retain selected actual interpreter,
compiler and Darwin Xcode/SDK-header identities, not a complete SDK/OS/toolchain
hash. Legacy Python neighbors delete their own temporary native outputs; their
retained logs establish outcomes without claiming archived deleted binaries.

My [report index](ownership-declaration-projection/report-sha256.json) seals the
first terminal, corrected phases, commands, source/tool/provider endpoints,
current rehashes and content-addressed artifacts. This query does not grant mixed
runtime authority, publish source bindings or close full5.1 parents. Independent
review and actual merge remain separate requirements.

My seal contains367 reports,598 unique retained artifacts totaling234593121
bytes,12,106 references and37 phase endpoint sets (74 separate source/tool
equalities). The combined artifact archive SHA256 is
`73b3a047fe2c0e014d46899f14e51e10b67daa6a59589c080f2f96f6ecdabf72`.
Current original/corrected Linux and corrected Darwin source, selected tools and
final products rehash without mismatch; all three trees retain28,172 tracked files.

## My independent integration review

I retain [root independent review](ownership-declaration-projection-root-review.json): all367 actual Git reports,598 archived objects,74 source/tool endpoint pairs and37 phase terminals match, including the one attributed compile failure. All12,106 phase artifact references plus163 copied reuse references resolve; the complete archive hash matches. Current source/tool/provider hashes match on both Linux pins and Darwin. I integrated actual main99f390; the incoming assembler-capture rule is outside this query execution closure, and all eight selected source/fixture files remain exact5a367 bytes. No qualification replay is claimed for that unrelated rule. Public mixed admission, origin/mutation/alias/runtime proofs and full5.1 remain open.
