# Resource declaration view: focused qualification

I qualify source `946d18d56813fd897ffa4bc9dad9df26597b8449` with all eleven selected resource shadows passing in their original imported module context. I retain full dependency checking, twenty module initializations, and the original sixty-second shared shadow alarm. The complete compiler phase took 74.325 seconds; its shadow bodies totalled 13.150880265 seconds.

The five prior bodies took 4.091792, 0.638135, 0.687806, 2.486657, and 4.107863 seconds respectively. The six additive internal controls also passed. I preserve exact source/item identity and raw timing records in the report bundle.

I rebuild only the diagnostic evaluator object against 159 retained original objects. This time I preserve its target-specific `-ffp-contract=off -fno-fast-math` flags. Prior7174 omitted those flags, so I do not attribute the entire measured difference solely to the declaration view. The original CI environment retains `ASAN_OPTIONS=detect_leaks=0`; I claim no leak-sanitizer acceptance here.

I verify all sixty report members and 363 CAS objects (292,636,415 bytes), including 1,584 retained native product files. All four supervised phases pass with unchanged source/tool endpoints. `checks.json` binds the report bundle and local immutable CAS at the recorded report root.

I have not qualified the full original typechecker component, complete CI preparation, or the separate ten-second bootstrap shadow requirement. Earlier failed terminals remain retained.
