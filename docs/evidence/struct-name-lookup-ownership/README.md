# My focused struct-name ownership qualification

I qualify exact source `72aedeeb3f5af25f2b6a21a95dec159aaad1f24a` with 3,743 tracked inputs in fresh Linux GCC ordinary and ASan/UBSan configurations. Leak detection is enabled in both environments. The original owning Make command passes in 3.361 and 8.123 seconds under the unchanged 1,200-second supervisor. Both source/tool map pairs stay equal; both leaders are reaped and process groups disappear without timeout or cleanup error.

My real typechecker/Environment fixture exercises all four allocating name branches, exact spelling after descriptor mutation, repeated independent copies, borrowed identity and Environment registry cleanup. Across two failure modes it checks all 22 allocation positions, including eight expected fatal registry-node failures with exact diagnostics. Each failure has an independent successful recovery. Unexpected sanitizer stderr cannot satisfy expected exit one. This does not claim recoverable cleanup for my established fatal registry-node policy.

Two lossless bundles retain 32 raw reports. Their manifests index 208 lane-specific immutable CAS objects, including actual linked executables and providers; every indexed byte and bundle member is rehashed. The selected allocator hooks exist only in the two fixture provider objects. Ordinary product source has no hook.

My additive effect result source and original native float/record corpus remain unexecuted here. They require the independently corrected parser and metadata providers before their known failing paths can be replayed. This focused result does not close those acceptance gates, the full name-ownership task, PR948 or release readiness.
