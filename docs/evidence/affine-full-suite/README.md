# I retain my complete affine-script acceptance

I freeze `3c728c4371c6897f5ff08807adab1a528f3ca48d`, incorporating canonical
`85141cdf8259151848fdb66a8c5c1b0a09138d60`. The later parser877 change is outside
this qualification. My source and fixture assertions are unchanged. I run the
exact `bash tests/test_affine_selfhost.sh`: one frontend-parity method, twenty
contract-boundary methods and twelve owned-record-pattern methods. My boundary
fixture retains all36 original source hashes (14 positives,22 negatives) and
144 case/authority observations. The expected authority-specific checked
refusals remain assertions; I do not claim every positive source runs on every
backend. All original PREFIX programs and shadows remain selected.

My Linux fresh bootstrap passes in282.734s; exact prerequisite preparation passes
in1.236s; all33 script methods pass in85.286s. I use explicit `/usr/bin/gcc`.
The bootstrap produces778 inputs, preparation adds11, and all789 remain exact
through the script. The three fresh stage hashes match across all phases.

I preserve the first Darwin bootstrap failure (9.5246s, make exit2, missing
openssl/sha.h) under task273296. My noninteractive PATH omitted Homebrew's brew
command used by the existing Makefile. A separate same-pin tree uses explicit
Homebrew PATH, Apple Clang and the selected SDK; I retain resolved OpenSSL
header/library hashes and package-discovery output. No source or flag change
repairs this runner configuration. The original failed tree remains intact.

The manifests seal reports and every retained temporary artifact. Archive
contents are individually rehashed after creation. External retention copies
temporary directories before deletion and records subprocess output without
changing unittest selection, assertions or timeouts. These are ordinary host
builds; I make no sanitizer claim. Full source/tool before/after maps distinguish
bootstrap outputs from immutable execution inputs.

This is the complete33-method shell-script boundary, not the whole
`make test-affine-selfhost` target (which also runs module/generic identity),
full product, compiler fixed point or release qualification. I keep full
ownership/managed/product parents open.

My corrected Darwin preparation passes in1.084s; all33 methods pass in77.369s. I seal 56 Darwin reports and 1487 artifacts, including the original failed bootstrap. Fresh stage hashes agree across all corrected phases.
