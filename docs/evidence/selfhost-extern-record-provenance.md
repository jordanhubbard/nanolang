# My retained foreign record provenance qualification

I retain an explicit `is_extern` Boolean in my schema-owned active `ASTStruct`.
My parser records it when consuming `extern`, my nominal copy keeps it, and my
ordinary advisory publication refuses foreign identity even when original tokens
are absent. I preserve source coordinates, field order and the resource flag.
My existing conservative import/extern token guards remain; I add no executable
ordinary-record authority or LLVM/Wasm admission.

My [preimplementation contract](../SELFHOST_EXTERN_RECORD_PROVENANCE.md) is8a2ec43c,
clarified e0783caa. Production44a00d90 is unchanged by later fixture repairs.
My legacy standalone `nanoc_integrated.nano` prototype is outside the active
bootstrap/import AST graph and is explicitly outside this change.

## My observed pins and first failures

- At5b4085f7, initial build exits2 in0.555s: default Clang23 cannot select the
  available GCC runtime warning-free under the existing Werror policy. It does
  not reach compiler qualification or execute a failed output.
- At0096a692, explicit runtime IR `clang-18`/`opt-18` proceeds but exits2 in50.265s
  during the Stage1 seed shadow: my name-rewrite fixture disabled module owners
  before registering its rename. The foreign identity assertion itself passes.
- I record that setup failure before1ffa8bc8 enables the fixture owner. Its fresh
  actual Stage1/Stage2 bootstrap and all requested tool targets PASS233.355s.
  The new schema uses only this isolated tree's fresh module cache/libraries.
- The first direct three-compiler fixture at1ffa8bc8 fails43.274s before native
  publication: its driver references lexer/parser/module-binding calls only
  through transitive imports. I record c85f49c9 before test-only d8460cc1 adds
  direct defining-module imports. Every assertion remains.
- At d8460cc1, the corrected direct fixture PASSES47.684s. Actual C-seed,
  Stage1 and Stage2 each execute ordinary/resource/foreign flags, original
  coordinates, copied identity and distinct-owner foreign collision refusal.
  Normal imported shadows execute; none are disabled.
- The unchanged `make test-ordinary-record-producers` PASSES187.736s, with all
  six methods in185.901s. Ordinary nested/empty records, field initializers,
  selected shadows and scoped local tags remain admitted. Unsupported array
  fields retain ordinary execution; foreign record provenance stays UNKNOWN.
  Existing paired backend refusals remain asserted.

The corrected direct+ordinary gate records identical before/after hashes for
all tracked schema/source/tests/scripts and ten selected tools. Make relinks
prerequisites normally; I report observed hash equality, not an assumption that
Make never rebuilds. The actual bootstrap pin remains1ffa8bc8; the later change
is the external test driver's imports, not a new compiler bootstrap claim.
Four generated schema artifacts were independently compared with their schema
generator output before direct qualification and matched byte-for-byte.

I seal all first and corrected logs/results/manifests in
[my artifact manifest](selfhost-extern-record-provenance/report-sha256.json).
No retained failed artifact was replayed. This is Linux acceptance for the
bounded provenance repair; I make no Darwin or full product release claim.
MAC parent `task_f366494d5921451eaf57128629eb9e4c` and fixture child
`task_abe5111511c548648b38fd336d16a357` await review and canonical integration.

I integrated canonical main1e9d9519 at decd62b3 after qualification. No active
Nano source, schema/generated AST or these producer test files changed in that
integration. I preserve both roadmap histories. This integration includes
separately qualified public-C/reconstruction/docs work; I do not relabel my
sealed bootstrap or tool hashes as a fresh current-main build.
