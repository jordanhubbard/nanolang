# I qualify private File carriers, not a dispatcher

I qualify the first carrier/context checkpoint of
[my private runtime contract](../NANOISA_FILE_PRIVATE_RUNTIME.md),
`task_82ffef0affec2b23c4390931370f6774`. My production is `a02d5ab19`;
subsequent corrections change fixtures, driver setup or documentation only.
I do not close the runtime child, hosted6fc, full72556,6931 or d03c.
No File CODE handler, frame/call adapter, native lowering, public selector,
paired source or installed File route is qualified by these manual API calls.

I freshly validate serialized nominal/body/startup facts, allocate bounded
carrier/reference/region/frame storage and create a private invocation context.
My fixtures call the primitives directly: real temporary-file acquisition,
write/rewind/read/EOF, Result handling, moves, borrows and terminal cleanup.
Later adapters still have to enforce CODE order, frame/local placement, argument
staging and each pending body obligation. My reserved frame arena has no claimed
working dispatcher yet.

## I retain the exact runs

| Host and source | Phase | Seconds | Result |
|---|---|---:|---|
| Linux `565a4fada` | Ordinary GCC, two methods | 5.822 | PASS |
| Linux `565a4fada` | GCC ASan/UBSan/LSan, two methods | 20.182 | PASS |
| Linux `565a4fada` | Clang ASan/UBSan/LSan, two methods | 20.048 | PASS |
| puck `565a4fada` | Apple Clang ordinary, two methods | 4.775 | PASS |
| puck `565a4fada` | Homebrew Clang23 instrumented method | Within retained 10.610 phase | PASS before the linked compile terminal |
| puck `be65d0368` | Homebrew Clang23 linked method only | 5.987 | PASS |

Each complete instrumented method reports84,504 checks,436 preparation allocation
refusals, zero recovered carriers after an injected failure,64 acquisition
attempts,63 successful real opens and63 real closes. Each linked method reports
11,933 checks. These include fixture/builders/allocator assertions; I do not
call them84,504 independent language programs. Linked mode does not intercept
File host calls, so its zero host counters are not observations of zero I/O.

I retain unchanged hosted/body/flow/private-values/opcode neighbors on both
hosts. Linux durations are18.867/5.046/4.704/1.574/3.100 seconds; puck durations
are9.602/3.429/3.262/1.727/3.387. The final wrapper suite passes in3.128 seconds
on Linux and6.113 on puck after the separately retained provider correction.
The passing final wrapper runs retain their temporary fixture artifacts through
an outer cleanup-retention shim; their production cleanup and assertions remain
unchanged. The first failed Linux wrapper run used its ordinary disposable
temporary directory, so I retain its log/provider inventories but do not claim
its deleted temporary wrapper binary is archived.

## I preserve first terminals and their corrections

- Linux `5345717bd`: setup passes11.004 seconds, then normal fixture compilation
  stops2.196 seconds because my new `opened` counter collides with a reused
  logical fixture helper. No carrier service executes in this attempt.
  `565a4fada` renames only that counter. Its fresh setup passes10.534 seconds.
- The first setup uses Make's default `cc`; a postphase explicit comparison
  establishes `/bin/cc` and `/bin/gcc` are identical. I do not invent a missing
  before-phase cc inventory. Corrected drivers pass the compiler on Make's
  command line and record the actual cc binary too.
- puck `565a4fada`: setup passes4.940 seconds, then normal compilation stops1.249
  seconds because the newly rebuilt `vm_ffi.c` lacks libffi's header path.
  I select the installed `pkg-config --cflags libffi` result and record its
  actual tool, `ffi.h`, `ffitarget.h` and selected SDK libffi link-stub identities.
  The corrected ordinary gate passes without changing production or assertions.
- In the following puck sanitizer phase, the instrumented method passes. The
  linked compile then encounters Clang23's unused-but-set-global diagnostic for
  two observation counters asserted only in instrumented mode. `be65d0368`
  adds those existing counters to the summary output. I freshly qualify only
  the previously unsuccessful linked method and the unreached neighbors;
  I retain the original instrumented pass at565 rather than relabel it be65.
- Linux's last original wrapper phase stops0.275 seconds at a daemon link:
  my outer setup omitted `vmd_protocol.o` and `vmd_client.o`, separately listed
  by the existing wrapper target. I build only these missing objects in0.193
  seconds, then run the remaining wrapper gate. Puck adds them in0.242 seconds
  before its first wrapper gate. Earlier providers remain hash-identical.

I do not disable warnings, sanitizers, leak detection or any assertion. My
wrong explicit-user puck SSH preflight is rejected before transfer; the
supported default-user `ssh puck.local` route succeeds. CX's full-disk evidence
and qualified trees are untouched. No diagnostic reruns execute preserved
failed binaries.

## I delimit the observations

I rebuild all thirteen allocating hosted-plan providers plus private NSI core
providers for full-chain fault injection. The instrumented fixture includes the
exact carrier source to inspect owning `sizeof` values; the linked fixture
compiles it separately. I check counted core storage exactly, peak bounds,
allocation-prefix and transient-failure cleanup, three core-acquisition failures,
input independence and successful reuse. A direct `fr_add` overflow/limit test
is an arithmetic unit boundary, not an actual64MiB-refusing creation fixture.
The64MiB policy counts project-owned allocations, not libc stream internals.

I test exact scalar/passive nominal payloads, both arena modes and original/
permuted catalog maps; all live staging slots unwind, and owner-copy/overwrite/
borrow-conflicting transfers preserve their inputs on refusal. Nested formal
aliases share a caller origin and cannot release it prematurely. Initializer
cleanup failure prevents transition to entry. A first execution failure and
first/secondary close errors survive terminal cleanup. Only a clean completed
entry publishes scalar output. Nested callback attempts cannot acquire, mutate
or destroy the active context. This is serialized reentry testing, not concurrent
thread-safety qualification.

My instrumented open hook counts attempts before failure, and real descriptor
checks observe stream closure while preserving an unrelated caller descriptor.
For progress errors I actually read/write one byte, then model `ferror` and
saved errno; I assert all seven Error fields before and after take, and retain a
usable owner after rewind. This does not claim a real short transfer or device
failure. My close hook really closes then injects an error report; it does not
establish arbitrary failing-libc-close behavior.

Both fixtures rebuild the direct FFI bridge with loader-init/open/fork attempt
sentinels and demand zero attempts for public File refusal. I separately prove
those sentinels count direct calls. Existing full opcode/CLI/wrapper refusal
neighbors pass; I do not extend the new attempt-counter claim to every installed
CLI or executable. Shared public guards remain unchanged.

## I retain reproducible evidence

[My manifest](file-runtime-carrier/report-sha256.json) seals phase commands,
logs, selectors, driver source, source/tool inventories and original artifact
maps. [My summary](file-runtime-carrier/qualification-summary.json) records26
equal before/after source/tool pairs, each with7,282 tracked input files.
[My artifact store](file-runtime-carrier/artifact-store.json) indexes1,209 unique
retained files,687,855,034 bytes, covering9,248 phase inventory references.
Actual archives are in `/tmp/nanolang-file-runtime-artifacts`; downloaded puck
archives are independently hash-checked against the remote packaging report.
I retain all original intermediate fixture/provider binaries that existed at
collection, including the successful instrumented puck sanitizer executable
before the later linked compilation failed.

My local qualified roots are `/home/jkh/Src/nanolang-file-runtime-qualified`
(first534) and `/home/jkh/Src/nanolang-file-runtime-corrected` (565).
My remote roots are `puck.local:/tmp/nanolang-file-runtime-565` and
`puck.local:/tmp/nanolang-file-runtime-be65`; the latter copies byte-identical
prepared providers and changes only the documented fixture summary and ledger.
The per-phase before/after inventories and separately labeled current checks
are distinct evidence. I make no new Stage1/2 bootstrap, public File execution
or release-readiness claim.

I attach the independent273-report/artifact/current-input audit and its Linux
and puck refreshes. My final manifest contains277 reports after those three
audits and the ready-integration record. Ready `bf2d16248` integrates canonical
856 (`179626d39`) in a separate tree; its only upstream change is ROADMAP.
I preserve both additive ledger rows, verify exact `a02d5ab19` production and
`be65d0368` fixture/Make identities, and repeat no unchanged gate.

## I preserve measured pins while integrating canonical861

I integrate canonical59ffceccf422047c91395e1cf5d08e62e5a1d64c through an
automatic additive merge. Independent static review checks all ten carrier
production paths against a02d5ab19, the three fixture/hook/driver paths against
be65d0368, and all ten incoming source paths against canonical59ff. They match.
I seal that review in `file-runtime-carrier/canonical-861-static-identity.json`.

Canonical861 changes source array-binding/evaluation/lowering support and
associated source fixtures. It changes no File, NanoISA or VM carrier authority.
Its common source objects are part of the wider link closure, while the private
carrier fixture does not exercise source evaluation/lowering. I retain the
measured carrier and neighbor results at their original pins. This static
integration is not a new build, execution or tool-inventory qualification, and
I do not relabel canonical source acceptance as a carrier gate.
