# My synchronous service-admission acceptance

I qualified source c95c2366e and fixture 1f9d29625 in fresh Linux and puck trees. Both hosts passed the unchanged full imported emitter command under its original ten-second shadow policy. Linux returned 0 in 20.259 seconds overall and puck in 15.651 seconds overall; these include preceding compilation/module work and are not shadow-only timings. Both published nonempty emitter modules, whose distinct bytes/hashes are retained. No diagnostic macro was enabled. Current canonical integration and independent evidence review remain outstanding, so I do not close task_4931a66f39c04b1a96981a4e00d43646 or claim full roadmap acceptance here.

## My measured boundary

I retain the prior diagnostic failures and scopes in seals cd85e0f2038f9c01400d9e0ab3cc616a03d5dafb, e8ba5f324 and c9fc46d86. They established repeated service scans, not permission to retain service facts across callbacks. This change computes exact service presence on the stack for one synchronous core-admission transaction. The complete ownership authorities remain unchanged; public classifier wrappers remain fresh, different modules receive independent classification in order, and all previous ordinary-certificate invalidations remain. The tree begins from 8d73174ec, before the later canonical ownership integration, which must be reviewed separately.

## My focused matrix

Each of five configurations ran both true switch and computed-goto executables, with explicit preprocessor assertions and 3506 checks per executable. Linux GCC ordinary passed in 3.922 seconds, GCC ASan/UBSan/LSan in 11.340 seconds, and Clang23 sanitizer in 10.341 seconds. Puck Apple ordinary passed in 2.944 seconds and Homebrew23 sanitizer in 6.222 seconds. Fresh ordinary provider preparations passed in 9.494 and 4.415 seconds. These are recorded phase durations, not a performance benchmark.

The fixture runner inserts one observer at the entry of a retained exact copy of the real service predicate. It then executes the original body for every caller, including verifier and service-provider internal calls. The original service object is excluded from that fixture link. The original and observed source bytes, exact insertion, compile/link argv and output/status are retained. Sanitizer instrumentation covers the fixture, included VM and observed service TU; other linked providers are ordinary. ASan/UBSan and leak detection are enabled; LSAN_OPTIONS is empty. I make no full-provider sanitizer claim.

Exact counts passed: one service predicate per standalone ordinary full private entry, zero on allowed ASSERT-only reuse, three per public direct-core call, 39 for ordinary vm_invoke, 20 for nested callable invocation, one additional scan at a non-ASSERT host resume, and 41 for the modeled callback-pump sequence. NULL/mismatched facts trigger fresh checks; root/linked order and early refusal, public freshness, malformed ownership/service precedence, root and uncalled-helper bare File refusal all retain their status, output and root assertions. Existing raw-store, helper, fusion, managed condition, effect, scalar-owner, exclusion and cleanup controls remain. PRINT/FFI/pump hooks in this focused fixture are expressly modeled.

## My actual unchanged neighbors

Both hosts passed actual callback lifecycle (including 40000 cross-thread calls), callback failures/allocation and all 28 vm_ffi cases using freshly built real shared-library fixtures. Both passed the existing mixed admission/heap allocation corpus, mixed VM/native lifecycle and public owner ARRAY corpus. The mixed lifecycle phases took 33.146 seconds on Linux and 21.409 seconds on puck; owner ARRAY phases took 11.541 and 12.397 seconds. Existing native O0/O2, refusal/output-sentinel, lifetime, allocation failure and cleanup predicates remain unchanged. These neighbor tests provide the real host ABI checks separately from focused modeled hooks.

Provider/compiler selections use GCC13 or Apple for ordinary tools and supported explicit native sanitizer compilers. LLVM/opt are explicit, with Linux native GCC13 selection flags; no warning suppression or Wasm flag substitution is used. Existing neighbor runners retain their own checks and final products; the outer driver does not claim every intermediate legacy child artifact was archived before overwrite. The focused runner retains its individual commands and products before assertions.

## My retention and identity

All phases on this pin passed on their first attempt; earlier failed qualification and diagnostic trees remain unchanged. I retain 22 equal Linux input-map pairs and 20 equal puck pairs. Current verification rehashed 3777 Linux and 3769 puck paths against the final selected endpoint. The newly generated vm/verifier/service dependency files all contain service_classification_private.h, confirming automatic dependency coverage.

CLI setup products are recorded in before/after product maps rather than misclassified as immutable inputs. Source, selected tools and prepared providers are separately compared before/after every phase; later existing CLIs are also treated as inputs. Source scope is all tracked files excluding historical docs/evidence. Tool maps include selected compilers, named subtools/runtimes and build tools; they do not represent every transitive operating-system component. Fresh module/cache and executable outputs are archived as products.

The Linux seal contains 278 reports and 4391 unique objects (914756253 bytes), with 159425 archived references. Puck contains 225 reports and 4409 objects (286130564 bytes), with 152098 references. The downloaded puck archive is 72879386 bytes, SHA256 `e3e0b9c02eb366eeafd49acf19e08ad5756276023d6ac055e1cca9d239ffb6ea`; all 4635 regular members were checked, including every sealed report and object. My combined publication seals 510 reports and maps 5134 unique local objects (1161707153 bytes). Ten selected source/fixture/Make/deadline inputs are separately pinned. Original qualified trees, reports and archives remain available.
