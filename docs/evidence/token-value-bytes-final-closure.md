# I verify the final token provider closure

I freeze `8856cfa0c34bed720d45967c373be3eb89a9ee6a` on actual PR913 main
`933e8a590d28f0d2b4c746cc58c106011b43b01b`, including PR912. My
[static comparison](token-value-bytes-final-closure-identity.json) retains all17
token source/fixture inputs, all Nano/compiler schema/module inputs, and exact
canonical bytes for the nine incoming source/fixture paths. Only passive CFG,
nvm2c helper referencing, and a private indirect query affect owning C providers.

I rebuild C-seed/NanoISA/nano_virt providers from empty object directories on
both hosts. I copy only Stage1 and Stage2 from the final d4f qualified endpoints,
checking both hashes against their sealed product maps. Before copying, I check
all507 src_nano/schema/module inputs and both complete selected token/generator
import graphs. The new module cache starts empty. My `retained-stages.json`
records these facts; this run is not a fresh bootstrap or a claim that retained
stages contain the newly linked C providers.

| Host | Fresh providers | Complete ordinary token/paired/schema | Wrapper controls |
| --- | --- | --- | --- |
| Linux GCC | PASS21.016s | PASS27.776s | PASS3.822s |
| puck Apple Clang | PASS8.985s | PASS26.758s | PASS7.779s |

All three producers retain exact76 token and63 generator shadows, exact nine
output rows, and complete four-file equality to canonical/Python output. I do
not reduce the fixture to compilation, selected substring checks or counts.

[My final manifest](token-value-bytes-final-closure/report-sha256.json) records
253 actual Git report blobs,759 retained artifacts totaling279,182,981 bytes,
12 equal source/tool pairs,12 exact selected-name reports and8 complete generator
comparisons. Fresh current checks match26,190 source files and12 selected tools
on each host,333 Linux and332 puck endpoint products. Both source maps match.

I retain source roots `/home/jkh/Src/nanolang-token-final-885` and
`puck.local:/tmp/nanolang-token-final-885`, reports
`/tmp/nanolang-token-final-{linux,puck}`, and CAS
`/tmp/nanolang-token-final-artifacts`. Transfer/archive identities, selected
compiler/SDK/library records, commands, outputs and generated artifacts remain
in the seal. These are selected-tool and persistent-artifact claims, not all
transitive tools or every deleted transient file.

Original315, fresh51d and freshd4f bootstraps, first failures and sanitizer
attribution remain separate and unchanged. Root review precedes actual PR911
merge. Full File parser/resolver/lowering, generated behavior shadows, source
publication and compiler-policy follow-ups remain open under8bbc and its parents.

My [independent root audit](token-value-bytes-integrations-independent-review.json)
rehashes all756 Git report blobs across the three supplements, every retained
CAS object and reference, all36 exact shadow-selection multisets and all24
four-file generator comparisons against actual Git and CAS bytes. The final2453
selected source/schema/test/build inputs match frozen885. Current26,190 sources
and12 tools per host plus333 Linux/332 puck endpoint products also match. This
confirms the stated scoped evidence; no full bootstrap fixed point is claimed.
