# My private native adapter ready integration

I merge canonical9c90a55e1 into a separate tree from qualified seal5fd98c018.
Only additive Make/roadmap sections conflict. I retain both sections and all
canonical separator comments. My final Make delta is the five-line explicit
private target; default provider lists do not change.

All eleven production/managed-runtime/header/fixture inputs match the qualified
seal, and its original992-report manifest is byte-identical. My only linked
production TUs are portable_read_host.c, portable_read_managed.c and unchanged
managed_strings.c, using unchanged binary64_parse.h. Incoming File/source-plan/
indirect/cyclic changes do not enter this direct adapter closure. The input map
records actual bytes. I run no unchanged baseline again and attribute all gates
to their original b9ba/7b66 pins, not this integrated head. The qualified trees
and all first terminals remain intact. Wasm, public/source/installed linkage and
parentb7ef/2d2 remain open.
