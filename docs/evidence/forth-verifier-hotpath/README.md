# I retain my bounded verifier hot-path diagnostic

My original787 full sanitizer Forth session stops at1200seconds. These separate diagnostic histories use its retained linked providers without changing them. The first external probe compilation fails strict formatting warnings before execution. The corrected formatting-only probe runs under a30-second bound, prints only create-start, and is terminated with its leader reaped and group absent.

Four read-only debugger samples show File instruction scans during per-primitive verification and per-function linked verification inside session creation. They identify a repeated hot path; they do not locate the original full-suite last assertion or prove that every remaining cost has the same cause. The probe never reaches its arithmetic or PI commands. I retain exact command, source, provider hashes before/after, debugger samples, raw outputs and terminal. The completed probe executable remains at the exact indexed local path and hash; I do not claim a new complete compiler tool inventory beyond the original787 qualification maps.

My later source correction and corrected full-corpus results are separate evidence. I keep this30-second terminal as a diagnostic timeout, not a passing product gate.
