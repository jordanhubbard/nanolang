# My first admission fixture terminal

At source2a4caa269d1ea2d08120b9b6c24c995a436ba14b, Darwin fresh ordinary
stage1 passes10.841s, then the whole checker stops in the new positive fixture
at line1092 after0.928s. The explicit bounds do not expire and groups are gone.
Evaluator, codegen controls, sanitizer and Linux remain unrun on this pin.

My bounded checker-only diagnostic compiles the exact suppressed positive
against unchanged measured providers, then returns1 with three E034
EXPRESSION HAS NO EFFECT errors. It never evaluates the program. This
supersedes my initial implicit-return hypothesis. It also shows why an
outer statement refusal alone cannot establish the new arithmetic storage
guard. The fixture correction consumes positive results in typed bindings
and checks actual parsed operator results independently.

The seal preserves28 report members and327 durable local CAS objects
(50820065bytes), source/provider/tool endpoint equality, both executables,
exact commands and the diagnostic source. The Git bundle carries reports;
CAS paths identify local object storage. No corrected acceptance is claimed.
