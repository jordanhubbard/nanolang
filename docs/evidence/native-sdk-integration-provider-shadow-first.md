# I retain the first combined provider-shadow terminal

My exact `17e43cd9a02b2f3f0ee447e767635743fd26352d` Linux source passes the complete component build in 83.888 seconds. The separate bootstrap exits 2 in 61.204 seconds after three `native_provider_add` shadow assertion failures (first at line 1423) and the original ten-second shadow deadline. The outer supervisor does not time out; it records no cleanup errors and confirms all groups/descendants absent. Source and tool endpoints are unchanged. No dependent check runs.

I preserve all 40 original report files and the exact selected Git source manifest under `native-sdk-integration-provider-shadow-first/manifest.json`. Each row retains original/stored hashes, lengths and modes; gzip reports receive full decompressed readback verification. Actual products remain in `/tmp/nanolang-native-sdk-17e43-linux-source`, inventoried across `bin`, `obj` and `lib`. Private component logs remain attributable to this invocation.

My source audit separates the shared-array independent-case mistake from the later unassigned deadline. The approved `4e4a17be0` shadow correction retains all original assertions and adds an explicit alias assertion. I do not infer scheduling cause or claim a corrected terminal before it completes. Darwin combined bootstrap, all SDK/File acceptance and release remain open.
