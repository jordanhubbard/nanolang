# My optional-helper object diagnostics

I retain eight nonexecuted object-comparison phases: four proposals on Linux and
Darwin. I compile counterfactual C derived from the actual retained first-failure
303 shadow C. These are object diagnostics, not newly emitted producer gates.
The baseline keeps its unused-function warnings visible and nonfatal only for
comparison. Each corrected object retains `-Wall -Wextra -Werror` at O0.

- `e10e1c4e7`: discarded function designators. GCC has 85 unchanged undefined
  symbols. Apple grows from 40 to 88; I stop before Homebrew.
- `6087edbec`: unevaluated `sizeof` references. GCC remains 85. Apple rejects the
  corrected object for unneeded internal declarations; Homebrew is unreached.
- `ab76263c4`: after-definition annotations. GCC remains 85. Apple rejects the
  attribute placement; Homebrew is unreached.
- `83370647b`: actual definition qualifiers. GCC remains 85; Apple and Homebrew
  each remain 40. All three corrected objects pass strict compilation. I replace
  the two exact canonical embedded headers and match all 44 emitter definitions;
  the shared headers contain the remaining eight definitions.

All eight phase input maps are equal. I retain 179 report files in the manifest
before this scope note, and 3,765 unique content-addressed objects totaling
665,908,109 bytes in the persistent local roots named by my artifact index.
Large reports are gzip-compressed with both original and stored hashes. The
Darwin reports and objects have persistent home originals and local copies.

No diagnostic links or executes an object. Equal undefined symbols establish
only that comparison; fresh actual producer bootstrap, strict links, eighteen
original source methods, eight additive native methods, and bounded binary64
adjacency remain required. The prior 303 bootstrap/focused successes and native
strict-compilation failures remain separately attributed to their original pin.
