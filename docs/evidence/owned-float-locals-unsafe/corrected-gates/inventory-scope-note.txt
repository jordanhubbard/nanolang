Static runner review before runtime phase: the existing runtime Make targets create three test-specific heap instrumentation objects (obj/test_owned_result_heap.o, obj/test_consuming_call_heap.o, obj/test_multiple_consuming_heap.o). These are outputs compiled from frozen inputs during the tests, not previously frozen inputs. The current runner inventories every object and its strict whole-map equality may report additions as a tool change. Preserve all phase outcomes and full map delta. Existing input hash changes would still invalidate the phase; additions of only these documented test outputs require explicit evidence interpretation, not a claim that complete maps were equal. No source/tool mutation is made to the active gate.
