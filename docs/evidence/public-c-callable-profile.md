# I qualify my direct-call public C profile

I qualify task9ba422 under preimplementation contract0c7118bb and independently reviewed production9bdf95be. Integrated f5855788 includes canonical PR779 at7ebc9e9c; the reviewed backend and new fixtures remain byte-identical across integration. Final frozen harness30a561e1 changes only four old private API expectations after recorded correction e8354e25. My adjacent JSON preserves46 final file identities, three compiler/wrapper identities, eight logs and the complete initial freeze. All final identities match after terminal gates.

My three initial focused methods pass GCC2.298s and Clang3.652s. Four ordinary named callable source programs reach the backend refusal and preserve previous output. API controls cover callable locals/globals, parameters/results/externs, record/union fields, nested retained nominal and returned-function signatures, declared-function identifiers, expression callees, local-bound callees, qualified missing-exact-declaration and lexical-collision boundaries. Both output APIs retain prior bytes and recover in the same process. Direct descriptive scalar signatures, exact qualified declarations and lexical scalar identifiers sharing a function name remain accepted. Ordinary recursive direct calls and string results execute under C99/C11 O0/O2 ASan/UBSan.

I retain my first adjacent outcomes:46/50 each, GCC33.688s and Clang41.029s. Four concat/int/length/format private fixtures asserted successful text rendering for function-bound and expression callees, contrary to the newly reviewed refusal contract. These old checks established spelling only, not a callable ABI. Their assertion failures and frozen identities remain recorded; I did not replay those failed artifacts. The approved harness-only correction requires the exact callable diagnostic and zero emitted expression bytes, then clears invocation-local error state for independent later controls. Direct declaration and descriptive scalar-signature success assertions remain unchanged. No production correction or new ABI was introduced.

Fresh combined gates pass all53 methods under GCC31.558s and Clang44.439s. This combines three callable methods with50 adjacent arithmetic, conversion, literal, string, union, guarded match, lifting, options and array methods. The seven existing public C programs also pass using the same unchanged tools. Emitted programs retain established strict/pedantic C99/C11 O0/O2 ASan/UBSan flags; API harnesses additionally use-Wall-Wextra-Werror. I claim no bootstrap, Darwin, universal-language admission or global unused-helper-warning qualification.

Canonical merge precedes task9ba422 closure. Original6ade remains open for a consolidated supported-type inventory: unqualified legacy kinds must not silently inherit default-int64 representation. This child preserves existing qualified scalar/string/local nominal paths and makes no new type, array or callable ABI claim. Shared language-policy obligations remain independent.
