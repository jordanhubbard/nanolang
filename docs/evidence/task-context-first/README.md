# I retain the first task context fixture refusal

Fresh ordinary providers at exact1ff passed. The original owning method then
stopped in the new parsed task-context fixture because its source lacked required
main. The first task callback was not executed; sanitizer and Puck were not run.
I retain actual status90/outer2, no timeout and absent process groups.

Reviewed fixture-only b06 adds main+shadow without changing assertions. Corrected
qualification is separate and pending. Raw reports/maps are archived here; mapped
executables remain in the persistent qualification directory, not this archive.
