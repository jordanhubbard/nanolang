# I qualify explicit public C options and profile refusal

I qualify task_eab6b4dde0634f698d35c1012eafeaa6 under original6ade. Contract e549fa2a precedes reviewed production f9803408. I integrate canonical775 before production, build fresh local tools, and retain unchanged production throughout qualification. The final frozen8ef38de7 manifest records41 file identities, three compiler/wrapper identities and eight logs. All final identities match after gates.

My final two focused methods pass GCC3.260s and Clang5.444s. Direct API controls test NULL/default options, verbose, both static_strings compatibility values, no_main with and without source main, internal calls to source main, an uncalled private source main and separate C driver linking. Seven option modes each compile/run under C99/C11 O0/O2 with ASan/UBSan. Actual strcmp observes retained literal contents in library output. no_stdlib and no_main/global-startup combinations are refused. Each exact unsupported tuple/effect/async/try AST is tested at top-level, statement and expression boundaries; anonymous declarations and retained lambda metadata also refuse. Both path and FILE output preserve prior bytes, followed by successful same-process recovery. Two ordinary parsed anonymous functions, with/without capture, reach the expected backend refusal before prior output changes. Handcrafted AST controls are backend API evidence, not a claim of frontend admission.

All44 unchanged adjacent public C methods pass GCC23.520s and Clang31.966s. These include the integrated lifting source interpreter/verified-VM comparisons. All seven unchanged existing public C programs pass. The initial two focused methods also passed; after every gate terminated and41 frozen hashes matched, I added actual static-literal and uncalled-main observations only to the harness. I froze again and reran both focused compiler gates. No production/tool mutation occurred during any gate.

My emitted-code flags retain strict C99/C11 -pedantic-errors, implicit-function/return/local-typedef errors and ASan/UBSan; the API harness additionally uses -Wall -Wextra -Werror. I do not claim global emitted unused-helper cleanliness: preamble static helpers can remain unused. Source main's private language function is nonstatic, so no_main introduces no private static-function warning. No warning suppression or new helper ABI was added.

I implement no_main only as hosted-wrapper omission and continue to refuse globals without an initialization entry. I refuse unavailable no_stdlib semantics. static_strings values share established static literal lifetime; dynamic scalar/concat snapshots retain their existing process-exit ownership. Header claims now reflect hosted dependencies and checked unsupported lambda/effect/async/try/tuple semantics; dead GNU/effect stubs are removed. Semantic refusal preserves output; external FILE I/O failure during final copying still has its documented partial-write boundary.

This bounded Linux evidence does not claim bootstrap, Darwin or universal source admission. Canonical merge precedes this child closure. Original6ade still needs its final clause-by-clause inventory, including existing checked insertion-context boundaries; broader shared match/source/release tasks retain their own acceptance.
