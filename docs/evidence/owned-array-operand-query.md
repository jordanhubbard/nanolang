# I qualify private optional FLOAT operand obligations

I qualify reviewed query production `3898b0fad` at frozen `e235ff26db4f6b4ff1c3cb41318b5753d8a5c4ad`. My first query terminal at `faa07406f` is preserved in the [original seal](owned-array-operand-first.json): strict compilation passed, but the new branch fixture asserted a revisit which its initial work-queue scheduling did not produce. I changed one fixture line to delay the optional predecessor with eight NOPs, retaining the revisit assertion and exact final operand masks. I did not change production to address this fixture failure.

The corrected setup passes in 1.145 seconds and all16 phases pass. GCC normal, GCC sanitizer and Clang18 sanitizer each pass3,812 checks. Thirteen existing adjacency targets pass: owner origins, owner descriptors, retained layouts, managed record plans, mixed layout views, mixed FLOAT proof, mixed Samples, owned value results, nested owned results, owned binary64, owned STRING fields, owned STRING joins and mixed Samples admission. The earlier terminal remains a failed aggregate run; I do not relabel it as a pass.

I check all six typed FLOAT comparisons with exact, left-optional, right-optional and both-optional operands. Both branch and loop controls require more transfer visits than instruction rows and retain the final per-operand FLOAT|VOID sets and check masks. Exact VOID, INT, STRING and uninitialized operands remain refused. Optional arithmetic, negation, local storage and bit transport remain refused. PRINT/PRINTLN accept exact INT and refuse BOOL/FLOAT/STRING/VOID/ARRAY/U8. Origin and independent authority rows agree for every decoded instruction, including zeroed RET operand rows and exact opcode/location identity. These are queries, not executed comparisons or output operations.

I migrate exactly the previous authority F64_EQ optional comparison refusal to prepared-with-obligation. The original refusal evidence is retained at PR830's frozen source. Extended struct sentinels use designated initialization; other old negative controls remain. Allocation-prefix sweeps still identify564 directly classified MEMORY failures and five structural INVALID failures only at prefixes563–567. Output/module bytes, cleanup and later recovery checks remain intact.

I integrate canonical PR828 before freezing. Its production changes are paired Samples source lowering; the query/runtime provider files are unchanged by that merge. All1,743 source hashes and157 compiler/tool/object hashes are identical before/after and at sealing. My [machine-readable seal](owned-array-operand-query.json) retains69reports,177artifacts and22current input identities, including source copies, actual runner, commands and query object/executable archives. The first terminal seal remains separately linked. Existing adjacent generated intermediates retain their original harness retention limits.

I use GCC13 and Clang18.1.3 on Linux. ASan/UBSan with leak detection instrument the fixture and retained-layout, layout-decoder, cursor, affine-state and verifier translation units; other providers are ordinary GCC objects. I claim no whole-program sanitizer coverage. All pending owner ARRAY modules remain unexecuted. Existing runtime adjacency exercises its previously admitted profiles. Private runtime implementation, actual tag checks, public activation and unchanged paired Bundle/PREFIX source acceptance remain open under430220.
