# I qualify private generated-native File execution

I qualify task_9a23711b4d9afb8b3408ccc6deff609c under runtime82ff through
[my reviewed contract](../NANOISA_FILE_PRIVATE_NATIVE.md). Productione501 emits
real static C functions, fixed scalar expressions, labels and direct calls.
Correction6fdb only separates an emitter `break` onto another source line. I do
not activate any public service route or claim installed standalone C, source
shadows, LLVM/Wasm or complete File/5.1 acceptance.

| Gate | Linux | Darwin puck |
| --- | ---: | ---: |
| Fresh common providers | acff PASS20.966s | 4e8 PASS9.527s |
| Ordinary native O0/O2, instrumented and linked | 4e8 PASS50.137s | 4e8 PASS36.839s |
| GCC ASan/UBSan/LSan, both native optimizations | 4e8 PASS139.821s | Not selected |
| Clang ASan/UBSan/LSan, both native optimizations | 6fdb PASS70.390s | 6fdb PASS52.909s |
| Original private VM | 6fdb PASS6.731s | 6fdb PASS5.392s |
| Original frame/carrier | 6fdb PASS6.382s | 6fdb PASS5.033s |
| File opcode/public refusal | 6fdb PASS3.023s | 6fdb PASS3.428s |
| Ordinary wrapper publication | 6fdb PASS3.020s | 6fdb PASS6.144s |

Each native matrix runs the original complete VM fixture corpus first to capture
exact serialized cases, then offline emits and compiles each accepted native
program. Instrumented capture retains73 distinct cases:71 emit and2 refuse.
Linked capture retains69:67 emit and2 refuse. Each O0/O2 replay makes567
instrumented or73 linked calls and retains every original value/status/site,
cleanup, alias, generation and allocation assertion. The copied-fact VM unit
controls remain in the full fixture harness; they are not evidence of native
execution by themselves.

Separate isolated O0/O2 binaries execute the original first integer boundary
case without the private VM object or interpreter fixture; symbol records show
no `nvm_file_vm_execute`. Deliberate ABI-revision and startup-fact mismatch
binaries refuse before acquisition with unchanged output. Malformed and dead
unsupported modules have no compiled artifacts: their checks remain explicitly
at emission. The test registry performs exact byte lookup, never bytecode
interpretation. Native entrypoints use a fresh NATIVE plan and checked arena.

Instrumented emission sweeps retain334 checked allocation refusals, require a
terminal attempt without injected failure, and restore allocation/output/host
baselines. The output-limit control is extent arithmetic at128MiB, not a claimed
constructible valid module of that size. Real File hooks and actual carrier
operations preserve temporary acquisition/close, modeled I/O errors after real
byte progress, direction/domain failures, argument/return/scratch generation
limits, first and secondary cleanup, and initializer suppression. Linked mode
has no interposed host descriptor counts; I do not attribute instrumented
observations to it.

I rebuild selected hosted/NSI/carrier/emitter/VM-sentinel and fixture providers
with the chosen sanitizer flags. The remaining common/compiler/VM objects are
ordinary, explicitly inventoried inputs. ASan/UBSan and detect_leaks=1 remain
enabled, inherited LSAN_OPTIONS is cleared, and no warnings are disabled.
Actual compiler/linker executable hashes and command selectors are retained.
Darwin uses Apple for ordinary runs and Homebrew LLVM for leak detection, with
explicit SDK and libffi include selection. Those SDK paths/version and link
flags are recorded; my selected tool map does not claim a complete inventory of
all system headers or libraries.

## I preserve every earlier terminal

- Initialacff Linux setup passes. The normal phase stops10.488s compiling the
  generated **test registry**, whose negative-case return shares a line with an
  `if`. VM capture/emission and71 production O0 units already passed compilation;
  no native program had executed. Correction4e8 changes only that fixture newline.
- LinuxClang at4e8 stops9.036s compiling the emitter's unbraced else/break line.
  Darwin Homebrew at the same pin stops5.094s at the same warning after Apple
  ordinary passes. Neither failed Clang phase executes capture/native code.
  Correction6fdb changes only source formatting. I preserve passing4e8 evidence
  and do not relabel it as6fdb. All71 instrumented and67 linked emitted C files
  compare byte-for-byte across4e8 GCC and6fdb Clang; root independently confirms.
- The first Darwin preparation selects an older SSH Python and fails archive
  extraction on its unsupported filter argument. My orchestrator then wrongly
  attempts the dependent driver, which stops immediately in the empty tree.
  The retained JSON records the observed errors/statuses; there is no complete
  raw stderr archive for these two initial tool invocations. No build/service
  executes. A new checked launcher uses explicit Homebrew Python and preserves
  its complete subsequent command/error logs.
- The first6fdb Darwin reuse launcher verifies extraction but fails containment
  at `/tmp` versus `/private/tmp`. It correctly stops before the driver. I retain
  the full launcher terminal, resolve both paths and create another fresh tree.
  Corrected9583-source verification and163-provider copying pass before launch.

No failed historical binary is replayed. The successful Linuxacff provider setup
is reused only after hashing164 copied inputs. Darwin6fdb similarly reuses163
inputs from its successful4e8 setup. Fresh fixture/provider and generated-native
builds occur for every corrected selected phase. The source trees and setup
attribution remain distinct.

## I seal actual inputs and outputs

[My report manifest](file-private-native/report-sha256.json) records reports,
commands, terminal states, source/tool maps, original and corrected launchers,
provider reuse and generated-byte comparison. The artifact store records20,890
references to6,199 unique files (2,112,141,322 bytes); these include generated C,
raw stdout/stderr, compile commands, object files and actual binaries. My
[archive record](file-private-native/artifact-archive.json) identifies their
retained combined archive. Downloaded puck archives are independently rehashed
before extraction, then every referenced artifact is rehashed again locally.

Eighteen source/tool before-after pairs are equal. Every measured source map has
9,583 entries. Current three Linux source trees and both measured Darwin trees
match their retained maps, as do9 Linux and10 Darwin tool labels. Compiler labels
may resolve to the same executable; they are not counts of distinct compilers.
Setup output creation is not claimed immutable. Each advertised report is checked
against the committed Git blob as well as its local bytes before publication.

Measured Linux roots are `nanolang-file-native-acff`, `nanolang-file-native-4e8`
and `nanolang-file-native-6fdb`. Measured Darwin roots are
`/tmp/nanolang-file-native-4e8-corrected` and
`/tmp/nanolang-file-native-6fdb-ready`. Failed preparation directories remain
separate. I keep these frozen trees intact. Later canonical integration is a
separate review, and bounded completion awaits actual merge reconciliation.
