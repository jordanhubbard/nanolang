# My paired ordinary record producers

I qualify frozen `f5fb7efb` over canonical `b147e9bb`, including source-owned
routing PR744, portable allocation harness PR739 and shadow identifier PR747.
I attach this bounded checkpoint to task15f. Broader array-element, generic,
import, forward-order and nominal provenance remain open, as does executable
record admission in LLVM/Wasm.

I publish retained layouts and explicit ordinary declaration authority together
for plain local records with scalar, string and earlier ordinary record fields.
My complete emitted function table includes initializers and selected shadows.
Parameter, result and local tags are exact; nominal descriptor identities remain
NO_INDEX. Unknown local/schema provenance omits both optional tables and keeps
ordinary execution. My selfhost AST still loses extern-record provenance
(taskf366); a retained-token exclusion supplies no positive authority.

I passed a fresh integrated bootstrap and six paired methods in 181.558 seconds.
Each method uses the Cseed producer and raw selfhost emitters built by Cseed,
Stage1 and Stage2. I execute verified ordinary modules, compare nested/empty/
identical-shaped record layouts, check exact function/local/parameter metadata,
retain every selected shadow and roundtrip layout/authority bytes. Unsupported
array fields and extern records remain executable without optional authority.
LLVM/Wasm record refusal preserves existing output.

I also passed all 90 NanoVirt methods and 123 lexical local-name controls,
including the existing five local-name and twenty marker allocation boundaries.
My two focused owned-source routing methods passed in 164.369 seconds,
including normal VM and sanitized generated-native execution.

Before restacking, the same six producer methods passed at `6eec6838` in
179.859 seconds. The authority helper, selfhost producer and test source remain
byte-identical after integration. C codegen additionally includes canonical
PR744 resource dispatch; its imported selfhost borrow producer changed, so I
ran a new bootstrap and affected integration checks.

I preserve intermediate failures separately: a reserved `byte` variable stopped
parsing; an overly strict optional TypeInfo assumption initially omitted C facts;
canonical C shadow names exposed the separately merged PR747 grammar gap; and a
private parser token helper stopped the next bootstrap before execution. I fixed
these at their appropriate boundaries and qualified fresh corrected inputs.
My first additional owned-routing invocation lacked the isolated checkout's
`bin/nvm2c`; both methods stopped at FileNotFoundError. I preserved that log,
built the missing tool and reran only those controls without source changes.
I do not infer release readiness from this checkpoint.
