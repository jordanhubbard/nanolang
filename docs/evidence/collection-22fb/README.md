# Dynamic children and complete string-array results

I qualify exact reviewed source `22fb6ff455f1a37940f1b176befba464f7c6be3c` through the unchanged owning collection and full C typechecker Make targets. I retain the typechecker target's stage1 prerequisite, fresh providers per configuration, original1200-second phase bounds and actual executables before their normal removal commands.

| Host/configuration | Collection seconds | Full C checker seconds |
| --- | ---: | ---: |
| Linux ordinary | 4.705 | 3.068 |
| Linux GCC ASan/UBSan/LSan | 13.514 | 7.666 |
| Darwin ordinary | 4.334 | 2.536 |
| Darwin Homebrew ASan/UBSan/LSan | 37.404 | 4.801 |

All eight phases pass with equal source/tool endpoints and no remaining process groups. The collection target retains the five original allocation-prefix cases, their transient/persistent failures and recovery, completed map/array task leases, twelve record projection cases and original adjacent evaluator groups. New controls cover dynamic string/record mutation aliases, slicing, callbacks, all three string concatenation forms and a completed dynamic result's Environment lease. Four checker-only refusals preserve non-string and unsupported-operator boundaries. All full C typechecker tests pass.

I verify126 report members and690 unique CAS objects (506,681,107bytes), including eight actual retained test executables. Source scopes come from exact Git blobs and every extracted source hash is checked before execution. `seal.json` binds report bundles, endpoint maps, original command logs and local CAS paths.

The earlier c34 checker refusal and its checker-only diagnostic remain retained. These gates qualify the reviewed isolated source. The subsequent combined candidate includes separate packed record-name layout changes and requires fresh complete compiler/evaluator qualification; I do not infer full evaluator, Nano bootstrap, hosted CI or release acceptance from these results.
