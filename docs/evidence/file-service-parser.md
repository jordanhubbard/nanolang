# I retain actual File declarations in both source parsers

I qualify a source grammar/AST prerequisite, not File source execution. My real
C and Nano parsers retain counted service interface/path text, exact catalog1,
and an unresolved origin. I preserve it across definitions, constructor copies,
cloning and export boundaries; unresolved consumers refuse rather than taking
an FFI fallback. Companion snapshots, authoritative resolution, both executable
lowerers and full generated behavioral shadows remain mandatory parent work.

My initial production appends the service tag and canonical schema records,
updates all Parser constructor copies, and supplies the standard generated AST
list runtime closure. Both actual frontends accept exact zero-field `None()`
match syntax distinctly from named bindings and underscore discard. They reject
empty bindings for nonzero/unknown variants. My renderer emits `octet` as its
local name, retaining the catalog's `value` and `eof` fields and all five shadow
assertions.

I retain all earlier first terminals. These include missing AST enum/runtime
closure, reserved identifier and const spelling, zero-field grammar and helper
qualification, a missing fixture free observer, generated-C comparison warning,
frontend diagnostic stream assumptions, and measured ordinary match leaks.
The missing observer did not prove a product leak. The later independent Linux
GCC/puck Homebrew LSan162-byte/12-allocation traces did measure cleanup leaks.
The compiler comparison-parentheses follow-up is separate task_f8dc08d9; typed
fixture locals did not repair that compiler path.

My reviewed cleanup releases only an immediately constructed empty union header
and its identity strings, with exact escaping-pointer preservation. Identifier,
call, field, nested and payload roots retain their existing ownership behavior.
I retire owned names from interpreted match bindings and native function scope
metadata, preserving borrowed type facts. General union/payload lifetime remains
open; caller-owned fixture roots are explicitly freed by their fixture owner.

My C fixture includes the actual parser with allocation failure hooks, actual
owning environment free observation, and separately labeled direct interpreter
cleanup units. It retains208 persistent/transient allocation rows and four exact
summary rows. Actual parsed ordinary programs repeatedly execute and emit C;
preexisting metadata names/counts remain checked. I parse the complete output
of the actual seven-provider publisher rather than rewriting a reduced fixture.

The paired method retains all actual C-seed/Stage1/Stage2 token/parser outputs,
exact selected shadow multisets, and full four-file canonical/Python/actual
schema generator byte comparisons. My new parser fixture selects947 shadows.
Ordinary good/bad source controls have no service declaration that could mask
arity checking. C seed, both self-hosted compilers and NanoVirt explicitly
refuse the unresolved complete generated service graph without publishing output.

Sanitizers cover the included parser plus fresh env/lexer/UTF8/eval/transpiler
providers. The eval fixture includes the actual interpreter; direct helper and
synthetic guard-return controls are labeled separately. Other common/runtime
providers, including the iterative C emitter, remain ordinary. Publisher and
strict-document tests have their own explicit fresh seven/five-provider scopes.
I clear LSAN_OPTIONS and retain leak detection. This is not full-compiler
instrumentation or a whole-parser recoverable-OOM claim.

Final qualification source:5a0e9b8967ee24b6834ccbbbe7994fbb8f6b9588.
Fresh initial source maps match26455files; both product maps are empty.
Linux fresh bootstrap293.615s; puck fresh bootstrap289.202s. All planned phases passed. I retain Linux GCC/Clang ordinary and scoped
sanitizers, and puck Apple/Homebrew ordinary plus Homebrew scoped sanitizers.
Both ordinary paired methods passed with actual C seed/Stage1/Stage2 outputs,
selected shadows and complete schema equality. All seven publisher/strict-document
configurations and both parser/module/wrapper adjacency phases passed.

My ordinary adjacency Make graph invokes bootstrap again as a dependency. I
retain its actual product endpoints separately; it does not change attribution
of the earlier paired and sanitizer results. Final current inventories verify
26455 source paths and12 selected tools per host, with955 Linux and712 puck
products. Final library/module-cache inventory is explicitly postphase-only;
I do not manufacture earlier maps for those paths.

I retain the complete puck transport archive and its hash. Local extraction
first stopped at an intentional absolute symlink and then an intentional FIFO
from publisher controls. My corrected extraction stores regular files only,
records30 special entries without following them, and retains all13078 archive
member identities in the original archive. These packaging terminals did not
execute or invalidate qualification.

The evidence directory stores lossless gzip for reports larger than40000 bytes,
with original and stored SHA256 identities in reports.json. Artifact-index.json
addresses retained artifacts by content hash. Historical failed pins remain
separate from the final fresh-bootstrap acceptance. All full source resolution,
publication, lowering, imports, startup and service execution obligations remain
open under the parent contract.
