# I qualify exact public C union results

I qualify task810698 under my [preimplementation contract](../PUBLIC_C_UNION_RESULTS_CONTRACT.md). Reviewed production `b6d13150` plus context reset `8e00eabb` shares exact result declaration resolution across function prototypes, definitions and direct-call values. Return/local checks retain owner identity and compatible scalar payloads; empty variants keep their standard C representation. No foreign aggregate ABI, function-value call, generic instantiation or expression lifting is inferred.

My first frozen `863d156d` GCC and Clang gates each passed one API method and refused both source methods before C output. Static inspection identified an omitted existing representation: checked selected constructors can remain dotted `AST_STRUCT_LITERAL`. I recorded the correction before production, then reviewed `13aa9efa` added full declared-owner/variant lookup and reused the existing field validator through an unmodified temporary AST view. I retained the first logs and did not execute refused artifacts. The original source fixture/assertions stayed unchanged; corrected API controls add exact dotted-name, spread and AST-immutability checks.

Frozen harness `43b03d23` passes three focused methods under GCC in 1.655s and Clang in 2.318s. Both source methods compile generated C99/C11 at O0/O2 with ASan/UBSan, execute interpreter and verified NanoVirt VM observations, and check scalar/empty results, alias parameter/local returns, forward calls, nominally distinct unions, private names and once evaluation. FLOAT payloads use integer-bit observers; STRING payloads use content equality. The API method checks publication preservation, exact refusal and later valid same-process recovery.

I passed all 36 adjacent methods under GCC in 17.788s and Clang in 23.775s, plus all seven existing public C programs. My [manifest](public-c-union-results.json) retains 31 source/provider/harness/tool hashes, two compiler hashes and nine logs. All 33 identities stayed unchanged during qualification and after integrating canonical `2a0d3167`; that merge added unrelated emitted-array tests/documentation and no production changes. I did not rerun unchanged gates solely for that integration.

I qualify this Linux source/tool pin, not a new bootstrap or Darwin gate. Canonical merge precedes task closure. Full6ade, statement-aware block/match values, shared wildcard/no-success policies, generic/foreign/function-value results and option/capture obligations remain required and open.
