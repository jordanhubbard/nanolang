# I qualify private retained V2 declaration facts

I run the original test-ownership-declaration-projection recipe at frozen
8bf4112cf8dbb893e2bb7c48c9e0cb7bc925d066 on Linux and Darwin, ordinary and
ASan/UBSan/LSan. Each lane builds fresh NanoISA/UTF8 providers, then runs both
linked and allocation-instrumented fixtures with every original assertion plus
additive retained V2 controls. All eight fixture executions pass:1,987 linked
checks and14,279 instrumented checks per lane. The recipe keeps240 seconds per
command; the outer setup/owning bounds remain1,200 seconds.

I retain124 reports, raw inner commands/status/stdout/stderr, source inventories,
provider identities and tools. Source/provider/tool endpoints remain equal; all
inner leaders are reaped and groups absent, as are outer supervised groups.
The archive members and complete archives are verified against seal.json.
Mapped built objects/executables remain in persistent qualification directories.

The new controls exercise actual selected, duplicate and unused signature rows,
wrong-selector/tag and STRING-name-kind refusal, revision2 refusal, dimension
bounds, independent copied facts after source destruction, and every measured
allocation prefix in persistent/one-shot modes followed by recovery. Existing
legacy layout/array/union/maximum/refusal fixtures remain unchanged.

This is private declaration-query qualification. ARRAY_FIELDS revision2,
combined SDK caller budgets, complete provider cross-validation, consumer
admission and installed canonical acceptance remain required and unqualified.
