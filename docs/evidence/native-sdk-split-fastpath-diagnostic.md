# I retain successful selections followed by a deadline refusal

My single copied diagnostic uses exact `fe6450ea0ec020ee8cf1296711bfc1977723df52` source, C seed and complete providers. Its original ten-second child and 120-second outer bounds remain unchanged. All 1,156 selected shadows finish with status zero; the JSON report records success, completion and no failures. `interpreter_end` occurs after 9.950475 seconds wall and 9.939312 seconds process CPU. `selection_end` also reports zero.

The parent wait record appears about 1.166 seconds later with raw status zero. The existing polling deadline flag nevertheless makes the compiler return 1; the sentinel remains unchanged. The outer harness has no timeout or cleanup errors and records absent groups/descendants. This result does not establish the cause of the exit-observation delay. It establishes no accepted bootstrap or installed SDK result.

My [manifest](native-sdk-split-fastpath-diagnostic/manifest.json) retains all 2,532 diagnostic files. Raw logs, timing analysis, driver and exact source/tool/product maps are in Git, using lossless gzip for large reports. Generated temporary sources and binaries are retained by original digest in the named immutable local content store; modes, original paths and byte lengths remain explicit. Both original and copied product/source identities are checked before and after. No unchanged diagnostic is repeated.

The completion protocol proposal and later source checkpoint have separate review and qualification requirements in [my completion contract](../SHADOW_COMPLETION_DEADLINE.md). All original bootstrap, File and installed SDK gates remain open.
