I verify my environment-free raw target prerequisite at `de60d37c4`: CALL, TAIL_CALL and FUNCREF require an existing zero-upvalue target. Independent source review passed before execution. Seven fresh configurations each pass 1,063 nonexecuting codec checks: Linux GCC/Clang ordinary and ASan/UBSan with leak detection, plus Darwin Apple/Homebrew ordinary and Homebrew sanitizers. I preserve all earlier payload allocation-failure/recovery controls and add exact direct target, truncation, work-limit, input-preservation and no-allocation checks.

Each archive retains the external driver, complete reports and actual fixture executable; its hash and size match the product inventory. Source/tool before/after maps agree, all terminal statuses pass and owned process groups are gone. Darwin archive hashes match across hosts. Full source trees and products remain in each host's `nanolang-qualification/capture-direct-de60-*` directories.

These are structural transport checks. I have not established initialization, effect summaries, indirect or linked target environments, producer emission, capture execution or release readiness. Those full shared-capture obligations remain open.
