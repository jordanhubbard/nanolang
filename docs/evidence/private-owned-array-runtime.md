# My private owned ARRAY runtime qualification

I qualify frozen `431aeeaf27a329c3e9e7eaaf212a441a45c18363` in my isolated private runtime tree. My reviewed runtime production remains08be; the separately recorded array growth accounting correction is a73d. My adapters exist only in explicit private test builds. I do not claim public activation, source lowering, Darwin acceptance or completion of430220.

My [machine seal](private-owned-array-runtime.json) hashes266 reports,462 artifacts and17 current inputs, including immutable source/tool copies. All1754 source inputs and all161 original tools/objects stay unchanged during the gate. Final File nominal adjacency adds `obj/nsi.o`; the after-inventory has162 entries. I do not call their membership identical.

## Observed acceptance

I pass all18 phases below. Each of three compiler configurations runs two computed-goto VM fixtures,4739 checks per execution, each covering fused and unfused observations. My original switch label did not disable automatic computed-goto selection; those runs do not qualify switch dispatch. Generated native programs run seven cases at O0/O2 per configuration with exact failure statuses, preserved result sentinels, visible stdout prefixes, zero allocator roots and allocation-prefix recovery.

| Phase | Seconds | Result |
|---|---:|---|
| gcc-query | 10.608 | PASS |
| gcc-san-query | 34.751 | PASS |
| clang-san-query | 28.274 | PASS |
| owned-array-authority | 1.247 | PASS |
| owned-array-origins | 0.584 | PASS |
| owned-array-layouts | 0.550 | PASS |
| retained-layouts | 0.535 | PASS |
| managed-record-plan | 3.253 | PASS |
| mixed-layout-view | 0.540 | PASS |
| mixed-float-proof | 0.696 | PASS |
| mixed-samples | 1.440 | PASS |
| owned-value-results | 9.875 | PASS |
| nested-owned-results | 5.018 | PASS |
| owned-binary64 | 5.660 | PASS |
| owned-string-fields | 4.352 | PASS |
| owned-string-joins | 1.150 | PASS |
| mixed-samples-admission | 0.858 | PASS |
| file-nominal-module | 9.071 | PASS |

My GCC and Clang18 sanitizer phases use address/undefined instrumentation and leak checking. Instrumented units are the private VM, translator, heap, C fixture and complete generated native programs. Other linked providers are ordinary GCC objects; this is not whole-program instrumentation. Exact commands and compiler identities are sealed.

I observe nested factory/relay owner results, two shells sharing mutable ARRAY identity, surviving aliases and growth after reverse unpack, STRING sibling retention, empty owners/arrays, optional left/right/both FLOAT failure checks, assertion/bounds cleanup and exact INT output. Normal objects lack both private symbols and public routes continue refusing these modules. Defensive retain checks test the maximum guard without incrementing through overflow. This finite corpus does not establish every staged-operand/call composition or public continuation behavior.

My narrow growth controls test both FLOAT packed storage and STRING boxed storage. A failed required realloc leaves pointers, capacity, length, values, references and counters unchanged; successful recovery charges exact64/128-byte deltas and cleanup restores zero object/byte counts. Full VM cleanup independently enumerates all remaining objects and exact internal/module references before collection, rejects extra retains, then requires the original object and byte baseline. I retain the same assertions after the accounting fix.

## Retained terminals

I preserve the fixture's first CLI-global link failure, first unlocalized cleanup assertion, graph-localized byte assertion and native expected-status assertion in the four linked seals. The graph terminal did not log a numeric post-GC byte discrepancy; the native terminal did not log actual status before its assertion. I do not retroactively supply those observations. Static analysis identified missing growth accounting and the existing native SET status mapping. Corrected diagnostics now precede assertions. Passing corrected gates do not reclassify earlier terminals as infrastructure or as demonstrated leaks.

I keep the original historical tools, sources, logs and artifacts separately. My parent430220 still requires reviewed public activation, paired source acceptance and mutation controls.

## Dispatch evidence correction

Static review after PR836 merge finds vm.c automatically defines NANO_COMPUTED_GOTO under GCC/Clang unless NANO_NO_COMPUTED_GOTO. The sealed commands lacked that disable in the switch-labeled build. I preserve every original report/artifact/hash and reinterpret those six successful runs accurately as computed-goto evidence. I require separately frozen true-switch-only qualification with explicit preprocessor evidence; no original runtime failed because of this coverage gap.
