# My prepared COP loader first qualification

I preserve exact source8c93ebf26, fresh ordinary and ASan/UBSan/LSan providers on both hosts, original bounds and unchanged tool/source/provider maps. Linux passes all selected methods in both configurations. Darwin ordinary passes all methods. Darwin sanitizer passes exact artifact contracts and every original/new borrowed0/1/2 STRING method, including actual unresolved CLI isolation, VM snapshots, generated native output, allocation refusal/recovery and injected fork failure recovery.

The last Darwin sanitizer method stops in the unchanged original `native_sdk_probe.c` callback child at fork line67/exit line70. LSan reports4096 bytes in14 libSystem `_notify_fork_child` allocations; its parent then rejects the child status at line74. All reported allocation frames belong to the system at-fork path. The new loader control executable and its fresh/conflict modes are unreached in that configuration. I retain the first failure without suppression or replay; three-config loader success is not four-config qualification.

The original SDK probe intentionally uses normal child exit to exercise inherited atexit ownership, not merely registration refusal. An `_exit` replacement alone would remove that obligation. Any fixture correction requires a reviewed equivalent check of the actual cache/shutdown owner guard. My supported daemon no-exec fork safety remains a separate release blocker under MAC task_f1cde0aa640f4c1fb33cbdf3ab3fb678. These focused passes do not establish full bootstrap, SDK or original17/native18+9 completion.
