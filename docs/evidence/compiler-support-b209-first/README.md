# My first compiler-support focused terminals

I retain exact b2094af7e focused raw reports, per-command status and fixture products, source/provider/tool hashes and the owning driver. Linux ordinary passes all selected controls. Linux sanitizer passes fresh setup, exact signature refusals and mutable-string snapshots, then real-provider driver compilation stops at the retained ASan module_builder_verbose ODR violation. Darwin ordinary passes setup, signature refusals and mutable snapshots, then stops before executing its real provider at the incorrect one-library assertion. Both corrected source/fixture successors remain separately attributed.

The archives contain relative report paths only. Original executable artifacts remain in the persistent host qualification roots. All terminal tool maps report unchanged endpoints; failed groups have been reaped. Full bootstrap and the original public source corpus remain unqualified.
