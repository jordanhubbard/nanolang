# My partial owned-array mutation source qualification

I qualify production `9f7ead937f983e6441f113488534835a046b2781` separately
from final canonical integration. **My complete evaluator gate remains failed
pending PR855. I do not claim merge, parent completion, or release acceptance.**

| Gate | Linux | Darwin |
| --- | --- | --- |
| Fresh C providers/setup | PASS 52.500s | PASS 150.921s |
| New initializer lookup control | PASS in both evaluator attempts | PASS alone 3.742s |
| Declared push identity, six methods | PASS 54.793s runner | PASS 128.963s runner |
| Mutation/source, twelve unchanged methods | PASS 600.951s runner | PASS 584.664s runner |
| Full unchanged owned-record suite, twelve methods | PASS 79.167s runner | PASS 149.175s runner |

I retain all original programs, PREFIX shadows and backend routes. I compare
raw Stage1/Stage2 ordinary outputs and each producer's repeated bytes. The twelve
owner-profile methods retain exact C/selfhost dump equality. These programs do
not establish a compiler-bytecode fixed point.

My selfhost binaries are reused from frozen 0c54, with matching Nano sources,
retained C-provider dependency maps, binary hashes, symbols and linked-library
reports. They are not a fresh bootstrap. Current canonical File providers change
that linked closure; final PR855+canonical integration requires fresh stages.

My complete evaluator first rejects a guarded-only integer match under E035.
The reviewed one-line fallback fixture 70c12 preserves the exact result 7 assertion;
the corrected gate then returns 4099 because the old evaluator keeps only the last
wildcard. I retain both failures and depend on separately reviewed PR855. The
first corrected-overlay preparation also stops before compilation on the retained
untracked failed test binary; the later runner permits only its exact path/hash.

Darwin's first mutation setup ends with truncated traceback headers and no method
results or final maps. A later filesystem check observes 100% capacity. I retain
the missing exception boundary and do not infer its sole cause from a passing
fresh run. Exact duplicate source archives were removed only after local/remote
hash verification. The recovered run checks at least 2GiB free, source/stage
identity and records any changed setup outputs before compiling fresh artifacts.

My Darwin archive is verified file by file. To retain room for final bootstrap,
I relocate 2435 files from 18 completed `artifacts-*` directories, 1171242255 logical
bytes, into the verified local archive and extracted copies. Every remote file is
rehashed before removal. Remote reports/maps, raw fixture workdirs, source trees
and providers remain. The first removal preflight safely refuses root inventory
JSON files as directory candidates before any deletion; its intent/result remain.
The v2 relocation manifest records the corrected exact directory/file set.

My manifests hash the report files and archived artifacts. Binaries stay outside
Git. Linux archive SHA256:
`71678b698c6092080ad9ea2d3db2ce5396deca56c69c389dcff7755d344c9665`.
Darwin archive SHA256:
`ae39b5efb12941f7ab91acf32c86548df80ce63fc52b73c031ae9cd74d9bc84d`.

After qualified PR855 integration I still require a fresh bootstrap, complete
evaluator and affected source gates. Parent 430220, mutation bba622, full 4be,
product and release remain open.
