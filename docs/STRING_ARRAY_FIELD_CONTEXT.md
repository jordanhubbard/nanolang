# I restore the STRING destination adapter at borrowed field assignment

I retain the first corrected `70f8a7de8` direct-context terminal: the fixture compiles, then its original assertion finds no complete array result metadata after checking a STRING-field assignment. It exits by assertion before the paired corpus or installed acceptance runs. I do not weaken that assertion or replay the failed product.

My integrated `AST_SET` field branch checks the RHS and invokes `check_nominal_contract`. Its nominal-array helper deliberately returns early for scalar leaves. My original SDK branch also called `check_concrete_union_arrays`; the integration omitted that call. Without it, this field destination neither checks the shared symmetric STRING compatibility nor requests `try_get_expr_type_info`, which publishes the intrinsic result through the retained `NominalView` and checked native binder. This is an adapter omission under my existing STRING contract, not a defect in the native nominal-only helper's scalar policy.

I propose restoring exactly that existing adapter after the single RHS check and before the existing nominal contract. I pass the field annotation and declaring owner already snapshotted before recursive RHS checking. I do not retain a potentially moved `Symbol` or `StructDef` entry, infer an owner from spelling, widen borrowed-source admission, change numeric/unknown compatibility or introduce another result registry.

All original direct controls remain: complete owned STRING result, stable repeated lookup, INT mismatch, explicit binding refusal with no published row, six STRING/numeric/empty literal controls and the lower-level scope label. The checked binder retains transactional row publication; failed public checker entries retain their existing cache-suffix rollback. The direct statement fixture remains explicitly below that public boundary and does not prove public borrowed-array admission.

The native owner independently confirms this restoration matches the integrated contract. Source review precedes execution. I first qualify the owning adapter with exact C-provider identities and preserve the original full paired corpus. Retained Nano products, if used for that prerequisite continuation, require explicit original-source/provider exclusion proof; no current-source bootstrap claim follows from retained products. The separately reviewed import-prefix optimization and every full both-host bootstrap/installed SDK gate remain distinct requirements.

My source checkpoint restores that two-line call in the actual field branch. The RHS check, pre-RHS snapshots, nominal contract and coarse type comparison remain in their original order. No fixture assertion changes. Strict syntax-only compilation of the complete checker and the included-context probe passes; source review still precedes any corrected execution.

My corrected direct control passes on Linux at `dc9c37fc1` in 3.539 seconds, including compilation, with every original assertion retained. I use the exact ordinary providers from the retained `bae599477` build, excluding `main.o` and `typechecker.o`; the test compiles the corrected checker directly. The complete C/Make/module/schema/script input comparison differs only in `src/typechecker.c`. Source, tool and retained product identities agree before and after the control. My [sealed reports](evidence/native-sdk-field-context-corrected/manifest.json) retain the original provider preparation terminal without treating its failed bootstrap as a pass.

This control parses no imports and invokes no module compilation or metadata extraction. My import-dependent corpus remains held for the separately discovered module snapshot auxiliary-vector ownership correction. I do not infer a full paired, installed, sanitized or Darwin pass from this ordinary Linux adapter check. A prior export stopped before any compiler or product invocation because archive extraction preserved group-write modes; I retain that staging refusal and the fresh export's explicit Git-mode verification.
