# My bounded classifier timing observation

I retain the prior single-run result at e8ba5f324: the unchanged ten-second gate fails, while 495 full admissions dominate the measured interval. I start this separate diagnostic from 1e53cba55 and leave all qualified trees untouched.

I partition each successful private core admission into disjoint outer intervals: initial/final remainder, owner-array routing, mixed routing/preparation, runtime readiness, and ownership support. Stage boundaries surround the existing statements without changing their order or short-circuit conditions. Reused entries remain in remainder. Their integer nanoseconds must sum to the independently retained full-plus-reuse total.

I additionally wrap five actual query definitions: service pending, owner-array routing, mixed candidate, ownership contracts validation, and owned-transfer scanning. These nested inclusive measurements cover internal calls from those queries, not only their VM callers. They overlap each other and the outer intervals; I never sum them as exclusive cost. I retain exact call counts and nanoseconds. Service pending still performs its existing bindings/presence and File instruction scan; I remove no scan or check.

I enable collection only during a core admission interval. Pending counters are fixed storage, reset at entry, discarded before any early return, and merged only on successful completion. All counters/time additions are checked. Outside this interval the forwarding wrappers invoke their original bodies without collecting. Every original function return, body, call order and argument is preserved. With the private macro disabled, wrapper declarations expand to their original function definitions and observation hooks are no-ops. The diagnostic is serialized, as was the existing observer; I make no concurrent execution claim.

I keep the existing 8192-row cap, exact marker placement, name bound, clock/output failure exit92 and original ten-second/outer120-second limits. Extra fields are printed on each existing marker, not new unbounded events. Final unmarked intervals, early refusals and public admission outside the core remain unmeasured. Clock and wrapper overhead are explicit. After complete source review I permit only one fresh ordinary GCC13 build and one original full imported emitter command. No broader cache or pointer-identity proof is authorized by this diagnostic.
