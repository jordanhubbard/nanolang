# I map my held source checkpoint to the reviewed runtime interface

I inspect the paired producer source from reviewed835 head18593, now on canonical
2cb8728a3 after actual merges835/836.
I make no production edit and execute no unavailable public route. This design
implements the already approved source18731 and ordered mutation bba622 contracts;
source admission still waits for qualified public activation after private836.

## I keep separate profile facts

I add an owner-array module fact beside, not in place of, BorrowCG.mixed and
nb_mixed. Declaration inspection records exact direct array<float> fields and
propagates managed-leaf presence through prior owner children. COMPLETE|RESOURCE03,
original layout indices and retained field/name ordering remain unchanged. I
refuse ordinary Samples rows plus owner-array rows until their composition is
separately qualified. Borrowed signatures remain disallowed for this value graph.
All nb initialization and fixture reset sites must reset the new fact; C's calloc
initialization is retained.

I do not widen borrow_field_tag/nb_field_tag or parameter/reference leaf helpers.
A record/field-aware helper supplies ARRAY only for the exact retained owner-array
field. A separate exact-array availability predicate serves literal, local,
alias and at paths without granting ordinary AGG_PACK or borrowed authority.

## I preserve transfers and observations

I change paired borrow_owner/nb_owner constructor branches to invoke existing
contextual array emission only for an exact declared array<float> field. Every
field still stages one result in a hidden slot in source order. Declaration-order
reload uses LOAD_LOCAL for retained arrays and OWN_MOVE_LOCAL for unique children;
OWN_PACK preserves the nominal layout. Hidden roots consume the same256-slot bound.

I change unpack field-slot classification, keeping full pattern validation before
OWN_UNPACK_LOCAL. Full scalar-leaf destruction in any owner-array graph uses the
already qualified direct-unpack path, including original close shadows; it cannot
emit reference opcodes. Dead-parent binding projection retains the exact recorded
parent/field/live-slot checks. ARRAY aliases use ordinary loads/stores and never
become unique child transfers. Existing consumed-owner and source-exit checks stay.

Field observation begins with LOAD_LOCAL on an exact live mode-zero owner and
follows AGG_GET through the already checked nested path. The origin query keeps
nested STRUCT values as nonescaping observations of the same live root; source
lowering must never store them as copied owners. ARRAY and STRING leaves publish
retained ordinary roots. This path does not widen reference places or scalar
field writes. Whole-owner moves with outstanding observations remain rejected by
the final authority.

## I preserve optional scalar obligations

The existing Samples source emitter returns source FLOAT type after ARR_GET,
while complete origin analysis retains actual FLOAT|VOID. I can reuse emission
only with the final owner-array conjunction: comparisons retain per-operand runtime
checks, but exact FLOAT locals/arithmetic/negation/casts/signatures do not gain
admission merely from this source tag. An annotation does not discharge a pending
runtime obligation. Negative outputs must remain untouched after final refusal.

## I pin public-interface dependencies

Native_effects reports the approved986e interface being implemented after836:
`owned_array_admission.h`, `nvm_owned_array_route` returning NOT_SELECTED,
SELECTED or INVALID, and `nvm_owned_array_admit(module, plan**)` returning existing
NvmOwnerAuthorityResult PREPARED/UNRESOLVED/INVALID/LIMIT/MEMORY. The final exported
spellings will be checked against the qualified commit before preparing the paired source production checkpoint.

For my distinct owner-array C producer branch I require ordinary nvm_verify
success, explicit SELECTED, and fresh successful complete admit, then free the
plan. I must not run the old scalar-owned verifier afterward. Service presence
refuses before route/admit; selected owner-array failures never fall through to
Samples or old ownership. Selfhost assembly goes through the same fresh public
verifier. Existing no-owner-array Samples and STRING paths stay unchanged.

## I order the mutation extension explicitly

After first source qualification, exact unbound array_set lowers receiver, index,
value once in that order, emits ARR_SET then POP and returns VOID. Exact unbound
array_push lowers receiver then value once, emits ARR_PUSH and retains ARRAY
result identity. Local/declaration resolution precedes builtin selection. Ordinary
managed roots already prepared on the stack remain live across later consuming
calls; no source drop substitutes for runtime cleanup qualification.

A static inventory found neither specialized producer currently emits ARR_LEN
or handles array_length. Mutation growth/alias controls need this read operation.
My [mutation addendum](NANOISA_OWNED_FLOAT_ARRAY_MUTATION_SOURCE.md#i-measure-mutation-through-a-bounded-length-operation)
now explicitly requires only unbound array_length(array<float>)->int, one receiver
evaluation and ARR_LEN, with exact origin/profile/name guards. This is a recorded
source requirement, not a demonstrated runtime defect or broad collection admission.
The initial18731 constructor/pattern/at slice need not depend on that extension.

I retain the unchanged original Bundle/PREFIX and every selected shadow in both
phases, plus Samples/STRING/reference/owner-free refusals and exact byte/dump
comparison. Public activation, paired producer checkpoints and fresh platform
qualification remain required before any source milestone closes.

## I divide implementation and qualification checkpoints

1. I inspect the reviewed runtime activation production checkpoint and pin its
   actual public route/admit symbols, status handling, service-first guards and
   optional operand semantics. The approved986e contract alone does not prove
   that an executable public route exists. No source execution occurs here.
2. After that API review, I prepare the paired source18731 production delta in a
   fresh isolated tree: explicit profile/category facts, declaration tags,
   constructors, full patterns, observations, exact array locals/at and C final
   fresh admission. I add meaningful mandatory shadows for new helpers. I send
   both producer diffs for independent review before bootstrap/source fixtures.
3. Only after qualified public runtime activation is available and the source
   checkpoint is reviewed do I freeze integrated tools and run a fresh bootstrap,
   Cseed/Stage1/Stage2/NanoVirt and both canonical emitters. I retain unchanged
   Bundle/PREFIX and every selected shadow, exact byte/dump parity, VM/native
   alias/lifetime and refusal/output controls, plus Linux/Darwin boundaries.
4. I separately prepare mutation bba622's ordered set/push/length source delta
   against those qualified interfaces. I retain exact builtin identity, INT
   length/VOID setter/ARRAY append results, once-only evaluation, prepared roots,
   growth/shared-alias observations and current failure semantics. Independent
   review precedes its fresh paired producer/runtime controls. It closes no
   managed-owner parent until the parent's complete acceptance is met.

The supported default MAC profile accepts the bba622 description addendum.
Its independent auto-worker failed history remains unchanged; I neither dispatch
another worker nor relabel that attempt as passing. Task13d1 is independently
reconciled from actual835 merge; source18731, mutation bba622 and full parents
remain unfulfilled.
