# I retain my historical public C acceptance inventory

I retain this pre-options snapshot as historical evidence. My current finite clause/context/option audit is [PUBLIC_C_ORIGINAL_ACCEPTANCE_AUDIT.md](PUBLIC_C_ORIGINAL_ACCEPTANCE_AUDIT.md); the remaining-option descriptions below no longer describe current production.

I audit original `task_6ade6d62ef644390bb9645b15077c8df` against integrated436ea9c8. Its actual requirement is an inventory of supported options and expression contexts, portable lowering or checked refusal, and ordinary C99/C11 controls. It does not require admitting every source construct or inventing new closure/effect ABIs.

My merged scalar arithmetic/transport, stable conversion/concatenation ownership, literal normalization, content equality, named union payloads/results and guarded statement matches have their own exact evidence. Current child33f74 qualifies function-body block/match/conditional values through real scoped statements. It preserves checked refusal for lifted loop headers/global initializers, unknown types, unsupported nominal payloads and unsupported callable forms. The old GNU AST_BLOCK emitter text remains unreachable behind the `cb_needs_lift` guard; removal of dead fallback text can accompany the final profile cleanup.

Remaining original-scope findings are concrete. `CBOptions.no_stdlib` and `static_strings` are never consumed. `no_main` only refuses globals; a source main still gets the hosted wrapper. The driver initializes all options to zero and sets only verbose, so false static_strings is the established ordinary path, not an opt-out from literal lifetime. The header advertises bare-metal no-stdlib and captured-lambda helpers, plus longjmp/setjmp effects, without corresponding implementations. Default expression dispatch refuses unknown nodes, but top-level/statement context handling and each advertised option need direct public-API publication/refusal and strict compiler controls before I claim an accurate profile.

My next bounded contract should specify each option rather than silently ignore it: retain zero/default hosted behavior, establish or refuse requested no-stdlib/no-main modes explicitly, and document static literal behavior without claiming an alternate lifetime mode. I must audit captured/computed call and effect nodes in every accepted context for checked refusal, correcting unsupported header claims while preserving actual supported declarations. No new production or option policy is established by this inventory. Task6ade remains open pending reviewed contract, controls and canonical merge.
