# My exact temporary owner argument contract

I record `task_9460b469e4a349cab05cbdcf0ae48760` before production, under installed-product affine acceptance `task_e8d860a16da0464891dd32e91c42bef1`. My frozen 950f36d1 gate and all 55 positive-source refusals remain immutable on `docs/product-gates-950f36d1` at 0ab42bcc. This proposal changes no production or tests.

## My observed dependency order

I keep all original source expectations and mandatory shadows. My first refusals divide into 28 declaration-output probes and 27 executable subcases. No failed negative diagnostic assertion accounts for them.

1. I can independently qualify direct temporary owner actuals using existing qualified value-call runtime authority. My unchanged shadows call `close Handle { fd: 7 }` and `close (acquire)`. Both producers reject the actual before CALL today. Four positive case families across three producers first reach this boundary; additional cases can expose later restrictions after it is repaired. I do not promise all 27 pass from this change.
2. I separately need checked executable dependency selection for unused foreign declarations. Twenty-six probes contain `extern fn consume_handle` and an uncalled checked `probe`; their zero-argument `main` returns zero and there are no shadows. The current specialized route scans every declaration and fails at missing body. This is not permission to admit foreign resource ABI calls. Two union probes fail earlier at the union guard. My comparison below identifies the necessary selection contract before any repair.
3. Reachable transitive ownership in ordinary record wrappers needs exact layout classification and complete field transport, preserving ordinary managed fields and their lifetimes. `Bundle` with string or array<float> is not just a spelling change from `resource struct`: runtime retained layouts, ownership traversal and native carriers must support every field before source admission. Resource collections remain refused.
4. Full reachable owned-union, indirect owner-call, foreign consuming-call and managed-field acceptance remain separate prerequisites. Source `unsafe` lexical blocks and ordinary scalar siblings must preserve exact scopes and effects; I do not erase a block to bypass unsupported contents. No early guard removal establishes any of these semantics.

## My first proposed source checkpoint

I retain the existing bounded acyclic value graph, at most eight emitted functions/frames and one to eight exact positional arguments when ownership is transferred. I change only whole-owner arguments to direct mode-zero calls. CALL_REF, borrowed/owned mixtures, projected partial owners, closures, indirect targets and new foreign ABI remain unchanged refusals.

I admit an existing live named whole owner, an exact complete declared resource constructor, or a direct call producing the exact owned nominal result already supported by `borrow_owner`/`nb_owner`. I reuse those helpers rather than infer nominal identity from a storage tag. Constructor fields retain their existing supported shape, exact names/counts/types and source evaluation order; nested inline resource fields are not newly admitted merely because a top-level actual can be temporary. A factory's declared result and emitted result layout must both match the consuming formal.

I evaluate arguments once, left to right. I move a named owner exactly once. A constructor or factory produces a stack-owned value whose lifetime extends through preparation of every later argument and whole-call preflight. Existing verified stack owner state and VM/native argument cleanup remain authoritative. Nested factory calls while earlier owned actuals are prepared must pass verifier and lifecycle tests; if they expose a runtime prerequisite, I record and repair it separately before widening source guards.

I preserve exact instance/layout identity, no outstanding reference authority, moved-state and pending-disposal checks. I do not invent lexical names for temporary values or publish their compiler slots as advisory source bindings. Failure leaves prior output intact; I do not publish a partial module after a later argument fails. No implicit disposal of a live source owner is introduced.

## My comparison with unused-extern selection

Ignoring a body-less declaration inside `nanoisa_borrow_emit` is insufficient: it still schedules all defined functions, including unreachable probes that call that declaration. The ordinary producer already distinguishes program-mode direct-call closure from full module mode, but the ownership dispatch occurs before that selection. The smaller apparent declaration guard change therefore requires an explicit routing/selection prerequisite, not one deleted condition.

A separate bounded selection contract must keep full parsing, nominal resolution, typechecking and affine checking for all source declarations. It may omit executable definitions and unused extern imports only from an exact selected closure: `main` for program publication, and every selected shadow plus its actual direct dependencies for shadow publication. Local/formal name shadowing, imports, callbacks/function references and unknown indirect targets must not be mistaken for a proved absent dependency. Unsupported or uncertain reachable ownership/ABI stays a checked refusal. Full module emission must retain its existing all-definition contract. Resource layout selection must follow the admitted closure without reclassifying a reachable owner as an ordinary value.

That contract could recover the declaration probes without executing or admitting their foreign operations, but it spans dispatch, dependency identities and complete shadow selection. I recommend the narrower temporary-actual checkpoint first, with explicit closure work reviewed independently. I do not alter the declaration-probe tests or retarget them to the legacy backend.

## My acceptance before publication

I require fresh C-seed, Stage1 and Stage2 producers, exact canonical module metadata, VM/native values and stdout, and all original selected shadows. I run unchanged positive `scalar_terminal_operation`, `empty_resource_pattern`, `initializer_evaluated_once` and `tests/test_resource_tracking.nano` cases through their existing routes. Any next first refusal stays recorded rather than reducing those fixtures.

I add normal paired controls for constructor and factory actuals, multiple ordered actuals, a factory after an already prepared owner, repeated calls and nested direct factories within the existing graph bound. Observable scalar fields/PRINT distinguish once-only source order. Exact nominal mismatch, duplicate/moved named owner, outstanding borrow, projected actual, missing/duplicate constructor fields, indirect/foreign target and cyclic graph controls preserve prior outputs and require the intended checked diagnostics, not parse failure.

I retain existing source-borrow graph/shadow/name/consuming gates and applicable owned-call verifier, VM/native failure-cleanup and allocation controls. I execute only corrected accepted modules; verifier refusals are never run. No full-affine, full-product or release completion follows from this bounded checkpoint.
