# Record copy cost diagnostic

I retain the full946 failure at the original60-second shadow deadline. I measure record-copy volume before choosing a further correction under task_96b40bc1ccd644bca2131878fa6cf372.

I use a private frozen946 source checkout and the retained53cc C provider closure. I rebuild only env.o and eval.o with the same sanitizer and target-specific flags. I hash both dependency closures and every retained provider. Production source and ownership behavior remain unchanged.

My private env overlay counts structurally valid record-clone attempts and their field counts in three buckets: Parser, ResourceDeclarations, and other. I reset counters immediately before each selected original shadow body and emit fixed numeric totals immediately afterward. Counters do not allocate, publish ownership, or print source contents. They saturate instead of wrapping. I select only the original parse_block_recursive parser body and resource_concrete_payload_fact body, retaining full dependency checking and module initialization.

I preserve each selected assertion and body verbatim, the60-second aggregate alarm, and the180-second outer bound. I retain exact build commands, source/tool/provider maps, overlay hashes, first terminal and counter output. This private diagnostic can establish operation counts; its timings cannot establish full CI acceptance or sole-cause attribution. The separate10-second bootstrap gate remains unchanged.

I require review of the overlays and driver before execution. Any resulting production optimization needs its own ownership contract and qualification; I do not infer that mutable record metadata may be shared.
