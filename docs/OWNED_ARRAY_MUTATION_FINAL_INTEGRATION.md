# My final mutation and match integration gate

I combine my preserved source checkpoint `950a0cfd5`, canonical
`179626d392349c26d7bd1eb165f29d7bd2c32c7b`, and the complete reviewed match
branch through `10f448deb`. I also retain the checkout-sequencing history from
`e1f703257`. My merge conflicts are additive ROADMAP tails only: I preserve both
histories. My evaluator, checker and evaluator fixture merge automatically;
relative to the match branch they retain exactly my declared-push changes and
initializer-binding unit. Relative to my source checkpoint they add the reviewed
match production, corrected backstop/reduce fixtures and match tests. My existing
handler fallback remains present with its exact result assertion.

I keep my earlier 9f7 Linux/Darwin source acceptance partial. Its full evaluator
failed, and its reused selfhost stages remain attributed to the original 0c54
bootstrap. I do not rewrite those results after integrating the dependency.

## My fresh qualification boundary

I freeze this integrated source and the chosen host tools before execution. I use
new isolated Linux and Darwin checkouts and new evidence directories. I verify
completed transfer, exact HEAD and the expected clean tree before launching any
runner. I require sufficient Darwin disk headroom before each phase; I use puck
if CX cannot sustain the complete gate. I do not modify old trees or execute
preserved failed binaries.

I build fresh selfhost stages with `make -j2 -f Makefile.gnu CC=<host-cc>
bootstrap` (1800-second bound). I do not reuse old stage binaries: canonical File
providers changed their linked dependency closure. I retain the actual bootstrap
source, compiler, SDK, provider/cache input and output inventories separately.
After successful bootstrap I build the reviewed source tools, shadow driver and
metadata prerequisites, recording a new post-setup boundary and all producer
hashes. Linux uses explicit GCC; Darwin uses explicit Apple Clang and SDK for
ordinary setup, then explicit Homebrew LLVM Clang for native sanitizer controls
with leak detection enabled. Compiler selector variables and actual resolved
executables are recorded, not inferred from make's CC.

I run the complete evaluator from the checkout root, retaining both the new
initializer unit and every match/reduce/backstop assertion, followed by all eight
match-totality tests. Only after those pass do I run all SIX unchanged declared
push identity methods, all TWELVE mutation/source methods, then the entire
unchanged TWELVE-method owned-record-pattern suite on each host. I retain all
original PREFIX programs, mandatory shadows, backend routes, timeouts,
publication/refusal checks, selfhost raw equality and per-producer determinism.
No replacement fixture or narrowed source corpus stands in for these gates.

Each phase records its first terminal and exact counts, source/tool/provider
maps before and after, complete logs and generated artifacts. Setup-created
objects and caches are recorded as outputs rather than claimed immutable inputs.
A failed phase stops dependent execution; any correction requires a recorded
contract and review. Successful evidence is sealed before any authorized
hash-verified duplicate offload. I require archive and per-file hashes to match
before deleting a duplicate, retain reports/source/provider trees, and record
exact relocation paths and bytes.

This gate can close only its measured source, identity and fixture children after
actual merge reconciliation. Parent430220, full4be, product and release remain
open pending their complete acceptance; this is not a full product rerun.

## My evaluator binary retention correction

I preserve the first integrated runner terminal under taska9c5d7f9b908eab1d423f1265f665129.
Both fresh bootstraps pass; Linux setup and all123 evaluator tests pass, but my
runner then tries to archive tests/test_eval after Makefile.gnu intentionally
removes it. I retain the successful command/log separately from the failed runner
status and make no deleted-binary archive claim.

I resume only unrun Linux totality and source gates. On Darwin I use a build-only
external evaluator target with the identical Makefile flags/provider expression,
retain its binary, and execute from the checkout root. All assertions stay intact.
I record exact runner/tool/source hashes and first terminal, retain both fresh
stage sets, and repeat neither bootstrap nor the passing Linux evaluator.
