# My direct mixed LLVM and Wasm lowering

I continue the complete generated-consumer requirement under
`task_f36b179a0f2b4a1b99c29ccd2af66f99`,
`task_15f955fae5cf402d92bf88794122e9a2` and
`task_488a05eb5e2a417caf83a8353363a30d`. My separate C qualification remains
frozen at its recorded revisions. This source-preparation branch does not
establish that the pending GCC sanitizer corpus has passed.

## Shared preparation and exact target boundary

I reuse the copied execution plan and independently checked93-operation recipe
ledger. I factor the existing output/work accounting and decoded fact checks
mechanically before adding the LLVM writer. C output must remain byte-identical
for all73 captured products after that factoring. LLVM emission owns its complete
staged text and publishes text/length/cost together only on success. The retained
plan128MiB and consumer128MiB domains remain separate; output growth accounts
both old and replacement buffers. I charge scans, formatting and ABI/table
materialization without adding an unaccounted secondary preparation copy.

My new source-private entry point takes an explicit native or wasm32 target.
It does not infer a target from file suffixes or change an old public selector.
I emit the existing generated, validated target layout/triple constants before
any instructions. The target is part of the artifact identity; native and
wasm32 output cannot be interchanged. No shell command, C subprocess or embedded
bytecode interpreter participates in emission.

The output defines real LLVM functions and labels. Each body receives only the
opaque instance pointer and runs until a generated direct call/return yields to
the existing1024-frame scheduler. Source CALL never becomes recursive host CALL.
The same runtime owns all locals, arguments, operand roots, suspended frames,
globals and retained results. LLVM temporaries only borrow scalar bits or values
until the same checked move/replace helpers transfer ownership.

## ABI agreement before creation

I spell each generated table's LLVM field types explicitly: NmsValue is
`{ i64, i32 }`; function/program/view tables contain target-width pointers;
record descriptors, fields and offsets contain their existing32-bit fields.
I derive alignment and padding from the selected target layout, never the host
emitter's pointer width. A pure source-private runtime ABI query reports exact
sizes, alignments and every field offset for these shared structs, plus ABI
revision/frame/root constants. Generated startup compares every reported value
before calling nrg_create. A mismatch preserves the caller's output pointer and
performs no allocation or instance acquisition. The query changes no existing
struct or C entry-point contract.

All runtime calls use the actual C declarations. Integer and enum parameters
retain their exact widths; bool parameters/results retain the C target ABI and
its required extension attributes. Small boxed values cross the boundary only
through pointers, avoiding target-dependent aggregate argument/return rules.
Independent compiler-generated ABI probes for native and wasm32 must agree with
the hand-emitted declarations and table layouts before executing any product.
A layout probe is a prerequisite, not a substitute for full product parity.

## Operations and failure roots

I emit all93 operations and reject all other opcode bytes according to the exact
current ledger before publishing any output. Integer arithmetic is modular;
checked division/remainder covers zero and signed-overflow cases without LLVM
poison. Floating arithmetic uses ordinary IEEE operations without fast-math,
canonicalizes arithmetic NaNs exactly as the C route, and preserves transported
bits including signed zero. F64/int conversions guard invalid ranges before
conversion, matching the existing runtime semantics rather than relying on
undefined LLVM casts. Type checks precede consuming helpers, preserving owned
wrong-tag roots for first-error unwind.

Every branch uses the exact copied control successors. Helpers publish scalar
or heap results through checked root operations; allocation helpers retain the
same safe-point/operand staging protocol. No runtime opcode switch is added.
Unreachable decoded blocks and all declared functions still receive complete
coverage. Initializer, repeated entry, failed-result preservation, committed
globals and1024/1025 frames keep the already required behavior.

## Acceptance order

- [ ] I review mechanical common-preparation extraction and prove unchanged C
  generated bytes before relying on it in the new emitter.
- [ ] I review complete LLVM93-operation emission, exact target/runtime ABI
  agreement and unchanged consumer budgets before execution fixtures.
- [ ] I compile the unchanged73-product/404-action corpus as native LLVM and
  wasm32 at O0/O2. Both supported Wasm engines execute actual linked products.
  Isolation links contain only generated code, the shared counted runtime and
  the replay driver; no VM/query/decoder service appears in the product.
- [ ] I retain full allocation-prefix coverage in both modes with independent
  recovery, exact status/root/alias/scalar observations, ABI mismatch refusal,
  output sentinels and actual emission allocation/work boundaries. Supported
  native IR sanitizer instrumentation is explicit; I do not label ordinary
  generated IR instrumented because only the C runtime has sanitizer flags.
- [ ] I qualify import-free Wasm memory limits and installed-only runtime/package
  boundaries, then propose public selection only after the complete matched
  VM/C/LLVM/Wasm conjunction. Paired producers and general nested/cyclic graphs
  remain mandatory following dependencies, not exclusions from5.1.

## Complete first emitter source checkpoint

I share the exact byte/work allocator, independent93-case recipe and decoded
instruction/operand/edge checks. The LLVM entry independently materializes the
same plan fields using the C entry's unchanged allocation order and checked
limits, then emits target tables, all function/resume blocks and complete
startup comparisons. It consumes only the target-prefix strings from the
existing validated runtime generator; it does not embed that legacy adapter IR.

My additional accounting is explicit:

| Storage/work | Reservation or charge |
| --- | --- |
| Owned execution plan | Existing independent128MiB/33554432-step domain |
| Function/layout/record/start arrays and output state | Existing exact sizeof sum |
| Field and instruction materialization | Exact checked requested bytes; unchanged C allocation order |
| LLVM helper named scratch | Conservative4096-byte reservation including256/256/128/128-byte buffers, two block states, value arrays and scalar temporaries |
| Output replacement | Old plus new capacity overlap, existing128MiB consumer ceiling |
| Every instruction's LLVM control work | Additional512 units before emission |
| Formatted/copied output and target prefix | Existing byte-based formatting/growth charges |

The scratch reservation does not claim full compiler-spill/libc/process stack
measurement. Native/Wasm table alignment is computed from emitted LLVM types
and checked against all50 pure runtime ABI facts before nrg_create. Every table
field, pointer and parameter byte also receives an explicit startup comparison.
No output publication occurs until all functions/edges/tables and byte/work
limits succeed. The old C entry and public routes remain distinct.

Strict GCC syntax checks pass for the emitter, factored C entry and runtime after
the preserved missing-header-fact compile correction. Syntax used an existing
qualified generated target-prefix header solely as a declaration prerequisite;
no new compiler package, generated LLVM module or runtime product has executed.
Independent native/wasm ABI probes and full source review remain prerequisites
to the execution fixture checkpoint.
