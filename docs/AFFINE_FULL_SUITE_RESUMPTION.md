# I resume my complete affine acceptance for 5.1

I continue installed-product task `task_e8d860a16da0464891dd32e91c42bef1` after
PR861 and the reviewed distinct-array overwrite supplement26691. I preserve the
original950f full-quick failure and every later first terminal. This contract
prepares a fresh acceptance run, not a claim that the whole product passes.

I integrate canonical `85141cdf8259151848fdb66a8c5c1b0a09138d60`, including the
qualified U8/STORE normalization and native TCO changes. I retain the old a4d
runtime fixture qualification separately. The ready merge is automatic; the
three overwrite fixture files remain byte-identical to a4d and array VM/heap,
authority and native emitter sources remain unchanged by the canonical delta.
Compiler/evaluator and reconstruction sources do change, so I do not reuse the
old source compiler or label its provider closure current.

I freeze the reviewed new pin and actual tool/SDK identities before each host's
fresh isolated preparation. Linux uses explicit GCC and Darwin explicit Apple
Clang plus the selected SDK. I build bootstrap from scratch (1800-second bound),
retain all newly produced stages, then build nano_virt, nano_vm and the exact
obj/test_affine_c_frontend target. The complete provider/tool maps distinguish
pre-build inputs, generated outputs and the post-setup boundary. A completed
transfer, exact HEAD, clean expected tree and adequate disk space precede any
runner launch. I preserve the old qualified trees and use new artifact roots.

I then run the unchanged `tests/test_affine_selfhost.sh`, whose complete corpus
is one frontend-parity method, twenty contract-boundary methods and twelve
owned-record-pattern methods:33 methods total, without filtering. I keep all
36 boundary case names,14 positives,22 negatives and their existing source-hash
guards, every original PREFIX/shadow, all frontend/backend routes and exact
refusal/output assertions. The dedicated AFFINE_PROFILE_EVIDENCE_DIR retains
its existing route reports. An external artifact-retention wrapper may copy
otherwise temporary outputs and logs without changing source, assertions,
selection or compile/run timeouts. I record the exact shell invocation and
strict33-method successful terminal; a nonzero terminal is retained and stops
further acceptance until a separately reviewed correction.

I do not call this isolated script run the entire make test-affine-selfhost
target: that target also invokes affine-module and affine-generic identity suites.
Those remaining target/product gates stay explicit in the broader roadmap. No
compiler eligibility exclusion, ownership fallback, changed expected diagnostic,
source partition or shadow omission is authorized here. A later new failure is
classified by its actual first checked route before any implementation proposal.

The full suite runs on Linux and Darwin with fresh immutable inputs. I seal exact
sources, tools, commands, statuses, all available generated artifacts and their
hashes, retaining first failures and measured successes independently. Passing
this33-method prerequisite can discharge its original affine-suite boundary;
full managed/ownership target coverage, complete product/fixed-point gates and
release publication require their remaining evidence. I reconcile no full parent
merely because a narrower prerequisite passed.
