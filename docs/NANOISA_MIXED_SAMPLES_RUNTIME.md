# I execute closed ordinary values alongside owners

I record task_78c9ce49bf93416da6d426b05a14929b under
`task_4be28fef163f42069064357639b3b5cc` from canonical
b7040dcdf061b58700aef7ffbb7315217b415c6a. PR812 completed only private
composition1bf5. Its report still sets `runtime_admitted` false. This is my next
preimplementation contract; I change no production or fixtures before review.

## My exact first scope

I retain the unchanged Samples test and its complete PREFIX from
`test_owned_record_patterns.py::test_ordinary_inferred_field_and_alias`:
Handle, consuming close, shadow close, Samples containing array<float>, main,
and shadow main. The synthetic entry preserves every selected shadow and its
order. I neither split the module nor omit owner-bearing shadows.

I use the already proved closed entry0, at most eight functions, acyclic direct
value-call graph and bounded instruction/state/origin budgets. All functions,
including unused bodies, must pass the complete conjunction. Parameters/results
remain the composition's exact scalar or INT/BOOL/U8-leaf RESOURCE signatures;
ordinary managed parameters/results, borrowed CALL_REF, strings, PRINT, globals,
imports, links, callbacks, indirect calls, captures and recursion remain outside
this first mixed profile. Nonparameter ordinary locals keep exact ARRAY/STRUCT
identity. Record mutation and owner-shell managed fields remain outside it.

This narrow mixed profile does not replace the qualified ordinary, borrowed,
consuming-owner or new STRING-field profiles. Their existing accepted paths remain
accepted independently. A STRING owner is not forced through a Samples analysis
that deliberately refuses it. Bundle with owner and ARRAY fields remains separate
`task_430220ce190946518d404088533531b6`; proving Samples supplies no authority over
that owner shell. Source lowering remains a subsequent checkpoint.

## My executable authority is a fresh conjunction

I retain the descriptive query and private composition API as non-admitting.
A new internal executable admission result must be constructed from the same
immutable module, never from caller-supplied maps or mutable proof fields. It
requires structural/instruction and transport validity, positive closed shape,
independent affine/scalar composition, and implementation support for every
opcode and every recorded runtime scalar obligation. Positive shape alone, an
ARRAY tag, or `affine_checked` alone cannot select execution.

I separate candidate classification from admission. Classification may identify a
mixed candidate but grants no authority; malformed or unsupported metadata never
falls through into ordinary lowering. The explicit mixed route cannot be reached
merely because the old verifier failed. Existing ordinary/advisory metadata
behavior and existing profile precedence remain unchanged outside the exact new
candidate. Allocation failure and work limits remain failures, not fallback.

Every public verifier/API independently establishes its required checks. I audit
nvm_verify, individual-function/max-stack/linked variants, VM initialization,
execute/call/invoke/invoke_callable, direct public core and native emission.
Linked mixed admission remains refused, including otherwise valid zero-link versus
nonzero-link distinctions. LLVM/Wasm and existing managed selectors retain their
current refusals; I do not pass a mixed report to an ordinary NvmRecordPlan API.
No public unchecked facts constructor or persistent module trust is introduced.

## My metadata and state retain their identities

I preserve source per-kind STRUCT ordinals, original global layout indices,
RESOURCE flags, declared local tags and nominal indices. The checked private facts
constructor remains internal. I do not erase resource flags, rewrite ordinary
locals to VOID or manufacture a filtered fake module to satisfy old validation.
Only positively RESOURCE values may enter record_fields, owner pack/unpack,
put/take, observation, move, call/return or owner-exit transitions. Ordinary slots
have a separate availability/tag/origin state and cannot gain owner authority.

Runtime CALL and STRUCT RET currently use nvm_affine_state_create and the existing
parameter/result queries. I introduce an internal checked mixed query path using
fresh admitted facts with the original indices; I do not weaken the old public
constructor. Every positional argument keeps count/tag/nominal/mode checks,
whole-argument preflight and frame-capacity checks before transfer. Results are
checked while rooted, capacity is reserved before detach, and caller publication
occurs only after successful cleanup. Ordinary managed signatures stay refused,
so an ordinary STRUCT can never masquerade as the existing owned return carrier.

The certified ordinary storage view alone maps original global indices to compact
managed ordinals and back. RESOURCE, unknown and unproved pending rows never enter
that map. Every pack/get checks the original nominal, field position and all
receiver alternatives. Descriptive pending classes remain pending; successful
closed proof supplies separate per-field authority. I do not reuse an all-record
count bound to address a filtered table. Nms record binding validates storage
shape but does not itself prove field tags or nominal relationships.

I require format1/format2 conversion, serialization/deserialization and text
roundtrip to preserve the existing descriptor bytes and identities without a new
wire ID, version or flag. nvm_v2_convert currently uses old ownership validation;
any new mixed transport route must explicitly validate the same complete retained
facts and remain distinct from executable admission. No malformed or unknown row
becomes valid because another row is ordinary. Shared old authority queries retain
their decisions. I send any necessary converter factoring for separate source
review before qualification.

## My VM roots and live checks

I reuse VM arrays/records and their existing heap lifetime machinery. Exact value
category determines copying versus consuming: ordinary LOAD/DUP/projection keeps
an alias rooted; owners move once and observations retain their original owner
identity. Replacement protects the new value before retiring the old root. POP
and frame cleanup retire ordinary roots with the VM's existing conventions.
Successful owner exits still require explicit consumption; error cleanup is not
implicit source disposal.

I audit stack/local roots, scratch operands, prepared arguments, partial records,
array growth, pending results and every frame transition. Allocating operations
keep all input objects visible to the collector. Failure before activation retains
caller authority; failure after transfer releases actual callee owners and all
ordinary roots exactly once. Reentry must not retain stale references or roots.
Active reference generations remain monotonic and exact even though this mixed
profile admits no reference opcodes. Existing borrowed/caller-reference paths and
STRING roots retain their independent controls.

I may extend the existing private C-stack invocation proof only after a fresh
complete admission. It is tied to the same immutable standalone root module and
supported profile, never stored in VmState as persistent trust. Separate public
calls verify afresh; target/module changes, links, callbacks, tracing, external
transitions and unsupported resumptions invalidate reuse. All live argument,
frame, nominal, tag, owner and generation preflights remain mandatory. True-ASSERT
internal continuation may reuse only the already established invocation proof;
false ASSERT follows complete terminal cleanup. I preserve the existing module
lifetime/decode-cache contract rather than promise arbitrary bytecode mutation.

## My scalar checks precede storage access

ARR_GET keeps raw FLOAT-or-VOID semantics for negative and missing indices,
including empty arrays. I neither invent zero nor add an unproved bounds fact.
At every recorded typed-F64 operand, VM and native handlers test exact FLOAT before
reading its floating payload. Both operands are checked where required, with the
existing runtime type-error behavior and complete cleanup of live owners/ordinary
roots. Exact static FLOAT still keeps ordinary runtime safeguards where required.

Generic EQ/NE implement the recorded value semantics for actual FLOAT/VOID
alternatives, signed zero and NaNs. Ordering remains the composition's exact
matching non-VOID scalar subset; possible-VOID ordering, mixed numeric tags and
heap/owner comparisons stay refused. Typed versus generic NaN predicates retain
VM behavior. Existing binary64 arithmetic providers and error policies are reused;
this contract does not change integer arithmetic or infer tags from native storage.

## My native carrier has explicit lifetime rules

I extend the owned backend with an explicit ordinary-managed category alongside
scalar, owned record and the separately qualified retained STRING carrier. A
pointer or shared STRUCT tag alone never determines owner authority. I reuse
qualified NmsRuntime arrays/ordinary records, with one context shared by all helper
frames of one public invocation. I keep affine owner records and reference
provenance separate; I introduce no second ad hoc array allocator.

I use the managed runtime's VM-array policy where needed to match empty allocation,
growth, writes and reads. The proof's exact FLOAT write restriction stays narrower
than any general runtime integer-to-float coercion. Missing reads map to VOID,
not zero or a new error. I bind only certified ordinary descriptors before values
are created, preserving original global identities and checked compact ordinals.

| Value/operation | My native root rule |
| --- | --- |
| Scalar | I copy exact tag and bits without handle ownership. |
| Owner | I transfer one owner and clear the source; I never retain it as an ordinary alias. |
| Ordinary LOAD/DUP/GET | I retain the managed handle before publishing another root. |
| STORE/field construction | I protect every incoming root before releasing replaced state; partial construction rolls back every acquired child. |
| POP/temporary discard | I release ordinary handles and qualified STRING cells according to category. |
| CALL/result | I preserve prepared roots through complete preflight, clear transferred carriers before overlapping scalar output and retain pending results until safe publication. |
| Exit/error | I release stack, locals, scratch a/c values, partial constructors, prepared arguments and pending values in every active frame before disposing the invocation runtime. |

The finite record-to-flat-array graph is noncyclic under this scope. Reference
counting is not a general cycle-collector claim. Allocation/retain/bind failure
leaves prior outputs intact, releases partial state and never publishes a stale
handle. Native selection consumes only the fresh complete supported admission;
failed mixed selection cannot silently invoke the ordinary backend. Existing
STRING cell copies, nested owner cleanup and borrowed reference paths remain
separately qualified and unchanged in authority.

## My review and qualification order

1. I review explicit candidate/conjunction, checked descriptor/signature queries
   and transport changes first, including unchanged old public decisions. I review
   VM and native implementations before executing any newly admitted module.
2. I freeze corrected source/tools and qualify complete valid Samples/PREFIX-shaped
   bytecode graphs with all shadows, aliases, repeated sites, lower-index callees,
   zero-iteration control flow and exact descriptor maps. Private reports retain
   their non-admitting fields. Public mixed acceptance begins only at the reviewed
   complete implementation checkpoint; a pending/refused module is never executed.
3. I compare VM/native results and errors, exact NaN/zero/VOID semantics and both
   typed-F64 operands. I exercise all four public VM APIs and direct-core admission,
   repeated invocation, false entry/helper assertions, helper failure with prepared
   values and aliases surviving replacement. I preserve exact owner joins and
   observation refusals, wrong nominal/mode/count/tag cases and prior outputs.
4. I sweep actual heap/native allocation boundaries through successful terminal
   outcomes, explicitly separating collector object faults, analysis allocation,
   frame reserve, managed binding/retain and generated native allocation. I do not
   infer uncovered allocation paths from one injector. Cleanup counters and
   ASan/UBSan/available LSan qualify roots; initial failures remain evidence.
5. I run existing STRING-field, owned-value graph, borrowed/caller-reference,
   ordinary managed, verifier, descriptor, shape and private composition controls.
   Closed-profile unsupported/linked/LLVM/Wasm refusals and conversion atomicity
   remain explicit. I seal source/tool/object hashes and measured outcomes.
6. Only after this prerequisite is qualified and merged do I separately contract
   paired source lowering using C-seed, both selfhost stages and NanoVirt for the
   unchanged test/PREFIX, exact metadata/name intervals and mandatory shadows.
   False close or main shadows must prevent publication. This runtime child cannot
   claim that future source acceptance or close full ownership/managed/release.

I preserve all earlier sealed product artifacts and failures without replay. This
contract is reviewable work only; it records no new runtime acceptance.
