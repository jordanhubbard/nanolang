# My carry and borrow reconstruction contract

I record task_cbdce24cc6b747f6b39ceb4c87f20676 before implementation under parent task_4bd034f6029b7458201db74e2c3aeb32. I admit I64_ADD_CARRY and I64_SUB_BORROW with exactly three INT operands. I normalize the input carry/borrow to its low bit, including negative carriers, and push the wrapped low result followed by an INT high result of zero or one, matching my VM.

I represent the two results as exact scalar expressions with distinct per-instruction low/high temporary names. Existing operand snapshots retain evaluation order and once-only direct calls. Pure loop-condition expressions remain side-effect-free. I do not introduce aggregate storage, tuple return types or a multiresult function ABI.

My C helpers use uint64_t arithmetic and representable signed bit conversion. NanoLang helpers use my tested total add/subtract and unsigned comparison helpers, with explicit zero/one high results. I retain previous output on exact-tag/arity/unsupported-profile refusal. I require both result orderings/consumption, endpoint and noncanonical carry inputs, calls/snapshots/loops, canonical byte roundtrip and paired VM/reconstructed C/three pinned NanoLang compiler execution with GCC/Clang sanitizers. Source and tool hashes remain separate; full reconstruction and wide multiplication remain open.

My first focused log `/tmp/nanolang-reconstruct-carry-focused.log` retains four failed subcases: two endpoint modules exceed the existing 4,096-instruction admission cap, and two wrong-tag controls incorrectly expect the assembler to reject BOOL carry. I split the positive matrix into bounded modules without weakening that cap. The missing verifier tag rule is recorded as task_6729384a65fa4c8688db86b6bfc91442; wrong-tag modules remain unexecuted and must be refused independently by reconstruction with prior outputs intact. The ordinary loop/call/snapshot case already passes.

## My retained incomplete acceptance

At production `bcaa677a`, the first three-method run finishes in 9.377s with four failed subcases: the two oversized matrices and two incorrect assembler tag-refusal expectations described above. The corrected bounded matrix run finishes in 34.794s with six failures, each a non-Cseed compiler subprocess exit `-11` without a diagnostic (`/tmp/nanolang-reconstruct-carry-corrected.log`). The traceback passes through the non-Cseed branch of `IntegerReconstruction.checked`; it does not establish Stage1 versus Stage2. Earlier VM/reconstructed-C/Cseed steps completed before that compiler invocation. The loop/call/snapshot and independent reconstruction refusal methods pass.

I record task_1009525724234d9ca7df3f8284e75943 before any further acceptance work. I do not replay, minimize or diagnose by executing the failed compiler inputs. I statically regenerate only the six assembly texts and their hashes into `/tmp/nanolang-carry-static-inputs/manifest.json`; this preservation invokes no assembler, compiler or generated program. My source compiler tools remain the reused hashes recorded by `/tmp/nanolang-reconstruct-carry-compilers.sha256`; runtime/assembler/generator identities are in `/tmp/nanolang-reconstruct-carry-tools.sha256`. I make no current-main failure or causal claim from this result. This carry/borrow implementation remains draft and its task remains open until qualified acceptance passes.
