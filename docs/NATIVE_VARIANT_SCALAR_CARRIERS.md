# My native scalar variant carrier contract

I record task_6697993e325847d9a27c4af9e6507e65 before code. Source parent6550 now emits unchanged Choice<int,string> fixtures that verify and run in VM, but native classification conflates variant field0 as one exact scalar kind. A single Right(string) call also classifies the unused Left integer-display branch as string, and my CAST_STRING plain-string emission has no identity arm. I retain the checked refusal logs supplied by the source owner; no refused native output is executed.

I introduce an explicit scalar-set present-payload shape only for scalar AGG_VARIANT constructor storage with established int/bool/float/string producer facts. Exact producer nodes remain exact; conversion into the storage does not unify their kinds. This is distinct from unconstrained VALUE and NUMERIC. I do not infer scalar provenance from an arbitrary boxed value. Struct/tuple/heap/resource storage keeps its existing constraints, and declaration wire IDs are unchanged. Variant runtime tags and widths remain intact; scalar consumers keep exact runtime extraction checks. Strings retain existing root/alias lifetimes. Missing variant padding remains unconstrained, not an invented scalar value.

I require unchanged generic fixtures plus ordinary same-module VM/native tests for both variant orders, same-variant generic int/string values, bool/float cases, return/call/local transport, padding and previous-output refusal. I run shape solver and native gates, GCC/Clang ASan/UBSan/LSan ownership checks, and preserve initial failures. Plain string CAST_STRING must be identity with its existing ownership, matching VM semantics. Full generic/nominal/heap support is not implied.

I explicitly leave mixed scalar/array generic instantiations reaching common storage to task_7eb14a92a31d4c6982292f93165f65e5. Source6550 admits array<int> payloads, but this scalar-only carrier does not establish all combinations of those generic instances. My ordinary scalar/array variant test retains checked translation refusal and previous output; I do not widen heap ownership or element constraints to bypass it.

I retain the first focused log `/tmp/nanolang-variant-carrier-first.log`: all new positive/refusal and adjacent ownership controls passed, but the old union-padding test expected the newly supported mixed scalar variants to fail. I replace that obsolete negative input with a still-unsupported heap/scalar conflict; both scalar variants remain covered positively. An initial strict Clang invocation failed the installed GCC-directory warning before generated code compilation (`/tmp/nanolang-variant-carrier-clang.log`); I use the existing pinned GCC13 Clang wrapper for corrected qualification, without weakening warnings.

The initial carrier checkpoint1e640b59 retained two integration gaps. The unchanged generic source gate passed its25 affine methods but two inherited expression-match methods refused projected boxed/concrete joins (`/tmp/nanolang-generic-union-native-companion.log`). The full native suite passed2421 checks and retained one required known-string-as-int return refusal failure (`/tmp/nanolang-variant-carrier-full-native.log`). I retain classifier-only per-field member masks, merge them only with other variant masks, carry provenance through projections/locals/calls/joins, and reject statically impossible declared scalar returns after convergence. These field codes never enter runtime records. I preserve runtime exact tag extraction.

I also validate UNKNOWN-to-variant-scalar conversion only after graph convergence; late exact scalar resolution remains accepted. Missing record edges are not traversed, so empty variant padding remains unconstrained. Unproved boxed U8/heap payloads cannot acquire scalar-set evidence merely by sharing a destination. This is part of the original provenance contract, not permission to broaden arbitrary VALUE.
