# My shadow compiler parent ownership

My reviewed bf56ca5f8 loader repair passes every ordinary focused phase on Linux and Darwin. Both sanitizer runs pass fresh compiler/VM/AOT providers, actual fork-failure controls, exact artifact contracts, borrowed string controls and prepared loader controls. The first callback constructor case then exits from the compiler parent with LeakSanitizer: 664 bytes in 15 allocations. I preserve both original terminals; this is not a timeout or a loader admission failure.

I own ModuleBuildMetadata returned by module_load_metadata in collect_headers_from_module. Its optional headers vector only determines iteration. It does not determine ownership of the complete metadata object. I must free that object even when headers is NULL, using the existing recursive destructor once after optional iteration. Header strings added to the collector are independently copied by add_module_header.

I allocate a fallback module_name in load_module_internal and lend it through Environment.current_module while recursively importing and typechecking. Existing success/failure comments deliberately leak it because function and annotation contexts may retain its address. I must preserve that lifetime, not free it immediately on context restoration. I transfer this one allocation into the existing Environment checker allocation registry before first publication; its descendants can retain the pointer until Environment teardown. Every failure restores prior context and leaves registered storage to that owner. An explicit module declaration may replace current_module with a separate registered copy; it does not free the fallback. Metadata snapshots copy struct/enum/union owner names independently; exported function snapshots clear module_name rather than retaining this address.

I retain the existing registry allocation contract: a NULL input is not registered, and allocation of a registry entry remains fatal on failure. I do not claim recoverable registry allocation failure. I explicitly refuse failure to allocate the fallback name before changing current_module.

I require the unchanged full callback suite under fresh corrected ordinary and sanitizer compilers on both hosts, including the direct interpreter and nested callback constructor/thread placement controls. The earlier focused successes remain separately attributed. I do not disable leak detection or weaken compiler exit assertions.

My original negative callback fixture checked the expected compiler refusal but did not expose unrelated sanitizer suffixes in captured stderr. I add unconditional sanitizer diagnostic exclusion for every compiler outcome and write both captured streams to the owning test log before assertions. The original positive/negative cases and exit/report predicates remain unchanged. I preserve running2039 results separately and require the corrected observable negative case before claiming this sanitizer closure.

I qualified this bounded ownership correction in fresh2039 ordinary/sanitizer gates on both hosts, then completed all six callback cases with observable sanitizer diagnostics in the fixture-only bafadf continuation. [My sealed evidence](evidence/shadow-loader-qualified/README.md) preserves prior failures, exact provider reuse, generated artifacts and symlink identities. Task task_5d66fb2d7ec34cdba6f5df7a09368f7f is complete for these two parent allocations; compiler shadow integration and full bootstrap remain open.

My source/test work above is complete, but the MAC task remains open. The hub rejected open-to-completed, then rejected claiming it with the existing agent_cli_aa517b99ddeec98e identity as agent_status_unavailable. I do not reconfigure that agent or its connection. The parent release owner must reconcile the task's hub lifecycle; this does not change the retained test results.
