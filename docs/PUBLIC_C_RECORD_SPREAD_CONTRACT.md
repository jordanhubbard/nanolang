# I refuse record spread before dropping its base

I execute task_9eb3251d63eb48668b67755d9fea851b under original6ade's portable-lowering-or-checked-refusal acceptance. This contract precedes production and follows the explicit accepted-expression inventory, not a universal-language expansion. My parser retains `{..base, overrides}` as AST_STRUCT_LITERAL.spread_source. The checker permits fewer explicit fields for spread, and my interpreter copies base fields before overrides. Public C plain record literal emission currently writes only explicit field initializers; unlike its dotted union-result path, it never handles or refuses the spread base. No failed artifact was executed to establish this static mismatch.

I choose a direct checked refusal whenever a reached record/struct literal has spread_source. I apply it at ordinary value emission and lifted value analysis before constructing a successful destination or spelling a literal that discards the base. This covers initializers, returns, arguments, nested fields and discarded values where the current profile otherwise reaches them. It also retains the existing dotted-union spread refusal boundary. I do not evaluate source effects during compilation, introduce copying or retention rules, fabricate omitted fields, or add a record ownership ABI. Unsupported surrounding contexts can retain their earlier checked refusal; no refused program is published.

I preserve exact explicit-field record and union construction, prior complete declaration ordering, scalar field typing, ordinary record calls/results and stable scalar string ownership. Missing/unknown nominal identities, arrays/callables and other unsupported types retain their established refusals. The header and final context inventory state that record spread is outside this profile. Shared parser, checker and other producers remain unchanged; this target restriction is not a source-language prohibition.

I send production for independent review before fresh gates. Ordinary source controls use an annotated record with a base and an override, require the public C spread diagnostic and preserve previous output, and retain explicit-field equivalents as strict C99/C11 positives with actual inherited-equivalent field observations. API controls cover a plain literal, nested/discarded uses and lifted base/override shapes, require refusal before successful output at both path and FILE entrypoints, and recover on later valid invocation. No old faulty emitted artifact or rejected output executes. Existing63 public C methods plus seven programs retain their affected regression scope; focused and adjacent compiler/tool pins remain exact.

I freeze source/harness/tools, preserve first terminal outcomes and distinguish frontend/backend refusal phases. Canonical merge precedes child closure. After this explicit context boundary is qualified I map original6ade's actual options/expression clauses to evidence; I do not turn unrelated broader semantics into invented acceptance requirements. Shared policy, platform and full-release obligations retain their own scope.
