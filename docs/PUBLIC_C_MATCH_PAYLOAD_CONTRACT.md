# My standard C union payload contract

I execute task_f5782cbcbe2d45fc8ac7f04b4f05b4e4.

I execute a bounded required child of public C parent task_6ade6d62ef644390bb9645b15077c8df from canonical main83524d33 after PR758. I replace GNU __typeof__ in exact declared union statement-match payload bindings with private named payload struct types shared by union members and binding copies. Type names resolve from exact union declaration and selected variant identity, never a guessed field shape; the existing private prefix protects generated types and match temporary names. I preserve tags, field order, existing field types, value-copy behavior, pointer borrowing and source binding scope. I qualify existing nongeneric scalar payloads (INT/BOOL/FLOAT/STRING), multi-field and single-field variants, empty variants, nested matches and both variant orders, with no generic/heap/foreign ABI expansion.
I emit no payload copy for underscore discard or empty variants. All-empty unions require a standard C nonempty storage union; I use an unused private byte member only when no variant carries payload, preserving tag-based semantics. I replace the fixed _match_val generated identifier with the selected private namespace so an ordinary same-named source input cannot capture itself in the generated initializer. Scrutinee evaluation remains once; nested scopes retain their own value. Missing declaration/variant identity remains a checked emission error with output preservation; no typeof fallback.
I do not claim guards/wildcards repaired: task_7a99c348dd0f4d619955092f1c51d070 records guard-before-binding and wildcard tag defects as required followup. I preserve existing guard ordering in this representation-only child and do not blanket-refuse valid constant guards; acceptance is explicitly unguarded exact-variant matches. Full parent6ade stays open; no release narrowing. GNU expression blocks need statement-aware lifting that preserves final-value/return/break/continue semantics, not a comma-only rewrite; advertised options/capture obligations remain.
I freeze source/harness/tools before fresh GCC/Clang strict C99/C11 O0/O2 sanitized public-C controls, ordinary interpreter/verified VM comparisons where admitted, exact scalar field observers, nested shadow bindings, generated-name collisions, empty/discard cases and once counts. API controls verify exact nominal lookup refusal, first diagnostic, path/stream preservation and same-process recovery. I retain the unchanged7 backend programs and adjacent28 publicC methods/provider controls. Contract and roadmap precede production; independent production review precedes gates; historical artifacts stay untouched.

## My static source boundaries

My source checker/evaluator in typechecker.c AST_BLOCK and eval.c AST_BLOCK treat only a final value expression as the block value and propagate enclosing control flow. Public c_backend.c AST_BLOCK still emits a GNU statement expression; I leave its required statement-lowering work open. I do not infer a safe comma-only transformation from a single-expression example.

My public MATCH currently declares anonymous payload structs, copies them with __typeof__, emits guards before payload declarations and uses wildcard spelling as a concrete tag. The source checker independently distinguishes underscore discard/wildcard. The named representation child corrects the representation and discard construction boundaries needed for standard C. The separate guard/wildcard task retains the remaining control-flow obligation; successful unguarded examples do not close it.

I retain existing field C representations rather than admitting new generic/aggregate/foreign shapes. Private typedef identifiers must encode exact declaration/variant indexes without name-concatenation ambiguity; the same resolver supplies both member and binding type. Empty storage is private and unobservable to source tag matching; I do not make an external layout ABI promise.

## My exact complete-match termination prerequisite

I record task_b0351cb144594a3ca8a66a415e903242 before production.

I execute a prerequisite of named payload task_f5782cbcbe2d45fc8ac7f04b4f05b4e4 under public6ade. Frozen source641165d9 passes ordinary scalar/nested/hygiene, empty/discard and API methods on GCC/Clang, but unchanged05_match/06_single_field_variant both stop at strict C compilation: existing exact-match if/else-if chains leave a syntactic fallthrough and -Werror=return-type rejects them. I retain first logs and27verified identities; no rejected binary ran. I require a precise complete-coverage test over one exact declared union: every declared variant exactly once, no guard, matching arm count and identities. Only then I append a final else with a first-person diagnostic and exit(EXIT_FAILURE), expressing no continuation for an invalid runtime tag while preserving all valid arm behavior. I do not make the last variant unconditional, add a guessed return value, suppress warnings or claim general guarded/wildcard exhaustiveness. Partial/guarded/duplicate/unresolved identity matches do not gain this terminal assumption. Static API emission controls test those boundaries without executing invalid tags; unchanged normal complete-match fixtures qualify both C99/C11 O0/O2 sanitized routes. Contract precedes production, checkpoint precedes fresh gates;7a99/full6ade remain open.
