# I isolate public C boolean text conversion

I execute task_f5265b429b4044e7bcc83201f2ccbbd3 after FORe0cd, under original6ade's remaining context/helper inventory. This contract precedes production. Static review finds fixed generated `nano_bool_to_string` outside my selected private namespace, and the builtin bool_to_string emission branch ignores the declaration/binding predicate already used by other scalar conversions. An ordinary same-named source function can collide with the helper; an exact API declaration can select the wrong call path. No old generated artifact or failing fixture was executed.

I move this helper into the existing selected private prefix through emit_private_source, using a nano_rt_ source identifier and the corresponding prefixed call spelling. I preserve its body: one BOOL value selects the stable literal `true` or `false`. No allocation, mutable static buffer, new ownership rule or lifetime change is introduced. Both literal pointers retain static storage; no new cleanup is needed. Existing whole-declaration/reference namespace planning continues to avoid user helper-like identifiers and private-prefix candidates.

I recognize the builtin only after actual lexical bindings, expression callees, checked direct signatures and exact declared functions have received their existing handling. A direct scalar declaration/signature with the same spelling is descriptive metadata, not a builtin or a first-class callable. Shared source builtin reservations remain unchanged; such declarations are API controls, not a source override claim. Local-bound and expression callees retain the callable-profile refusal before fallback. Builtin inference returns STRING only for exactly one argument whose retained type is BOOL; emission requires the same facts and otherwise reports a checked diagnostic before publication. I evaluate the argument once using the existing expression/lifting rules; no eager duplication or evaluation-order change is added.

I send production for review before freezing fresh controls. Ordinary source positives use the builtin on literal and stateful BOOL producers, retain aliases across subsequent conversions, and declare ordinary functions named nano_bool_to_string and private-helper candidates without collision. Exact stdout/content results qualify under strict C99/C11 O0/O2 GCC/Clang ASan/UBSan. API controls retain same-spelled direct and qualified declarations, checked scalar signatures, local/expression refusal, wrong arity/type/unknown facts, prior-output preservation for both entrypoints and later valid same-process recovery. Source wrong-type cases retain their actual shared-checker refusal phase and never execute rejected output.

I retain the existing60 adjacent public C methods and seven ordinary programs unless reviewed integration identity justifies a narrower rerun. I freeze source/harness/tools, preserve first failures and make no new bootstrap, Darwin, full-language or global unused-helper-warning claim. Original6ade still requires its final actual clause-to-evidence inventory; this child does not change FOR refusal, array/callable ABIs, expression lifting or shared language policy.
