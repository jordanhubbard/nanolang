# My passive-flow and emitter-shadow attribution

I preserve `/tmp/nanolang-match-call-vm-c140-linux/full-test.log` and its status: make returned 2 after 870.355 seconds. All 90 flat-record methods ran; 88 passed. The two terminals are invalid passive eligibility metadata before shadow execution for `tests/nanoisa/fixtures/passive_flow.nano`, and my ten-second shadow deadline while compiling `src_nano/nanoisa_emit.nano`. Existing TemporaryDirectory cleanup removed temporary failed products.

I build fresh canonical af8809 and repaired source in isolated trees, with retained source, compiler, provider, command, status and output hashes. I compare only the two implicated commands, without rerunning the full suite or historical failed executables. My first bounded attribution preserves the ten-second shadow policy and a separate outer process-group bound.

Static inspection finds short-circuit AND/OR lower to conditional jumps inside passive node intervals, while the current node validator excludes those jumps. This is a source-level inconsistency, not yet a measured baseline diagnosis. Any repair needs explicit internal-edge/stack/local/dependency proof and independent review; adding jumps to a whitelist alone is insufficient. My timeout remains unexplained until evidence distinguishes finite expensive shadows from a loop.

I keep both tasks open and outside PR910. Passing a bounded match repair does not establish full-suite or release acceptance.

## My fresh baseline observation

I rebuilt nano_virt using GCC13 on Linux from canonical af8809 (docs-only ledger77f9) and independently from8538. Both builds passed, respectively19.888 and20.192seconds. Both unchanged passive commands returned1 in0.033seconds with identical invalid passive eligibility diagnostics. Both unchanged emitter commands returned1 after21.435 and21.579seconds including module preparation, with the ten-second shadow diagnostic. Neither published its output module.

I measured unchanged tracked participating source and selected executable tool maps for each comparison. Passive execution changed no inventoried providers. Emitter preparation added405 module artifacts per tree and changed none of the preexisting providers; I retain before/after maps and content-addressed bytes without claiming an immutable output set. My runner uses a separate120-second process-group bound; neither command reached it. This comparison establishes both failures on the canonical baseline independently of my generic-call repair. It does not establish the timeout's cause.

PR910's c140 arm64 CI run35500487170/job106051271254 independently ran90 methods with the same two failures (162.886seconds). I retain its complete compressed log/API result and hashes. The original userguide generic-call failure and VM underflow fixture failure are absent from this later terminal; I do not call CI or full make test green.

## My separate Darwin native timeouts

My c140 supported-selector fullmake returned2 after1114.818seconds. Its90-method corpus ran689.274seconds with the same two failures plus four TimeoutExpired subtest errors in two other methods. Existing projected-record-array native store-first/main-first and tuple seed/source native executables each exceeded120seconds. Literal cc is a separate compiler path from my corrected reference-transport selector. Native production is unchanged by PR910; that alone does not identify a timeout cause. Existing cleanup deleted the failed temporary executables; I do not claim they were retained. Task `task_2f52721aac374ac592b61438315dc981` remains open for separate diagnosis.
