# My passive-flow and emitter-shadow attribution

I preserve `/tmp/nanolang-match-call-vm-c140-linux/full-test.log` and its status: make returned 2 after 870.355 seconds. All 90 flat-record methods ran; 88 passed. The two terminals are invalid passive eligibility metadata before shadow execution for `tests/nanoisa/fixtures/passive_flow.nano`, and my ten-second shadow deadline while compiling `src_nano/nanoisa_emit.nano`. Existing TemporaryDirectory cleanup removed temporary failed products.

I build fresh canonical af8809 and repaired source in isolated trees, with retained source, compiler, provider, command, status and output hashes. I compare only the two implicated commands, without rerunning the full suite or historical failed executables. My first bounded attribution preserves the ten-second shadow policy and a separate outer process-group bound.

Static inspection finds short-circuit AND/OR lower to conditional jumps inside passive node intervals, while the current node validator excludes those jumps. This is a source-level inconsistency, not yet a measured baseline diagnosis. Any repair needs explicit internal-edge/stack/local/dependency proof and independent review; adding jumps to a whitelist alone is insufficient. My timeout remains unexplained until evidence distinguishes finite expensive shadows from a loop.

I keep both tasks open and outside PR910. Passing a bounded match repair does not establish full-suite or release acceptance.
