# I preserve foreign record declaration provenance

I execute existing task_f366494d5921451eaf57128629eb9e4c under ordinary authority15f. My selfhost parser currently consumes extern before storing a record as a plain ASTStruct. My nominal binder reconstructs foreign identity from a preceding token and source coordinates; that fact is lost when the declaration is copied without its token stream. My ordinary producer conservatively excludes every module containing an extern token, which prevents positive authority but does not repair the AST.

I add is_extern:bool to ASTStruct in schema/compiler_schema.json and regenerate the matching C and Nano declarations with the existing generator. I retain declaration order, source coordinates and all existing fields. Parser storage/field parsing carries an explicit flag: only the existing extern-struct syntax sets true, while ordinary and resource declarations set false. I do not admit a new extern-resource spelling or change foreign ABI layout selection.

Every active explicit ASTStruct copy in the nominal merge/rewrite path retains the flag. Nominal foreign-collision checks consume the declaration fact rather than guessing from token adjacency; existing imported owner/name identities and refusal remain. Optional ordinary record authority/layout publication explicitly omits its tables for foreign definitions (UNKNOWN, not whole-source refusal) even when retained tokens are absent or different. The current conservative token/import exclusions stay in place; this prerequisite creates no new ordinary authority, resource eligibility or executable target admission.

I update meaningful shadows for changed parser/binder functions and test direct parser flags for local/resource/foreign records, comments/newlines after extern, retained coordinates, copied/renamed module definitions and same-name foreign collision refusal. Fresh isolated C-seed/Stage1/Stage2 bootstrap with newly built schema runtime libraries is mandatory because the compiler AST ABI changes. I qualify actual parser/nominal shadows and paired ordinary publication controls, including existing foreign-schema absence and unchanged ordinary positive layouts. Schema regeneration must be deterministic. I preserve old qualified compiler caches/libraries and historical failure artifacts; no old binary may load a new AST runtime library. Full ordinary authority, managed records and release remain open.

I update the manual ASTStruct FieldMetadata table in typecheck.nano with the same BOOL field. The auxiliary standalone nanoc_integrated.nano is an older separate prototype, with its own reduced ASTStruct and no active bootstrap/driver import; this checkpoint does not claim to modernize or qualify that prototype. Active schema-backed parser, nominal rewrite and producers are the ABI boundary.

My first frozen5b4085f7 build stopped before compiler execution while the managed runtime package selected default Clang23/GCC14 and treated its GCC include-directory selection warning as an error. Source hashes stayed unchanged. I retain that log and explicitly select the existing matched clang-18/opt-18 runtime IR tool pair for the corrected build; I do not disable warnings or change production source.

Corrected runtime-tool build0096a692 reached bootstrap shadows and stopped at the new nb_rewrite renamed-name assertion. The fixture had disabled module binding state immediately before mb_add, so the existing API deliberately ignored the mapping; its is_extern assertion passed. I preserve the log and require an active owner in that fixture before repeating fresh bootstrap, with the same flag/name assertions.

## My direct-fixture import correction

My1ffa8bc8 fresh bootstrap passes both stages in233.355s. My next direct fixture fails in43.274s before publication: it calls lexer/parser/module-binding symbols through a transitive nominal-binding import. I preserve the failed log and unchanged source/tool snapshot in `/tmp/nanolang-extern-record-qualified`. I will add direct imports from their defining modules, without changing production44a00d90 or any assertion. I then qualify the corrected fixture and existing paired ordinary producers. MAC `task_abe5111511c548648b38fd336d16a357` tracks this fixture correction.
