# My next admission timing observation

I retain the focused ordinary-admission acceptance and both unchanged emitter deadline failures sealed atcd85e0f2038f9c01400d9e0ab3cc616a03d5dafb. I keep task_4931a66f39c04b1a96981a4e00d43646 open. This separate diagnostic tree starts from qualified8d731; it does not change those frozen sources or relabel their results.

I reapply my reviewed86c758 macro-only monotonic admission/shadow observer to the corrected ordinary certificate implementation. The interval starts at private core entry, before matching or classification, and ends after classification, owned frame/reference checks and ordinary certificate publication, immediately before dispatch setup. It records only completed successful intervals, not early refusals, interrupted work or public admission outside the core. The same bounded8192 progress markers retain function indices/names/offsets and original shadow entry/return placement; the exact parse_owned_pattern function remains included. Counter/clock/output failure exits92, with no unbounded trace.

I add separate full-classification and reuse interval counts/nanoseconds while retaining the total and ASSERT trap count. The already-computed private `reuse` boolean selects the observation bucket; it grants no permission or new lifetime. This distinguishes fewer complete admissions from faster repeated entries. All additions remain behind NANO_SHADOW_PROGRESS_DIAGNOSTIC. With the macro disabled I change no production execution or authority. With it enabled I add clock/stdio overhead and make no uninstrumented performance qualification claim.

I compare deltas between identical marker boundaries because total admission includes pre-marker work while elapsed origin is the first marker. I disclose any unobserved final interval and use the actual final marker without presuming which shadow will be active. Each aggregate uses checked64-bit arithmetic. Full plus reuse totals must agree with the independently retained total, subject only to printed rounding.

After source review I build one fresh diagnostic nano_virt with the same GCC13 ordinary O3 flags plus the private macro. I execute the original full imported nanoisa_emit.nano command once under its unchanged ten-second shadow policy and120-second outer process-group bound. I retain source/tool/provider maps, raw commands/status, all markers and products. I do not enable root-only shadows, reduce stress inputs, change authority invalidation, reuse failed executables, or infer safety of a broader cache. Subsequent changes require a separately reviewed diagnosis/design.
