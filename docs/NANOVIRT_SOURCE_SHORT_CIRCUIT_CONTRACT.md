# I preserve source boolean short-circuit evaluation

I execute `task_0b335beafefb4f25a6a28221fb0f18cf` as a required producer prerequisite of public C lifting child33f74, from canonical `1e9d9519`. This contract precedes code. SPECIFICATION8.5 explicitly requires `(and false rhs)` and `(or true rhs)` not to call `rhs`. My interpreter's TOKEN_AND/TOKEN_OR branches implement that selection; CANONICAL_STYLE's resource/call discussion does not replace the explicit rule with eager source evaluation.

The first frozen lifting `e923c05d` focused gates pass four of five methods under GCC and Clang. All public C and interpreter observations pass; the ORDER fixture's verified VM assertion fails. I retain `/tmp/nanolang-public-c-lifting-{gcc,clang}.log`, the source artifacts and all 37 unchanged source/tool/compiler identities. Static NanoVirt binary-prefix emission compiles both operands before BOOL_AND/BOOL_OR. I do not replay or minimize the failed module or infer a crash cause.

I special-case source TOKEN_AND/TOKEN_OR with exactly two BOOL operands before the ordinary eager binary path. I compile the left expression once, retain its BOOL result, and branch around the right expression when it determines the answer. On the continuing right path I remove the saved left BOOL and compile the right once. Each continuing join carries exactly one BOOL. The RHS is still checked for valid source type, independent of whether it executes. I preserve source-order effects and enclosing control exits, with no runtime allocation or new owner manipulation.

My implementation uses existing DUP, conditional jump and POP operations, preserving the assembler/verifier/VM contracts and existing jump patching. I change neither eager ISA BOOL_AND/BOOL_OR nor generic AND/OR operations, truthiness, arithmetic, call ABI, resource admission or source type rules. Nonboolean/incorrect-arity source remains refused before publication. Other binary operators retain their existing code path.

I require independently reviewed production before fresh ordinary execution. Focused controls cover both selected/skipped RHS cases for and/or, nested logical operators, counters establishing left/RHS evaluation exactly once, RHS match values and enclosing return, and ordinary loop conditions. I compare interpreter with newly built NanoVirt and verified VM output, retain unchanged source diagnostic/output controls and run existing codegen checks. The unchanged lifting fixture then runs with an explicitly identified corrected external producer; final own-tool acceptance follows canonical integration. First logs remain separate. No unsupported/malformed artifact executes.

Selfhost canonical emission has a separately recorded analogous mnemonic/operand audit under task_6e16a089a27a43af8703ff1610399a01. Its repair and fresh Stage1/Stage2 qualification are required separately; this child does not claim all-source-producer parity. Full public C lifting, original6ade scope inventory and release acceptance retain their own gates.
