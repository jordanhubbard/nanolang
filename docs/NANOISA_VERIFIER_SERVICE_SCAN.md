# I reuse service classification only within one verifier call

My retained787 sanitizer Forth session exceeds its1200-second worker bound. A separate benign30-second diagnostic stops while creating one session, before arithmetic or PI execution. Four debugger samples show whole-module File opcode scanning inside ordinary verifier candidate selection: per-primitive nvm_verify_function, then per-function checks during nvm_verify_linked in the final kernel rebuild. This establishes a measured hot path, not the complete cost of the failed full suite. My original providers remain unchanged; I preserve both the diagnostic terminal and its earlier external formatting-only compile refusal.

Before implementation I specify invocation-local reuse of the existing private NvmServiceClassification. Public nvm_verify_function and maximum-stack entry points create fresh facts at their original first service-query point. nvm_verify and nvm_verify_linked create fresh facts for the original root module at the same ordering position, then pass them through their synchronous per-function loops. Linked-module classification/refusal order remains unchanged; I do not cache an entire linked graph or add allocation.

Inside those calls I use existing classified service/owner-array/mixed candidate helpers and pass the same original-module facts into structural range checks. I retain every original structural, ownership, import, stack-height, type and bytecode check, including the repeated structural checks; only repeated service scans are reused. I preserve NULL checks, candidate/refusal ordering, exact output/max-stack publication and first errors. I do not skip a full module proof because it succeeded in a prior call.

Existing private preparation and affine wrappers keep their current signatures and NULL-facts behavior through thin wrappers, including charged work reservations and allocation order. They may conservatively charge the same scan budget. No persistent field, trusted bit, rewritten module, public ABI or admission change is introduced. A fact names the exact original module and expires before the public verifier call returns. Forth mutations, subsequent calls, different modules and VM dispatch always receive fresh classification as before. The existing API already requires synchronous read-only inputs during verification.

My acceptance retains the original Forth session/Core/Double/float and NanoISA verifier controls under ordinary and supported sanitizer configurations, plus service/owner/mixed refusal adjacency and source-query budgets. Additive regression controls must mutate ordinary code to File service code between calls, test metadata-only service refusal and restore ordinary acceptance, preserve maximum-stack sentinels on early refusal, and cover linked service rejection. The bounded benign diagnostic is repeated only after source review; the corrected complete original worker then keeps its1200-second bound. I do not raise deadlines, suppress sanitizer instrumentation or remove checks.

This work belongs to task_4605a913b21847f19101e5706851c5f2 and does not by itself resolve the separate120-second source compilation timeout, although that path also invokes verification. I require measured corrected evidence before attributing any benefit there.
