# My total reconstructed integer operations

I record task_1b40ff24267d45c99e0217352765e39d before implementation under reconstruction parent4bd034. I admit only typed I64_ADD, I64_SUB and I64_NEG into the existing exact int/bool structured region grammar. I preserve modulo 2^64 integer results, including signed endpoints and negation of the minimum integer. I do not reinterpret bool, enum or other tags as int.

My C surface uses unsigned arithmetic and a representable signed reconstruction. My NanoLang surface uses named helpers with signed boundary branches whose arithmetic intermediates are all representable. I emit only needed helpers, preserve operand snapshots and evaluation order, and retain existing structured-loop/call and expression-size restrictions. Helper names are separate from indexed reconstructed source identities. Generated helper shadows validate my known implementation; they are not recovered original source shadows.

I require the same retained module to pass VM execution, canonical disassembly/reassembly identity, reconstructed C with GCC/Clang sanitizers and reconstructed NanoLang through C seed, Stage1 and Stage2. I check endpoint crossings, cancellation, helper calls, local snapshots and loops. Exact type and remaining-opcode refusals must preserve an existing output. MUL/DIV/MOD, generic arithmetic, wider values, runtime contracts and general reconstruction remain open; I do not edit the product emitter or nvm2c in this child.

## My measured acceptance

At production checkpoint `07cf1143`, I pass nine combined reconstruction methods with GCC in 87.701 seconds and Clang in 84.790 seconds. The three new methods include 106 endpoint/cancellation/local-snapshot comparisons, arithmetic in a structured pretest loop, exact canonical byte roundtrip, remaining-opcode refusal preserving output, and bool operands refused by the existing assembler verifier. Both output surfaces come from the same retained module after deleting its assembly source. Reconstructed C uses ASan/UBSan; reconstructed NanoLang executes through C seed, Stage1 and Stage2. The C-seed-generated NanoLang C additionally runs with UBSan and no recovery; the final three focused methods pass in 21.562 seconds. Generated helper shadows add direct endpoint assertions without claiming original-shadow recovery.

I reuse the freshly bootstrapped compilers from `/home/jkh/Src/nanolang-functional-array-builtins/bin`, built at source `4a75f984`. These are pinned compiler tools, not a current-main bootstrap: the child base additionally contains C match-arm metadata changes outside this admitted grammar. A combined shell check initially masked the nonzero source comparison; I correct that earlier source-equality statement here. I retain their hashes in `/tmp/nanolang-reconstruct-add-compilers.sha256`. My logs are `/tmp/nanolang-reconstruct-add-gcc.log`, `/tmp/nanolang-reconstruct-add-gcc-ubsan.log` and `/tmp/nanolang-reconstruct-add-clang.log`. This child changes the host reconstruction script, not compiler or runtime production code, so I use those actual stage compilers rather than repeat an unchanged bootstrap.

The first focused log, `/tmp/nanolang-reconstruct-add-focused.log`, records two test expectation failures: the assembler already refused bool operands before a module could reach reconstruction. I corrected the controls to require that earlier exact-tag refusal and retained previous-output checks. No rejected module was executed. MUL/DIV/MOD remained open at that checkpoint; [the multiplication child](NANOISA_RECONSTRUCT_INTEGER_MUL.md) now records its separate implementation. DIV/MOD, generic numeric reconstruction and the full parent remain open.
