# Independent record name storage

I measured44,239 Parser record-clone attempts and3,406,403 field-name copies in the unchanged parse_block_recursive shadow, using the private946 diagnostic retained atb2f3de1fb. This is allocation-volume evidence, not proof of a deadline repair. I continue task_96b40bc1ccd644bca2131878fa6cf372 without relaxing60-second CI or10-second bootstrap policy.

I preserve independent record graphs and writable name bytes. I combine only one cloned record's field-name bytes into one independently allocated buffer. I do not intern names, share storage between records, borrow AST metadata, or alter field values, record names, lookup order, nominal identity, or reference-leaf ownership.

I append an explicitly owned field-name storage pointer to the interpreter StructValue. Zero retains the existing individually owned field-name representation. Nonzero owns the combined buffer; field_names remains a separately allocated pointer array into that buffer. env_discard_value_snapshot releases either representation exactly once and remains the public disposal boundary. Callers constructing borrowed StructValue views initialize the complete structure, as the inspected designated/zero initializers already do. This changes the internal C carrier layout and requires fresh compilation of all consumers; I do not claim binary compatibility with previously compiled C interpreter clients.

My source audit finds one heap StructValue constructor, clone_value_at, and one destruction implementation, env_discard_value_snapshot; create_struct delegates to that constructor. Other production occurrences are borrowed views or record pointers, including list and array storage. GCStruct in runtime/gc_struct.c is a separate representation and remains unchanged. In-tree fixtures construct borrowed views with designated initializers and dispose owned snapshots through the public helper.

Before allocation I validate every field-name pointer and checked total byte size. I allocate the record, record name, name-pointer array, value array, and combined field-name bytes independently, retaining transactional failure cleanup and publishing only a complete graph. Nested records each own their own names. Empty records have no field-name buffer. Names may be mutated in place without changing source or sibling snapshots; replacing/freeing individual interior name pointers is not the disposal API.

I preserve every original lifetime assertion and allocation-prefix/failure recovery control. I add empty, repeated, long and mutable names, nested records and source-destruction independence. Original owning lifetimes and collection controls must pass ordinary and ASan/UBSan/LSan on both hosts before integration. Only then do I compare compiler diagnostic timing using fresh complete providers and rerun the original full component gates. No unchanged failed full compiler invocation is replayed in advance.
