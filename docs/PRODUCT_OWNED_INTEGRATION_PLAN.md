# My fresh owned-source product integration

I continue product parent `task_d76ae44a12fd4d27a2b4aa84c30d7bc6` and affine
example requirement `task_c4351c720aee424ea9b90187e51a08f2`. I create a new
worktree and branch from preserved product fa14d95f, then merge canonical main
2e0d4144, including qualified PR761. My source merge is285d9126. I do not modify
or execute any retained e9 failure artifact, sealed tree, compiler or library.

My integration worktree is `/home/jkh/Src/nanolang-product-owned-integration`,
branch `feat/nanoisa-product-owned-integration`. I resolved four conflicts:

- I preserve both roadmap histories and their open parent acceptance.
- I reset both product module facts and current ordinary declaration metadata.
  Direct metadata calls retain their product identity check; binary64 bit
  intrinsics retain current lexical/global binding guards.
- I keep the NanoISA-only product driver byte-for-byte equal to fa14d95f.
  Its verified module, complete supervised shadows, translator and atomic
  native/C publication route remain; the old AST C route is not restored.
- I retain the current transpiler error-state handling for retained component
  qualification, using the product's renamed `diag_lowering_error` API for
  both existing and newly retained diagnostics.

I have not built or executed this integration yet. After review I will freeze
its exact final head and record source hashes, then run these ordered phases
with separate commands, logs, status and elapsed time:

1. `make -j2 bootstrap` in the new tree, building fresh C-seed, both selfhost
   stages and required VM/native tools. I assert that `bin/nanoc` resolves to
   this tree's Stage2 compiler before subsequent component compilation.
2. `make -j2 nanoisa_emit nano_virt nano_vm nvm2c nvm2c-runtime nanoisa_dump`.
   I retain compiler/tool hashes and selected paths before and after each
   phase, distinguishing intended build changes from frozen execution evidence.
3. `make test-quick` with unchanged eligibility, assertions, shadows and
   existing per-command limits. I retain its first terminal outcome. The
   repaired unchanged affine example must rejoin this installed-product gate;
   focused PR761 acceptance alone does not close originalc435 step5.

All tools, object files and module libraries originate in this fresh tree.
I explicitly bind `NANO_BUILD_CACHE` to its `obj/module_cache` and VM, translator
and AOT-runtime paths to its `bin` files. I reuse no sealed binary or library.
Source hashes and Git identity must remain unchanged; I do not assert that a
Make gate never intentionally rebuilds a tool. I record compiler selection at
phase boundaries and preserve build-command evidence for actual component use.

The parent coordinates the Darwin gate at this exact pin. New fixed points and
full release qualification remain separate; older e9 passing fixed points do
not qualify these new sources. Any new failure remains recorded before repair.

## My documentation-corrected continuation

My first cd72dab3 attempt is sealed in
[evidence](evidence/product-acceptance-cd72dab3.md). It passes all 244 eligible
examples, then stops at the documentation check. I leave that tree, tools and
libraries unchanged. Its example boundary completes c435; full parent d76
remains open.

I create `/home/jkh/Src/nanolang-product-docs-integration` on branch
`feat/nanoisa-product-docs-integration` from evidence commit 03b8c39e. I apply
only PR771's documentation correction as f48c82ba, retaining the recorded
product roadmap history. My compiler, runtime, tests, scripts, schema and
Makefiles are byte-identical to cd72dab3. The unchanged documentation target
passes with all 158 builtins and every section count accurate.

After review, I freeze this new pin and repeat the three phases above from
fresh local build outputs and module cache, including the full unchanged
`make test-quick`. I do not skip earlier targets or substitute combined partial
results for a complete run. I record the first terminal result, compiler
selection and source/artifact manifests independently of cd72. This docs
correction neither incorporates newer compiler changes from canonical main
nor qualifies them. Darwin coordination uses the exact new pin explicitly.
