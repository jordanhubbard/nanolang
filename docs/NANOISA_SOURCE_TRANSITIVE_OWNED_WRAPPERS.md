# I retain exact transitive ownership in scalar record wrappers

I execute task_704c4435f4614a8389502ef6872436bb under product-affine task_e8d860a16da0464891dd32e91c42bef1 and the affine equivalence parent task_28f2fb4b1f3c8a5ce93df628bb569d76. This contract precedes production and fixture execution. I preserve the frozen950f product and its first failure evidence; I do not replay failed compiler artifacts.

## I select one remaining original family

The original product log `/tmp/nanolang-product-quick-950f36d1/test-quick.log` records55 checked-lowering subcase failures across33 methods. Task9460/PR781 subsequently qualifies temporary whole-owner actuals; its unchanged original-pattern gate still retains one NanoVirt refusal for `tests/test_affine_integration.nano`. That file's ordinary `Connection { raw: FileHandle }` is a concrete reachable transitive-resource wrapper. Its main and shadows construct, destructure and consume the nested resource. I preserve that source unchanged as the primary acceptance fixture.

I do not infer all original failures share this cause. Unused foreign declarations/owner-free selected closure belong to fa08/PR788. `Bundle {file:Handle,label:string}` and array<float> fields require managed ownership transport, and ordinary Samples array aliases require ordinary managed records. Owned unions, indirect results, unsafe-block gaps and foreign consuming calls remain distinct. Root's slice792, VM owner selector and managed-record work remain independently owned.

## I retain the existing representation

Static evidence at canonical4f4f4cb7:

- `src/nanovirt/borrow_codegen.inc` source routing examines explicit `StructDef.is_resource` at lexical declarations/results/formals; its declaration collector rejects every AST_STRUCT_DEF without explicit `is_resource`.
- `src_nano/compiler/nanoisa_borrows.nano` has equivalent explicit-flag source detection and a finite-record declaration guard at its layout-emission loop.
- Shared `src/resource_tracking.c`/`.h` already classifies transitive record/union resource contents for checked source obligations. I preserve that checking; emitter admission remains narrower than the complete checker domain.
- Both specialized producers already emit retained INT/BOOL scalar fields, prior owned-record child indices, complete/resource authority03 and exact function ownership descriptors. The validator in `src/nanoisa/ownership_contracts.c` explicitly requires resource authority on a parent with a resource child and restricts such layouts to the finite scalar tree. This supports transitive scalar wrappers without changing wire format or runtime authority.

I admit an ordinary local named record only if its exact finite declared field graph contains an explicit resource record and every field belongs to the existing INT/BOOL/owned-record tree. An explicit resource record retains its existing eligibility, including empty records. An ordinary empty or scalar-only record does not become an owner. Every admitted ordinary wrapper gets complete/resource authority because of its actual resource child, not because all structs are reclassified. I do not mutate parser declaration flags or nominal identities.

I preserve prior-child declaration ordering, exact field names/types, duplicate and unresolved-name refusals, nongeneric/nonextern/import-free limits, field count256, path depth32, existing path budgets, eight-function/frame bound and exact acyclic direct-call graph. I retain the current extern declaration fact and finite-layout checks in both producers. Earlier-name resolution never treats a forward/cyclic or unknown field as an owner. Strings, float fields, arrays, unions, collections, references stored in owners and ordinary managed children remain refused by this child; their broader required tasks stay open.

## I preserve execution and source selection

I update source routing and specialized declaration admission coherently so a wrapper-only result/formal/local cannot escape to ordinary lowering simply because its explicit declaration flag is false. Routing may identify transitive ownership more broadly than executable admission; the specialized profile must still reject every unsupported shape. I use exact retained nominal declarations and bounded graph analysis, preserving lexical/function identities and existing declaration/module modes. Pending788 selected closure must still route unknown wrapper types conservatively to this specialized path.

Existing constructor, move, destructive pattern, direct value-call argument/result, borrowed leaf projection and cleanup rules remain authoritative. Construction prepares each field once in source order, consuming each whole child exactly once. Destruction transfers children before later disposal. Exact wrapper identity survives locals, direct calls/results and supported joins; no cast, copy, implicit drop or partial-owner fallback is added. I do not change the VM/native owned runtime, verifier authority or graph limits merely to pass source fixtures. A newly exposed unmet runtime condition must be separately recorded and reviewed before any widening.

## I qualify fresh corrected sources

After production review I build fresh isolated tools and a fresh selfhost bootstrap. I qualify C-seed NanoVirt, canonical Stage1 and Stage2 on unchanged `tests/test_affine_integration.nano`, preserving all mandatory shadows. I compare complete ownership/layout facts and ordinary canonical text, verify modules, and execute the same admitted modules in VM and strict sanitized native C (GCC and Clang). Metadata stripping/roundtrip controls retain exact executable ownership rather than dropping it.

Focused ordinary controls cover two wrapper levels, INT/BOOL siblings, wrapper parameters/direct factory results, named and temporary whole-wrapper actuals, source-order/once-only scalar observations, and destructive transfer of all children. Negative source controls cover duplicate/moved children, outstanding borrow, wrong nominal wrapper, missing/duplicate fields, forward/cyclic shapes and unsupported managed fields with prior output preservation. Existing checker refusals remain checker refusals; I do not execute rejected modules. I retain existing nested-resource/temporary-owner/result/borrow lifecycle tests and exact runtime bounds as affected regression evidence.

I freeze source/harness/tool pins before each gate, retain first terminal outcomes and distinguish source/checker/lowering/native refusals. Original family acceptance is not full33-method or installed-product acceptance: a new root-owned product pin must establish that separately. No historical failed artifact, old tool rebuild, fixture weakening or eligibility exclusion qualifies this child.

## I correct routing work bounds before qualification

Independent review of ce3d28e8 finds that depth32 bounds stack depth but not repeated work in an owner-free shared DAG. I retain that static finding without executing a stress artifact. Before gates I replace the new recursive routing query with an iterative per-query worklist and visited concrete type spellings. Finite declaration, pending-entry and spelling/expanded-byte budgets conservatively select the specialized refusing route on exhaustion; they do not grant ownership or layout admission. Ordinary empty/scalar-only and ordinary array<int>/array<float> records still classify as nonowners within those limits.

My routing parity audit also finds that full checking does not universally reject source-valid resource-bearing union wrappers. Collection guards reject unsupported resource collections, but cannot justify ignoring all union/generic field spellings. I therefore traverse exact record fields, union payload fields and concrete generic substitutions in the bounded worklist. Collection arguments are traversed for routing only. I preserve phantom generic behavior by expanding actual payload fields instead of assuming every resource-typed generic argument is stored. A visited declaration/spelling prevents repeated DAG expansion; concrete expanding generic cycles terminate at the explicit budget. Missing/ambiguous unsupported facts conservatively refuse where exact classification cannot be established, without classifying every ordinary array record as an owner. The executable collector remains unchanged from the reviewed narrow INT/BOOL/prior-owner tree.
