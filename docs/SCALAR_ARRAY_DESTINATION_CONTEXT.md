# My scalar-array destination context

I track this source obligation as task_b6987cc2a0da313d0675e1932954b3e1, separate from the corrected native byte carrier. My native root3a base and integrated f73 source skip scalar array leaves in the additional nominal contract. I have not executed a known-invalid scalar-array program to demonstrate that static gap.

I will implement this adapter on the integrated candidate's complete contextual facts, not backport its ownership system into the isolated byte backend branch. My expected destination is an owned NominalView retained before recursive checking. Substituting a formal changes its annotation, declaring owner and substitution context together. Existing values use nominal_value_view and exact nominal_view_equal/checked_annotations_equal_context; INT and U8 arrays remain different representations. Missing or inconsistent facts refuse. I do not infer a representation from a later mutation or from an intrinsic-looking name.

Actual array literals are the contextual boundary. An empty literal is a genuine hole; a nonempty literal checks every actual member recursively. At a scalar U8 member destination I accept known INT, U8 or ENUM with the existing computed modulo256 policy and direct literal0..255 policy. Other leaves retain their defined scalar or exact nominal contract. Nested arrays, tuples and callable leaves retain their complete child proof. An existing array nested inside a literal still receives exact array comparison; literal context does not become a container conversion. I will audit all-member checking before ordinary homogeneous inference so valid mixed INT/U8 literal members are not rejected before their destination is known.

I will traverse value-producing block/if/cond/match tails through established lexical bindings. Terminating branches remain control flow rather than empty unknown values. Only branches with a value participate in value compatibility; ordinary statement branches retain their existing rules. Missing facts do not count as an empty literal.

All destination entrypoints must use this contract: local/global initialization, assignment, return, direct and indirect arguments, record and union payload fields, and existing retained callback/tuple contexts. Declaration/field expectations are snapshotted before recursive checking can grow Environment vectors. Literal annotations and emission metadata publish only after the complete checked boundary succeeds; existing array/tuple/nominal cache marks preserve prior rows on failure. I do not claim rollback of already accepted nested child annotations if a later sibling fails.

I will preserve the candidate's exact intrinsic dispatch and owner-aware call results. A local callable, declared function, extern or imported declaration named array_new/map/array_push does not authorize builtin result facts. I will reuse the complete nominal result path rather than the legacy spelling-based inferred-element helper.

My additive controls will pair valid byte literals/computed members/empty holes with existing INT-array refusals at each destination; retain direct literal range and wrong-type refusals; include nested existing arrays, mixed literal members, enum-owner collisions, branch-local bindings, declared callable overrides and incomplete metadata. Allocation-prefix controls must preserve expected snapshots, prior emission rows and fresh recovery. The original17 and native18+9 methods remain intact. Source/fixture review precedes corrected execution and full owning qualification remains required.

## My actual checker entry boundary

My isolated adapter starts at candidate80e8 plus reviewed9bc discriminator correction. I do not edit the shared candidate. No product from this adapter is built or executed before review.

The current `check_concrete_union_arrays` often runs before `check_expression`. Its recursive value-tail scan does not establish block locals or match binders. I must not turn that scan into eager scalar-array comparison. The actual expression checker establishes those bindings; contextual array checking belongs at that entry, with an owned destination frame scoped to the synchronous check. Nested checks save and restore the prior frame. Context propagates only into value-producing tails and actual literal members, not arbitrary call operands or nested declarations. Selected call parameter contexts remain their own destinations.

I will preserve ordinary nominal/generic constructor preparation before its existing consumers. Array destination comparison runs when actual expressions have their lexical facts. Every destination frame owns its complete expected view before recursive calls; it does not borrow a Symbol/StructDef vector entry across a possible reallocation. The frame is temporary and cannot become a new long-lived cache owner.

The current `nominal_value_view` re-infers array literal children, so `[1]` still describes an INT child even after checked U8 destination conversion. I will retain the validated literal's complete view through the existing checker expression-proof ownership mechanism. Emission annotations remain consumers rather than identity authority. Proof publication must occur after all literal members succeed; existing arrays must never gain a new proof merely because an incompatible destination requests one. A repeated check validates the old complete proof against the new destination and cannot silently relabel it.

This extension requires direct-expression failure rollback of newly published checker expression-proof rows as well as the existing array/tuple emission cache marks. Prior proofs and snapshots stay unchanged. New owned objects that are unlinked on failure remain in the existing Environment ownership registry until its normal teardown; I do not invent a second destructor or free borrowed child metadata. Allocation-prefix tests must cover preparation, proof registration, native publication, nested late failure and independent subsequent recovery.

My source audit will explicitly cover the current let/global/set/direct-call/indirect-call/return/record-field/union-payload entry sites, the actual block/cond/match value paths, tuple literals, and inferred callback result facts. Unsupported parser routes do not become tested expression paths by adding a fixture spelling. Ordinary statement IF and terminating branches keep their prior meaning.

My frame key is the exact AST node plus its Environment and independently owned complete destination view. Only an explicit value-child call carries a frame onward. Conditions, indices, callees, ordinary call operands and nested declarations retain normal checking without that frame. Every nested frame saves/restores its predecessor on success or refusal. Retained literal proof equality includes destination kind, declaring owner and substitution context; a second incompatible destination refuses instead of reusing or overwriting the proof.

## My source checkpoint

I add an owned `ArrayDestinationFrame` keyed by the exact expression and Environment. Explicit block/cond/match tail calls and literal-child calls propagate it; normal condition/index/callee checks do not. Array and tuple destinations enter this path through the existing complete annotation checker, including retained-container assignment and borrowed-field prechecks. Actual array values use exact complete view equality. Scalar conversion is confined to actual literal child checking; enum-valued literal children retain declaration equality when the destination is enum.

I retain validated array/tuple literal views in the existing checker expression-proof list. Repeated raw checker queries validate those proofs and members rather than replacing them with raw child inference. A different destination refuses the existing proof. New direct-expression rollback unlinks newly published proof rows along with restoring native array/tuple cache marks. Pending outer literal annotations publish only after complete checking, proof registration and native metadata succeed; nested accepted annotations may remain after a later sibling refusal, as previously documented.

My additive actual checker controls cover local/global/set/return/direct/indirect/record/generic-union destinations, nested arrays, cond/match bindings, direct ranges/wrong types, exact builtin versus local callable array construction, mapped INT results, enum identities, repeated incompatible destination checking, and a borrowed-field entry below its separate public resource-profile admission boundary. Existing mixed-owner callback controls also enter the new array-of-tuple path with a swapped-owner refusal. Fault controls seed a prior complete proof/cache row, sweep every observed transient/persistent allocation position across nested destination preparation/publication, preserve the invalid outer annotation, and require independent successful recovery. Original17, original native18+9 methods and previous assertions remain unchanged. No source in this checkpoint has been compiled or executed.
