# My parser and record ownership prerequisites

I track this bounded integration as `task_8d5d87f2c93a4553889f0f9478b497df`. I preserve current PR948 units07 evidence at head82/mergec2c: parameter-name allocations at parser399/993, record auxiliary vectors at checker7397/7419 and their names at7400. My helper-created name copies are a distinct correction already qualified at72a.

I reuse two independently reviewed native-lane corrections without importing its broader nominal semantics. From09e9ed6bf, I release the independently allocated `parse_type_with_element` auxiliary parameter name before adopting the already allocated source-spelling name. The complete TypeInfo and function signature keep their existing ownership; no fallible operation is introduced between release and replacement. I do not import unrelated malformed-block cleanup here.

From73b61d06f, I destroy each owned `StructDef.field_type_names` string, its vector and the `field_element_types` vector. My two checker registration paths and NanoVirt imported-struct registration each allocate independent copies. My initial claim that metadata extraction independently copies all outputs was incorrect. The focused lifecycle fixtures do not extract metadata. Before broader imported paths, I found that metadata snapshots still alias these auxiliary vectors; I track that separate static finding under `task_c65cfe23d7434c429b7f6f79d3838178` and hold those paths until the snapshot contract and correction are reviewed. I leave borrowed `field_type_info` annotations and module-owner strings unchanged; the later46b owner-name registry is a separate rule. My manual lookup fixture removes its stack-backed descriptor slots before real Environment destruction, so these borrowed test descriptors never enter ordinary descriptor teardown.

Before product execution I review additive parser-only ordinary/qualified parameter controls, parsed record/function lifecycles in both AST/Environment destruction orders, and manually allocated zero/nonzero record auxiliary vectors with borrowed stack TypeInfo. I then qualify the existing ownership fixture, unchanged native float-record suite and additive effect name-lifetime control using fresh ordinary and ASan/UBSan with LSan providers. Every original assertion and inner deadline remains. Any new first failure stops dependent work and stays distinct. I do not modify the active PR948 head or close the full native parser/nominal parent tasks from this bounded result.
