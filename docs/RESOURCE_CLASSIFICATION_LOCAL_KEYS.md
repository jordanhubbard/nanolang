# Invocation-local resource declaration keys

I measured my original 53cc full-selection typecheck preparation under its unchanged 60-second aggregate shadow deadline. The retained failure and exact source-item timing are sealed at c5084b874. My first 26 typechecker shadows consume 16.991 seconds; resource payload and declaration controls account for 13.484 seconds. Another 164 checker shadows remain, so this change alone has no established whole-preparation timing claim.

I will cache struct/union counts and resolved declaration identity strings within `resource_classify`, after its existing special-case guards. I preserve the resource-root scan and its empty-root early return. After that return, I resolve each declaration key once, in original struct-then-union order, and reuse it in subsequent fixed-point rounds. I retain exact `resource_type_identity(parser, def.name, def.line)` results rather than synthesizing keys from indexes: duplicate spelling and module ownership still decide identity.

The fixed-point loop mutates only local bearing/collections arrays. Its identity and payload helpers read parser declarations, generic spellings and `mb_owner`; they do not call parser-store operations, `mb_reset`, or module-binding setters. Therefore the cached counts and keys describe the same parser and module-owner state throughout this synchronous call. The array slots retain the returned strings under existing string/array value semantics; no borrowed C pointer or global cache is introduced. A later invocation recomputes everything.

I preserve field traversal order, separate ownership/collection evaluations, monotone updates, recursive active sets and depth bounds, selected-variant behavior, every early refusal, and every original shadow, including both 520-statement parser controls. I add a repeated-call control that changes module-owner state between calls and uses distinct parser graphs. Source review precedes execution. Qualification will measure the original affected shadows under the original bounds, then attempt complete preparation only when its source prerequisites are qualified. I will not infer remaining-suite timing from this small optimization.
