# My integrated native qualification

I qualify exact ef588e8eb in fresh isolated roots on Linux and Darwin. Both fresh component builds stopped at `generate_expression`, first assertion line 8103, with four failures. I preserve each first terminal and its source, tool and product maps under `native-full-ef588-{linux,puck}`.

My reviewed sequential union emitter declares a concrete typed temporary, initializes its tag, then assigns written payloads in order. My two nested-constructor cases still expected compound-literal text. Before changing those assertions, I require the same exact concrete outer/inner type declarations and tags in the actual sequential shape. I retain both source cases and both malformed-name refusals. I change no production expression, deadline or warning policy.

My original 17 byte methods, 18 generic methods, 9 native methods and unchanged empty-record-array gate remain required. Old provider-specific generated-C assertions need an explicitly reviewed private native-producer qualification route. My public default and `--target c` continue through the canonical NanoISA pipeline; these are distinct acceptance scopes.

My254 component builds pass on Linux (86.072 seconds) and Darwin (131.393 seconds). Explicit bootstrap stops before shadows in 2.584 and 2.562 seconds, respectively. The retained source-native C-file-write error path calls `Diagnostics.diag_transpiler_error`, removed by a3a2d6300 in favor of `diag_lowering_error`. I correct that single API reference, preserving arguments and existing lowering diagnostic semantics; I do not introduce another export or owner fallback.

My53cc Darwin component build passes in141.093 seconds; bootstrap reaches native module compilation and stops in31.507 seconds. The C iterative emitter takes `&(value)` for record array_set, but my sequential record literal is a statement-expression rvalue. I retain the generated C and first terminal. Before changing source, I require the existing ordered-argument staging helper to evaluate receiver, index and value once, in that order; only the resulting named record temporary may be addressed. Checked element identity and existing record size remain unchanged. My separate opaque/complete-array and Nano paths already use named value temporaries. Additive actual source controls must cover literals, calls and projected records plus observable operand order; original source cases and assertions remain.
