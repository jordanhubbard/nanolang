# My integrated native qualification

I qualify exact ef588e8eb in fresh isolated roots on Linux and Darwin. Both fresh component builds stopped at `generate_expression`, first assertion line 8103, with four failures. I preserve each first terminal and its source, tool and product maps under `native-full-ef588-{linux,puck}`.

My reviewed sequential union emitter declares a concrete typed temporary, initializes its tag, then assigns written payloads in order. My two nested-constructor cases still expected compound-literal text. Before changing those assertions, I require the same exact concrete outer/inner type declarations and tags in the actual sequential shape. I retain both source cases and both malformed-name refusals. I change no production expression, deadline or warning policy.

My original 17 byte methods, 18 generic methods, 9 native methods and unchanged empty-record-array gate remain required. Old provider-specific generated-C assertions need an explicitly reviewed private native-producer qualification route. My public default and `--target c` continue through the canonical NanoISA pipeline; these are distinct acceptance scopes.
