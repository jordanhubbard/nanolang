# I preserve OpenCL module identity before unload

I investigate `task_315caf01b3764a3eb655d825c9203e24`, the retained 75-byte, three-allocation GCC sanitizer failure. My original semantic fixture passed its 48 buffer, 24 kernel and 384 integer observations, then unloaded the OpenCL loader before exit-time leak reporting. That report contains unknown module frames. I do not infer the allocator owner from the platform-enumeration call site alone.

I retain the original source, binary and terminal. In a fresh isolated checkout I link the unchanged actual-GPU fixture with a diagnostic-only GNU linker wrapper around its `dlclose` call. Immediately before the real call, the wrapper copies `/proc/self/maps` to stderr using bounded stack storage and checked read/write loops; it then invokes the real `dlclose` once with the original handle. It does not retain the library, change driver teardown, replace allocation routines, change assertions, or suppress sanitizer findings. Capture failure terminates the diagnostic rather than producing incomplete attribution.

I record the exact source, compiler/linker, loader, ICD and driver identities before execution. I build one fresh GCC ASan/UBSan fixture with the original flags plus the wrapper, then run it once with leak detection and halt-on-error unchanged, with a 120-second parent bound. I capture the first terminal, map any new unknown frame PCs only against this same process's recorded mappings, and rehash source/tools/providers afterward. I do not map historical PCs against a different process or treat semantic success as sanitizer success.

A mapped allocation stack identifies its module and offset; it does not by itself prove ownership, correct shutdown behavior, or an upstream defect. If the first fresh diagnostic differs or the allocation remains unresolved, I retain that result and choose any next experiment under a new explicit contract. This diagnostic changes no production API and closes no GPU-service or release gate.
