# I restore public C lexical metadata after numerical for loops

I execute task_e0cdaf25cbfe4a11b053a4fd408ab0e0 after supported-type782 under original6ade's final context inventory. This contract precedes production. Static AST_FOR inspection finds ctx_add_sym(iterator,INT) occurs before the body scope is pushed; only that body scope is popped. The generated C iterator belongs to its for-declaration scope, but my compiler metadata retains it afterward. In particular, an outer STRING with the same spelling can be inferred INT for later type-directed printing/conversion. No fixture or failed artifact was executed to discover this mismatch.

I give the iterator an explicit enclosing compiler scope spanning the existing numerical for header and body, and restore that scope on every normal/error return. The inner body scope remains distinct so its declarations cannot replace the iterator or leak outward. I preserve symbol high-water/capacity errors, first-error retention and publication staging. Nested loops with the same iterator name restore the immediately enclosing loop binding and then the original outer binding. C return/break/continue retain their real enclosing targets; no synthetic runtime loop or extra iteration is introduced.

This repair does not change emitted numerical bound evaluation, supported range syntax, iteration order, count, runtime storage, or existing checked refusal for lifted loop headers. I make no new claim about header forms that remain outside this profile, and do not admit arbitrary arrays, iterator protocols or a new ABI. It repairs compiler metadata lifetime to match the currently emitted C scope; source-order/effect semantics of other loop forms are not silently declared complete.

I send the production delta for review before frozen execution. Ordinary source controls observe an outer STRING before and after an INT iterator of the same name, an outer INT similarly shadowed, nested same-named loops, block-local declarations and break/continue paths. Exact stdout/string conversion and arithmetic results under C99/C11 O0/O2 GCC/Clang ASan/UBSan demonstrate restoration, without executing the old incorrect output. Direct API controls require supported bindings to recover after refused header/body emission and both public APIs to preserve prior output. Existing53 profile tests plus the four supported-type methods and seven ordinary programs remain the adjacent acceptance boundary unless review narrows checks justified by this isolated change.

I preserve first outcomes and freeze source/harness/tools before each gate. Original6ade remains open until canonical integration and its actual clause-to-evidence audit; no retrospective failed-artifact replay or universal-language claim is required.
