# I observe owned-array bits and signed index boundaries

I track task_15ae8e5bd52b4c7296c452004218908a under430220. This is a fixture-only
acceptance prerequisite, not a demonstrated production defect. I preserve the
active paired mutation fixtures and all Linux/Darwin qualified trees. No runtime,
source syntax, opcode admission or public selector change is proposed.

## I map the remaining parent requirement

The original [parent contract](NANOISA_OWNED_FLOAT_ARRAY_FIELDS.md), ordered
acceptance4, explicitly asks for signed/extreme indices and exact FLOAT bits with
native sanitizers. Merged846 qualifies unchanged Bundle/PREFIX/all shadows on
Linux/Darwin; its optional source controls use index99. Merged836/838/842 qualify
unique/shared lifetime, origin/authority, native/VM and public refusals. Merged
847/849 close actual prepared-receiver realloc and post-append pack failures on
Linux/Darwin. Those owner graphs use finite1.5/2.5/etc. The ongoing source mutation
fixture uses99/-1 and finite values, and claims no extreme-index/bit observations.

Existing shared `test_managed_array_copy.c` lines42–49 observes quiet-NaN payload
and negative-zero bits, but does not establish their complete owned-shell path.
My native emitter already uses memcpy-based nown_float_bits and emits PUSH_F64
from its exact operand bits. I do not infer a literal-formatting defect.

After this bounded runtime acceptance and the complete paired mutation source
acceptance land, I find no additional runtime implementation obligation in430220's
stated slice. Wider targets, ordinary-record/owner composition, borrowed managed
roots, real services and whole-product gates remain their separate parents.
The older unchecked public-activation roadmap row is stale beside completed
activation01e144/PR842; final parent reconciliation should mark it from actual
merge evidence, not invent another activation run.

## I use exact admitted graphs and integer representation observers

I construct fresh bounded modules with the existing exact owner layouts,
ARRAY field facts, helpers and public hosted INT result. I obtain fresh complete
public authority before execution/emission; invalid modules remain query-only.
I encode PUSH_F64 operands by their uint64 bit patterns, not decimal NaN parsing.
The required pattern set includes positive zero0000000000000000, negative
zero8000000000000000, positive quiet NaN7ff8000000000042 and negative quiet
NaNfff8000000000123. Finite/subnormal controls may accompany these. I compare
uint64 representations obtained with memcpy (or existing explicit packed-byte
payloads), with sizeof storage checks. I never use floating equality to decide
NaN identity, pointer punning, out-of-range numeric casts or arithmetic-generated
NaNs. This is transport, not a change to arithmetic canonicalization policy.

The graphs retain aliases through literal creation, pack, rooted projection,
exact owner factory/relay result, reverse unpack and unique child consumption.
SET and PUSH transport exact supplied FLOAT patterns; append crosses actual
capacity, and subsequent reads through retained aliases observe the same bits.
No FLOAT helper result, F64_TO_BITS admission or source cast is needed.

For VM observations I propose a fixture-only wrapper around vm_array_get: compile
heap.c with its implementation renamed, expose the normal symbol in the fixture,
call the unchanged implementation and capture each exact returned FLOAT with
memcpy before returning it unchanged. The wrapper asserts expected operation
sequence, array identity/index and observation count. Missing/extreme reads are
checked separately through their public result/error behavior, since VM rejects
invalid indices before calling vm_array_get. This observes actual public module
execution and does not alter any value or suppress failure.

For native representation observations I include the complete unmodified emitted
C in a harness. After fresh full-module admission/emission, a separate carrier
probe calls the actual emitted no-argument owner factory and exact owner relay
helpers using their existing signatures and an initialized NmsRuntime. I inspect
only their successfully published rooted owner result and retain an ARRAY alias
with the existing runtime API before consuming the shell. I observe NmsValue's
uint64 payload through nms_value_array_get before/after relay, unpack/consumption
and shared mutation/growth; I release every probe root and require zero live
objects/bytes. Generation/parameter/result conventions and helper indices must
be statically checked in the fixture checkpoint. This internal carrier probe is
explicitly separate from normal nvm_program execution of the same admitted graph;
it does not claim a new public helper-entry API or caller-supplied authority.
I do not rewrite generated function bodies or introduce a production observer.

## I preserve signed boundary behavior and cleanup

I qualify GET and SET for -1, INT64_MIN and INT64_MAX, with exact INT operands
encoded safely. Valid zero and last-element controls distinguish missing values
from valid bits. GET followed by POP may complete with its missing VOID; a
qualified optional FLOAT comparison must reject VOID before reading a payload.
SET must retain existing VM bounds/native status conventions and exact visible
prefixes, with no out-of-bounds write or leaked roots. A neighboring surviving
alias/control observes unchanged contents where the existing callable runtime
permits nonterminal inspection; terminal public invocations require complete
cleanup and fresh same-process recovery, not invented continuation semantics.

I retain all existing all-body origin/ownership checks and output sentinels.
I do not execute refused modules, infer success from a compiler return alone or
relax graph/byte accounting for unusual bit values. Public synchronous result
conventions remain those already qualified, including post-entry VOID where
specified. The full fixture diff and actual observer boundaries receive review
before execution; a needed production change stops this fixture-only plan.

## I freeze Linux and Darwin separately

After fixture review I prepare providers once, archive exact sources/tools and
run direct fixture recipes with per-phase maps. Linux GCC ordinary/GCC sanitizer/
Clang sanitizer and Darwin Apple ordinary/Homebrew sanitizer retain exact compiler,
SDK and leak-detection attribution. Both VM dispatches have preprocessor evidence,
fusion controls and native O0/O2. Instrumented units and ordinary linked providers
remain distinct. Existing mutation/private corpus adjacency is bounded by actual
shared-fixture changes, not automatically replayed. I preserve first terminals
and stop on source/tool drift. No existing source gate is interrupted or broadened.

## My complete fixture and helper mapping before execution

My new fixture includes the unchanged qualified runtime corpus with its configurable
main name; no existing fixture or production file changes. Ten fresh modules share
four functions and the existing five-layout descriptor builder:

| Function | Signature / locals | Exact role |
| --- | --- | --- |
|0|()->INT; Bundle,array|Public root calls factory/relay, retains field alias, invokes consuming mutation helper, then normal reads or boundary operation|
|1|()->Bundle; array|Construct four exact bit-pattern values, read each before pack, create Handle7+ARRAY+STRING Bundle|
|2|(Bundle)->Bundle; Bundle|Move its exact owner argument to its result|
|3|(Bundle)->INT; Bundle,array,Handle|Reverse unpack, release STRING, consume Handle, SET index0 to negative quiet NaN, append five exact patterns crossing capacity, read all nine values|

The emitter's existing helper signature is
`static int nown_function_N(nown_value *argument,uint64_t *next_generation,uint64_t generation,nown_value *result,NmsRuntime *managed)`.
The harness checks exact generated signatures for N1/2/3. The internal native
probe initializes a fresh NmsRuntime, calls1 to publish Bundle, retains the
ARRAY alias, calls2 consuming the original owner, then calls3 consuming the relay
result. It checks cleared argument carriers and exact owner layout/category,
observes unchanged bits before/after relay and changed/grown bits after consumption,
releases result/alias and requires zero managed objects/bytes and tracked allocation
balance. It is separately labeled from normal nvm_owned_entry runs of every module.

Normal public root has27 VM observations: four factory reads, four alias reads,
one previous-value read inside valid ARR_SET, nine helper reads and nine surviving
alias reads. Boundary roots have14 observations before the invalid operation:
four factory reads, the SET previous-value read and nine helper reads. The wrapper
checks the same array pointer, exact function, decoded next instruction PC, index,
FLOAT tag and uint64 bits for every observation, then returns the untouched value.
Thus extra/missing/reordered observations fail rather than weaken the oracle.
The three signed indices -1/INT64_MIN/INT64_MAX each have missing-GET/POP,
missing-GET/typed-comparison and failed-SET variants. Valid index0 and last-element
reads are in every helper's nine-value scan. Public API result conventions, exact
`7\n9\n` success and `7\n` failure output, original root/byte cleanup and repeated
fresh-VM same-process execution remain required.

Native missing raw GET reports VOID and succeeds when popped; its optional typed
comparison fails status3. The packed ARRAY SET helper currently reports NMS_STATE
for out-of-range indices, mapped to status3, while VM reports OUT_OF_BOUNDS. The
internal probe checks that exact established helper status and unchanged aliases;
it does not replace it with generic NMS_BOUNDS. Pattern operands are patched using
isa_decode/isa_encode and memcpy from uint64 before fresh complete admission.
No NaN decimal formatter or FLOAT equality supplies the representation oracle.

The Python gate builds true switch/computed-goto with explicit macro assertions,
compares emitted C bytes across dispatch configurations and compiles every native
module at O0/O2. The observer macro renames only heap.c's vm_array_get definition;
VM calls bind the forwarding fixture symbol. Fixture strict flags remain unchanged.
This checkpoint has only Python AST parsing and whitespace validation; no C compile,
module execution or generated native run precedes review.
