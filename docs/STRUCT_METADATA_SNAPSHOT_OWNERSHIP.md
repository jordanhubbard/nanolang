# My struct metadata snapshot ownership

I track this static prerequisite as task_c65cfe23d7434c429b7f6f79d3838178. I found it before broader imported execution after focused02c passed. My metadata extractor starts with a complete StructDef assignment, then copies only name, field names and field types. This leaves original_name, auxiliary field names/elements and complete TypeInfo annotations borrowed from Environment or AST. My Environment destroys original_name and the auxiliary vectors; my AST destroys complete annotations. Native46b already gives module_name an independent metadata copy and remains a separate prerequisite.

I will make every pointer in the metadata StructDef independent: name, original_name, module_name, field_names, field_types, field_type_names, field_element_types and field_type_info. I preserve optional NULL vectors, zero fields, scalar flags, exact spellings and field order. Environment descriptors continue to borrow AST field annotations; only metadata copies own complete annotations. The metadata destructor releases exactly its own copies, including each complete annotation through free_payload_type_info.

I use the existing complete payload copier for annotations, after verifying the schema at each integration pin. Its allocation/depth failures are fatal before extract_module_metadata returns; this legacy API does not promise recoverable allocation failure. New string/vector allocations must follow the same checked fatal policy, without publishing a partial snapshot. I do not infer transactional cleanup on process exit or change that policy silently. Module-name copies from46b and the already identified duplicate enum-value allocation prerequisite must be integrated before their respective fixtures execute.

Before execution I require independent source review and actual snapshot controls that destroy AST and Environment before inspecting all copied fields, destroy the snapshot first in a separate run, preserve optional/empty vectors, and check nested array/callback annotations and exact original/module spellings. Fault controls must distinguish the specified fatal diagnostic from sanitizer failure; successful and recovery paths run with leak detection. No unchanged shallow-snapshot path is replayed to demonstrate the known lifetime defect.

My broader metadata snapshot API contains other descriptor families. This correction does not claim those unreviewed families are fully independent merely because structs become independent. Their existing consumers and ownership contracts remain separate audit obligations.

For complete selected struct-only allocation-prefix controls, I include the extractor's outer ModuleMetadata allocation and module-name copy in the same checked fatal policy. They were previously unchecked before the new struct helper; I correct them before fault execution rather than inject a known unchecked failure. The fixture interposes malloc/calloc/strdup in the actual module object and calloc in the existing payload copier object, arms only around extraction of a struct-only Environment, measures every allocation, and forks each transient/persistent failure position. It requires the exact allocation-family diagnostic and exit1, unchanged source descriptor bytes, no published result, and a fresh successful extraction/destruction after every failure. Normal paths retain LSan; I do not claim partial fatal-process allocations are recoverably reclaimed. Other descriptor families are excluded from this bounded fault fixture, not from their broader obligations.

Before parsed module/enum snapshot controls I integrate the exact09e duplicate-value fix: the module checker already allocates and checks enum variant_values, then overwrites its pointer with a second allocation. I remove only that second allocation and fill the existing owned vector. I reuse the reviewed46b two-module borrowed-context/source-first snapshot control. For native integration I also cover complete tuple children stored in type_params alongside independent flat tuple views. Native4855 andd555 payload-copy/free implementations are byte-identical (5077bytes, SHA256 bdd9386d13a45da5291a539a077e876cd25c26781ca1f13b33b1d74715d081e2); both recurse type_params unconditionally. New fixtures still require review and fresh execution.

After the first Darwin lookup diagnostic overflow I preserve default-all execution and add only an explicit `snapshot` fixture argument to select the independently reviewed metadata group. Unknown arguments refuse before tests. The default still includes every old assertion. Both fatal-child readers retain up to64KiB of unexpected stderr before asserting; overflow prints the retained prefix, kills/reaps that exact child, then fails. Expected diagnostics and exit codes remain exact. A selected metadata pass does not close the original Darwin lookup terminal or qualify the full target.

I track fresh fault-worker images as task_79357f1f2e614035bdc2d89559f0d355. At6cf the complete14,061-byte report identifies4096 bytes in14 Darwin libSystem notify atfork allocations after the first expected fatal snapshot refusal. I preserve the earlier truncated lookup terminal without retroactive attribution. Each corrected fault worker will use posix_spawn of the same instrumented executable with inherited environment, exact internal case/position/mode arguments, fresh source setup, stderr file actions and bounded parent capture/reaping. All measured positions, both modes, atexit source/publication assertions and independent successful recoveries remain. I neither disable LSan nor suppress allocator reports; no further unchanged fork diagnostic runs.
