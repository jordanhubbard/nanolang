# I retain complete indirect File startup facts

I continue task_2c135a488bd61576caf83debb2786270 after the canonical private
[indirect ownership composition](NANOISA_FILE_INDIRECT_FLOW.md), merged by
PR904 at36fb22d9114a83748a5919c27cd12a5f7ffb0f6b. This is a proposed private
serialized conjunction. It grants no execution or installed public selection.
I preserve the separate cyclic public lane and all remaining callable argument,
result, richer-borrow and paired-source requirements within5.1.

## My distinct owning plan

I introduce opaque NvmFileIndirectHostedPlan, not an old cyclic plan with a
hidden alternate mode. Preparation accepts immutable exact v2 bytes, owns one
snapshot and the complete NvmFileIndirectFlow, and publishes only on success.
It uses the existing bounded complete hosted-envelope preflight, checked decode
and bridge. I compare retained code and complete checked original tables; I do
not trust a caller-supplied report. I expose copied startup/function/order,
local/type/import/instruction/variant/region/reference and indirect-call facts,
plus exact bounded wire-span copies. Invalid getters preserve caller outputs.
No accessor exposes an inner cyclic plan as executable authority.

The startup report has its own revision1 and runtime_admitted=false. It retains
original entry/first initializer indices, features and input bytes. Entry has
zero parameters and exactly one clean INT or BOOL result. The first original
`__init__` has zero parameters and VOID/no result. I retain exact function-name
bytes and reject embedded NUL. These are the existing startup restrictions;
callable/owned/reference values cannot escape the host through this plan.

## I account for every candidate and alternative

I check every original function and instruction, including uncalled bodies and
zero-variant instructions. Every reachable ownership alternative retains its
complete input/output, pending/discharged checks and cleanup obligation. Direct
calls and File services retain their exact existing facts. FUNCREF is an
initialized nonowning FUNCTION value; it creates no grant or host operation.
CALL_INDIRECT has CALL cleanup and the common all-target obligation, whose
single target is NO_INDEX. Its candidates must be nonempty, in range, and equal
to checked_candidates. I verify each candidate declaration, parameter/result
signature and common ownership obligation. No candidate is chosen by order or
accepted merely because another candidate fits.

I use the complete combined graph's callee-before-caller order, with unique
original function indices. Every possible callee must already have validated
bounds. I derive local/operand/staging/frame/reference/region capacities from
all variants and every candidate, retaining the maxima rather than summing
mutually exclusive callees. The callable occupies one input operand and is
consumed in staging before parameter transfer. For VM live-value bounds at an
indirect call I subtract that callable and the actually supplied ordinary/owned
parameters from the original input stack, then add caller locals/staging and
each candidate's bound. Native frames remain conservatively live during a
callee. I reserve the callable staging slot explicitly; I do not accidentally
count it as a borrowed argument or omit it from a capacity bound.

The first target profile still refuses borrowed indirect formals, callable
parameters/results and recursion. Direct-call borrowed arguments retain their
existing counts. Declared nonzero max_stack must cover observed operand peaks.
I retain existing64-function/frame and64*769 value-slot limits, per-frame
reference/region ceilings, and exact startup maxima across initializer and
entry. Arithmetic is checked before narrowing; exhausted capacity is LIMIT.
Fuel, actual target membership at runtime and host cleanup are later obligations.

## My storage and failure boundary

I keep the existing64MiB hosted-preparation ceiling. The preflight's full16MiB
flow-query allowance is replaced with the complete32MiB indirect-query bound;
I preserve other conservative wire/bridge/old-plan allowances and charge the
new fixed plan before allocating it. I do not merely charge retained query
bytes while simultaneous decoder/workspace allocations exist. Retained bounds
include the owned snapshot, plan and conservative query peak. Checked arithmetic
precedes every allocation. Invalid input, unsupported profiles, limits and
memory failures retain their existing qualified distinctions; ambiguous legacy
codec failures do not become invented precise OOM reports. All failures free
owned prefixes and leave outputs unchanged. Calls remain externally serialized.

## My ordered qualification and next boundary

I review complete source before fixtures and fixtures before runs. I retain the
original target/indirect-flow/cyclic-hosted/public refusal neighbors separately.
New tests cover entry/initializer shape and order, exact envelope identity,
permuted function/catalog tables, both branch arms and zero iterations, unused
bad bodies, non-first candidate larger frame/depth/stack peaks, callable staging
and malformed candidate/obligation disagreement. Input-destroyed lifetime,
invalid getters, exact limit boundaries, all measured persistent/transient
allocation refusals and fresh recovery remain required on Linux and Darwin.
Sanitizer coverage names actual instrumented providers.

Matched VM/native execution is a later reviewed checkpoint: it must retain the
exact module/function identity in each FUNCTION value, check membership before
any owner/frame transfer, share fuel through initializer/CALL/RET, and unwind
failed setup without changing the caller's first error. Native dispatch selects
real generated functions, never generic VM/FFI fallback. Public grants/installed
archive selection, callable argument/results, richer borrows and source/full
mandatory shadows remain open after this private plan qualifies.
