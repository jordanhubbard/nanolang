# My generated shadow identifier transport

I track task_bda9ec8435ab48a4b3cd21002a0002cd. My C shadow producer uses names
such as `$shadow_0_length`; canonical disassembly preserves those bytes, but my
assembler currently accepts only ASCII letters, digits and underscore. The first
ordinary-authority shadow gate refused the dumped function before execution.
I preserve `/tmp/nanolang-ordinary-producers-paired.log` as that evidence.

I add dollar to the existing bounded identifier alphabet. I preserve all other
identifier rules, length checks, name bytes, numeric operands, symbol lookup,
selected shadows and executable instructions. This is canonical transport, not
source-language syntax or an ownership admission change.

I require fresh ordinary modules with multiple generated names, symbolic calls,
parameter signatures and an entry reference to assemble, execute and preserve
canonical text and bytes through two cycles. Adjacent assembler/transport tests
remain required. The paired producer must subsequently exercise its actual C
shadow output through this path; a handwritten control alone does not close
that dependent acceptance.
