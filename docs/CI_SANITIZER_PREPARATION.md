# My sanitizer preparation contract

I retain PR948 head82c30f5 and actual equal-tree mergec2c813 from run35690205396. My actual units01 compiler products establish a concrete instrumentation gap: nanoc_c has ASan and UBSan symbols, while nanoc_stage1 and nanoc_stage2 have neither. CFLAGS alone does not configure the C compilation performed by a Nano compiler. Both canonical native drivers read NANO_CFLAGS for that operation.

I retain the current complete unit and negative-test inventory, all inner shadow and command deadlines, the20-minute test step and30-minute worker bound, max-parallel4 and fail-fastfalse. I do not change the existing leak-detection policy or add suppressions. Preparation and instrumentation do not establish a fix for an inner60-second shadow timeout.

Before source edits I select this bounded correction under task96b40bc1ccd644bca2131878fa6cf372 and scheduling task9dd1a4c93be74bc79cb4de808eef0d83:

- I resolve the actual Make dependency graph with the same sanitizer flags as the unit inventory. Each target records whether its transitive prerequisites require native bootstrap. I fail closed on ambiguous graph syntax and regenerate the exact manifest at each worker.
- My existing make sanitize phase continues to build and validate C-reference/component stages. A following accurately named provider preparation phase runs real bootstrap3 only for workers whose assigned targets require it. Other workers retain make build validation. I do not remove any owning test prerequisite.
- I pass explicit NANO_CFLAGS with ASan/UBSan, frame pointers, debug information and O1 to both canonical generated-native paths. O1 is the self-hosted driver's ordinary default; it is an explicit instrumented product configuration, not a claimed timeout fix. The C-reference object's existing CFLAGS/LDFLAGS remain unchanged. I retain verbose native compilation logs and the effective flags in the manifest and command records.
- Before tests I require actual compiler files and successful nm output with both sanitizer families. All workers check nanoc_c; bootstrap consumers also check nanoc_stage1 and nanoc_stage2. I hash those exact products and the nm tool. Missing, ordinary or mismatched products refuse the worker before tests. Symbol evidence complements the audited flag path; it does not prove every execution path safe.
- I require this instrumentation assertion as a separate worker result and aggregate obligation. Unit tests cover dependency cycles/shared edges, new consumers, role tampering, missing sanitizer families and failed nm. Source review precedes any affected hosted execution.

My separate deadline audit finds53 native-bootstrap consumers across13 of14 remainder partitions. The misleading previous preparation step calls make build and completes in zero seconds after make sanitize; actual full bootstrap then consumes test-step time. Units03 begins its original17-module scalar reconstruction command at05:38:05Z after the test step starts05:33:12Z; the last completed comparison is05:52:17Z before its20-minute terminal. I first move genuine provider work into preparation. If the unchanged17-module corpus still needs scheduling separation, I will partition its exact discovered module/method obligations with complete-union aggregation and unchanged subprocess bounds. I do not increase the timeout or discard modules. Inner60-second bootstrap shadow failures remain a separate product/performance diagnosis on the corrected candidate.
