# My total guarded public C match contract

I execute task_d00106e9062a40d480908daed04e099b.

I execute a bounded public C child of task_7a99c348dd0f4d619955092f1c51d070 and full parent6ade from mainc7709f24, after canonical760 reconciles named payload and exact terminal children. I admit exact nongeneric scalar union statement matches with source-ordered named arms (repeated variants allowed), exact BOOL guards and at most one final wildcard. Payload binding precedes guard inference/emission; wrong-tag arms do not evaluate their guards, false guards leave their scope and try the next arm, and the first successful guard/body wins. Wildcard binds no payload and does not hide an outer underscore identifier. Scrutinee evaluation remains once.
I require static coverage for every legal declared variant: an unguarded exact arm or literal true guard covers its variant; a final unguarded or literal-true wildcard covers all. Other guarded arms do not establish coverage. A final guarded wildcard is supported only with independent exact coverage. Early/multiple wildcards and incomplete guarded profiles are checked public-target refusals with previous output preserved, pending required shared semantics tasks477bdd/70c5. I do not change global checker/source acceptance, infer policy from backend agreement, or close general7a99/full6ade.
I lower arms as sequential scoped condition blocks and private uniquely numbered match-end labels, using a jump after a successful normally completed body. I introduce no wrapper loop that could capture source break/continue; return still leaves its enclosing function. Nested labels are unique across a C function, including separate statement blocks; planning/function/initializer counters reset deliberately. Prefix selection protects generated labels/temporaries from source names. Existing named payload metadata is pushed before guard and restored after each arm. Exact tag checks and static nominal identities remain intact. After a statically total dispatch, a controlled diagnostic/exit handles an impossible legal-tag fallthrough; no fabricated result or last-variant fallback. I neither evaluate a guard eagerly nor duplicate scrutinee/body effects.
I preserve statement-match values as discarded and leave expression-form matches/GNU block value lifting required separately. Primitive/literal/or-patterns, resources, generic or heap payloads gain no admission. Fresh strict C99/C11 GCC/Clang O0/O2 sanitizer gates qualify repeated named guards, false-to-next-arm selection, skipped other-variant guards, wildcard fallback, exact bool/float/string payload observers, side-effect order, once scrutinee, nested same-name bindings, outer restoration and return/break/continue in enclosing loops. Fresh interpreter and verified VM controls cover this common total profile. API controls cover guard types, exact nominal/refusal, early/multiple/incomplete profiles, label hygiene and path/stream recovery. I retain existing payload4 and adjacent28 plus unchanged7 programs. Contract/roadmap precede production and source review precedes frozen execution; old artifacts remain untouched.

## My specification and source audit

- `docs/SPECIFICATION.md` section3.4.3 and `spec.json` match entries describe union destructuring without early/multiple-wildcard precedence. `docs/CANONICAL_STYLE.md` Match Values and `docs/MATCH_EXPRESSION_SCOPE.md` define enclosing return, final block value and arm-local binding scope.
- `userguide/part1_fundamentals/05_control_flow.md` section5.7 states that guards are bool, may reference payload bindings and only select matching patterns with true guards. Its repeated Active example returns the first high-score result for150 and the second active result for50, supporting named first-success order. Section5.8 and quick-reference examples place wildcard last but do not define earlier or multiple wildcard precedence.
- `src/parser.c` accepts optional guards on named/wildcard arms without requiring wildcard last or unique. `src/typechecker.c` checks payload scope before guards; statement checks allow BOOL or UNKNOWN guards and do not establish unconditional total coverage. Expression exhaustiveness excludes guarded arms from coverage and accepts an unguarded wildcard. These are not a normative early-wildcard ordering decision.
- `src/eval.c` AST_MATCH scans specific arms first and executes the last wildcard afterward. `src/nanovirt/codegen.c` AST_MATCH and guarded `src/transpiler_iterative_v3_twopass.c` emit source-order wildcard checks. Shared policy prerequisite `task_477bdd430a1442e7bc19cbacdbac0bde` retains this discrepancy.
- Interpreter no-success reports an error and yields VOID; NanoVirt terminates through false ASSERT/HALT; public C previously falls through partial chains. Required policy prerequisite `task_70c5a56802e44142af0f19da2469f654` retains this discrepancy. Static total coverage in this child avoids claiming an agreed no-success policy for source-valid inputs.

I make no global semantic policy decision in this child. I document target refusal boundaries and keep their required implementations/decisions open. This slice does not turn the public target into the full language or satisfy full release acceptance.
