# My compiler-support private implementation boundary

I track this correction as `task_7ef1745209494d87930f705ffda1dec6`.

My b209 Linux sanitized gate builds fresh providers and passes exact import refusals plus the mutable borrowed-root VM/native case. Compiling the real artifact driver stops before selected shadows at ASan's ODR check: `module_builder_verbose` is defined in both the compiler host and `compiler_support.so`. I retain the first report and do not disable ODR detection.

My provider manifest deliberately compiles `module_builder.c` and `module_build_dir.c` to perform real SDK preparation and immutable module publication without requiring a compiler host. Their implementation symbols are not compiler_support's foreign API. The declared facade is exactly `nlc_module_artifact`, `nlc_runtime_root`, and `nlc_native_array_abi`. The host's `module_builder_verbose` remains externally accessible to its actual `module.c` writer; renaming or privatizing that host variable would change a separate contract.

Before implementation, I propose provider-local hidden visibility for its compiled C definitions, with explicit default visibility on the three facade declarations. All internal direct calls retain the provider's actual implementation and private state. Ordinary host compilation and externally resolved runtime dependencies remain unchanged. I do not replace the builder with unresolved host callbacks, alter its data-path/root semantics, or suppress sanitizer checks. Existing shared SDK cJSON/UTF8 providers retain their separate compilation contract; I do not claim that every linked helper becomes private through this manifest flag.

I will add an actual loader control that requires all three facade symbols and proves the provider does not expose `module_builder_verbose`, `module_build`, or `nano_native_sdk_prepare` through its own handle. Every selected immutable generation remains independently hashed and oracle-checked. Fresh both-host ordinary/sanitizer fixture execution, including the original driver compilation which exposed the collision, is required before the broader canonical bootstrap. Provider/source maps and the first b209 terminals remain retained.
