# I spell only established public C value representations

I execute task_f29a0f0a1b204994a6d7a784e2173a6e under original6ade. This contract precedes production and follows the frozen terminal callable780 gates. I retain the original task's portable-lowering-or-checked-refusal requirement; I do not invent universal language admission or new ABIs. My starting tree is reviewed780 head5e11f2ab, with canonical779 integrated. I integrate canonical780 before final qualification.

## I inventory the remaining spelling boundary

My c_type switch explicitly spells INT, U8, FLOAT, BOOL, STRING and VOID; its default spells every other kind as int64_t. Separate sites spell named STRUCT/UNION locals, parameters and results; record fields special-case named STRUCT, while union fields and scalar globals have their own stricter constraints. ARRAY and FUNCTION refusal already has qualified recursive metadata guards. Tuple expression syntax, anonymous captures, effects, async and try already refuse, but unsupported annotations can still bypass those syntax guards. ENUM definitions have an existing C enumerator emitter and integer-valued storage fallback; that historic representation is distinct from arbitrary list/map/pointer kinds.

I consolidate BSTRING, unresolved GENERIC, all LIST kinds, HASHMAP, TUPLE value annotations, OPAQUE, OPEN_RECORD, UNKNOWN and BORROW_SHARED/BORROW_MUT into one checked unsupported-type boundary. I add no binary-string, list, map, closure, opaque FFI, row-record or borrow ABI. Existing ARRAY/FUNCTION-specific diagnostics retain their precedence. Unsupported retained nested metadata must not disappear behind an otherwise supported outer kind; descriptive function signatures remain descriptions of direct calls, not callable values. Optional absent metadata remains distinguishable from an explicit UNKNOWN type: I may use the existing exact declaration/lexical inference where qualified, but I must not treat absent identity as permission for integer storage.

## I retain supported context-specific identities

I preserve INT/U8/FLOAT/BOOL/STRING spelling and existing exact scalar conversion rules. VOID is legal as a function result or existing effect-only expression, not a local/parameter/field storage type. ENUM's existing integer carrier becomes an explicit spelling rather than the catch-all fallback; I do not introduce enum value syntax or change nominal checker rules. Ordinary enum controls establish the actually accepted source path; API-only declaration/carrier coverage is labeled if source lowering independently refuses enum expressions.

STRUCT and UNION remain supported only through the existing exact local declaration paths, with a real owner of the correct kind and the currently supported payload/field constraints. Missing names, wrong declaration kind, uninstantiated generic declarations and nominal values reaching a scalar-only spelling path receive checked refusal. I preserve already qualified record fields, union results/parameters/locals and scalar payloads, including empty/discard variants. I do not repair declaration dependency ordering, add a new aggregate ABI, broaden global storage, or make a union field legal in a path that currently has only scalar spelling. Exact retained owner metadata and existing namespace selection remain authoritative.

The scalar spelling helper must have an error path for every unhandled kind; no default-int64 success remains. Every caller must retain invocation-local diagnostics through staged publication. Type checks cover declarations, locals/globals, parameters/results including externs, record/union fields, explicit checked field/call facts and actual resolved identifier/use types. Unsupported root declarations may not be silently skipped into an apparently successful supported value ABI. Imported/extern descriptions with an already qualified exact scalar signature remain as today; this child does not establish external linkage or flatten imports anew. Unknown bare call/value facts retain their existing checked context boundaries rather than being promoted to INT by a spelling fallback.

## I qualify refusal and preservation independently

I send production for review before fresh execution. API controls enumerate every Type kind at representative storage/signature/field boundaries, exercise nested retained metadata and absent/wrong nominal identities, and verify both path and FILE semantic output preservation followed by valid same-process recovery. Direct descriptive signatures and exact local nominal positives remain controls. Ordinary source cases use supported parser/checker forms; I distinguish frontend rejection from backend refusal and never count an API-only case as source acceptance.

Strict GCC/Clang C99/C11 O0/O2 ASan/UBSan controls preserve exact scalar/string/record/union behavior and explicit enum/U8 behavior where already admitted. The existing53 callable/profile/array/lifting and adjacent methods plus seven ordinary C programs remain regression gates. No global unused-helper-warning, fresh bootstrap, Darwin, universal type admission or new ABI claim follows from these checks. I freeze the full harness, source and tool identities before each gate, preserve first terminal failures, and do not execute historical failed artifacts.

After canonical merge I can finish original6ade's actual clause-to-evidence context/option inventory. Shared wildcard/no-success language policy and other release requirements stay independent; supported C profile refusals do not silently close those tasks. Any newly found unhandled semantic stub is recorded rather than hidden by this type inventory.

## I require complete by-value record declarations at the emission point

Exact local STRUCT ownership alone does not establish a usable C field type. My current source-order typedef emission requires a by-value record field's complete declaration to precede its containing record. I check that order explicitly and refuse forward, self-recursive and cyclic by-value dependencies before publication; I do not add topological sorting, pointers or a recursive record ABI. A prior-order nested-record positive and forward/self/cyclic output-preserving refusals qualify this boundary. Function/local record uses occur after the type-definition pass and still require an exact usable local declaration. This clarification was reviewed before production.
