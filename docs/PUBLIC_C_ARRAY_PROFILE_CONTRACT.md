# I refuse public C arrays without an array value ABI

I execute task_8026bbfbddec40afae385ad608c8b692 under original6ade after canonical777. My preceding static roadmap08781814 records the gap before production: arbitrary AST_ARRAY_LITERAL emits bare braces, local array annotations emit element pointers, and parameter/result array types can reach c_type's int64 fallback. No failing artifact was executed. This contract chooses checked refusal, explicitly permitted by original6ade, rather than inventing array storage/ownership/call semantics.

I reject array literals in every reachable value/statement context, including empty and nested forms. I reject explicit TYPE_ARRAY declarations for function results and parameters (including extern declarations), local/global bindings, ordinary record fields and union payloads before generating their storage/signatures. Complete retained TypeInfo and FunctionSignature metadata containing arrays cannot hide behind another annotation or an indirect callee signature. I preserve exact supported scalar, string and nominal scalar/empty union declarations; I do not replace unresolved array facts with int64 or a bare pointer. Missing required callable/type metadata remains a checked boundary, not permission to guess.

At expression use, I refuse array-typed identifiers/fields/call results/arguments from checked metadata or exact declaration lookup before emitting operations. Array operations require a qualified array operand ABI; they do not fall through to undeclared C helpers. I audit existing builtin array families (construction, length/access/mutation, slices/concat and functional map/filter/reduce) and array-producing conversions such as string split. Exact source declarations/bindings and actual callee expressions retain precedence over builtin spelling: a user scalar function named array_length is not an array operation merely because of its name. I use exact known builtin names or exact retained type facts, never an arbitrary array_ prefix ban. Module-qualified array calls without retained exact supported signature facts remain refused rather than receiving fabricated scalar authority. This does not promise general module/function-value ABI support.

I remove obsolete bare-brace/element-pointer array emission paths or make them explicitly unreachable behind the same checked profile predicate. I add no compound-literal array ABI, heap lifetime, copied-array semantics, allocation ownership or shared checker restriction. Other backend/source producers retain their existing array support. Header/profile inventory explicitly identifies arrays as unavailable in this public C profile.

Both c_backend_emit path and c_backend_emit_fp stream entrypoints must refuse before prior caller bytes change, retain the first diagnostic and recover on a subsequent valid invocation in the same process. I preserve sibling-file staging/rename and semantic stream staging, including the documented external stream partial-I/O limitation. Option handling remains the independently qualified777 contract.

Before fresh execution I send production for independent review. Ordinary checked source and direct API controls cover empty/nonempty/nested literals, return/parameter/local/global/record/payload array annotations, array-valued calls and scalar-result array operations, retained indirect signature metadata, and nested use in other expressions. API refusal is distinguished from frontend rejection; no rejected output executes. Same-spelled declared scalar functions and existing scalar/string/union/lifted values remain positive controls. Strict GCC/Clang C99/C11 O0/O2/sanitizer controls observe accepted outputs, both publication APIs and recovery; affected adjacent44 methods and seven existing programs remain required. I preserve first failures and freeze production, harness and tools together. Canonical merge and a renewed original6ade clause audit precede closure; universal admission and independent source-array ownership requirements are not inferred.

## I distinguish source reservation from API declaration precedence

My first frozen55a0d2af focused gates pass3/4 under GCC/Clang. The ten array-source refusals, API publication/recovery and exact32-name registry audit pass. My proposed source array_length declaration is refused by the shared checker before backend entry: builtins cannot be redefined. I retain both logs and all44 unchanged file identities. This was a fixture assumption, not evidence that source declarations may override reserved builtins. I preserve that source reservation as an expected refusal, qualify exact same-spelled direct/qualified declarations through the public AST API, and use an ordinary array_length_like source function to reject arbitrary prefix matching. No shared checker or production change is authorized by this correction.
