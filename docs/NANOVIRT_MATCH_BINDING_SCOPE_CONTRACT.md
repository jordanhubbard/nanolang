# I restore match-arm binding scope

I track this bounded repair as `task_6d9b923cd7854003a8430dddde22d4b2`, required by guarded public C acceptance child d00106. My first frozen GCC and Clang paired runs at `66824443` passed public C and interpreter observations but failed the verified VM outer-payload assertion following a nested same-name match binding. I retain both logs at `/tmp/nanolang-public-c-guarded-{gcc,clang}.log`; each passed three of four methods. I do not replay those failed artifacts.

My `local_add` assigns a fresh runtime slot using `local_count`, while it appends lexical metadata using `local_binding_count`. Blocks and loops restore the binding count while retaining allocated slots. My old match-arm cleanup instead uses slot indexes into the binding array. These indexes cannot be interchanged after a lexical scope closes.

I snapshot the binding count before adding each arm's payload binding. After emitting the arm I close advisory name ranges through `local_names_end` and restore that binding count, matching my ordinary block cleanup. I retain the runtime slot highwater, unique slot allocation, emitted stores, all guard branches, operand stack cleanup and VM frame ownership. I neither pop runtime locals early nor free borrowed compiler metadata. The enclosing binding becomes visible again by reverse lexical lookup; later sibling arms start from the same enclosing scope. I preserve nominal metadata and leave source wildcard/no-success policy unchanged.

I qualify fresh corrected-source ordinary nested and sibling payload shadows, guard-false fallthrough, prior loop/block locals, outer bindings and discard names. I then qualify the unchanged guarded public C fixture with an explicitly identified corrected external NanoVirt producer. I retain source/tool identities separately. I require focused codegen checks and ordinary interpreter/VM results, not execution of malformed bytecode or historical failed artifacts. Full public C expression lifting and shared matching policy remain open.
