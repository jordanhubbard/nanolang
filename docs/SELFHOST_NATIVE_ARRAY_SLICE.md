# I emit the missing native array slice helper

I execute task_c9927df8c5674cf3bcf6ecdf7032f505. This contract precedes implementation. Fresh accepted source in /tmp/nanolang-native-slice-baseline-2lkg4g54 passes C-seed compilation and execution, while the independently qualified Stage1 and Stage2 compilers fail native shadow linking on undefined `nl_array_slice`. I retain all three first outcomes and their exact tool identities. I do not execute failed historical artifacts.

My C seed already emits `nl_array_slice`; the self-hosted native transpiler emits calls but no definition. I add the same bounded helper definition to `gen_c_runtime`. I preserve the existing source meaning: arguments are array, start and length (not end); negative start and length clamp to zero, start beyond length clamps to length, and requested length clamps to remaining elements before addition. Extreme signed arguments cannot overflow the addition. I preserve the existing DynArray element kind and push/get policy for U8, INT, FLOAT, BOOL, STRING, ARRAY and STRUCT. Slice creates fresh outer storage, retaining the existing shallow element semantics for nested references; it does not introduce deep cloning or a new owner ABI. I retain the existing allocation-failure behavior and null-input fallback.

This correction changes no parser, checker, builtin registry, generic call argument lowering, VM slice opcode or C-seed helper behavior. I require source controls for one evaluation per argument but do not claim a new global C argument evaluation order from this definition. Existing array_slice type checks and source-shadow selection remain intact. Unsupported source profiles remain their own checked boundaries.

I qualify actual fresh bootstrap before corrected compiler execution, then the exact original minimal source through C-seed, Stage1 and Stage2 native drivers with normal shadows. Focused sources cover ordinary, empty, negative, clipped and extreme signed bounds; INT, BOOL, STRING, FLOAT, nested ARRAY and ordinary record elements; independent outer mutation and retained shallow nested identity. U8 and unsupported type spellings are tested at the actual admitted source or direct helper boundary without inventing source support. I compile extracted generated helper controls with strict GCC/Clang C11 and ASan/UBSan, including allocation cleanup and binary64 transport bits. Existing canonical VM behavior is a comparison route, not a changed implementation.

I preserve the first terminal result, freeze source/tool hashes, and retain existing shadow deadlines and sanitizers. Passing this child closes only the missing native helper defect, not full array semantics, affine ownership, compiler fixed points or product release acceptance.
