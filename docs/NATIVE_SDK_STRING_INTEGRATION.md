# My native and SDK source integration

I prepare this integration in a new isolated worktree and branch. I preserve
both original lane branches and every frozen qualification tree. My starting
native source is 8028ab194; my incoming SDK source is 243747e4f. Their common
ancestor is 4b2422ffbee8ed3a621846b868ed9982407f3457. The accompanying
NATIVE_SDK_INTEGRATION_INVENTORY.json pins complete identities, changed paths
and the read-only merge-tree preview. I have not checked out or executed its
conflict-marker tree.

I need this integration because the valid Words.Some SDK prerequisite reaches
the native lane's existing constructor identity repair. Its NominalIdentity
and contextual checker foundation are absent from the isolated SDK lane.
I do not copy an isolated suffix rewrite or replace four whole source files.

## My inventory before source edits

The preview reports 15 textual conflict paths, including three documentation
or build paths. There are 28 overlapping functional paths, 45 native-only
functional changes and 150 SDK-only functional changes relative to the common
ancestor. Clean textual merging is not semantic approval. In particular,
module.c/module_metadata.c, parser.c, generated schema consumers, evaluator and
native array/name helpers have meaningful overlap despite no conflict marker.

The conflicts are Makefile.gnu, my roadmap, the computed-U8 contract, env.c,
env_opaque_keys.inc, env_opaque_types.inc, nanolang.h, nominal_types.c,
transpiler.c, transpiler_iterative_v3_twopass.c,
transpiler_opaque_declarations.inc, typechecker.c, nanoc_v06.nano,
transpiler.nano and typecheck.nano.

## My proposed resolution boundaries

1. I retain the native foundation's complete owner/context substitution and
   per-child NominalView proofs. Materialized spelling never replaces nominal
   authority. Checked copies own independent contexts; assignment snapshots
   the destination before recursively checking a value. Existing source-order
   staging and tuple child accessors remain, with no competing tuple schema.
2. I combine header/schema/environment ownership explicitly. The native owned
   module-name registry and snapshot lifetime (46b), callable nested-result
   dual-slot borrowed merge, and append-only array/tuple emission-cache rollback
   at recursive/program/module/shadow failure boundaries all survive. The SDK
   opaque key registry, canonical owner/kind/declaration facts, exact tuple and
   callback carriers, ABI2 width and complete copied TypeInfo remain. I review
   each addition's copy/free/rollback owner before resolving its hunk.
3. I preserve native constructor repairs as a unit: transactional constructor
   name allocation and AST tag/field transfer, complete imported brace parsing,
   actual variant/owner classification, contextual payload annotations and
   exact enum identity. I apply SDK retained parser declaration capture and
   visibility metadata without changing ordinary parse wrappers or File caps.
4. I preserve both native emitters' full derived tuple/callback declaration
   graph and source-order staging, contextual checker facts and nominal list
   specialization. SDK C-symbol encoding remains separate from semantic keys;
   checked typed array copies/traps and exact widths stay intact. All global
   startup and discovered-type ordering changes require hunk-level comparison,
   including the current native substring search optimization.
5. I add SDK module visibility and STRING-result changes through the native
   checker entry wrappers. Failure must roll back newly published metadata.
   Exact-owner selective preflight, staged aliases, known-owner private access,
   lexical callbacks and body-bearing source str_split precedence survive.
   C symmetric STRING-array contexts and Nano global/qualified/store/union
   comparisons retain actual annotation authority and explicit unknown limits.
6. I preserve the scheduler active-callback latch, owned result clone/drop,
   Environment leases and exact module-cache generation leases. Teardown
   preflights precede AST/cache destruction; borrowed results clone before
   escaping their owner. Legacy fatal allocation sites remain distinct from
   checked graph fault domains. I combine installed SDK discovery, native provider ownership, callback/cache
   disposal, metadata quoting/include closure and native module-name lifetimes.
   I audit every successful and failed ownership transfer. Both ordinary and
   installed source-hidden paths must use the same complete provider inventory.
   Required generated install manifests must include new native foundation
   sources/headers and executable modes; no source-tree assistance is assumed.
7. I retain both fixture families and append both roadmap/evidence records.
   Actual parser/generator schemas regenerate through canonical generators;
   fixture syntax or pure textual merge checks do not establish byte parity.
   Prior first failures retain their exact pins, including 0e6 constructor
   refusal and native bootstrap diagnostics. No prior passing stage is relabeled
   as an integrated result.

## My review and qualification boundary

I first resolve source with an explicit hunk/owner inventory. The native owner
remains responsible for its separate bootstrap diagnosis and reviews shared
contextual adapters; I do not modify that lane. Root reviews the complete
integrated production and fixtures before execution. I report any ambiguity
that changes representation, authority or lifetime before choosing a new rule.

This integration changes compiler providers and ownership layout, so retained
0108 refreshes cannot qualify the result. Fresh C-seed/Stage1/Stage2 closure on
both hosts is required after source approval. I then run the unchanged focused
STRING/File controls and native nominal/allocation controls plus both lanes'
full required acceptance, including original native18+8 and complete source-
hidden/read-only installed SDK, all provider owners, ABI-before-entry, wrappers,
output preservation and uninstall. Source/tool/product manifests include lib.
Actual unchanged closures may retain their old qualification with explicit
provenance, but no broad gate is presumed redundant before the final source
review. Full source File execution, backend/shape requirements and release
publication remain open until their complete acceptance is demonstrated.

## My resolved source ownership audit

I preserve native coarse argument matching followed by the complete contextual
comparison. I do not add the older SDK flattened callable equality before that
proof. I remove automatically reintroduced eager array/map metadata publication:
actual builtin `str_split` now obtains a copied STRING-array NominalView and uses
the native checked publication/rollback path. I retain only one copy of each
nongeneric-union discovery loop and each tuple helper/cleanup owner.

I add the seven native foundation source/header files missing from the committed
installed input list and regenerate the C required-input table. The object list
is unchanged: these files belong to existing translation units. I import the
root-reviewed signed string-length primitive correction and its ninth native
fixture from ecaf373f0; I preserve all original eighteen-plus-eight controls.
I have not executed these integrated products.

I move the three old `native_source_already_linked` shadow properties to the
actual `native_provider_add` shadow: identical canonical cJSON input has one
row, a different directory with the same basename has two, and an empty source
refuses without a row. The old helper is superseded by the reviewed whole
runtime inventory and registry; I do not restore a special cJSON suppression.
The static shadow report records this explicit replacement, not an execution
or a silently omitted property. The new list-generation refusal also runs the
native Environment teardown preflight before cache destruction.
