# My checked ownership dependency-selection prerequisite

I record task_fa08b9ffba9c40f0bb4ce22a63321589 before production, under installed-product affine task_e8d860a16da0464891dd32e91c42bef1. My first950f product result remains sealed at0ab42bcc on docs/product-gates-950f36d1. This checkpoint is a bounded proposal for review; no production or tests change here.

## My observed boundary

Twenty-six failed positive declaration probes include a body-less `consume_handle` declaration and a checked, uncalled `probe`; their main returns zero and has no shadows. Two additional union-return probes stop at the union guard before reaching that extern guard. The original tests request C output, preserve prior output on refusals, and deliberately do not execute foreign functions. All whole-source ownership obligations remain required.

My ordinary selfhost `nanoisa_emit_program_nasm` already schedules direct executable dependencies from main, whereas `nanoisa_emit_parser_nasm` retains full-module definitions. Both currently enter `nanoisa_has_borrows` over every declaration/local/constructor before that distinction; `nanoisa_borrow_emit` then schedules all declared functions and refuses any body-less function. Deleting that guard would neither select a valid closure nor establish a foreign ABI.

My C-seed `codegen_compile` currently retains its full-module behavior and has no equivalent program-selection flag. The original C-seed native C-output probes already pass. I do not quietly redefine that public API or claim C-seed NanoVirt full-module emission has acquired executable pruning from this first selfhost prerequisite.

## My proposed first checkpoint: prove an owner-free selected closure

I change only routing for program publication and selected-shadow publication after the canonical frontend's full source checks. I add a conservative, nonmutating selection analysis with three outcomes: proved owner-free selected closure, selected ownership, or unknown/unsupported dependency. Only the positive proof permits ordinary program/shadow lowering when unrelated declarations contain ownership. Selected ownership and unknown cases retain the existing specialized route and its existing checked refusals. I do not prune or weaken an owned graph in this checkpoint.

The proof retains original parser/node/function identities, source locations and lexical binding scopes. It does not copy an unchecked subset back into the checker. The source parser, nominal binding, typechecker and affine checker run on the entire original source before executable selection, including unused function bodies and foreign declarations. Existing declaration-only intrinsic validation remains before public lowering. Raw parser APIs retain their current caller-supplied-checked-parser obligation; this analysis is not a replacement source checker or a reusable trust token.

Program roots are the exact zero-argument int main selected by the current driver. Shadow roots are every body in the requested first_shadow suffix; a selected shadow that calls main adds that exact function dependency. Naming a shadow after a function does not itself invent an executable call, and the function's unselected shadow is not substituted for its body. I neither omit selected shadows nor change import/root selection defaults.

I compute a bounded direct-call worklist over original function indexes. The initial bound remains the existing ownership declaration/function limit of eight; I do not widen it through pruning. Each selected function is scanned at most once after marking queued. Recursion/cycles and unknown dependency forms return unknown rather than borrowing ordinary recursion assumptions. Every body traversal remains bounded and rejects unsupported node kinds conservatively. No emitted function index, layout index or metadata interval is modified by the analysis.

A direct call must resolve to the original unshadowed declaration or an explicitly recognized existing scalar builtin. I track parameter/local names, initializer-before-binding order, nested scopes, loop binders and pattern binders. A local/formal callable, function value, indirect/module-qualified/foreign call or uncertain resolution returns unknown in this first checkpoint. Any imports or global initializers also return unknown; their existing publication refusal remains. I do not infer direct identity from a matching spelling.

The positive proof admits only selected scalar signatures and supported scalar expressions/statements for which every operand/body is inspected. A selected resource/borrow signature, owner constructor, owner-typed binding, destructive pattern, aggregate/union type with possible transitive ownership, or unclassified type returns selected ownership or unknown. I conservatively leave aggregate classification outside this first proof rather than guess that an ordinary record lacks a resource. Existing scalar code that does not trigger whole-source ownership routing keeps its current behavior.

When proved owner-free, the existing ordinary program queue and ordinary selected-shadow emitter perform actual lowering and retain their verifier, exact builtin/extern checks and atomic publication. The original parser still contains every declaration. No owned instruction, reference or layout authority is admitted by this proof. If ordinary lowering later refuses an unsupported scalar operation, its original diagnostic/output behavior is retained; I do not retry a different backend.

Full-module `nanoisa_emit_parser_nasm`, raw full-module tools and C-seed `codegen_compile` keep all-definition ownership routing unchanged. A separate future contract is needed to select subsets inside owned graphs or introduce explicit program-selection behavior in the C NanoVirt API. That distinction is testable, not an implicit exception.

## My acceptance

I require a checked program/shadow harness compiled by C-seed and both selfhost stages, calling the actual program and selected-shadow APIs only after the same complete source checks. I compare canonical module text, absence of unselected foreign imports, VM/native results, complete selected-shadow behavior and prior-output preservation. No foreign operation is executed by these declaration probes.

Positive controls include main returning zero with an unused checked owner function and unused extern declaration, scalar main calling an ordinary scalar helper amid those declarations, and selected scalar shadows with exact suffix selection. Original positive declaration-probe source strings stay unchanged; I qualify their whole-source checking against C-seed and both selfhost checkers, then canonical product-mode emission. Raw full-module emission retains its current refusal for these unsupported foreign-containing ownership modules.

Negative controls retain invalid ownership in unused functions, invalid ordinary types in unused functions, reserved/invalid intrinsic declarations, reachable owner or extern calls, lexical/formal shadowing of a function name, indirect calls, unknown aggregate ownership, recursion and missing/invalid main. Selected shadows that reach unsupported ownership/foreign operations must still refuse publication; a shadow-only ownership edge cannot disappear because main is scalar. Refusals must preserve output and identify the intended phase, excluding parse failure as a substitute for ownership or selection evidence.

I retain current source-borrow metadata/shadow/authority gates. A fresh selfhost bootstrap is required for production changes, followed by exact integrated acceptance. Full product remains held until unchanged original suites pass at a new installed-product pin. This checkpoint does not close owned ordinary wrappers, managed fields, owned unions, indirect/foreign ownership calls, full selected owned-graph routing, or full affine ownership parents.

## My existing selected-shadow expectation migration

After the focused0989576c qualification passed all three methods (154.282s), I record the exact existing controls affected by positive owner-free shadow selection. In `test_selected_shadows_execute_and_preserve_suffix_identity`, I keep the original source (including its false shadow) and suffix2; its empty selected suffix now produces an ordinary verified scalar entry rather than failing the owned-transfer requirement. The suffix0 false-shadow assertion remains a failing VM/native execution control. In `test_false_shadow_and_unsupported_graph_preserve_publication`, I keep the original scalar-only-selected-shadows source and require positive VM/native publication from Stage1 and Stage2; C-seed NanoVirt all-definition behavior retains its original refusal. Every other selected-owner/unsupported graph refusal and prior-output assertion remains unchanged. I review this test-only migration before running the complete ownership gate with its declared tool/probe prerequisites.

The accidental imported C-exporter probes at6ae02fa1 are separate reviewed public C-profile limitations (indirect callables and non-scalar union results), recorded in the installed-product route matrix under task_e8d860a16da0464891dd32e91c42bef1. I do not invent new public C ABI requirements or claim these are repaired by canonical executable selection.
