# I preserve declaration authority through bootstrap integration

I retain the first `1bf6ab758` terminals from both hosts. My complete component build and entry assertions pass. My separate C-seed-to-Stage-1 bootstrap stops in checking before shadows or output publication. My full SDK, File and release acceptance remain open.

I separate two observed boundaries before changing production:

1. My root driver explicitly declares `file_read`, `file_write`, `file_exists` and `file_delete` as extern functions. Imported `std_fs` declares matching private externs. Existing registration treats a matching extern signature as idempotent and skips the later declaration, leaving the earlier function row's module owner. My actual visibility check then refuses the root's calls against that foreign private row. I must retain each actual declaration's owner and visibility while allowing the existing compatible C ABI symbol to be shared. I must not export private module declarations or restore an unnamed-root accessibility bypass. I will audit both registration passes, function lookup/index invalidation, exact signature agreement, copied owner lifetime and failed publication before selecting the representation. Controls must distinguish an actual local extern redeclaration from an unrelated private imported declaration, include reversed import order and conflicting signatures, and preserve all current negative controls.

2. My qualified call `SourceCompanion.source` returns its public `SourceInput` record, but the root destination `SourceCompanion.SourceInput` is refused as an ordinary record identity mismatch at line 976. The later `copied.source` use in the `CollectResult.sources` array reconstruction produces two element-identity diagnostics. I do not assume those later errors are independent until I trace the failed call-result fact. I will inspect the actual qualified function owner, return annotation owner/context and namespace alias normalization through the retained nominal view. I must preserve canonical declaration identity, complete nested facts, independent owned copies and failed-check metadata rollback. I will coordinate this adapter with the native lane; I will not substitute ownerless spelling comparison or infer authority from a C name.

I require a completed source and fixture checkpoint before corrected execution. I preserve the original compiler source, all selected shadows, original deadlines and actual source-hidden installed SDK controls. My qualified fixes must cover both producers' existing declaration semantics rather than bypassing one bootstrap call.

I now use a report-owned `TMPDIR`, already honored by Make's `BOOTSTRAP_TMPDIR`, and retain each component log with byte identities. Historical Linux `186843370` buffered output remains unassigned; I do not use a potentially overwritten shared temporary log as its evidence.

## I retain extern declarations in the existing function table

I use one existing `Function` row for each actual declaring owner, with its own visibility. I compare every already registered row sharing the external ABI spelling, including aliases' original spelling. I compare complete parameter and return annotations through my existing owner-aware checked signature comparator; arrays include their element facts, callbacks include nested signatures, and records retain nominal owners. A repeated declaration in the same owner is idempotent only with matching visibility. A different compatible owner receives a separate row. I preserve the ordinary owner-first indexed lookup and `env_define_function` invalidation.

I stage the two declaration-label allocations before publication. A failed name or owner copy frees both temporary labels and leaves the function count unchanged. Parameter and annotation pointers retain the existing AST/module-cache lease lifetime; I do not claim a new independently owned metadata graph. Environment table growth and checker ownership-node allocation retain their existing fatal policy. My direct C fixture covers both actual root/module registration orders, complete array/callback/tuple comparisons, duplicate visibility refusal and each staged label failure with recovery. My paired fixture adds actual local extern declarations before/after a private namespace import and retains refusal without local authority and for explicit private qualified access.

## I audit a canonical ordinary-record representation before implementing it

The shared binder currently rewrites a namespace-qualified annotation to an unmangled short record name when that declaration has no collision. Its later checker no longer has the namespace owner. I propose reusing my existing owner-derived canonical record-name representation for every non-extern record declared in a named/imported owner, rather than only colliding declarations. I retain `original_name` for source visibility and namespace exports. The canonical key remains backed by the registered record ordinal/owner; it never grants visibility itself. Unnamed-root ordinary naming and all extern ABI record names remain unchanged unless the existing collision/formal rules already apply.

This proposal changes generated C storage names for ordinary imported records and requires full source review. Before implementation I will audit declaration registration, qualified/selective imports, both collectors, complete nested annotation rewrite/copy/free, nominal comparison, generated struct/prototype/list/tuple/callback consumers, and the Nano producer's existing owner-key behavior. I must retain actual selected public names and refuse private/ambiguous imports, same-basename distinct owners and contradictory signatures. I will not use global uniqueness, synthesize an unqualified import, or erase declaration context. No canonical-record source change is included in the extern checkpoint.

## I keep imported ordinary records canonical before losing source qualification

My source correction uses the existing representation, without adding a layout or an ownerless lookup. In C, every non-extern record whose declaration has a module owner receives the existing hex-owner key. The unnamed root keeps ordinary names unless the existing collision/formal rule applies. In Nano, dependency owners precede the final root in `merge.files`; I use the existing reserved-prefix/owner-index key for every non-extern dependency record. The root keeps ordinary names unless the existing collision rule applies. These internal keys need not have the same bytes across producers. Both resolve actual source declarations, and extern C ABI names remain unchanged.

I audited the owning consumers:

- `bind_nominal_records` stages the allocated key before transferring the old name into `original_name`; a failed allocation does not publish that declaration's replacement. Its existing later recursive failure can leave a partially normalized AST, which the failed compilation discards. I do not claim whole-AST rollback. `nominal_scoped_info` traverses arrays, complete tuple children and function signatures, preserving generic formal exclusions and refreshing tuple flat views. Both checker collectors copy the declared name and original name into the registered record. AST and environment destructors free their separate owned strings.
- `register_nominal_imports` and namespace construction use `original_name`; public export registration also retains that name. `env_nominal_identity` still requires the actual namespace/import owner or an already registered canonical key, and returns the registered ordinal. No new source visibility is inferred from a short name. The retained contextual checker/materializer uses that ordinal and `env_nominal_name`.
- Native derived declarations, prototypes, record literals, tuple/callback children and record-list storage consume registered canonical names. Existing `check_list_operation` resolves the source element through `env_nominal_identity` before publishing `list_<canonical>_<operation>`; users retain `list_Item_*` spelling. No provider is removed or guessed from a prefix.
- Nano registers source-name binding rows before rewriting declarations. Actual imports reuse those rows. `nb_rewrite` covers record fields/literals, lets (including parameter lets), returns, arrays and union payload annotations. Token-based C parameter emission uses `nb_type`. `checked_list_element` resolves the source element with the same owner binding before selecting a declaration; native list discovery uses the resulting canonical annotations. Root-only component parsing remains unchanged when module bindings are disabled.

I preserve the existing allocation policies and all extern/opaque/union identity paths. This checkpoint changes record key selection only. I add shadows for unique dependency records, root and foreign names, unavailable unqualified names, and exact namespace target ownership. Paired controls retain a qualified record result through its string-array field, callback parameter/result and array-of-record field; same physical origin through two namespace aliases; a selective original name; two distinct explicit modules with the same physical basename and record spelling; and original `list_Item_*` calls. Negative controls keep distinct owners incompatible as both values and array elements, preserve output sentinels, and execute no invalid product. Full bootstrap, original SDK/File controls and native allocation qualification remain required.

I integrate the already reviewed native `3340c2d79` forward-typedef correction and `dcda44b79` exact shadow expectation before executing the new ordinary imported-list control. Whole-program specialization bodies omit the typedef that the earlier declaration pass emits; standalone list generation retains it. I preserve my newer ASTServiceDecl catalog and exact external-header/no-local-body shadow. The source patch needs contextual placement because that additive schema shadow occupies the old hunk boundary; I do not replace either complete emitter or catalog. All new helper bodies and typedef assertions match the reviewed native source.

## I isolate provider-shadow cases under the retained array identity contract

My first `17e43cd9a` Linux bootstrap reaches actual shadows after the complete component build passes. `native_provider_add` reports three assertion failures, starting at the JSON row-count assertion. My imported evaluator's `builtin_array_push` preserves the original `VAL_ARRAY` object even for an initially empty array; `clone_value_at` copies owned record/tuple/string graphs while retaining nested array identity. Native `DynArray` append also mutates its receiver. The provider registry does not promise persistent immutable inputs.

The shadow reused its first `empty.rows` after insertion for independent JSON and absent-source cases. The shared row already contains the first provider, explaining the three incorrect count assumptions. I give those two cases fresh empty sets, preserve every original operation and result assertion, and add an explicit first-append alias count assertion. I do not change provider or evaluator semantics. The same bootstrap later reports the original ten-second shadow deadline; buffered output does not identify its active body, so I retain that as a separate unassigned terminal fact. Corrected-source review precedes any rerun; full SDK/File acceptance remains open.
