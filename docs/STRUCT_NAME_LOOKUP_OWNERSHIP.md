# My struct-name lookup ownership

I track this correction as `task_a4e083d9cddb4588a3e01bd20ede6a54`. PR948 units07 retains real LSan allocations from `get_struct_type_name`, inlined through typechecker callsites. I preserve that terminal and do not replay the known faulty fixture.

My lookup returns borrowed names. Existing AST, symbol, function and registered nominal names keep their current owners. Four branches currently create unowned copies: a parsed `List_Name_get` fallback, a concrete generic union field, a declared union field and a declared record field. No audited caller frees the result. I register each newly created copy exactly once with `env_own_checker_allocation`, preserving its exact spelling. I do not replace parsed qualified spellings with a canonical declaration name or mutate an AST cache.

The existing registry shallowly frees each unique block at environment destruction, independently of mutable symbol/function slots. This is cumulative environment lifetime storage, not expression-scoped reclamation. Its established failure policy is explicit: a NULL payload stays NULL; registry-node allocation failure reports an error and exits. I do not claim recoverable allocation failure for that owner. The list fallback retains its existing temporary/copy sequence and releases its temporary; temporary allocation failure must return NULL before writing through it.

Most consumers compare, format or copy the name before recursive mutation. The effect emitter is different: `build_effect_handle` retains the selected result name in a synthetic operation across symbol insertion and recursive handler/body emission, then formats it again. I duplicate and register this final selected name before those mutations. A failed snapshot must stop emission with an explicit allocation diagnostic; it must not silently remove nominal identity. Existing borrowed names are never registered directly or freed by this change.

My caller inventory contains 37 textual occurrences in the diagnosis source, including declarations and definition, with no include-fragment callsites. The native lane independently audited its 33-occurrence source. Permanent symbol/AST publications already copy names. Array-set emission retains separately owned symbol/function/definition strings; helper-derived container names are consumed by immediate lookup. The opaque-value query performs read-only metadata lookup before using its borrow.

Before affected execution I review the complete source and additive ownership fixture. The fixture must exercise all four allocating branches, exact returned spellings, borrowed branches, repeated calls, environment destruction and the effect result snapshot across mutation. Ordinary and ASan/UBSan with LSan enabled must retain actual providers and outputs. Original native float/record acceptance remains required, but independent parser and field metadata leak corrections must be integrated before replaying their known failing paths. The local-name overwrite repair is a separate qualified task.
