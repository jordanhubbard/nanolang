# My bounded emitter-shadow progress diagnostic

I investigate task_4931a66f39c04b1a96981a4e00d43646 after fresh canonical and repaired compilers both reached the unchanged ten-second shadow deadline. I do not increase that deadline, remove imported shadows, or interpret a timeout as proof of a loop.

My next private diagnostic build adds macro-only observations to the existing VM's direct-call activation and ordinary return cleanup. It prints only generated `$shadow_` function entries and exits, with bounded names, function indices and code offsets. It does not change generated bytecode, arguments, results, selected shadows, verifier admission, dispatch, cleanup or the alarm. The existing shared VM call/return handlers cover both dispatch forms. Entry is observed after successful frame activation; exit is observed after result validation and local cleanup, before caller restoration. An entry without exit identifies the active shadow interval, not its internal cause.

I cap the observer at 8192 rows (twice the existing compiler's 4096-function bound), fail with diagnostic-only exit92 if the cap or output recording fails, and flush each row. Stdio may allocate and introduces timing overhead. The result is diagnostic evidence only; it cannot establish that an unobserved build completes within its deadline. I compile this separately from immutable baseline and PR910 qualification trees, retain the exact compiler/source/provider inputs and bounded process status, and run only the original emitter command once with an outer120-second process-group bound. No qualification gate is replayed or relabeled. I review the source checkpoint before building or executing it.

After the active interval is observed, I inspect its actual loops and call graph. A further diagnostic needs its own reviewed checkpoint. Native aggregate executable timeouts on Darwin are a separate task and are not explained by this observer.

I place stdio declarations before the helper. Static source review confirms my generated function names use `$shadow_%d_%.32s` and the entry wrapper is `$shadow_entry`; both share the checked eight-byte prefix. The entry wrapper can produce a final leave observation without a direct-call enter. I do not count that as a missing trace event.

## My first observed interval

At source115b, fresh GCC13 preparation passed in20.697seconds. My single original emitter command returned1 after21.725seconds including module preparation, with the unchanged ten-second shadow diagnostic. The outer120-second bound was not reached and no output module was published.

I retained401 marker rows:201 entries and200 exits, in balanced order except the final entry. The last completed interval was `$shadow_199_parser_mark_owned` (function762, code166385). The open interval was `$shadow_200_parse_owned_pattern` (function763, code166525). Its original shadow at `src_nano/parser.nano:5067` parses `let Box { item } = box` and `let Choice.Some { item } = payload`. The observation does not determine which of those calls was active or whether preceding work consumed most of the deadline.

Tracked participating sources and selected executable tools remained equal. Module preparation added405 provider/cache files and changed no existing provider. Exact reports and CAS inventory are under `docs/evidence/emitter-shadow-progress`; source115b and its prepared products remain frozen. This is interval attribution, not a timing qualification or root-cause claim.

## My derived two-input probe

I next copy the original `parse_owned_pattern` shadow body, retaining both input strings, all constructor/call arguments and all assertions in their original order. A new root-file `shadow main` adds only before/after text markers around the two parser calls and checks a trivial main result. I retain the existing legacy parser import to exercise those same private helper names; this is a diagnostic, not a new public module interface.

The reviewed command will use my existing `--root-shadows-only` opt-out for this separate fixture, with the unchanged ten-second shadow policy and a120-second outer process-group bound. I do not apply the opt-out to the original emitter qualification. A pass leaves accumulated work and shared-state hypotheses open. A failure can narrow the active two-input sequence but cannot alone prove its cause.

My private VM observer adds `CLOCK_MONOTONIC` elapsed milliseconds relative to its first observed event, fails with92 if the clock fails, and additionally observes the exact `parse_owned_pattern` function name. I preserve the8192-row/name bounds and output-failure handling. The source checker/emitter register that imported helper under its unchanged source name. A timestamp measures the instrumented diagnostic, not the original full shadow run. No other observer or production behavior changes. I review this source checkpoint before any build or probe execution.

## My two-input and timed fullgraph observations

I retained the exact5333 source checkpoint for both diagnostics. The derived root-only probe passed and published its module. Its first parser call ran from2.222 to8.534milliseconds; its qualified call ran from21.065 to27.345milliseconds. The root shadow returned at39.806milliseconds, with all copied assertions satisfied. Seven VM marker rows and all four source markers are retained; source-marker stdio appeared after the flushed VM observations, so I do not infer event order from their line placement.

I then copied325 verified provider files into a separate identical-source tree, copied no executable, and linked a fresh timestamped nano_virt. The link log contains no C-source compilation commands. One original fullgraph command returned1 after21.607seconds including module preparation. Its407 marker rows show shadow200 entering at7381.855milliseconds, both parser calls returning, and shadow200 leaving at7484.041milliseconds. Shadow201, parse_block_recursive, entered at7484.043milliseconds before the unchanged deadline. That next shadow includes the existing520-statement stress input. I do not infer that it loops or that the full graph would pass under a different limit.

Both source/tool endpoint maps match. Probe module preparation added24 artifacts; fullgraph preparation added405; neither changed a preexisting provider. My paired seal retains32 reports and4163 unique content-addressed artifacts totaling104,081,685bytes, including provider reuse and exact link/build commands. I distinguish isolated success, completed fullgraph intervals, and an unresolved fullgraph deadline. Any admission optimization remains unimplemented and requires measured evidence and a separate reviewed authority contract.
