# I finish scalar tail frames before returning to my caller

My dbb qualification passes deep self-tail, scalar carriers and refusal boundaries. The mutual100000-tail case passes VM and LLVM lli, then generated C at O0 exits signal11. Static inspection finds nonself TAIL_CALL emits an ordinary recursive C call; self-tail alone uses a restart label. I retain the first terminal and do not replay it.

For task_7081b807b745458d8e80c777611a8e03 I select a private trampoline only for modules with TAIL_CALL admitted by the existing CLOSED_LITERAL_STRINGS profile. That profile excludes imports, ownership, aggregate layouts and managed operations. Other existing C profiles remain separate and receive no bounded-tail claim. Every selected local/result carrier must be an existing scalar, literal string or tagged scalar carrier after classification. I do not infer ownership from a generic pointer.

I preserve each existing typed function wrapper for ordinary calls, initializers and entry. Each wrapper owns a bounded transfer array sized by the module maximum arity and calls a private dispatcher. The dispatcher switches on a verified function index and invokes an actual compiled body. The body returns its next function index or UINT32_MAX completion. No VM loop decodes instructions. An ordinary nested wrapper creates an independent invocation; a tail transfer returns through the body before the next body begins.

Each body receives original typed arguments plus borrowed transfer/result pointers. A nonself tail instruction checks the existing exact callee signature and stack shape, pops every argument through the existing checked carrier API, stages all arguments before writing transfer cells, and reaches the existing cleanup epilogue. The epilogue releases body-local temporary storage/root metadata before returning the next index. It publishes the result only on completion. Existing self-tail restarts may retain their bounded local loop.

A private union carries int64_t, double, literal const char pointers, and the existing full tagged scalar value when that runtime is selected. Typed wrappers and dispatch use the exact classified member; float assignment preserves the existing ABI, and no string ownership is transferred. Exact tagged values retain their tag and payload. The maximum arity has the existing 1024-local bound; the completion sentinel is outside valid function indices. No global current-frame state, optimizer hint or generated native fault reproduction establishes this contract.

I keep the original100000 mutual test and all assertions, then require C O0/O2 and LLVM O0/O2 under512KiB stacks, all six original tail controls, all13 selected source controls, sanitizers, both hosts and relevant existing C qualification. Managed and full shared-capture release gates remain open. Source and fixture review precede corrected affected execution.
