/* Test import alias with qualified name resolution */

import "test_modules/math_helper.nano" as Math

fn main() -> int {
    (println "Testing import alias with qualified names...")
    
    /* Try calling Math.add */
    let result: int = (Math.add 10 20)
    (println "Math.add(10, 20) =")
    (println result)
    
    if (!= result 30) {
        (println "ERROR: Expected 30!")
        return 1
    } else {
        (println "✅ Math.add works!")
    }
    
    /* Try calling Math.square */
    let squared: int = (Math.square 8)
    (println "Math.square(8) =")
    (println squared)
    
    if (!= squared 64) {
        (println "ERROR: Expected 64!")
        return 1
    } else {
        (println "✅ Math.square works!")
    }
    
    (println "")
    (println "🎉 Import aliases working!")
    return 0
}

shadow main {
    assert (== (main) 0)
}
