from "modules/pybridge/pybridge.nano" import nl_pybridge_init, nl_pybridge_release

fn main() -> int {
    if false {
        let mut handle: int = 0
        unsafe { set handle (nl_pybridge_init "[]" 0) }
        if (> handle 0) {
            unsafe { (nl_pybridge_release handle) }
        }
    }
    return 0
}

shadow main { assert true }
