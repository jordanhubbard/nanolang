/* =============================================================================
 * std::result - Result<T, E>
 * =============================================================================
 */

module std_result

pub union Result<T, E> {
    Ok { value: T },
    Err { error: E }
}
