from "modules/http_server/http_server.nano" import HttpServer, create, set_static_dir, free_server

fn main() -> int {
    let server: HttpServer = (create 0)
    (set_static_dir server "build/userguide/html")
    (free_server server)
    return 0
}

shadow main { assert true }
