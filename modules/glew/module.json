{
  "name": "glew",
  "version": "1.0.0",
  "description": "GLEW - OpenGL Extension Wrangler for loading OpenGL functions and extensions",
  "headers": ["GL/glew.h"],
  "header_priority": 1000,
  "pkg_config": ["glew"],
  "frameworks": ["OpenGL"],
  "install": {
    "macos": {
      "brew": "glew",
      "command": "brew install glew"
    },
    "linux": {
      "apt": "libglew-dev",
      "command": "sudo apt install libglew-dev"
    }
  },
  "notes": "GLEW's Homebrew pkg-config on macOS doesn't include the OpenGL framework dependency, so we add it explicitly. On Linux, 'frameworks' is ignored and pkg-config provides -lGL instead."
}

