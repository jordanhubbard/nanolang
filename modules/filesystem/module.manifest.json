{
  "name": "filesystem",
  "version": "1.0.0",
  "stability": "experimental",
  "summary": "Advanced filesystem operations (walk directory trees, glob patterns).",
  "use_when": [
    "User needs to traverse directory trees",
    "User needs glob pattern matching",
    "User says 'walk directory', 'find files', 'recursive search'"
  ],
  "avoid_when": [
    "User only needs basic file I/O (use std module)"
  ],
  "capabilities": [
    "walk_directory",
    "glob_patterns",
    "recursive_search",
    "file_filtering"
  ],
  "keywords": ["files", "filesystem", "directory", "glob", "search"],
  "examples": [],
  "dependencies": {
    "nanolang": ["core>=1.0"],
    "modules": ["std"],
    "system": []
  },
  "io_surfaces": ["files"],
  "alternatives": [
    {"when": "basic file ops", "module": "std"}
  ]
}
