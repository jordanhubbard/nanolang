/* Test import aliases - this should work once we implement process_imports */

import "test_modules/math_helper.nano" as Math
import "test_modules/string_helper.nano" as Strings

fn main() -> int {
    /* Call Math.square - should use math_helper's square */
    let num_result: int = (Math.square 7)
    
    /* Call Strings.cube - should use string_helper's cube */
    let str_result: int = (Strings.test_strings)
    
    /* Both should work without conflict! */
    if (and (== num_result 49) (== str_result 1)) {
        (println "✓ Import aliases work!")
        return 0
    } else {
        (println "✗ Import aliases failed")
        return 1
    }
}

shadow main {
    assert (== (main) 0)
}
