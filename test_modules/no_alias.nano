/* Test without alias to verify basic import works */

import "test_modules/math_helper.nano"

fn main() -> int {
    let result: int = (square 5)
    if (== result 25) {
        (println "✓ Without alias works")
        return 0
    } else {
        return 1
    }
}
