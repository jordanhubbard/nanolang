fn my_max(a: int, b: int) -> int {
    if (> a b) {
        return a
    } else {
        return b
    }
}

fn main() -> int {
    let result: int = (my_max 10 20)
    (println (int_to_string result))
    return 0
}
