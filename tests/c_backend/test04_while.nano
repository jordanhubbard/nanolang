fn sum_to(n: int) -> int {
    let mut total: int = 0
    let mut i: int = 1
    while (<= i n) {
        set total (+ total i)
        set i (+ i 1)
    }
    return total
}

fn main() -> int {
    let s: int = (sum_to 5)
    (println (int_to_string s))
    return 0
}
