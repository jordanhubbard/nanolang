/* Test generic union instantiation syntax - parsing only */

union Result<T, E> {
    Ok { value: T },
    Err { error: E }
}

fn main() -> int {
    /* Test: generic union instantiation should parse without errors */
    let r: Result<int, string> = Result<int, string>.Ok { value: 42 }
    return 0
}

