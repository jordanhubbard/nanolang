# Simple test of Module.StructName

module "test_modules/simple_math.nano" as Math

fn main() -> int {
    # Test qualified struct literal
    let p: Math.Point = Math.Point { x: 5, y: 10 }
    
    (print "Point x: ")
    (println (int_to_string p.x))
    
    if (== p.x 5) {
        (println "✓ Test passed!")
        return 0
    } else {
        (println "✗ Test failed")
        return 1
    }
}

shadow main {
    assert (== (main) 0)
}
