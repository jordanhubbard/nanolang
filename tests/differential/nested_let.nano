/* Differential test: nested let bindings with dependent values */
fn main() -> int {
    let x: int = 3
    let y: int = (* x 4)
    (println (cast_string (+ y 1)))
    return 0
}
shadow main { assert true }
