/* Negative test: requires clauses must be boolean expressions */

fn bad_guard(x: int) -> int
    requires x
{
    return x
}

shadow bad_guard {
    assert (== 1 1)
}

fn main() -> int {
    return (bad_guard 10)
}

shadow main {
    assert (== 1 1)
}
