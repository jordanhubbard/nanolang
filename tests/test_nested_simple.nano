# Test simple nested array parsing

fn test_nested() -> int {
    let mut inner: array<int> = []
    set inner (array_push inner 42)
    
    let mut outer: array<array<int>> = []
    set outer (array_push outer inner)
    
    let row: array<int> = (at outer 0)
    return (at row 0)
}

fn main() -> int {
    let result: int = (test_nested)
    (println result)
    return 0
}
