fn nested_concat(a: string, b: string, c: string) -> string {
    return (+ a (+ b c))
}

shadow nested_concat {
    assert (str_equals (nested_concat "a" "b" "c") "abc")
}

fn main() -> int {
    return 0
}

shadow main {
    assert (== (main) 0)
}

