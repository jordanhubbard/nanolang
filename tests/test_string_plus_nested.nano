/* Test: nested string concatenation */
fn nested_concat(a: string, b: string, c: string) -> string {
    return (+ a (+ b c))
}

shadow nested_concat {
    assert (== (nested_concat "a" "b" "c") "abc")
}

fn main() -> int {
    return 0
}

shadow main {
    assert (== (main) 0)
}

