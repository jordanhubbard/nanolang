/* Test module forward declaration generation with topological sort */

import "test_modules/math_helper.nano" as Math
import "test_modules/string_helper.nano" as Strings

fn main() -> int {
    /* Test using functions from imported modules */
    let sum: int = (Math.add 10 20)
    let product: int = (Math.multiply 5 6)
    
    (print "Sum: ")
    (println (int_to_string sum))
    (print "Product: ")
    (println (int_to_string product))
    
    let result: int = (Strings.test_strings)
    (print "String test: ")
    (println (int_to_string result))
    
    if (== sum 30) {
        if (== product 30) {
            if (== result 1) {
                (println "All tests passed!")
                return 0
            } else {
                return 1
            }
        } else {
            return 1
        }
    } else {
        return 1
    }
}
