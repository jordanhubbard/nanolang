/* User guide test: getting started with arithmetic operations */
fn add3(a: int, b: int, c: int) -> int {
    return (+ a (+ b c))
}

shadow add3 {
    assert (== (add3 1 2 3) 6)
}

fn main() -> int {
    assert (== (add3 10 20 30) 60)
    return 0
}

shadow main { assert true }
