fn sum_pair() -> int {
    let t: (int, int) = (4, 6)
    return (+ t.0 t.1)
}

shadow sum_pair {
    assert (== (sum_pair) 10)
}

fn main() -> int {
    assert (== (sum_pair) 10)
    return 0
}

shadow main { assert true }
