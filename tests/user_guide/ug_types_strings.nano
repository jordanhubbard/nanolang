fn greeting() -> string {
    return (+ "hello" (+ " " "world"))
}

shadow greeting {
    assert (str_equals (greeting) "hello world")
}

fn main() -> int {
    (println (greeting))
    return 0
}

shadow main { assert true }
