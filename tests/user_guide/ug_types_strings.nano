/* User guide test: type system string operations */
fn greeting() -> string {
    return (+ "hello" (+ " " "world"))
}

shadow greeting {
    assert (== (greeting) "hello world")
}

fn main() -> int {
    (println (greeting))
    return 0
}

shadow main { assert true }
