/* test_optimizer.c — optimizer unit tests (stub)
 *
 * Full tests live in feat/compiler-advanced (PR #44).
 * This stub allows 'make test' to pass until that PR is merged.
 */
#include <stdio.h>

/* cli.c requires these globals */
int g_argc = 0;
char **g_argv = NULL;

int main(void) {
    printf("optimizer tests: stub (PR #44 pending)\n");
    return 0;
}
