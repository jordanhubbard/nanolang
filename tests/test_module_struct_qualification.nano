# Test: module-qualified struct type + struct literal sugar (Alias.StructName)

module "modules/vector2d/vector2d.nano" as Vec

fn main() -> int {
    let v: Vec.Vector2D = Vector2D { x: 1.0, y: 2.0 }
    assert (== v.x 1.0)
    assert (== v.y 2.0)
    return 0
}

shadow main {
    assert (== (main) 0)
}

