# Expected error: unknown effect operation

effect Console {
    log : string -> void
}

fn main() -> int {
    let x: int = handle {
        42
    } with {
        nonexistent msg -> {
            (println msg)
        }
    }
    return x
}

shadow main {
    assert (== (main) 42)
}
