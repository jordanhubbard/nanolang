# Expected error: duplicate effect declaration

effect Console {
    log : string -> void
}

effect Console {
    log : string -> void
}

fn main() -> int {
    return 0
}

shadow main {
    assert (== (main) 0)
}
