# Expected Error: break statement outside loop

fn main() -> int {
    let x: int = 5
    if (== x 5) {
        break  # Error: break outside loop context
    }
    return 0
}

shadow main {
    assert (== (main) 0)
}
