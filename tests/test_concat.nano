fn main() -> int {
    let a: string = "hello"
    let b: string = "world"
    
    # Test simple concat
    let c1: string = (+ a " ")
    (println c1)
    
    # Test nested concat
    let c2: string = (+ a (+ " " b))
    (println c2)
    
    # Test triple nested
    let c3: string = (+ "[" (+ "0" (+ "] " "test")))
    (println c3)
    
    return 0
}

