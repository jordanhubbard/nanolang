from "modules/std/env.nano" import set_env, get

fn test_it() -> int {
    let k: string = "NANOLANG_TEST_ENV"
    assert (== (set_env k "abc") 0)
    assert (== (get k) "abc")
    return 0
}

shadow test_it {
    assert (== (test_it) 0)
}

fn main() -> int {
    return 0
}

shadow main {
    assert (== (main) 0)
}

