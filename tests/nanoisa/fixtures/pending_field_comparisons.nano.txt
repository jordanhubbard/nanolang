# I retain this probe for task_8e367aeda3394b0bb1ed1c37f56edeef.
# My emitter currently refuses the string projection comparison. Removing it
# exposes I64_EQ instead of the C seed's EQ for the boolean comparison.
struct Options { enabled: bool, label: string }
fn check(options: Options, expected: bool) -> int {
    assert (== options.label "ready")
    assert (== options.enabled expected)
    return 0
}
shadow check { assert (== (check Options { enabled: true, label: "ready" } true) 0) }
fn main() -> int { return (check Options { enabled: true, label: "ready" } true) }
shadow main { assert (== (main) 0) }
